<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>暗无天日</title>
    <link>https://lujun9972.github.io</link>
    <description>=============&gt;DarkSun的个人博客</description>
    <pubDate>Wed, 15 Apr 2026 15:13:27 UTC</pubDate>
    <lastBuildDate>Wed, 15 Apr 2026 15:13:27 UTC</lastBuildDate>
    <docs>http://www.rssboard.org/rss-specification</docs>
    <generator>ego static site generator (https://github.com/emacs-china/ego)</generator>
<item>
<title>AI 时代的 PARA 方法：用 Org-mode 和 AI 打造个人知识管理系统</title>
<link>https://lujun9972.github.io/blog/2026/04/15/ai-时代的-para-方法：用-org-mode-和-ai-打造个人知识管理系统/index.html</link>
<description>* 从一个实验说起

Tiago Forte（PARA 方法的创始人、《Building a Second Brain》作者）最近做了一个实验：他让自己的电脑文件自由堆积了一个月，不整理任何东西。月底，桌面和下载文件夹里累积了 222 个文件。

这些文件五花八门：
- 财务文档：个人报税单、商业财务报表、退休账户表格、人寿保险地址变更单
- 视觉素材：工作坊宣传图、社交媒体照片、博客文章配图、采访视频录像素材
- 各种待读内容：PDF 论文、研究报告、他人推荐的文章

然后他手工将这 222 个文件按照 PARA 方法整理到文件系统中。整个过程耗时 36 分钟，平均每个文件约 10 秒。

很多人看到这个实验会想：&quot;这种事交给 AI 不就行了？&quot; Claude Code 确实能帮你整理文件。但 Tiago 认为效率和省时其实是整理文件最不重要的方面。有更深层的原因。

**而在 AI 时代，这个理由变得前所未有的重要：整理你的文件，是为了让 AI 能更好地利用你的信息。**

* PARA 四层结构速览

PARA 是一个通用的数字信息组织方法，将所有信息归入四个类别：

| 类别 | 含义 | 特征 | 举例 |
|------|------|------|------|
| Projects（项目） | 有明确目标和截止日期的任务 | 临时性、行动导向 | 写一篇博文、准备一次演讲、完成一个开发项目 |
| Areas（领域） | 持续维护的责任领域 | 永久性、标准导向 | 健康、财务、职业发展、人际关系 |
| Resources（资源） | 感兴趣的主题和参考资料 | 按兴趣分类、未来可能有用 | 编程技巧、设计灵感、历史知识 |
| Archives（归档） | 不再活跃的其他三类内容 | 保留但不活跃 | 已完成的项目、不再负责的领域、失去兴趣的资源 |

PARA 的妙处在于它只有四个类别，却涵盖了你可能想保存的任何东西。简单到不需要思考&quot;这该放哪&quot;。

如果你熟悉我这个博客的结构，你会发现它本质上也是一个 PARA 系统：
- =Emacs之怒=、=编程之旅=、=linux和它的小伙伴= → Resources（按主题归类的知识）
- =life-hacking= → Areas（持续维护的生活效率领域）
- =读书笔记= → Resources（参考资料）
- 博客的 git 历史本身就是 Archives

* AI 时代为什么更需要 PARA

Tiago 在文章中提出了一个关键洞察：

#+BEGIN_QUOTE
AI 正在让任何现有数据仓库的价值飙升——包括你电脑上的文件和文档。但这些数据需要（一定程度上的）组织。
#+END_QUOTE

原因有三：

**1. LLM 的上下文窗口有限**

你不能把整个数字世界一次性喂给 AI。你需要找到并提供当前任务所需的&quot;最小可行上下文&quot;。如果你的文件已经按照 PARA 组织好了，你可以直接指向一个 Project 文件夹——而不是一堆毫不相关的散碎文件。

#+BEGIN_QUOTE
搜索只能找到单个文件的精确匹配。PARA 给你更有价值的东西：预组装的上下文块，反映你真实的工作和思维方式。
#+END_QUOTE

**2. 本地文件 &gt; 云端**

像 Claude Code 这样运行在你电脑上的 AI 工具需要访问本地文件。云端访问有延迟、登录障碍、token 消耗等问题。你的本地文件更快、更可靠、更容易访问。

**3. PARA 天然适合 AI 辅助工作**

- *简单*：只有四个类别，人类和 AI 都容易理解
- *个性化*：类别基于你自己的承诺和兴趣，而不是固定层级
- *知道如何遗忘*：Archives 类别让 AI 不被无关信息淹没
- *跨平台*：同一套分类逻辑可以统一不同工具和平台

* 在 Org-mode 中落地 PARA

理论说完了，让我们来实际搭建。以下是在 Org-mode 中实现 PARA 的具体方案。

**目录结构设计：**

#+BEGIN_SRC text :tangle no
~/org/
├── projects/
│   ├── 2026-博客重构/
│   ├── 2026-Anki插件开发/
│   └── ...
├── areas/
│   ├── 健康/
│   ├── 财务/
│   ├── 职业/
│   └── ...
├── resources/
│   ├── emacs/
│   ├── python/
│   ├── linux/
│   └── ...
└── archives/
    └── ...
#+END_SRC

**org-capture 模板配置：**

通过 org-capture 快速将想法、任务、笔记归入 PARA 的正确位置：

#+BEGIN_SRC emacs-lisp :tangle no
(setq org-para-base-dir &quot;~/我的笔记&quot;)

(setq org-capture-templates
      &#39;((&quot;p&quot; &quot;Project&quot; entry
         (file+headline (lambda () (expand-file-name &quot;projects/inbox.org&quot; org-para-base-dir)) &quot;Projects Inbox&quot;)
         &quot;* TODO %?\n  %U\n  %a\n  %i&quot;)
        (&quot;a&quot; &quot;Area&quot; entry
         (file+headline (lambda () (expand-file-name &quot;areas/inbox.org&quot; org-para-base-dir)) &quot;Areas Inbox&quot;)
         &quot;* TODO %?\n  %U\n  %a\n  %i&quot;)
        (&quot;r&quot; &quot;Resource&quot; entry
         (file (lambda () (expand-file-name &quot;resources/inbox.org&quot; org-para-base-dir)))
         &quot;* %? :resource:\n  %U\n  %i&quot;)
        (&quot;j&quot; &quot;Journal&quot; entry
         (file+datetree (lambda () (expand-file-name &quot;journal.org&quot; org-para-base-dir)))
         &quot;* %?\n  %U&quot;)))
#+END_SRC

**org-refile 目标设置：**

让 org-refile 可以直接将条目归档到 PARA 目录下的各文件：

#+BEGIN_SRC emacs-lisp :tangle no
(setq org-refile-targets
      `((,(expand-file-name &quot;projects/*.org&quot; org-para-base-dir) :maxlevel . 2)
        (,(expand-file-name &quot;areas/*.org&quot; org-para-base-dir) :maxlevel . 2)
        (,(expand-file-name &quot;resources/*.org&quot; org-para-base-dir) :maxlevel . 1)
        (,(expand-file-name &quot;archives/*.org&quot; org-para-base-dir) :maxlevel . 1)))

(setq org-refile-use-outline-path &#39;file)
(setq org-outline-path-complete-in-steps t)
#+END_SRC

**org-roam 与 PARA 的结合：**

如果你使用 org-roam，可以用 filetags 来标记 PARA 类别：

#+BEGIN_SRC emacs-lisp :tangle no
(setq org-roam-directory org-para-base-dir)

;; 通过 filetags 区分 PARA 类别
;; 在 org 文件头部添加: #+filetags: :project: 或 :area: 或 :resource: 或 :archive:
(setq org-roam-node-display-template
      &quot;${tags:20} ${title:*}&quot;
      org-roam-node-annotation-function
      (lambda (node)
        (let ((tags (org-roam-node-tags node)))
          (when tags
            (format &quot; [%s]&quot; (string-join tags &quot;, &quot;))))))

;; 按类别过滤 org-roam 节点
(defun my/org-roam-filter-by-tag (tag)
  (lambda (node)
    (member tag (org-roam-node-tags node))))

(defun my/org-roam-list-notes-by-tag (tag)
  (mapcar #&#39;org-roam-node-file
          (seq-filter
           (my/org-roam-filter-by-tag tag)
           (org-roam-node-list))))
#+END_SRC

以上配置完成后，你就有了：
- =org-capture= 快速捕获 → 进入 inbox
- =org-refile= 重新归档 → 移到正确的 PARA 位置
- =org-roam= 双向链接 → 在 PARA 之间建立联系

* 用 Claude Code 增强整理流程

PARA 的整理可以借助 AI 来加速。这里演示如何用 Claude Code 自动将下载文件夹中的文件分类到 PARA 结构。

首先，让我们看看一个典型的 Downloads 文件夹会积累什么样的文件：

#+BEGIN_SRC shell :tangle no
ls ~/Downloads/ | head -20
#+END_SRC

然后用 Claude Code 进行自动分类：

#+BEGIN_SRC shell :tangle no
cd ~/org
claude &quot;请帮我整理 ~/Downloads/ 下的文件到 PARA 目录结构中：
1. 查看文件列表和文件类型
2. 根据文件名和类型判断属于 Projects/Areas/Resources/Archives 中的哪一类
3. 将文件移动到对应的 ~/org/ 子目录下
4. 如果是项目相关文件，放入 projects/ 下对应的子目录
5. 如果无法确定分类，放入 resources/inbox/ 待后续处理
注意：不要移动 .part 文件和不完整的下载文件&quot;
#+END_SRC

Claude Code 会：
- 逐个检查文件名和类型
- 根据上下文判断分类
- 创建必要的子目录
- 移动文件到正确位置

但正如 Tiago 所指出的，AI 有做不到的事情：

#+BEGIN_QUOTE
当我注意到桌面上有一个屏幕录制，那会提醒我要把它发给视频编辑。当我看到下载文件夹里有一组照片，它可能让我想到一些微弱关联的事情——比如我孩子学校即将到来的拍照日。这些关联是 AI 目前无法识别的。
#+END_QUOTE

所以最佳实践是：
- *AI 批量处理*：让 AI 处理大量文件的初步分类
- *人工复审*：你自己过一遍，捕捉那些需要联想和行动的项目
- *定期维护*：每周花 10 分钟整理，而不是积累一个月

* 总结

PARA 的核心不是分类学，而是行动学。它以你正在做的事情（Projects）为中心，而不是以信息的主题为中心。这正是 AI 最擅长辅助的场景——指向一个明确的项目文件夹，让 AI 帮你处理它。

Org-mode 为 PARA 提供了完美的载体：纯文本、本地存储、可编程、可搜索。再加上 AI 工具的辅助，整理数字生活的成本从&quot;每月 36 分钟&quot;可能降到&quot;每周 5 分钟&quot;。

但记住 Tiago 的提醒：整理文件最重要的收获不是速度。是在整理的过程中，你会重新审视自己的承诺、责任和兴趣。这个反思过程，AI 暂时代替不了。

-----

参考：[[https://fortelabs.com/blog/why-para-is-the-key-to-the-ai-era/][Why PARA Is the Key to the AI Era - Forte Labs]]
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/15/ai-时代的-para-方法：用-org-mode-和-ai-打造个人知识管理系统/index.html</guid>
</item>
<item>
<title>PARA Org-mode 测试配置</title>
<link>https://lujun9972.github.io/blog/2026/04/15/para-org-mode-测试配置/index.html</link>
<description>* 前置说明

这份配置用于测试博文《AI 时代的 PARA 方法》中的 Org-mode PARA 实现。
使用方式：

1. 先执行 [[创建目录结构]] 中的 shell 代码块（=C-c C-c=）
2. 将 [[Emacs 配置]] 中的代码加入你的 =init.el= 或 =*scratch*= buffer 执行
3. 用 =M-x org-capture= 测试四个模板
4. 用 =C-c C-w= (=org-refile=) 测试归档

基础目录设为 =~/我的笔记= ，可按需修改 =org-para-base-dir= 。

* 创建目录结构

#+BEGIN_SRC shell :tangle (format &quot;/tmp/para-setup-%s.sh&quot; (user-login-name))
PARA_DIR=&quot;$HOME/org&quot;

# 创建 PARA 四层目录
mkdir -p &quot;$PARA_DIR/projects/2026-博客重构&quot;
mkdir -p &quot;$PARA_DIR/projects/2026-Anki插件开发&quot;
mkdir -p &quot;$PARA_DIR/areas/健康&quot;
mkdir -p &quot;$PARA_DIR/areas/财务&quot;
mkdir -p &quot;$PARA_DIR/areas/职业&quot;
mkdir -p &quot;$PARA_DIR/resources/emacs&quot;
mkdir -p &quot;$PARA_DIR/resources/python&quot;
mkdir -p &quot;$PARA_DIR/resources/linux&quot;
mkdir -p &quot;$PARA_DIR/archives&quot;

# 创建 inbox 文件（org-capture 目标）
cat &gt; &quot;$PARA_DIR/projects/inbox.org&quot; &lt;&lt; &#39;EOF&#39;
#+TITLE: Projects Inbox
#+FILETAGS: :project:

,* Projects Inbox
EOF

cat &gt; &quot;$PARA_DIR/areas/inbox.org&quot; &lt;&lt; &#39;EOF&#39;
#+TITLE: Areas Inbox
#+FILETAGS: :area:

,* Areas Inbox
EOF

cat &gt; &quot;$PARA_DIR/resources/inbox.org&quot; &lt;&lt; &#39;EOF&#39;
#+TITLE: Resources Inbox
#+FILETAGS: :resource:
EOF

# 创建 journal 文件
touch &quot;$PARA_DIR/journal.org&quot;

# 创建示例项目文件（供 org-refile 测试）
cat &gt; &quot;$PARA_DIR/projects/2026-博客重构/index.org&quot; &lt;&lt; &#39;EOF&#39;
#+TITLE: 2026 博客重构
#+FILETAGS: :project:

,* TODO 重新设计博客首页
  SCHEDULED: &lt;2026-04-20 日&gt;
,* TODO 迁移到新的静态站点生成器
,* DONE 梳理现有文章分类
  CLOSED: [2026-04-10 四]
EOF

cat &gt; &quot;$PARA_DIR/projects/2026-Anki插件开发/index.org&quot; &lt;&lt; &#39;EOF&#39;
#+TITLE: 2026 Anki 插件开发
#+FILETAGS: :project:

,* TODO 调研 Anki 插件 API
,* TODO 设计卡片模板系统
,* INPROGRESS 编写核心功能代码
EOF

# 创建示例领域文件
cat &gt; &quot;$PARA_DIR/areas/健康/index.org&quot; &lt;&lt; &#39;EOF&#39;
#+TITLE: 健康
#+FILETAGS: :area:

,* 每周运动目标：3 次
,* 年度体检：每年 Q1
,* TODO 预约牙科检查
EOF

cat &gt; &quot;$PARA_DIR/areas/财务/index.org&quot; &lt;&lt; &#39;EOF&#39;
#+TITLE: 财务
#+FILETAGS: :area:

,* TODO 完成 2025 年度报税
  DEADLINE: &lt;2026-05-31 日&gt;
,* 每月记账复盘
EOF

# 创建示例资源文件
cat &gt; &quot;$PARA_DIR/resources/emacs/index.org&quot; &lt;&lt; &#39;EOF&#39;
#+TITLE: Emacs 学习笔记
#+FILETAGS: :resource:

,* Org-mode 技巧
,** 使用 columnview 生成任务报表
,** org-roam 双向链接配置
,* Elisp 常用模式
,** advice-add / advice-remove
,** defmacro 与 defun 的区别
EOF

echo &quot;PARA 目录结构创建完成：&quot;
echo &quot;&quot;
tree &quot;$PARA_DIR&quot; 2&gt;/dev/null || find &quot;$PARA_DIR&quot; -type f | sort
#+END_SRC

#+RESULTS:
| PARA                | 目录结构创建完成： |                   |           |
|                     |                    |                   |           |
| /home/lujun9972/org |                    |                   |           |
| ├──                 | archives           |                   |           |
| ├──                 | areas              |                   |           |
| │                   | ├──                | inbox.org         |           |
| │                   | ├──                | 健康              |           |
| │                   | │                  | └──               | index.org |
| │                   | ├──                | 职业              |           |
| │                   | └──                | 财务              |           |
| │                   | └──                | index.org         |           |
| ├──                 | journal.org        |                   |           |
| ├──                 | projects           |                   |           |
| │                   | ├──                | 2026-Anki插件开发 |           |
| │                   | │                  | └──               | index.org |
| │                   | ├──                | 2026-博客重构     |           |
| │                   | │                  | └──               | index.org |
| │                   | └──                | inbox.org         |           |
| └──                 | resources          |                   |           |
| ├──                 | emacs              |                   |           |
| │                   | └──                | index.org         |           |
| ├──                 | inbox.org          |                   |           |
| ├──                 | linux              |                   |           |
| └──                 | python             |                   |           |
|                     |                    |                   |           |
| 13                  | directories,       | 9                 | files     |

* Emacs 配置

#+BEGIN_SRC emacs-lisp :tangle (format &quot;/tmp/para-config-%s.el&quot; (user-login-name))
;;; para-config.el --- PARA Org-mode 配置 -*- lexical-binding: t; -*-

;; ============================================================
;; 基础设置
;; ============================================================

(defvar org-para-base-dir &quot;~/org&quot;
  &quot;PARA 方法的根目录。&quot;)

;; ============================================================
;; org-capture 模板：快速捕获到 PARA 各层
;; ============================================================

(setq org-capture-templates
      `((&quot;p&quot; &quot;Project - 项目任务&quot; entry
         (file+headline ,(expand-file-name &quot;projects/inbox.org&quot; org-para-base-dir)
                        &quot;Projects Inbox&quot;)
         &quot;* TODO %?\n  %U\n  %a\n  %i&quot;
         :empty-lines 1)

        (&quot;a&quot; &quot;Area - 领域任务&quot; entry
         (file+headline ,(expand-file-name &quot;areas/inbox.org&quot; org-para-base-dir)
                        &quot;Areas Inbox&quot;)
         &quot;* TODO %?\n  %U\n  %a\n  %i&quot;
         :empty-lines 1)

        (&quot;r&quot; &quot;Resource - 资源笔记&quot; entry
         (file ,(expand-file-name &quot;resources/inbox.org&quot; org-para-base-dir))
         &quot;* %? :resource:\n  %U\n  %i&quot;
         :empty-lines 1)

        (&quot;j&quot; &quot;Journal - 日记&quot; entry
         (file+datetree ,(expand-file-name &quot;journal.org&quot; org-para-base-dir))
         &quot;* %?\n  %U&quot;
         :empty-lines 1)

        ;; 便捷模板：带链接的选择式捕获
        (&quot;c&quot; &quot;Capture with choice&quot; entry
         (file+headline ,(expand-file-name &quot;projects/inbox.org&quot; org-para-base-dir)
                        &quot;Projects Inbox&quot;)
         &quot;* TODO %^{任务描述}\n  %U\n  %a\n  %i&quot;
         :empty-lines 1)))

;; ============================================================
;; org-refile 目标：归档到 PARA 各文件
;; ============================================================

(setq org-refile-targets
      `((,(expand-file-name &quot;projects/*.org&quot; org-para-base-dir) :maxlevel . 2)
        (,(expand-file-name &quot;projects/**/*.org&quot; org-para-base-dir) :maxlevel . 2)
        (,(expand-file-name &quot;areas/*.org&quot; org-para-base-dir) :maxlevel . 2)
        (,(expand-file-name &quot;areas/**/*.org&quot; org-para-base-dir) :maxlevel . 2)
        (,(expand-file-name &quot;resources/*.org&quot; org-para-base-dir) :maxlevel . 1)
        (,(expand-file-name &quot;resources/**/*.org&quot; org-para-base-dir) :maxlevel . 1)
        (,(expand-file-name &quot;archives/*.org&quot; org-para-base-dir) :maxlevel . 1)))

(setq org-refile-use-outline-path &#39;file)
(setq org-outline-path-complete-in-steps t)

;; ============================================================
;; org-agenda：PARA 视图
;; ============================================================

(setq org-agenda-files
      (list (expand-file-name &quot;projects&quot; org-para-base-dir)
            (expand-file-name &quot;areas&quot; org-para-base-dir)))

;; 自定义 agenda 视图：按 PARA 层级分组
(setq org-agenda-custom-commands
      &#39;((&quot;P&quot; &quot;PARA 视图&quot;
         ((agenda &quot;&quot; ((org-agenda-span &#39;week)))
          (todo &quot;TODO|INPROGRESS&quot;
                ((org-agenda-overriding-header &quot;活跃项目任务&quot;)
                 (org-agenda-files (list (expand-file-name &quot;projects&quot; org-para-base-dir)))))
          (todo &quot;TODO&quot;
                ((org-agenda-overriding-header &quot;领域维护任务&quot;)
                 (org-agenda-files (list (expand-file-name &quot;areas&quot; org-para-base-dir)))))))
        (&quot;p&quot; &quot;仅项目任务&quot;
         todo &quot;TODO|INPROGRESS&quot;
         ((org-agenda-overriding-header &quot;项目任务&quot;)
          (org-agenda-files (list (expand-file-name &quot;projects&quot; org-para-base-dir)))))))

;; ============================================================
;; org-roam 集成（可选，需要安装 org-roam）
;; ============================================================

(with-eval-after-load &#39;org-roam
  (setq org-roam-directory org-para-base-dir)

  ;; 通过 filetags 区分 PARA 类别
  ;; 在 org 文件头部添加: #+filetags: :project: 或 :area: 或 :resource: 或 :archive:
  (setq org-roam-node-display-template
        &quot;${tags:20} ${title:*}&quot;)

  ;; 按类别过滤 org-roam 节点
  (defun my/org-roam-filter-by-tag (tag)
    &quot;返回一个过滤函数，只保留带有 TAG 的 org-roam 节点。&quot;
    (lambda (node)
      (member tag (org-roam-node-tags node))))

  (defun my/org-roam-list-notes-by-tag (tag)
    &quot;列出所有带有 TAG 的 org-roam 笔记文件路径。&quot;
    (mapcar #&#39;org-roam-node-file
            (seq-filter
             (my/org-roam-filter-by-tag tag)
             (org-roam-node-list))))

  ;; 快捷命令：按 PARA 类别浏览笔记
  (defun my/org-roam-find-project ()
    &quot;查找或创建项目笔记。&quot;
    (interactive)
    (org-roam-node-find nil nil (my/org-roam-filter-by-tag &quot;project&quot;)))

  (defun my/org-roam-find-area ()
    &quot;查找或创建领域笔记。&quot;
    (interactive)
    (org-roam-node-find nil nil (my/org-roam-filter-by-tag &quot;area&quot;)))

  (defun my/org-roam-find-resource ()
    &quot;查找或创建资源笔记。&quot;
    (interactive)
    (org-roam-node-find nil nil (my/org-roam-filter-by-tag &quot;resource&quot;))))

;; ============================================================
;; 辅助函数：PARA 维护
;; ============================================================

(defun my/para-archive-project (project-file)
  &quot;将一个完成的项目移到 archives 目录。
PROJECT-FILE 是项目文件或目录的路径。&quot;
  (interactive
   (list (completing-read &quot;归档项目: &quot;
                          (directory-files
                           (expand-file-name &quot;projects&quot; org-para-base-dir)
                           t directory-files-no-dot-files-regexp))))
  (let* ((archive-dir (expand-file-name &quot;archives&quot; org-para-base-dir))
         (project-name (file-name-nondirectory project-file))
         (dest (expand-file-name project-name archive-dir)))
    (rename-file project-file dest)
    (message &quot;已归档: %s -&gt; %s&quot; project-name dest)))

(defun my/para-show-stats ()
  &quot;显示当前 PARA 各层的文件数量统计。&quot;
  (interactive)
  (let ((stats
         (cl-loop for category in &#39;(&quot;projects&quot; &quot;areas&quot; &quot;resources&quot; &quot;archives&quot;)
                  for dir = (expand-file-name category org-para-base-dir)
                  for count = (length (directory-files-recursively dir &quot;\\.org$&quot;))
                  collect (cons category count))))
    (with-output-to-temp-buffer &quot;*PARA Stats*&quot;
      (princ &quot;PARA 目录统计\n&quot;)
      (princ (make-string 30 ?=)) (terpri)
      (cl-loop for (cat . cnt) in stats
               do (princ (format &quot;%-15s %d 个文件\n&quot; cat cnt)))
      (terpri)
      (princ (format &quot;总计: %d 个文件\n&quot;
                     (cl-loop for (_ . cnt) in stats sum cnt))))))

;; 快捷键绑定（可选）
(global-set-key (kbd &quot;C-c p c&quot;) #&#39;org-capture)
(global-set-key (kbd &quot;C-c p a&quot;) #&#39;org-agenda)
(global-set-key (kbd &quot;C-c p s&quot;) #&#39;my/para-show-stats)
(global-set-key (kbd &quot;C-c p A&quot;) #&#39;my/para-archive-project)

(provide &#39;para-config)
;;; para-config.el ends here
#+END_SRC

* 测试步骤

** 1. 创建目录结构

在 shell 中执行：

#+BEGIN_SRC shell
bash /tmp/para-setup-$(whoami).sh
#+END_SRC

或者在 org 文件中 =C-c C-c= 执行 [[创建目录结构]] 的代码块。

** 2. 加载 Emacs 配置

#+BEGIN_SRC emacs-lisp
;; 方式一：直接加载
(load &quot;/tmp/para-config-$(whoami).el&quot;)

;; 方式二：在 *scratch* 中 eval-region
#+END_SRC

** 3. 测试 org-capture

| 快捷键 | 模板 | 目标 |
|--------|------|------|
| =C-c p c= → =p= | Project 任务 | projects/inbox.org |
| =C-c p c= → =a= | Area 任务 | areas/inbox.org |
| =C-c p c= → =r= | Resource 笔记 | resources/inbox.org |
| =C-c p c= → =j= | 日记 | journal.org |

** 4. 测试 org-refile

在任何 org 条目上按 =C-c C-w= (=org-refile=)，应该能看到：
- projects 下的项目文件
- areas 下的领域文件
- resources 下的资源文件
- archives 目录

** 5. 测试 agenda 视图

| 快捷键 | 视图 |
|--------|------|
| =C-c p a= → =P= | PARA 总览（按周 agenda + 项目任务 + 领域任务） |
| =C-c p a= → =p= | 仅显示项目任务 |

** 6. 测试统计

=C-c p s= 显示各层文件数量。

* 快捷键速查

| 快捷键 | 功能 |
|--------|------|
| =C-c p c= | org-capture |
| =C-c p a= | org-agenda |
| =C-c p s= | PARA 统计 |
| =C-c p A= | 归档项目 |
| =C-c C-w= | org-refile（在任何 org 条目上） |
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/15/para-org-mode-测试配置/index.html</guid>
</item>
<item>
<title>Linux 数据去重学习笔记</title>
<link>https://lujun9972.github.io/blog/2026/04/15/linux-数据去重学习笔记/index.html</link>
<description>记录 Linux 上数据去重（Data Deduplication）的原理、主流方案和实践要点。
参考文章：[[https://www.opensourceforu.com/2026/04/data-deduplication-done-the-right-way/][Data Deduplication Done The Right Way]]

* 什么是数据去重

数据去重（Data Deduplication）的核心思想很简单： *每个唯一的数据块只存一份，在需要重复使用时通过引用来共享* 。

一个典型的场景：如果你每天对同一台虚拟机做快照，连续做 30 天，你并不是真的存了 30 份完全不同的数据——绝大多数数据块都是重复的。去重就是把这些重复的部分消除掉。

在 Linux 上，通过现代文件系统（如 Btrfs、ZFS）、块设备层方案（如 dm-vdo）以及用户空间工具，可以实现不同层级的数据去重。

* 去重的工作原理

去重分为两个大步骤：先决定如何切割数据，再判断哪些数据已经存在。

** 1. 分块（Chunking）

数据首先被切成小块。有两种主要策略：

+ *固定大小分块（Fixed-size）* ：按固定字节数切分，简单高效，但当文件中间插入或删除字节时，后续所有块的边界都会偏移。
+ *内容定义分块（Content-Defined Chunking, CDC）* ：根据数据流中的模式来选择块的边界，这样即使文件中间有增删，也只有受影响的少量块发生变化，其余块的边界保持不变。CDC 的去重效率通常更高。

** 2. 指纹（Fingerprinting）

每个数据块计算一个指纹（通常使用加密哈希，如 SHA-256），这个指纹相当于数据块的&quot;身份证&quot;。

** 3. 索引（Indexing）

所有指纹存储在一个索引中。当新数据块到来时，系统检查其指纹是否已存在于索引中。

** 4. 复用（Reuse）

如果发现重复，系统不会再次写入该数据块，而是创建或更新引用计数。

** Inline vs Post-process

+ *Inline 去重* ：在写入路径上实时进行，数据在落盘前就被检查。优点是即时节省空间，缺点是增加了 CPU、内存和 I/O 开销。
+ *Post-process 去重* ：写入时不干预，等系统空闲时再由扫描器处理。优点是写入速度快，代价是延迟的去重效果。

* Linux 上的去重方案

Linux 没有一个统一的&quot;去重开关&quot;，而是需要根据你的场景选择合适的层级。

** Btrfs：CoW 文件系统 + 用户空间工具

Btrfs 是一个现代化的 Copy-on-Write（CoW）文件系统，在克隆和快照时天然不会重复存储相同的数据块。通过 reflink（=cp --reflink=）可以创建即时逻辑副本，共享数据块直到被修改。

但 Btrfs *不会自动进行 inline 去重* ，需要借助用户空间工具：

+ *duperemove* ：批量扫描工具，计算 extent 或 block 的哈希，在数据库中查找重复项，然后请求 Btrfs 将它们合并为共享 extent。适合在备份卷上定期运行。

  #+BEGIN_SRC shell
    # 扫描指定目录（只读模式，查看哪些文件可去重，不实际执行）
    duperemove -r /path/to/backup

    # 实际执行去重（-d 表示 dedupe）
    duperemove -r -d /path/to/backup
  #+END_SRC

+ *bees* ：一个长期运行的守护进程，增量遍历文件系统树并在后台持续去重。适合需要持续去重的场景。

管理员在 VM 集合、容器层和 ISO 库上报告了很高的空间节省率。但需要注意： *过于激进的去重加上碎片化可能会降低繁忙卷上的随机读取性能* 。

** ZFS on Linux：原生 inline 块级去重

OpenZFS 提供了原生的 inline 块级去重。每个写入操作都经过以下流程：

1. 对数据块计算哈希
2. 在去重表（Dedup Table, DDT）中查找
3. 如果已存在，增加引用计数而非写入新数据

  #+BEGIN_SRC shell
    # 在指定 dataset 上启用去重
    zfs set dedup=on poolname/dataset

    # 查看去重状态
    zfs get dedup poolname/dataset
  #+END_SRC

  #+BEGIN_SRC org
NAME      PROPERTY  VALUE          SOURCE
poolname  dedup     on             local
  #+END_SRC

  #+BEGIN_SRC shell
    # 查看 DDT（去重表）的统计信息
    zpool status -D poolname
  #+END_SRC

  #+BEGIN_SRC org
pool: poolname
 state: ONLINE
config:

	NAME                 STATE     READ WRITE CKSUM
	poolname             ONLINE       0     0     0
	  /dev/sdb1          ONLINE       0     0     0

errors: No known data errors

 dedup: no DDT entries
  #+END_SRC

ZFS 去重的优势是不需要运行外部扫描器。风险在于 DDT 主要驻留在 RAM 中，其大小随唯一块数量增长（而非逻辑容量）。

一个经验法则： *在 VM 启动盘场景下常见 3:1 到 5:1 的去重率，但需要精心规划内存——根据块大小和工作负载，每几 TB 唯一数据可能需要几 GB 的 RAM* 。

近期 ZFS 社区的改进包括：基于时间的旧条目修剪（Age-based pruning）、以及尝试利用空闲资源的&quot;机会性&quot;去重模式。

最佳实践： *将去重限定在特定用途的 pool 上* （如专门存放 VM 镜像的 pool），而非对所有 dataset 一刀切地开启。

** dm-vdo：文件系统之下的去重

dm-vdo（Virtual Data Optimizer）是一个 device-mapper 目标，位于文件系统之下，提供 inline 块级去重、压缩、零块消除和精简配置。

  #+BEGIN_SRC shell
    # 安装 vdo
    sudo pacman -S vdo  # Arch Linux
    # 或
    sudo apt install vdo  # Debian/Ubuntu

    # 创建 VDO 卷（逻辑大小可以大于物理大小）
    sudo vdo create --name=myvdo --device=/dev/sdb1 --vdoLogicalSize=100G

    # 在 VDO 卷上创建文件系统
    sudo mkfs.xfs /dev/mapper/myvdo

    # 挂载
    sudo mount /dev/mapper/myvdo /mnt/vdo

    # 查看 VDO 卷的统计信息
    sudo vdostats --human-readable myvdo
  #+END_SRC

VDO 的设计将职责分离为：
+ UDS（去重索引）：快速识别重复块
+ 引用计数的数据存储：映射逻辑块到物理块

因为 VDO 工作在块设备层，所以它是 *文件系统无关的* —— 你可以在上面运行 XFS、ext4 或 Btrfs。这对于不方便更换文件系统但又急需节省存储空间的场景特别有用。

* 去重的利与弊

** 收益场景

+ *备份* ：研究和厂商报告显示，备份工作负载通常能获得 60-80% 的逻辑空间缩减，因为日备份或周备份之间共享大量相同区域。
+ *VDI 和虚拟机* ：一组相似的虚拟机共享内核、库和基础镜像，去重率通常可达 3:1 或更高。
+ *网络与云* ：客户端去重可以避免发送重复数据，减少 WAN 使用量。

** 风险与代价

+ *碎片化* ：当多个逻辑块映射到共享物理块的拼图时，顺序读取可能退化为随机 I/O。这对基于 HDD 的阵列和冷存储影响尤其严重。
+ *元数据和内存* ：去重索引随唯一块数量增长。如果索引超出 RAM 容量，查找会溢出到磁盘，性能急剧下降。
+ *加密冲突* ：应用层加密（写入前加密）会有效破坏去重，因为相同的明文会产生不同的密文。加密去重需要特殊方案才能使密文保持可比较。

一个实用的经验法则： *去重非常适合备份和镜像类数据，对延迟敏感的主数据库有风险，对已经压缩或重度加密的数据则几乎无用* 。

* 实践建议

** 识别&quot;低垂果实&quot;

优先寻找以下工作负载：
+ 包含大量相似完整备份的大型备份仓库
+ VM 镜像库、黄金镜像和实验快照
+ ISO 镜像源、容器基础层、多分支代码仓库

这些工作负载天然包含大量重复块，且通常不像事务数据库那样对延迟敏感。

** 选择合适的层级

+ 如果已经在 Btrfs 上做备份盘，可以用 =duperemove= 每周运行或 =bees= 持续运行。先在测试目录上测量空间节省率和运行时间。
+ 如果想在 SAN 或本地 RAID 上实现文件系统无关的去重，可以在测试 LUN 上评估 =dm-vdo=，在上面叠加 XFS 或 ext4，然后重放典型备份工作负载对比逻辑使用量与物理使用量。
+ 如果用 ZFS 做 VM 池，只在包含高度相似数据的 dataset 上启用去重，并持续监控 DDT 大小、RAM 使用量和读取延迟。

** 结合快照策略

CoW 文件系统（Btrfs/ZFS）上的混合策略：
1. 定期在 CoW 文件系统上做快照
2. 对较老的快照进行更激进的去重，保持最新一代相对无碎片
3. 清理快照时优先删除碎片化严重的较老世代

这样把碎片化的代价推向很少被读取的旧数据，保持最近备份的快速可读性。

** 关注关键指标

+ 逻辑使用量 vs 物理使用量（有效去重 + 压缩率）
+ 备份和恢复任务的平均延迟和尾部延迟
+ 去重索引大小和内存占用（ZFS 的 DDT 大小、VDO 的索引大小、duperemove 的数据库大小）

如果去重率低于约 1.2:1，而你在付出显著的性能或复杂度代价，那么这个工作负载可能不值得做去重。

* 未来方向

Linux 存储去重的发展趋势：

+ *更快的 CDC 算法* （如 RapidCDC 及更新的变体），在保持高去重率的同时大幅降低 CPU 开销
+ *面向 NVM 和 SSD 优化的 inline 去重* ，随机 I/O 惩罚更小，元数据可以驻留在持久内存中
+ *容器感知的去重引擎* ，理解 Docker/OCI 层语义，在数千个镜像间避免冗余副本而不影响 pull 性能

去重将越来越不像一个昂贵的实验，而更像一个可预测的基础构建块——尤其在备份、VM 和容器镜像平台中。
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/15/linux-数据去重学习笔记/index.html</guid>
</item>
<item>
<title>Unix 系统中那些被埋没的配置开关——以 FontConfig 为例</title>
<link>https://lujun9972.github.io/blog/2026/04/14/unix-系统中那些被埋没的配置开关——以-fontconfig-为例/index.html</link>
<description>* 前言

本文灵感来自 [[https://venam.net/blog/unix/2025/04/18/mechanism_policy.html][Buried Toggles Begging to be Brought to Light]]（作者：Patrick Louis），文章探讨了一个 Unix 世界里的经典问题：很多系统软件提供了极其强大的配置能力，但这些能力被深埋在复杂的配置文件和晦涩的命令行选项中，几乎没有人知道它们的存在。

* Mechanism, Not Policy

Unix 设计哲学中有一条广为人知的信条：*&quot;提供机制，而非策略&quot;（Mechanism, not policy）*。意思是系统应该提供灵活的底层能力，而把&quot;怎么用&quot;的决定权留给用户。

这个理念催生了无数强大的工具，但也带来了一个副作用。Henry Spencer 曾一针见血地指出：

#+BEGIN_QUOTE
这种态度的缺点是，人们倾向于认为一旦完成了高度可配置和富有表现力的接口，工作就算完成了……即使结果是其他任何人如果不经过漫长研究几乎不可能使用。可配置性的反面是迫切需要良好的默认值和一种简单的方式将一切重置为默认值。表达力的反面是需要指导——无论是在程序中还是在文档中——关于如何入门以及如何实现最常见的需求。
#+END_QUOTE

ESR 在 *The Art of Unix Programming* 中也表达了类似的观点：

#+BEGIN_QUOTE
当人们说一个用户界面是直观的，他们的意思是它 (a) 可发现、(b) 使用时透明、(c) 遵循最小惊讶原则。
#+END_QUOTE

venam 在文章中列举了 Linux 系统中 9 个&quot;被埋没的配置开关&quot;的例子。本文将以 *FontConfig* 为重点深入探讨，其余领域简要带过。

* FontConfig：你可能从未了解过的字体魔法

** 大多数人只知道 fc-list

如果你问一个 Linux 用户怎么管理字体，大概率会得到这样的回答：

#+BEGIN_SRC sh
fc-list | head
#+END_SRC

#+RESULTS:
#+BEGIN_EXAMPLE
/usr/share/fonts/100dpi/courB18.pcf.gz: Adobe Courier:style=Bold
/usr/share/fonts/100dpi/helvBO18.pcf.gz: Adobe Helvetica:style=Bold Oblique
/usr/share/fonts/100dpi/UTRG__12.pcf.gz: Adobe Utopia:style=Regular
/usr/share/fonts/gsfonts/D050000L.otf: D050000L:style=Regular
/usr/share/fonts/100dpi/timB12-ISO8859-1.pcf.gz: Adobe Times:style=Bold
#+END_EXAMPLE

这确实是大多数人使用 FontConfig 的方式——列出系统里有哪些字体，然后就没了。

** FontConfig 的配置格式

但实际上，FontConfig 拥有一套极其强大的 XML 配置格式，可以对字体进行 *匹配和属性编辑* 。配置文件位于 =/etc/fonts/fonts.conf= 和 =/etc/fonts/conf.d/= 目录下。

#+BEGIN_SRC sh
ls /etc/fonts/conf.d/ | head
#+END_SRC

#+RESULTS:
#+BEGIN_EXAMPLE
10-hinting-slight.conf
10-scale-bitmap-fonts.conf
10-yes-antialias.conf
11-lcdfilter-default.conf
20-unhint-small-dejavu-lgc-sans.conf
20-unhint-small-dejavu-lgc-sans-mono.conf
20-unhint-small-dejavu-lgc-serif.conf
20-unhint-small-dejavu-sans.conf
20-unhint-small-dejavu-sans-mono.conf
20-unhint-small-dejavu-serif.conf
#+END_EXAMPLE

每个配置文件都是一个 XML 文档，核心结构是 =&lt;match&gt;= 元素，包含 =&lt;test&gt;=（匹配条件）和 =&lt;edit&gt;=（编辑操作）：

#+BEGIN_SRC fundamental
&lt;match target=&quot;font&quot;&gt;
  &lt;test name=&quot;family&quot;&gt;
    &lt;string&gt;Monospace&lt;/string&gt;
  &lt;/test&gt;
  &lt;edit name=&quot;hintstyle&quot; mode=&quot;assign&quot;&gt;
    &lt;const&gt;hintslight&lt;/const&gt;
  &lt;/edit&gt;
&lt;/match&gt;
#+END_SRC

这段配置的意思是：对所有 Monospace 族字体，将 hintstyle 设置为 hintslight。

** 预置的优化配置

你的系统上很可能已经有一套预置的字体优化配置，但大多数人从未留意过：

#+BEGIN_SRC sh
ls /usr/share/fontconfig/conf.avail/
#+END_SRC

#+RESULTS:
#+BEGIN_EXAMPLE
05-reset-dirs-sample.conf
09-autohint-if-no-hinting.conf
10-autohint.conf
10-hinting-full.conf
10-hinting-medium.conf
10-hinting-none.conf
10-hinting-slight.conf
10-no-antialias.conf
10-scale-bitmap-fonts.conf
10-sub-pixel-bgr.conf
10-sub-pixel-none.conf
10-sub-pixel-rgb.conf
10-sub-pixel-vbgr.conf
10-sub-pixel-vrgb.conf
10-unhinted.conf
10-yes-antialias.conf
11-lcdfilter-default.conf
11-lcdfilter-legacy.conf
11-lcdfilter-light.conf
11-lcdfilter-none.conf
...（共 70+ 个预置配置文件）
#+END_EXAMPLE

这些预置配置涵盖了各种渲染优化场景，如 LCD 滤波器设置、像素间距建议、特定语言的字体回退等。

** fc-match：查看字体匹配真相

=fc-match= 是理解 FontConfig 行为的关键工具：

#+BEGIN_SRC sh
fc-match Sans
#+END_SRC

#+RESULTS:
#+BEGIN_EXAMPLE
wqy-zenhei.ttc: &quot;文泉驿正黑&quot; &quot;Regular&quot;
#+END_EXAMPLE

这会告诉你当你请求 Sans 字体时，系统实际会使用什么字体。加上 =-s= 参数可以看到完整的替换链：

#+BEGIN_SRC sh
fc-match -s Sans | head -5
#+END_SRC

#+RESULTS:
#+BEGIN_EXAMPLE
wqy-zenhei.ttc: &quot;文泉驿正黑&quot; &quot;Regular&quot;
DejaVuSans.ttf: &quot;DejaVu Sans&quot; &quot;Book&quot;
DejaVuSans-Bold.ttf: &quot;DejaVu Sans&quot; &quot;Bold&quot;
DejaVuSans-Oblique.ttf: &quot;DejaVu Sans&quot; &quot;Oblique&quot;
DejaVuSans-BoldOblique.ttf: &quot;DejaVu Sans&quot; &quot;Bold Oblique&quot;
#+END_EXAMPLE

** fc-pattern：调试字体属性

=fc-pattern= 可以让你看到字体的完整属性列表：

#+BEGIN_SRC sh
fc-match --verbose Sans | head -20
#+END_SRC

#+RESULTS:
#+BEGIN_EXAMPLE
Pattern has 45 elts (size 48)
	family: &quot;文泉驿正黑&quot;(w) &quot;WenQuanYi Zen Hei&quot;(w) &quot;文泉驛正黑&quot;(w)
	familylang: &quot;zh-cn&quot;(s) &quot;en&quot;(w) &quot;zh-tw&quot;(w)
	style: &quot;Regular&quot;(s)
	stylelang: &quot;en&quot;(s)
	fullname: &quot;文泉驿正黑&quot;(w) &quot;WenQuanYi Zen Hei&quot;(w) &quot;文泉驛正黑&quot;(w)
	fullnamelang: &quot;zh-cn&quot;(s) &quot;en&quot;(w) &quot;zh-tw&quot;(w)
	slant: 0(i)(s)
	weight: 100(f)(s)
	width: 100(f)(s)
	size: 12(f)(s)
	pixelsize: 12.5(f)(s)
	spacing: 0(i)(w)
	foundry: &quot;WenQ&quot;(s)
	antialias: True(w)
	hintstyle: 0(i)(w)
	hinting: True(w)
	verticallayout: False(s)
	autohint: False(w)
	globaladvance: False(w)
#+END_EXAMPLE

** 为什么这么强大的能力几乎没人知道？

venam 指出，目前的字体相关界面仅限于显示已安装字体列表和不同尺寸的预览。曾经有一个叫 *fontweak* 的项目试图让 FontConfig 的配置更加可发现，但功能有限且无法展示全局视图。

FontConfig 的困境是典型的 *&quot;mechanism without discoverable policy&quot;* : 它提供了强大的匹配-编辑机制，但用户需要一个直观的界面来回答&quot;我的系统当前对字体到底做了什么？&quot;

* 其他被埋没的配置

venam 在文章中还列举了其他几个类似的例子，这里简要概述。

** Audio Restore List

PulseAudio 和 PipeWire 会记住每个设备、每个音频流的最后音量设置。但这个信息是隐藏的——当你插上耳机或打开视频时，你无法预知音量会是多少。可能震耳欲聋，也可能完全静音。

** Journald

=systemd.journal-fields(7)= 定义了一套固定的字段体系，Journald 本质上是一个固定列/字段的线性存储。但大多数人只会随机敲命令直到找到想要的结果，或者死记硬背一两个 =--unit &lt;service&gt; --reverse= 之类的技巧。

** sudo

每个人都知道 =sudo= ，但很少有人能解释 =sudo -ll= 的输出，或者完整配置 sudoers 文件。sudoers 文件包含默认全局设置、别名和匹配规则，但唯一的编辑器 =visudo= 对新手极其不友好。

** Polkit

Policy Kit 是一个很棒的桌面安全概念：标准化的动作和服务，通过间接总线触发，由集中的策略执行器决定调用者是否被允许。但它的规则和 JavaScript 格式的配置散落在系统各处，创建新的动作和规则非常困难。

** udev

udev 规则是另一个&quot;配置散落&quot;的典型。它有匹配模式和动作执行（甚至支持 =goto= 和标签跳转，是图灵完备的）。唯一&quot;可视化&quot;这些配置的方法是：

#+BEGIN_SRC sh
systemd-analyze cat-config udev/rules.d
#+END_SRC

#+RESULTS:
#+BEGIN_EXAMPLE
# /usr/lib/udev/rules.d/01-md-raid-creating.rules
# do not edit this file, it will be overwritten on update
# While mdadm is creating an array, it creates a file
# /run/mdadm/creating-mdXXX.  If that file exists, then
# the array is not &quot;ready&quot; and we should make sure the
# content is ignored.

KERNEL==&quot;md*&quot;, TEST==&quot;/run/mdadm/creating-$kernel&quot;, ENV{SYSTEMD_READY}=&quot;0&quot;

# /usr/lib/udev/rules.d/10-dm.rules
...
#+END_EXAMPLE

* 为什么这个问题难以解决

venam 在文章结尾提到了人们面对复杂配置的三种态度：

1. *保持默认* —— 用容器化/不可变系统的方式，一切保持出厂设置
2. *精雕细琢（RICE）* —— 乐在其中地折腾每一项配置
3. *探索中间地带* —— 尝试让隐藏的能力更容易被发现和使用

FontConfig 的例子很好地说明了核心矛盾：可配置性和可发现性之间存在天然的张力。提供更多配置选项意味着更复杂的界面，而简化界面又意味着放弃一些能力。

但也许问题不在于&quot;要不要简化&quot;，而在于&quot;如何让用户知道这些选项的存在&quot;。一个好的界面不一定需要展示所有选项，但它应该让用户在需要时能够 *发现* 选项的存在。这正是目前大多数 Linux 系统工具所缺失的。

正如 ESR 所说，一个好的界面应该是可发现的、透明的、符合最小惊讶原则的。我们还有很长的路要走。
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/14/unix-系统中那些被埋没的配置开关——以-fontconfig-为例/index.html</guid>
</item>
<item>
<title>Linux 输入栈全景解析：从硬件按键到屏幕响应</title>
<link>https://lujun9972.github.io/blog/2026/04/14/linux-输入栈全景解析：从硬件按键到屏幕响应/index.html</link>
<description>* 前言

本文整理自 [[https://venam.net/blog/unix/2025/11/27/input_devices_linux.html][The Input Stack on Linux — An End-To-End Architecture Overview]]（作者：Patrick Louis），是对 Linux 输入设备栈的一次系统性学习（很多内容不是很懂，依赖AI补充知识点，不确定正确率）。

当我们按下键盘上的一个键，到屏幕上出现对应的字符，中间经历了怎样的旅程？这篇文章试图回答这个问题。

Linux 的输入处理大致分为三层：

- **内核层**：硬件、总线、输入核心子系统（input core）
- **暴露层**：evdev 事件抽象、devtmpfs 设备节点、sysfs 属性、procfs 调试接口
- **用户空间层**：udev 设备管理、libinput 事件处理库、XKB 键盘映射、X11/Wayland 图形栈

让我们逐层深入。

* 内核的 Input Core

** input core 是什么

input core 是内核中负责管理输入设备及其事件的核心子系统，代码位于 =drivers/input/input.c= 。它提供了：

- 设备的分配与注册（ =input_allocate_device=, =input_register_device= ）
- 事件的分发机制：驱动通过 =input_event= 推送事件，input core 以扇出（fan-out）方式转发给所有注册的 handler
- 默认的 evdev handler 会将事件标准化后暴露到用户空间的 =/dev/input/eventX=

简单来说，input core 充当了硬件驱动和用户空间之间的中间层，实现了一种发布-订阅模式。

** 事件分发流程

#+BEGIN_SRC fundamental
驱动
  → input_event()
    → input core (遍历 input_handler_list)
      → handler.events() (如 evdev)
        → handler 遍历其 input_handle 链表
          → 通过字符设备 /dev/input/eventX 暴露给用户空间
#+END_SRC

[注] handler 和 handle 的命名很容易混淆：
- =input_handler=：事件处理模块（如 evdev、kbd、joydev），注册在 input core 上，实现 =events= 回调
- =input_handle=：内核中的一个绑定结构，连接一个 =input_dev= 和一个 =input_handler=，表示&quot;这个设备被这个 handler 处理&quot;。一个 handler 可以有多个 handle（对应多个设备）

用户空间的程序通过打开 handler 创建的字符设备文件来读取事件。

** 关键数据结构

- =struct input_dev=: 代表一个输入设备，包含设备名称、能力位图（支持哪些事件类型）等信息
- =struct input_handler=: 事件处理接口，核心是 =events= 回调函数，负责接收并转发事件
- =struct input_handle=: 连接一个 =input_dev= 和一个 =input_handler= 的桥梁，每个 handle 代表一组设备-处理器的绑定关系

* 设备拓扑路径

一个 USB 键盘在内核中的路径可能像这样：

#+BEGIN_SRC fundamental
/devices/pci0000:00/0000:00:14.0/usb1/1-1/1-1:1.0/0003:046D:C31C.0003/input/input6/event3
#+END_SRC

从左到右解读：
1. =pci0000:00/0000:00:14.0= — PCI 总线上的 USB 主控制器
2. =usb1/1-1/1-1:1.0= — USB 总线和端口层级
3. =0003:046D:C31C.0003= — HID 设备节点（总线 0003=USB HID，厂商 046D=Logitech）
4. =input/input6= — input 子系统设备
5. =event3= — evdev 接口，对应的字符设备 =/dev/input/event3=

** 内核对象层级

内核通过以下对象类型来管理系统中的设备：

| 对象      | 说明                                     |
|-----------+------------------------------------------|
| bus       | 总线，设备连接的基础                     |
| device    | 物理或逻辑设备                           |
| driver    | 与设备关联的驱动程序                     |
| class     | 按功能分类的设备类别（如 input、disk）   |
| subsystem | 系统结构视图，我们关心的是 input 子系统  |

** MODALIAS 与热插拔

内核通过 MODALIAS 机制实现驱动自动加载：

1. 设备接入时，内核构造 MODALIAS 字符串（包含厂商 ID、产品 ID 等）
2. 通过 uevent（基于 netlink）发送到用户空间
3. udev 收到后调用 =modprobe= 加载对应模块

查看设备的 MODALIAS：

#+BEGIN_SRC sh
cat /sys/class/input/event3/device/id/modalias
# 或通过 udevadm
udevadm info --query=property /dev/input/event3 | grep MODALIAS
#+END_SRC

#+RESULTS:

* sysfs 文件系统

sysfs 是内核对象在用户空间的文件系统映射，通常挂载在 =/sys/=。

对于输入设备，重要的目录包括：

| 目录               | 说明                                       |
|--------------------+--------------------------------------------|
| =/sys/devices/=    | 设备的层级结构，反映真实的硬件拓扑        |
| =/sys/class/input/= | 所有 input 类设备的符号链接              |
| =/sys/bus/=        | 按总线类型组织的设备视图                  |
| =/sys/module/=     | 已加载的内核模块信息                      |

实用命令：

#+BEGIN_SRC sh
# 查看设备的层级关系
udevadm info --tree

# 查看 input 设备的能力
cat /sys/class/input/event3/device/capabilities/ev

# 通过 procfs 查看所有输入设备
cat /proc/bus/input/devices
#+END_SRC

=cat /proc/bus/input/devices= 是非常有用的调试命令，它会列出所有输入设备及其绑定的 handler（如 event3、kbd 等）。

* HID — 人机接口设备

HID（Human Interface Device）是当今最重要的输入/输出标准协议。大多数新设备（鼠标、键盘、麦克风等）都通过 USB、I2C、Bluetooth 等传输层使用 HID 协议。

HID 的核心思想：设备先通过 =Report Descriptor= 描述自己的能力（有哪些按钮、轴等），然后按照约定的 =Report= 格式发送/接收数据。这意味着不需要为每种新设备写专门的驱动。

** HID Report Descriptor 示例

一个鼠标的 Report Descriptor 可能描述：

#+BEGIN_SRC fundamental
Usage Page (Button)     ; 按钮页面
  Usage Minimum (1)     ; 按钮 1
  Usage Maximum (5)     ; 按钮 5
  Logical Minimum (0)
  Logical Maximum (1)
  Report Count (5)      ; 5 个按钮
  Report Size (1)       ; 每个按钮 1 bit
  Input (Data, Variable, Absolute)  ; 绝对值

  Report Count (1)      ; 填充
  Report Size (3)
  Input (Constant)      ; 常量，被忽略

Usage Page (Generic Desktop)
  Usage (X)
  Usage (Y)
  Logical Minimum (-127)
  Logical Maximum (127)
  Report Size (8)
  Report Count (2)
  Input (Data, Variable, Relative)  ; 相对值
#+END_SRC

** HID 调试工具

#+BEGIN_SRC sh
# 解码 HID Report Descriptor
hid-decode /sys/bus/hid/devices/0003:046D:C31C.0003/report_descriptor

# 从 hidraw 设备读取原始 HID 数据
cat /dev/hidraw0 | hexdump -C
#+END_SRC

#+RESULTS:

** HID Core 子系统

HID Core 管理设备的生命周期，解析 Report Descriptor，然后将 Report 分发给注册在 HID 总线上的驱动。最重要的是 =hid-input=，它会将 HID Report 翻译为 evdev 事件，并注册 input 设备。

* evdev — 事件设备

evdev 是 input core 的默认 handler，它将各种输入设备的事件标准化后暴露给用户空间。

** evdev 事件格式

#+BEGIN_SRC c
struct input_event {
    struct timeval time;  // 时间戳
    __u16 type;           // 事件类型
    __u16 code;           // 事件代码
    __s32 value;          // 事件值
};
#+END_SRC

常见事件类型：

| 类型    | 说明                               |
|---------+------------------------------------|
| EV_KEY  | 按键和按钮                         |
| EV_REL  | 相对事件（鼠标移动、滚轮）         |
| EV_ABS  | 绝对事件（触摸屏坐标）             |
| EV_SYN  | 同步事件，标记一帧事件的结束       |

一个触摸板的 evdev 帧示例：

#+BEGIN_SRC fundamental
type: EV_ABS  code: ABS_X         value: 1234
type: EV_ABS  code: ABS_Y         value: 5678
type: EV_ABS  code: ABS_PRESSURE  value: 50
type: EV_SYN  code: SYN_REPORT    value: 0
#+END_SRC

** 设备能力

每个输入设备在注册时会声明自己的能力（capabilities），可通过 sysfs 查看：

#+BEGIN_SRC sh
ls /sys/class/input/input6/capabilities/
# ev  key  rel  abs  led  sw  ff  msc  snd
#+END_SRC

更方便的方式是使用 =libinput record=，它会以人类可读的格式展示设备的所有信息。

** evdev 调试工具

#+BEGIN_SRC sh
# 使用 evtest 查看事件
sudo evtest /dev/input/event3

# 使用 libinput record 记录事件
sudo libinput record /dev/input/event3

# 使用 evemu 录制和回放
sudo evemu-record /dev/input/event3
sudo evemu-play /dev/input/event3 &lt; recording.txt
#+END_SRC

* udev 与 hwdb

** udev 的职责

udev（systemd-udevd）是用户空间的动态设备管理器，它监听内核的 uevent 并执行相应操作：

- 根据 MODALIAS 加载内核模块
- 设置设备节点的访问权限
- 为设备附加属性
- 创建符号链接，使设备名称更可预测
- 通过规则系统在设备插拔时执行自定义操作

** udev 规则基础

规则文件存放在：
- =/usr/lib/udev/rules.d/= （系统默认）
- =/etc/udev/rules.d/= （管理员自定义，优先级最高）

一个简单的规则示例：当特定键盘插入或拔出时执行脚本：

#+BEGIN_SRC fundamental
ACTION==&quot;add|remove&quot;, SUBSYSTEM==&quot;input&quot;, ATTRS{idVendor}==&quot;1234&quot;, ATTRS{idProduct}==&quot;5678&quot;, RUN+=&quot;/path/to/script.sh&quot;
#+END_SRC

常用的匹配键：=KERNEL=, =SUBSYSTEM=, =ACTION=, =ATTR{...}=, =ENV{...}=

常用的赋值键：=SYMLINK=, =ENV{...}=, =RUN+=, =MODE=, =OWNER=, =GROUP=

** udevadm 常用命令

#+BEGIN_SRC sh
# 查看设备的属性层级
udevadm info -a /dev/input/event3

# 实时监控 uevent
udevadm monitor

# 测试规则匹配
udevadm test /dev/input/event3

# 查看所有已加载的规则
systemd-analyze cat-config udev/rules.d
#+END_SRC

** hwdb — 硬件数据库

hwdb 是 udev 的硬件属性查找表，以 =.hwdb= 文件定义，编译为 =hwdb.bin= 用于快速检索。

更新 hwdb：

#+BEGIN_SRC sh
sudo systemd-hwdb update
sudo udevadm trigger
#+END_SRC

* libinput

libinput 是现代 Linux 图形环境的统一输入处理库，被 Xorg（通过驱动）和 Wayland 合成器广泛使用。它封装了 udev 和 evdev，提供设备检测、事件处理、以及各种输入处理的抽象。

** 为什么需要 libinput

1. **统一性**：避免 X11 各个 xf86 输入驱动中的重复逻辑
2. **处理硬件缺陷**：许多设备的固件报告错误的 HID 描述符，libinput 通过 quirks 机制修正
3. **复杂输入处理**：触摸板多点触控追踪、按钮防抖、手势识别等

** libinput 的主要功能

- 按钮防抖（debouncing）
- 触摸板软件按钮（clickpad）
- 触摸板压力检测
- 手掌和拇指检测
- 滚动（两指滚动、边缘滚动）
- 点击行为（tap-to-click）
- 手势（滑动、捏合）
- 指针加速度

** Seat 的概念

Seat 是与用户会话关联的输入设备集合。大多数机器只有一个 seat，但在多座位场景下一个机器可以有多个用户同时使用不同的输入设备。

seat 通过 udev 的 =ENV{ID_SEAT}= 属性分配，可通过 =loginctl= 管理。

** libinput 调试工具

#+BEGIN_SRC sh
# 列出所有设备及其配置
libinput list-devices

# 调试所有事件
libinput debug-events

# 图形化调试触摸板
libinput debug-gui

# 录制和回放设备事件（用于 bug 报告）
libinput record /dev/input/event3
libinput replay recording.yaml
#+END_SRC

* 键盘处理细节

** 扫描码到键码

键盘处理涉及三层映射：

#+BEGIN_SRC fundamental
Scancode (硬件原始码) → Keycode (内核事件码) → Keysym (用户空间符号)
#+END_SRC

- =Scancode=：硬件产生的原始值，如 =0x1E=
- =Keycode=：内核映射后的事件码，如 =KEY_A=（定义在 =input-event-codes.h=）
- =Keysym=：用户空间的符号，如 &quot;a&quot; 或 &quot;A&quot;

键码映射可通过 hwdb 在运行时修改：

#+BEGIN_SRC fundamental
# /etc/udev/hwdb.d/61-custom-keyboard.hwdb
evdev:input:b0003v046DpC31C*
 KEYBOARD_KEY_70039=leftalt   # 将 CapsLock 映射为左 Alt
#+END_SRC

** 控制台键盘

在文本控制台中，kbd handler（=drivers/tty/vt/keyboard.c=）负责处理键盘输入，它通过 TTY 和 line discipline 机制工作。相关工具：

#+BEGIN_SRC sh
# 查看扫描码
showkey --scancodes

# 设置控制台键盘布局
loadkeys us

# 查看当前键盘模式
kbd_mode

# 设置键盘重复率
kbdrate
#+END_SRC

** XKB — 键盘布局系统

XKB（X Keyboard）是处理 keycode 到 keysym 映射的用户空间库和配置系统。虽然名字中有 &quot;X&quot;，但它也被 Wayland 和各种图形工具包使用。

XKB 的核心概念：

| 概念      | 说明                                               |
|-----------+----------------------------------------------------|
| RMLVO     | Rules-Model-Layout-Variant-Options，用户友好的配置 |
| KcCGST    | Keycodes-Compat-Geometry-Symbols-Types，内部组件   |
| Levels    | 按键的不同层级（如按 Shift 时切换到 Level 2）     |
| Groups    | 完全切换键盘布局（如切换到日语布局）              |
| Modifiers | 修饰键状态（Shift、Ctrl、Alt 等）                 |

[注] XKB 的 keycode = evdev keycode + 8，这是一个历史遗留的偏移量。

*** RMLVO 到 KcCGST 的解析过程

用户选择 RMLVO（如 layout=us, variant=dvorak），XKB 通过 rules 文件将其解析为 KcCGST 组件，编译成完整的 keymap。

#+BEGIN_SRC sh
# 查看当前 RMLVO 解析结果
xkbcli compile-keymap --layout us --variant dvorak

# 列出所有可用的布局
xkbcli list

# 交互式调试按键
xkbcli interactive-evdev

# 查看如何输入特定字符
xkbcli how-to-type --keysym &quot;eacute&quot;
#+END_SRC

*** Compose 键

XKB 的 Compose 功能允许通过组合键输入特殊字符，配置文件位于：

#+BEGIN_SRC sh
# 系统 Compose 配置
/usr/share/X11/locale/*/Compose

# 用户自定义
~/.XCompose
#+END_SRC

示例：=AltGr+&#39;= + =e= → =é=

* 指针设备细节

** 触摸板类型

| 类型        | 说明                                                 |
|-------------+------------------------------------------------------|
| Clickpad    | 整个触摸板可按下，无独立按钮（INPUT_PROP_BUTTONPAD） |
| Forcepad    | 类似 clickpad，通过压力检测代替物理按钮              |
| Trackpoint  | ThinkPad 中间的小红帽摇杆                            |

** 多点触控（MT）

支持多点触控的触摸板通过 evdev 的 slot 和 tracking ID 机制追踪每个手指：

#+BEGIN_SRC fundamental
slot 0: tracking_id=1  ABS_MT_POSITION_X=100  ABS_MT_POSITION_Y=200
slot 1: tracking_id=2  ABS_MT_POSITION_X=300  ABS_MT_POSITION_Y=400
slot 0: tracking_id=-1  (手指抬起)
#+END_SRC

** 指针加速

libinput 支持三种加速配置：
- =adaptive=（默认）：根据速度动态调整
- =flat=：1:1 线性映射
- 自定义配置

** 游戏手柄

游戏手柄不经过 evdev 和 libinput，而是使用 joydev handler，通过 =/dev/input/jsN= 暴露 =js_event= 格式的事件。

#+BEGIN_SRC sh
# 测试游戏手柄
jstest /dev/input/js0

# 校准
jscal /dev/input/js0
#+END_SRC

* 上层图形栈：X11 与 Wayland

** X11 输入栈

在 X11 中：
1. X Server 通过 =xf86-input-libinput= 驱动使用 libinput
2. 配置通过 =/usr/share/X11/xorg.conf.d/= 中的配置片段
3. 客户端通过 X 协议接收 XInput 事件
4. 键盘映射由 X Server 内部维护，通过 XKB 配置

#+BEGIN_SRC sh
# 列出 X Server 识别的输入设备
xinput --list

# 查看设备属性
xinput --list-props &lt;device_id&gt;

# 监控 X 事件
xev

# 设置 XKB 布局
setxkbmap -layout us -variant dvorak
#+END_SRC

#+RESULTS:

** Wayland 输入栈

在 Wayland 中：
1. 合成器直接使用 libinput，无中间驱动层
2. 客户端必须显式注册监听器来接收事件（更安全）
3. 事件只转发给当前获得焦点的客户端
4. 输入设备列表在协议中不暴露（合成器内部管理）
5. 配置方式因合成器而异

#+BEGIN_SRC sh
# GNOME Wayland 配置示例
gsettings get org.gnome.desktop.input-sources xkb-options
gsettings set org.gnome.desktop.peripherals.touchpad tap-to-click true

# wlroots 合成器通过环境变量设置 XKB
export XKB_DEFAULT_LAYOUT=us
export XKB_DEFAULT_VARIANT=dvorak
#+END_SRC

** Wayland 与 X11 的关键差异

| 方面     | X11                            | Wayland                                    |
|----------+--------------------------------+--------------------------------------------|
| 事件广播 | 全局广播（任何客户端可监听）  | 只发给焦点客户端                           |
| 输入注入 | XTEST 扩展可直接注入事件      | 需要 libei + portal 权限控制               |
| 键盘配置 | X Server 统一管理             | 各合成器自行实现                           |
| 设备列举 | xinput 可列出                  | 协议不暴露，需 libinput list-devices       |

* 虚拟输入与自动化

** uinput — 用户空间输入模拟

uinput 模块允许在用户空间创建虚拟输入设备：

#+BEGIN_SRC c
#include &lt;linux/uinput.h&gt;

// 创建虚拟设备
int fd = open(&quot;/dev/uinput&quot;, O_WRONLY | O_NONBLOCK);
ioctl(fd, UI_SET_EVBIT, EV_KEY);
ioctl(fd, UI_SET_KEYBIT, KEY_A);

struct uinput_setup setup = {.id.bustype = BUS_USB, .name = &quot;virtual&quot;};
ioctl(fd, UI_DEV_SETUP, &amp;setup);
ioctl(fd, UI_DEV_CREATE, 0);

// 发送事件
struct input_event ev = {.type = EV_KEY, .code = KEY_A, .value = 1};
write(fd, &amp;ev, sizeof(ev));
#+END_SRC

** XTEST — X11 测试扩展

XTEST 允许在 X11 中直接注入键盘和鼠标事件，无需 root 权限：

#+BEGIN_SRC sh
# 使用 xdotool 模拟输入
xdotool key a
xdotool mousemove 100 100 click 1
#+END_SRC

** libei — Wayland 的模拟输入

Wayland 通过 libei（Emulated Input）库来处理虚拟输入，架构如下：

#+BEGIN_SRC fundamental
客户端(libei) → xdg-desktop-portal(dbus) → 合成器(EIS侧) → 内部虚拟设备
#+END_SRC

这种方式下，合成器可以控制哪些客户端被允许模拟输入，提供更好的安全性。

相关工具：=ydotool=（基于 uinput）、=wtype=（基于 Wayland 虚拟键盘协议）

* 输入法框架

输入法（Input Method）用于输入键盘上没有的字符（如中文、日文等）。

** IMF 与 IME

- **IMF**（Input Method Framework）：输入法框架，负责管理输入法切换、与工具包集成
- **IME**（Input Method Engine）：具体的输入法引擎，负责将按键序列转换为文字

当今两大主流 IMF：
- **IBus**（GNOME/GTK 默认）
- **Fcitx5**（KDE/Qt 默认）

切换输入法框架的环境变量：

#+BEGIN_SRC sh
# GTK 应用
export GTK_IM_MODULE=ibus    # 或 fcitx

# Qt 应用
export QT_IM_MODULE=ibus     # 或 fcitx

# X 应用
export XMODIFIERS=@im=ibus   # 或 @im=fcitx
#+END_SRC

输入法的处理流程：

#+BEGIN_SRC fundamental
按键事件 → XKB(keycode→keysym) → 工具包 IM 模块 → IMF → IME(候选词) → 提交文本
#+END_SRC

* 总结

从按下键盘到屏幕响应，事件经过了这样的旅程：

#+BEGIN_SRC fundamental
硬件 → 总线驱动 → HID 驱动 → Input Core → evdev handler → /dev/input/eventX
  → udev(权限/属性) → libinput(事件处理) → 合成器/X Server
  → XKB(键盘映射) → 工具包 → 应用程序
#+END_SRC

每一层都有自己的职责，也都有相应的调试工具。理解这个栈对于排查输入相关问题、自定义输入行为都非常有帮助。

* 调试工具速查表

| 层级     | 工具                                                          |
|----------+---------------------------------------------------------------|
| 硬件/总线 | =lsusb -v=, =lspci -vn=, =hwinfo --short=                   |
| HID      | =hid-decode=, =usbhid-dump=, =hid-tools=                    |
| evdev    | =evtest=, =evemu-record=, =libinput record=                 |
| sysfs    | =cat /proc/bus/input/devices=, =udevadm info=               |
| udev     | =udevadm monitor=, =udevadm test=, =udevadm info=          |
| libinput | =libinput list-devices=, =libinput debug-events=, =libinput debug-gui= |
| XKB      | =xkbcli list=, =xkbcli interactive-evdev=, =xkbcli how-to-type= |
| X11      | =xinput=, =xev=, =setxkbmap=, =xdotool=                     |
| Wayland  | =wev=, =gsettings=, =loginctl seat-status=                  |
| 游戏手柄 | =jstest=, =jscal=                                            |

* 参考资料

- [[https://venam.net/blog/unix/2025/11/27/input_devices_linux.html][The Input Stack on Linux — An End-To-End Architecture Overview]]（本文主要参考来源）
- [[https://who-t.blogspot.com/][who-t (Peter Hutterer) 的博客]]（libinput 和输入系统的核心开发者）
- [[https://linuxtouchpad.org/docs/][Linux Touchpad 文档]]
- [[https://wiki.archlinux.org/title/Keyboard_input][ArchWiki: Keyboard input]]
- [[https://docs.kernel.org/hid/hidintro.html][内核 HID 子系统文档]]
- [[https://xkbcommon.org/doc/current/keymap-text-format-v1-v2.html][XKB Keymap 格式规范]]
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/14/linux-输入栈全景解析：从硬件按键到屏幕响应/index.html</guid>
</item>
<item>
<title>创建跨平台 ZIP 文件的隐藏陷阱：Extra Field</title>
<link>https://lujun9972.github.io/blog/2026/04/15/创建跨平台-zip-文件的隐藏陷阱：extra-field/index.html</link>
<description>在 Linux 下用 =zip -r something.zip something/= 打包文件分享给朋友，结果对方在 iOS 上怎么也打不开——文件明明没损坏，换个人用 macOS 又能正常解压。

这背后的原因是 ZIP 格式中的 /Extra Field/ （扩展字段）。

* 什么是 Extra Field

ZIP 格式规范中，每个文件条目除了基本的文件名、压缩数据之外，还可以携带一组可选的&quot;扩展字段&quot;（Extra Field）。这些字段用来存储标准 ZIP 头部没有覆盖的元数据。

Info-ZIP（Linux 上最常用的 =zip= 命令）默认会利用这个机制保存 Unix 特有的文件属性，包括：
- Unix 文件权限（owner/group/other 的读写执行位）
- UID 和 GID
- 修改时间、访问时间等时间戳

按 ZIP 规范的设计，实现应该忽略自己不认识的 Extra Field，只处理能理解的。但现实中，某些平台的 ZIP 实现（特别是 iOS 上的某些解压工具）遇到包含 Extra Field 的 ZIP 文件时会直接报错，而不是优雅地忽略它们。

* 用 zipinfo 观察差异

我们可以用 =zipinfo= 命令直观地看到有无 Extra Field 的区别。先创建一个测试目录：

#+BEGIN_SRC shell :exports both :results value code
  mkdir -p /tmp/zip-test/demo
  echo &quot;hello world&quot; &gt; /tmp/zip-test/demo/hello.txt
  chmod 755 /tmp/zip-test/demo/hello.txt
#+END_SRC

分别用默认方式和 =-X= 选项打包：

#+BEGIN_SRC shell :exports both :results output
  cd /tmp/zip-test
  zip -r with-extra.zip demo/
  zip -rX without-extra.zip demo/
#+END_SRC

#+RESULTS:
:   adding: demo/ (stored 0%)
:   adding: demo/hello.txt (stored 0%)
:   adding: demo/ (stored 0%)
:   adding: demo/hello.txt (stored 0%)

用 =zipinfo= 查看包含 Extra Field 的版本：

#+BEGIN_SRC shell :exports both :results output
  zipinfo /tmp/zip-test/with-extra.zip
#+END_SRC

#+RESULTS:
: Archive:  /tmp/zip-test/with-extra.zip
: Zip file size: 328 bytes, number of entries: 2
: drwxr-xr-x  3.0 unx        0 bx stor 26-Apr-15 23:01 demo/
: -rwxr-xr-x  3.0 unx       12 tx stor 26-Apr-15 23:01 demo/hello.txt
: 2 files, 12 bytes uncompressed, 12 bytes compressed:  0.0%

注意输出中的 =unx= 和 =bx= / =tx= ： =unx= 表示 Unix 格式的条目， =bx= / =tx= 中的 =x= 表示文件保留了可执行权限。

再看不含 Extra Field 的版本：

#+BEGIN_SRC shell :exports both :results output
  zipinfo /tmp/zip-test/without-extra.zip
#+END_SRC

#+RESULTS:
: Archive:  /tmp/zip-test/without-extra.zip
: Zip file size: 224 bytes, number of entries: 2
: drwxr-xr-x  3.0 unx        0 b- stor 26-Apr-15 23:01 demo/
: -rwxr-xr-x  3.0 unx       12 t- stor 26-Apr-15 23:01 demo/hello.txt
: 2 files, 12 bytes uncompressed, 12 bytes compressed:  0.0%

用 =zipinfo -v= 可以看到更详细的差异：

#+BEGIN_SRC shell :exports both :results output
  zipinfo -v /tmp/zip-test/with-extra.zip | grep -E &quot;(length of extra field|central-directory extra field|subfield with ID)&quot;
#+END_SRC

#+RESULTS:
:   length of extra field:                          24 bytes
:   The central-directory extra field contains:
:   - A subfield with ID 0x5455 (universal time) and 5 data bytes.
:   - A subfield with ID 0x7875 (Unix UID/GID (any size)) and 11 data bytes:
:   length of extra field:                          24 bytes
:   The central-directory extra field contains:
:   - A subfield with ID 0x5455 (universal time) and 5 data bytes.
:   - A subfield with ID 0x7875 (Unix UID/GID (any size)) and 11 data bytes:

对比不含 Extra Field 的版本：

#+BEGIN_SRC shell :exports both :results output
  zipinfo -v /tmp/zip-test/without-extra.zip | grep &quot;extra field&quot;
#+END_SRC

#+RESULTS:
:   length of extra field:                          0 bytes
:   length of extra field:                          0 bytes

文件大小的差异也很明显：328 bytes vs 224 bytes，差了 104 bytes，正好是两个条目各 24 bytes 的 Extra Field（包含 Universal Time 和 Unix UID/GID）加上头部开销。

* 解决方案

用 =-X= （或其长选项 =--no-extra= ）排除 Extra Field：

#+BEGIN_SRC shell
  zip -rX something.zip something/
#+END_SRC

这样生成的 ZIP 文件就是纯标准格式，兼容性最好。

如果你的项目使用 Python 打包， =zipfile= 模块默认 *不会* 写入 Extra Field，所以无需特殊处理。但如果你显式设置了 =external_attr= 来保存 Unix 权限，则需要注意这个问题。

* 什么时候该保留 Extra Field

=zip -rX= 并不是万能的最佳实践。以下场景 *应该* 保留 Extra Field：

- *系统备份* ：需要保留完整的 Unix 权限、所有者信息
- *在 Linux 之间传输* ：两端都支持 Extra Field，保留元数据有好处

而以下场景 *应该排除* Extra Field （用 =-X= ）：

- *跨平台分享文件* ：收件人可能在 macOS、Windows、iOS 等平台
- *上传到网站/网盘* ：不确定下载方会用什么工具解压
- *只需文件内容* ：不关心文件权限等元数据

一句话总结：分享给不确定的接收方时，用 =zip -rX= ；只在 Linux 内部流转时，保留默认行为即可。
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/15/创建跨平台-zip-文件的隐藏陷阱：extra-field/index.html</guid>
</item>
<item>
<title>X11 Forwarding 排障指南</title>
<link>https://lujun9972.github.io/blog/2026/04/15/x11-forwarding-排障指南/index.html</link>
<description>X11 Forwarding 让你通过 SSH 在远程服务器上运行图形程序，窗口却显示在本地桌面上。听起来很美好，但实际配置中踩坑无数。这篇文章先讲原理，再按顺序排查常见问题。

* X11 Forwarding 的工作原理

X Window System 采用 Client/Server 架构，但这个&quot;客户端&quot;和&quot;服务端&quot;的方向跟通常的直觉相反：

- *X Server* 运行在你面前的机器上（你的桌面），负责控制屏幕显示和接收键盘鼠标输入
- *X Client* 是远程服务器上你想运行的图形程序（如 =xterm= 、 =firefox= ）

X Client 需要连到 X Server 才能把窗口画出来。正常情况下它们通过 =/tmp/.X11-unix= 目录下的 Unix-domain socket 通信，或者通过 TCP 网络连接。

** SSH 隧道化的过程

1. 你用 =ssh -X= 连接远程主机
2. 远程 SSH 服务端在后台启动一个 *proxy X Server* ，监听一个 TCP 端口（通常从 6010 开始，对应 DISPLAY =localhost:10.0= ）
3. 远程的 X Client 连接这个 proxy X Server
4. proxy X Server 把所有 X11 协议数据通过 SSH 加密隧道传回你本地的 X Server
5. 本地 X Server 渲染窗口

#+BEGIN_SRC shell
  # 连接后检查 DISPLAY 变量，确认 forwarding 生效
  echo $DISPLAY
  # 正常输出类似: localhost:10.0
#+END_SRC

其中 =localhost:10.0= 的含义：
- =localhost= :: 连接地址（本机的 TCP 端口）
- =10= :: display number，对应 TCP 端口 6010（6000 + 10）
- =.0= :: screen number（通常为 0）

** 认证机制

SSH 连接时会在远程主机的 =~/.Xauthority= 文件中写入一条 MIT-MAGIC-COOKIE-1 认证记录。X Client 连接 proxy X Server 时需要出示这个 cookie，防止未授权的连接。

#+BEGIN_SRC shell
  # 查看 Xauthority 中的认证记录
  xauth list
  # 输出类似: localhost/unix:10  MIT-MAGIC-COOKIE-1  1a2b3c4d5e6f...
#+END_SRC

* 排障清单

理解了原理之后，排障就变成了逐层排查：客户端 → SSH 连接 → 服务端 → 权限 → 特殊场景。

**1. 本地 X Server 是否在运行？**

远程的图形要显示在本地，本地得有一个 X Server 在运行。Linux 桌面用户通常不需要操心，但如果你在 Windows 上用 SSH 客户端，需要额外安装 X Server（如 VcXsrv）。

#+BEGIN_SRC shell
  # 本地检查 X Server 是否运行（Linux）
  ls /tmp/.X11-unix/
#+END_SRC

**2. SSH 客户端是否开启了 X11 Forwarding？**

#+BEGIN_SRC shell
  # 方式一：命令行参数
  ssh -X user@host

  # 方式二：在 ~/.ssh/config 中配置
  # Host *
  #   ForwardX11 yes
  #   ForwardAgent yes
#+END_SRC

使用 =-X= 参数连接后，检查 =$DISPLAY= 是否被设置。如果为空，说明 forwarding 没有建立成功。

如果用的是 SecureCRT 或 PuTTY 这类图形化 SSH 客户端，需要在会话设置里手动勾选 X11 Forwarding 选项。

**3. 远程服务端：xauth 和 xterm 是否已安装？**

=xauth= 是 X11 Forwarding 的必要组件，没有它 SSH 无法设置认证。 =xterm= 则是最好的测试工具。

#+BEGIN_SRC shell :dir /sudo::
  # 检查是否安装
  which xauth
  which xterm

  # Debian/Ubuntu 安装
  apt install xauth xterm

  # RHEL/CentOS 安装
  yum install xorg-x11-xauth xterm
#+END_SRC

**4. sshd 配置是否正确？**

远程服务器上的 =/etc/ssh/sshd_config= 需要启用 X11 forwarding：

#+BEGIN_SRC shell :dir /sudo::
  # 检查配置
  grep X11Forwarding /etc/ssh/sshd_config
  grep AllowAgentForwarding /etc/ssh/sshd_config
  # 应该输出:
  #   X11Forwarding yes
  #   AllowAgentForwarding yes

  # 如果被注释掉或为 no，修改后重启 sshd
  systemctl restart sshd
#+END_SRC

**5. HOME目录和 .Xauthority 权限是否正常？**

SSH 登录时需要在家目录下创建或更新 =~/.Xauthority= 文件。如果家目录不可写，forwarding 会静默失败。

#+BEGIN_SRC shell
  # 检查家目录权限
  ls -la ~ | head -1

  # 如果有旧的 .Xauthority 出问题，删掉重新登录
  rm ~/.Xauthority
  # 重新 ssh -X 连接，会自动重建

  # ~/.ssh 目录权限也必须正确，否则 SSH 会拒绝使用
  chmod 700 ~/.ssh
  chmod 600 ~/.ssh/*
#+END_SRC

**6. IPv6 被禁用导致的隐蔽问题**

这是一个极其隐蔽的坑。如果你禁用了 IPv6，OpenSSH 在某些配置下会 *静默拒绝* X11 forwarding——不报错，只是 =$DISPLAY= 为空。

#+BEGIN_SRC shell
  # 检查 IPv6 是否被禁用
  sysctl net.ipv6.conf.all.disable_ipv6
  # 返回 1 表示已禁用
#+END_SRC

解决方法是在 =/etc/ssh/sshd_config= 中强制使用 IPv4：

#+BEGIN_SRC shell :dir /sudo::
  # 在 sshd_config 中添加
  AddressFamily inet
  # 然后重启 sshd
  systemctl restart sshd
#+END_SRC

**7. sudo 下运行图形程序**

用 =sudo= 运行图形程序时报 =X11 connection rejected because of wrong authentication= ，是因为 root 用户的 =~/.Xauthority= 中没有对应的认证 cookie。

#+BEGIN_SRC shell :dir /sudo::
  # 方法一：复制当前用户的 .Xauthority 到目标用户
  cp ~/.Xauthority ~root/.Xauthority
  xterm  # 现在应该能弹窗了

  # 方法二：用 sudo -u 而不是 sudo su -
  # &quot;sudo su -&quot; 的 &quot;-&quot; 会重置环境变量，包括 $DISPLAY
  sudo -u username xterm
#+END_SRC

方法二更干净——避免用 =sudo su -= ，因为它会清空环境变量导致 =$DISPLAY= 丢失。

* 调试大招

如果以上排查都没解决问题，开启 SSH 调试模式：

#+BEGIN_SRC shell
  # 增加 -v 参数查看详细信息
  ssh -v -X user@host

  # 需要更多细节就叠加 v
  ssh -vvv -X user@host
#+END_SRC

重点关注输出中关于 =X11 forwarding= 、 =DISPLAY= 、 =xauth= 的行。同时在服务端检查日志：

#+BEGIN_SRC shell :dir /sudo::
  # 服务端查看 SSH 日志
  journalctl -u sshd --since &quot;5 minutes ago&quot;
  # 或
  tail -f /var/log/auth.log
#+END_SRC
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/15/x11-forwarding-排障指南/index.html</guid>
</item>
<item>
<title>GIF不仅仅是一种图片格式——用GIF流做些奇怪的事</title>
<link>https://lujun9972.github.io/blog/2026/04/13/gif不仅仅是一种图片格式——用gif流做些奇怪的事/index.html</link>
<description>你有没有想过，有人用 GIF 动画在浏览器里玩游戏？

这不是玩笑。有人用 Haskell 写了一个贪吃蛇游戏，但它的画面输出不是通过窗口或 Canvas，而是通过 *GIF 动画流* ——服务器不断向浏览器推送新的 GIF 帧，浏览器自动显示，就这样实现了一个&quot;实时&quot;游戏。打开浏览器，访问一个 URL，就能看到不断变化的 GIF 画面，按 WASD 键还能控制蛇的移动方向。

这听起来很奇怪，但仔细想想又很巧妙：GIF 是一种几乎所有浏览器都支持的图片格式，不需要 JavaScript，不需要 WebSocket，甚至不需要现代浏览器——只要能显示 GIF 就行。

那么，这种&quot;GIF 流&quot;的技术原理是什么？除了玩游戏，它还能做哪些奇怪的事？让我们一起来探索。

参考资料: &lt;https://hookrace.net/blog/haskell-game-programming-with-gif-streams/&gt;

* GIF 流的原理

GIF 流的核心技术是 HTTP 的 =multipart/x-mixed-replace= Content-Type。这是一种 *服务器推送* （Server Push）技术，诞生于 Netscape 时代。

普通的 HTTP 响应是这样的：客户端发请求，服务器返回一个完整的响应（一个 HTML 页面、一张图片等），然后连接关闭。

而 =multipart/x-mixed-replace= 允许服务器在 *同一个 HTTP 连接* 上连续发送多个&quot;部分&quot;，每个部分都会 *替换* 前一个。浏览器收到新的部分后，会自动用新内容替换旧内容。

原始的 HTTP 响应看起来是这样的：

#+BEGIN_EXAMPLE
HTTP/1.1 200 OK
Content-Type: multipart/x-mixed-replace; boundary=frame

--frame
Content-Type: image/gif

&lt;GIF 二进制数据：第 1 帧&gt;
--frame
Content-Type: image/gif

&lt;GIF 二进制数据：第 2 帧&gt;
--frame
Content-Type: image/gif

&lt;GIF 二进制数据：第 3 帧&gt;
...
#+END_EXAMPLE

服务器 *永远不会关闭这个连接* ，而是持续不断地发送新的 GIF 帧。浏览器收到每一帧后立即显示，然后再等待下一帧。就这样，一个静态的 =&lt;img&gt;= 标签就变成了&quot;实时视频&quot;。

这项技术最常见于 *IP 摄像头的监控画面* （MJPEG 流），但用 GIF 来实现同样可行，而且 GIF 格式还有个优势：支持 256 色调色板和透明色，对于简单图形（如游戏画面、监控图表）来说文件体积可能比 JPEG 更小。注意 GIF 的 256 色限制可能导致颜色失真，但对于线条、文本等简单图形效果不错。

理解了原理后，接下来我们来看看 GIF 流能做哪些有趣的事。

* 用途 1：用 Python 实现一个 GIF 流服务器

理解了原理，我们来动手实现一个最简 GIF 流服务器。用 Python 标准库 + Pillow 就够了。

** 安装依赖

#+BEGIN_SRC shell
  pip install --user Pillow
#+END_SRC

** 完整代码

#+BEGIN_SRC python :tangle &quot;/sudo::/tmp/gif_stream_server.py&quot;
#!/usr/bin/env python3
&quot;&quot;&quot;最简 GIF 流服务器 - 在浏览器中显示实时变化的颜色&quot;&quot;&quot;
from http.server import HTTPServer, BaseHTTPRequestHandler
from io import BytesIO
import time, math
from PIL import Image, ImageDraw

class GifStreamHandler(BaseHTTPRequestHandler):
    def do_GET(self):
        if self.path == &#39;/&#39;:
            # 首页：返回 HTML 页面，用 &lt;img&gt; 嵌入 GIF 流
            self.send_response(200)
            self.send_header(&#39;Content-Type&#39;, &#39;text/html; charset=utf-8&#39;)
            self.end_headers()
            html = &#39;&#39;&#39;&lt;!DOCTYPE html&gt;
&lt;html&gt;&lt;head&gt;&lt;title&gt;GIF Stream Demo&lt;/title&gt;&lt;/head&gt;
&lt;body style=&quot;background:#111;color:#fff;text-align:center&quot;&gt;
&lt;h1&gt;GIF Stream Demo&lt;/h1&gt;
&lt;img src=&quot;/stream&quot; style=&quot;border:2px solid #555&quot;&gt;
&lt;p&gt;Open this page to see live GIF stream&lt;/p&gt;
&lt;/body&gt;&lt;/html&gt;&#39;&#39;&#39;
            self.wfile.write(html.encode(&#39;utf-8&#39;))
        elif self.path == &#39;/stream&#39;:
            # GIF 流端点：通过 multipart/x-mixed-replace 推送帧
            self.send_response(200)
            self.send_header(&#39;Content-Type&#39;, &#39;multipart/x-mixed-replace; boundary=frame&#39;)
            self.send_header(&#39;Cache-Control&#39;, &#39;no-cache&#39;)
            self.end_headers()

            t = 0
            while True:
                try:
                    # 生成 200x200 的图像，颜色随时间变化
                    r = int((math.sin(t * 0.05) + 1) * 127)
                    g = int((math.sin(t * 0.05 + 2) + 1) * 127)
                    b = int((math.sin(t * 0.05 + 4) + 1) * 127)

                    img = Image.new(&#39;RGB&#39;, (200, 200), (r, g, b))
                    draw = ImageDraw.Draw(img)
                    # 在中间显示当前时间
                    draw.text((50, 80), time.strftime(&#39;%H:%M:%S&#39;), fill=(255, 255, 255))

                    # 将图像编码为 GIF
                    buf = BytesIO()
                    img.save(buf, format=&#39;GIF&#39;)
                    gif_data = buf.getvalue()

                    # 按 multipart/x-mixed-replace 格式发送
                    self.wfile.write(b&#39;--frame\r\n&#39;)
                    self.wfile.write(b&#39;Content-Type: image/gif\r\n&#39;)
                    self.wfile.write(f&#39;Content-Length: {len(gif_data)}\r\n&#39;.encode())
                    self.wfile.write(b&#39;\r\n&#39;)
                    self.wfile.write(gif_data)
                    self.wfile.write(b&#39;\r\n&#39;)
                    self.wfile.flush()

                    t += 1
                    time.sleep(0.1)  # 10 FPS
                except (BrokenPipeError, ConnectionResetError):
                    break  # 客户端断开连接
        else:
            self.send_error(404)

if __name__ == &#39;__main__&#39;:
    server = HTTPServer((&#39;0.0.0.0&#39;, 8080), GifStreamHandler)
    print(&#39;Listening on http://0.0.0.0:8080/&#39;)
    server.serve_forever()
#+END_SRC

** 运行效果

#+BEGIN_SRC shell
  python3 /tmp/gif_stream_server.py
#+END_SRC

然后在浏览器中打开 =http://localhost:8080/= ，你会看到一个 HTML 页面，其中的 GIF 图像不断变色，中间显示着实时时钟。

注意： =multipart/x-mixed-replace= 需要 =&lt;img&gt;= 标签来承载，不能直接在浏览器地址栏访问流地址。这是因为现代浏览器（特别是 Chrome ）对直接导航到 multipart 流的支持有限。这也是 IP 摄像头监控画面的标准用法——通过 HTML 页面嵌入 =&lt;img src=&quot;/stream&quot;&gt;= 来显示 MJPEG/GIF 流。

整个过程：
- 服务器每 100ms 生成一帧 GIF 图像
- 通过 =multipart/x-mixed-replace= 协议推送给浏览器
- 浏览器自动替换显示，无需任何 JavaScript

你还可以用 =curl= 查看 GIF 流的原始数据，感受 =multipart/x-mixed-replace= 的格式：

#+BEGIN_SRC shell
  # 查看 GIF 流的前几个字节（Ctrl+C 停止）
  curl -s http://localhost:8080/stream | xxd
#+END_SRC

你会看到 GIF 文件头（ =GIF87a= 或 =GIF89a= ）以及边界标记 =--frame= 在二进制数据中交替出现。Pillow 对于简单的单帧图像默认输出 =GIF87a= （1987 年的原始格式），对于包含动画或透明色的图像则使用 =GIF89a= （1989 年增加了动画、透明色等特性）。两者在 GIF 流中都能正常工作。

* 用途 2：零依赖的实时系统监控仪表盘

回到 GIF 流的话题。既然我们可以用 Python 生成 GIF 帧并通过 HTTP 推送，那就可以做一个 *不需要 WebSocket 、不需要 JavaScript 的实时监控仪表盘* 。

核心思路：每隔一段时间采集系统状态（CPU、内存），用 Python 画一个柱状图，编码为 GIF 帧，通过 =multipart/x-mixed-replace= 推送给浏览器。

需要额外安装 =psutil= 来获取系统状态：

#+BEGIN_SRC shell :dir /sudo::
  sudo pacman -S python-psutil
#+END_SRC

#+BEGIN_SRC python :tangle &quot;/sudo::/tmp/gif_monitor.py&quot;
#!/usr/bin/env python3
&quot;&quot;&quot;GIF 流系统监控仪表盘 - 纯 GIF，无需 JavaScript&quot;&quot;&quot;
from http.server import HTTPServer, BaseHTTPRequestHandler
from io import BytesIO
import psutil
from PIL import Image, ImageDraw
import time

WIDTH, HEIGHT = 400, 200

def get_stats():
    &quot;&quot;&quot;获取系统状态&quot;&quot;&quot;
    return {
        &#39;cpu&#39;: psutil.cpu_percent(interval=0),
        &#39;mem&#39;: psutil.virtual_memory().percent,
        &#39;disk&#39;: psutil.disk_usage(&#39;/&#39;).percent,
    }

def draw_chart(stats):
    &quot;&quot;&quot;绘制柱状图&quot;&quot;&quot;
    img = Image.new(&#39;RGB&#39;, (WIDTH, HEIGHT), (20, 20, 20))
    draw = ImageDraw.Draw(img)

    labels = [(&#39;CPU&#39;, stats[&#39;cpu&#39;], (0, 180, 0)),
              (&#39;MEM&#39;, stats[&#39;mem&#39;], (0, 0, 180)),
              (&#39;DISK&#39;, stats[&#39;disk&#39;], (180, 0, 0))]

    for i, (label, value, color) in enumerate(labels):
        x = 50 + i * 120
        bar_height = int(value / 100 * 150)
        # 画柱子
        draw.rectangle([x, HEIGHT - 30 - bar_height, x + 60, HEIGHT - 30], fill=color)
        # 画标签
        draw.text((x + 10, HEIGHT - 25), f&#39;{label} {value}%&#39;, fill=(200, 200, 200))

    return img

class MonitorHandler(BaseHTTPRequestHandler):
    def do_GET(self):
        if self.path == &#39;/&#39;:
            self.send_response(200)
            self.send_header(&#39;Content-Type&#39;, &#39;text/html; charset=utf-8&#39;)
            self.end_headers()
            html = &#39;&#39;&#39;&lt;!DOCTYPE html&gt;
&lt;html&gt;&lt;head&gt;&lt;title&gt;System Monitor&lt;/title&gt;&lt;/head&gt;
&lt;body style=&quot;background:#111;color:#fff;text-align:center&quot;&gt;
&lt;h1&gt;GIF Stream System Monitor&lt;/h1&gt;
&lt;img src=&quot;/stream&quot; style=&quot;border:2px solid #555&quot;&gt;
&lt;/body&gt;&lt;/html&gt;&#39;&#39;&#39;
            self.wfile.write(html.encode(&#39;utf-8&#39;))
        elif self.path == &#39;/stream&#39;:
            self.send_response(200)
            self.send_header(&#39;Content-Type&#39;, &#39;multipart/x-mixed-replace; boundary=frame&#39;)
            self.send_header(&#39;Cache-Control&#39;, &#39;no-cache&#39;)
            self.end_headers()

            while True:
                try:
                    stats = get_stats()
                    img = draw_chart(stats)
                    buf = BytesIO()
                    img.save(buf, format=&#39;GIF&#39;)
                    gif_data = buf.getvalue()

                    self.wfile.write(b&#39;--frame\r\n&#39;)
                    self.wfile.write(b&#39;Content-Type: image/gif\r\n&#39;)
                    self.wfile.write(f&#39;Content-Length: {len(gif_data)}\r\n&#39;.encode())
                    self.wfile.write(b&#39;\r\n&#39;)
                    self.wfile.write(gif_data)
                    self.wfile.write(b&#39;\r\n&#39;)
                    self.wfile.flush()

                    time.sleep(1)  # 每秒更新一次
                except (BrokenPipeError, ConnectionResetError):
                    break  # 客户端断开连接

if __name__ == &#39;__main__&#39;:
    server = HTTPServer((&#39;0.0.0.0&#39;, 8080), MonitorHandler)
    print(&#39;监控仪表盘: http://0.0.0.0:8080/&#39;)
    server.serve_forever()
#+END_SRC

运行后在浏览器中打开，你会看到一个实时更新的柱状图，显示 CPU、内存和磁盘使用率。

这个方案的特点：
- *不需要前端框架* ：没有 React、Vue，甚至没有 JavaScript
- *不需要 WebSocket* ：纯 HTTP 长连接
- *兼容性极强* ：任何能显示 GIF 的浏览器都支持，包括 =w3m= 这类文本浏览器配合图片渲染器
- *但带宽消耗大* ：每秒 1 帧 GIF 的数据量虽然不大，但如果需要更高帧率就会显著增加

* 用途 3：在 GIF 帧延迟中藏数据

GIF 格式的每一帧都有一个 *延迟时间* （Delay Time），以 1/100 秒为单位。正常情况下这个值是 10（即 100ms），但我们可以用它来编码秘密信息。

比如，延迟 =20= 表示二进制 =1= ，延迟 =10= 表示二进制 =0= ：

#+BEGIN_SRC python :tangle &quot;/sudo::/tmp/gif_stego.py&quot;
#!/usr/bin/env python3
&quot;&quot;&quot;在 GIF 帧延迟中隐藏消息&quot;&quot;&quot;
from PIL import Image
import random

def encode_message(message, output_path, width=10, height=10):
    &quot;&quot;&quot;将消息编码到 GIF 帧延迟中&quot;&quot;&quot;
    binary = &#39;&#39;.join(format(ord(c), &#39;08b&#39;) for c in message)
    binary += &#39;00000000&#39;  # 结束标记

    frames = []
    for bit in binary:
        # 生成随机噪点帧
        pixels = [(random.randint(0, 255),) * 3 for _ in range(width * height)]
        img = Image.new(&#39;RGB&#39;, (width, height))
        img.putdata(pixels)
        frames.append(img)

    # 保存为 GIF，每帧延迟编码一个 bit
    # 20 = 二进制 1, 10 = 二进制 0（单位：1/100 秒）
    delays = [20 if b == &#39;1&#39; else 10 for b in binary]
    frames[0].save(
        output_path,
        save_all=True,
        append_images=frames[1:],
        duration=delays,
        loop=0,
    )
    print(f&#39;已将 &quot;{message}&quot; 编码到 {output_path}（{len(binary)} 帧）&#39;)

def decode_message(gif_path):
    &quot;&quot;&quot;从 GIF 帧延迟中解码消息&quot;&quot;&quot;
    img = Image.open(gif_path)
    binary = &#39;&#39;
    try:
        while True:
            delay = img.info.get(&#39;duration&#39;, 10)
            binary += &#39;1&#39; if delay &gt;= 20 else &#39;0&#39;
            img.seek(img.tell() + 1)
    except EOFError:
        pass

    message = &#39;&#39;
    for i in range(0, len(binary), 8):
        byte = binary[i:i+8]
        if byte == &#39;00000000&#39;:
            break
        message += chr(int(byte, 2))
    return message

if __name__ == &#39;__main__&#39;:
    encode_message(&#39;Hello GIF!&#39;, &#39;secret.gif&#39;)
    decoded = decode_message(&#39;secret.gif&#39;)
    print(f&#39;从 secret.gif 解码出: &quot;{decoded}&quot;&#39;)
#+END_SRC

运行后你会得到一个看起来像随机噪点的 GIF 动画，但帧延迟中隐藏了秘密消息。肉眼完全看不出区别。

当然，这种隐写术很容易被分析——只要检查 GIF 帧的时间间隔分布就能发现异常（正常动画的帧延迟应该是均匀的，而编码后的会出现双峰分布）。但作为一个 *GIF 的奇怪用途* ，它足够有趣了。

* 小结

GIF 流是一项&quot;复古&quot;的技术，诞生于上世纪 90 年代。在今天 WebSocket 、WebRTC 、Server-Sent Events 大行其道的时代，它看起来已经过时了。但正是这种简洁性赋予了它独特的价值：

- *零前端依赖* ：不需要 JavaScript，一个 =&lt;img&gt;= 标签就够了
- *极简协议* ：基于 HTTP 长连接 + =multipart/x-mixed-replace= ，实现起来只需要几十行代码
- *广泛兼容* ：几乎所有浏览器都支持，甚至包括一些非主流的浏览方式

GIF 证明了&quot;老技术&quot;也可以玩出新花样。下次当你看到一张 GIF 图片时，也许可以想想：它还能做什么更奇怪的事？

如果你也发现了 GIF 的其他有趣用途，欢迎在评论区分享。
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/13/gif不仅仅是一种图片格式——用gif流做些奇怪的事/index.html</guid>
</item>
<item>
<title>Google SRE Book 读书笔记</title>
<link>https://lujun9972.github.io/blog/2026/04/14/google-sre-book-读书笔记/index.html</link>
<description>本文是 Dan Luu 对 [[https://danluu.com/google-sre-book/][Google SRE Book]] 的读书笔记的中文整理版，保留了原文的笔记式风格和个人评论。

* 第 1 章：简介

本书以 Margaret Hamilton 的故事开篇。在阿波罗计划时期，Hamilton 带着小女儿去 NASA。在一次模拟任务中，她女儿按下了某些键，导致预发射程序在模拟任务期间运行，任务崩溃。Hamilton 提交了一个变更请求，添加错误检查代码来防止此类问题再次发生，但请求被拒绝了，因为&quot;这种错误不可能发生&quot;。

在下一次任务 Apollo 8 中，完全相同的错误条件发生了，一个本可通过简单检查就能预防的潜在致命问题，NASA 的工程师花了 9 个小时才解决。

#+BEGIN_QUOTE
这听起来很熟悉——我已经数不清有多少次开发团队的事后分析报告有着相同的基本结构。
#+END_QUOTE

** 传统方法：系统管理员

- 组装现有组件并部署以产生服务
- 响应发生的事件和更新
- 随着服务增长扩大团队以吸收增加的工作量
- 优点：
  - 因为是标准做法所以容易实施
  - 可以从很大的人才池中招聘
  - 有大量可用的软件
- 缺点：
  - 手动干预导致团队规模随系统负载增长
  - 运维与开发本质上是矛盾的，可能导致对变更的病态抵制，开发团队则会将&quot;发布&quot;重新分类为&quot;增量更新&quot;、&quot;标志翻转&quot;等来绕过

** Google 的方法：SRE

- 由软件工程师来做运维
- 候选人应能通过或接近通过普通开发的招聘标准，还可能拥有开发中罕见的额外技能（如 L1-L3 网络或 UNIX 系统内核）
- 职业发展与开发轨道相当
- 结果：
  - SRE 会因为手工操作而感到无聊
  - 拥有自动化任务所需的技能
  - 做与运维团队相同的工作，但用自动化替代手工劳动
- Google 将 SRE 的&quot;运维&quot;工作上限设为 50%
  - 这是上限，实际预期运维工作量要低得多
- 优点：
  - 扩展成本更低
  - 绕过了开发/运维分裂
- 缺点：
  - 难以招聘
  - 可能需要管理层支持的非常规做法（例如产品团队可能反对因错误预算耗尽而停止本季度发布的决定）

#+BEGIN_QUOTE
我不太理解这如何算是绕过了开发/运维分裂。从某种意义上说确实如此，但因错误预算耗尽而停止所有发布的例子，与&quot;系统管理员&quot;模式中团队反对发布的例子并无根本不同。看起来 SRE 有更多的政治资本可以花费，在给定的具体例子中 SRE 可能更合理，但没有理由认为系统管理员就不能是合理的。
#+END_QUOTE

** SRE 的信条

- SRE 团队负责：延迟、性能、效率、变更管理、监控、应急响应和容量规划

** 确保持久的工程专注

- 50% 运维上限意味着额外的运维工作会溢出转给产品团队
- 为产品团队提供反馈机制，同时保持负载可控
- 目标：每次 8-12 小时的值班轮班中最多 2 个事件
- 对所有严重事件进行事后分析，即使没有触发告警
- 无责事后分析

#+BEGIN_QUOTE
每次轮班最多 2 个事件，但平均值是多少？每周预期有多少值班事件从 SRE 团队转给开发团队？
#+END_QUOTE

#+BEGIN_QUOTE
如何从有责的事后分析文化转变为无责文化？既然现在每个人都知道应该做无责事后分析，每个人都会声称自己在做。就像拥有良好的测试和部署实践一样。我很幸运地在一个从未被呼叫过的值班轮换中，但当我与最近加入并在值班的人交谈时，他们有一些不太好的故事——指责、垃圾话和推卸责任。每个人都知道应该无责这一事实，似乎反而使指出有责行为变得更加困难，而不是更容易。
#+END_QUOTE

** 快速前进而不破坏 SLO

- 错误预算：100% 对几乎所有东西来说都是错误的可靠性目标
- 从 5 个 9 到 100% 可靠性对大多数用户来说不可感知，且需要巨大努力
- 设定一个承认权衡并留出错误预算的目标
- 错误预算可以用于任何事情：发布功能等
- 错误预算允许讨论分阶段发布和 1% 实验如何维持可容忍的错误水平
- SRE 团队的目标不是&quot;零停机&quot;——SRE 和产品开发的目标一致：花错误预算获得最大功能速度

#+BEGIN_QUOTE
虽然没有明确说明，但对于需要&quot;快速前进&quot;的团队来说，持续远低于错误预算可能被视为团队在可靠性上花费过多努力的信号。
#+END_QUOTE

#+BEGIN_QUOTE
我非常喜欢这个想法，但当我与 Jessica Kerr 讨论时，她对这一想法提出了质疑——也许你只是运气好所以低于错误预算，而一个真正糟糕的事件就可能消耗掉你未来十年的错误预算。后续问题：你如何对你的风险模型有足够的信心，以至于可以有意消耗错误预算来加快速度，而不用担心后续的坏事件会让你超支？Nat Welch（前 Google SRE）回应说，可以通过模拟灾难和其他测试来建立信心。
#+END_QUOTE

** 监控

- 监控永远不应该要求人类解释告警领域的任何部分
- 三种有效的监控输出：
  - 告警：人类需要立即采取行动
  - 工单：人类最终需要采取行动
  - 日志：无需行动
  - 注意，例如图表是日志的一种

** 应急响应

- 可靠性是 MTTF（平均故障间隔时间）和 MTTR（平均恢复时间）的函数
- 评估响应能力时，我们关心 MTTR
- 人类增加延迟
- 不需要人类响应的系统因 MTTR 更低而具有更高的可用性
- 拥有&quot;操作手册&quot;可产生 3 倍更低的 MTTR
  - 拥有万能的英雄人物可以响应所有事情是可行的，但有操作手册效果更好

#+BEGIN_QUOTE
我个人同意，但我们确实喜欢我们的值班英雄。我想知道我们如何培养文档文化。
#+END_QUOTE

** 变更管理

- 70% 的停机由实时系统中的变更引起。缓解措施：
  - 实施渐进式发布
  - 监控
  - 回滚
- 将人类从流程中移除，避免重复性任务上的标准人类问题

** 需求预测和容量规划

- 直截了当，但令人惊讶的是很多团队/服务不这样做

** 资源配置

- 添加容量比负载转移风险更大，因为通常涉及启动新实例/位置，对现有系统进行重大更改（配置文件、负载均衡器等）
- 成本高到应该只在必要时才做；必须快速执行
- 如果你不知道实际需要什么而过度配置，会浪费钱

** 效率和性能

- 负载使系统变慢
- SRE 配置容量以满足特定响应时间目标的容量目标
- 效率 == 金钱

* 第 3 章：拥抱风险

- 例如：如果用户的智能手机有 99% 的可靠性，他们无法区分 99.99% 和 99.999% 的可靠性

** 管理风险

- 可靠性与成本不是线性的。获得一个额外的可靠性增量可能轻松花费 100 倍
  - 冗余设备的成本
  - 为可靠性构建功能而非&quot;普通&quot;功能的成本
- 目标：让系统足够可靠，但不要过于可靠！

** 衡量服务风险

- 标准做法：确定一个指标来代表要优化的系统属性
- 可能的指标 = 正常运行时间 /（正常运行时间 + 停机时间）
  - 对全球分布式服务有问题。正常运行时间到底意味着什么？
- 聚合可用性 = 成功请求 / 总请求
  - 显然，并非所有请求都是平等的，但聚合可用性是一个可以接受的一阶近似
- 通常设置季度目标

** 服务的风险容忍度

- 通常不是客观上显而易见的
- SRE 与产品负责人合作，将业务目标转化为明确的目标

** 目标可用性

- 以 Bigtable 为例：
  - 一些消费者服务直接从 Bigtable 提供数据——需要低延迟和高可靠性
  - 一些团队使用 Bigtable 作为离线分析的后端存储——更关心吞吐量而非可靠性
  - 太昂贵了，无法通用满足所有需求
- 例如 Bigtable 实例：
  - 低延迟 Bigtable 用户想要低队列深度
  - 吞吐量导向的 Bigtable 用户想要中等到高队列深度
  - 这两种情况下的成功和失败是完全对立的！

** 成本

- 分区基础设施并提供不同的服务级别
- 除了明显的好处外，还允许服务将提供不同服务级别的成本外部化（例如，预期延迟导向的服务比吞吐量导向的服务更昂贵）

* 第 4 章：服务水平目标

** Chubby 计划停机

- Google 发现 Chubby 始终超过其 SLO，而全球性的 Chubby 停机会导致 Google 异常严重的停机
- Chubby 如此可靠以至于团队错误地假设它永远不会宕机，未能设计考虑 Chubby 故障的系统
- 解决方案：当 Chubby 在一个季度内远超其 SLO 时，主动将其关闭，向团队&quot;展示&quot; Chubby 也可能宕机

** 你和你的用户关心什么？

- 太多指标：难以关注
- 太少指标：可能忽略重要行为
- 不同类别的服务应该有不同的指标
  - 面向用户：可用性、延迟、吞吐量
  - 存储：延迟、可用性、持久性
  - 大数据：吞吐量、端到端延迟
- 所有系统都关心正确性

** 收集指标

- 通常可以从服务器端自然获取，但有时需要客户端指标

** 聚合

- 使用分布而非平均值
- 用户研究表明，人们通常更喜欢更慢的平均值但更好的尾部延迟
- 标准化通用定义，例如 1 分钟内的平均值、集群中任务的平均值等
  - 可以有例外，但合理的默认值使事情更容易

** 选择目标

- 不要基于当前性能选择目标
  - 当前性能可能需要英雄般的努力
- 保持简单
- 避免绝对化
  - 讨论&quot;无限&quot;规模或&quot;始终&quot;可用是不合理的
- 最小化 SLO 数量
- 完美可以等待
  - 总是可以随时间重新定义 SLO
- SLO 设定预期
- 保留安全余量（内部 SLO 可以比外部 SLO 定义得更宽松）
- 不要超额完成
  - 参见上面 Chubby 的例子
  - 另一个例子是确保系统在轻负载下不会太快

* 第 5 章：消除苦力活

Carla Geisser：&quot;如果人类操作员需要在正常操作期间触碰你的系统，那你就有了一个 bug。&#39;正常&#39;的定义会随着系统的增长而改变。&quot;

- 苦力活的定义：
  - 不是&quot;我不想做的工作&quot;
  - 手动的
  - 重复的
  - 可自动化的
  - 战术性的
  - 没有持久价值
  - O(n) 随服务增长
- 调查显示平均 33% 是苦力活
  - 数字可以低至 0%，高至 80%
  - 苦力活 &gt; 50% 是管理者应更均匀分配苦力活负载的信号
- 苦力活总是坏事吗？
  - 可预测和重复的任务可以让人放松
  - 可以产生成就感，可以是低风险/低压力的活动

* 第 6 章：监控分布式系统

** 为什么要监控？

- 分析长期趋势
- 随时间比较或做实验
- 告警
- 构建仪表板
- 调试

#+BEGIN_QUOTE
正如 Alex Clemmer 常说的，我们的问题不是走得太慢，而是构建了错误的东西。我想知道如何从现在的位置出发，拥有足够的 instrumentation，以便在构建新系统时能做出知情的决策。
#+END_QUOTE

** 设定合理期望

- 监控不是简单的
- 10-12 人的 SRE 团队通常有 1-2 人构建和维护监控
- 这个数字随着工具/库/集中式监控基础设施的改进而减少
- 总体趋势是向更简单/更快的监控系统发展，配合更好的事后分析工具
- 避免&quot;魔法&quot;系统
  - 复杂的依赖层次（例如&quot;如果数据库慢，告警数据库，否则告警网站&quot;）的成功有限
  - 主要（仅？）用于系统非常稳定的部分
- 为人类生成告警的规则应该简单易懂，代表明确的故障

#+BEGIN_QUOTE
避免&quot;魔法&quot;包括避免机器学习吗？
#+END_QUOTE

- 大量白盒监控
- 一些黑盒监控用于关键内容
- 四个黄金信号：
  - 延迟
  - 流量
  - 错误
  - 饱和度

* 第 7 章：Google 的自动化演进

- &quot;自动化是力量倍增器，不是万灵药&quot;
- 自动化的价值：
  - 一致性
  - 可扩展性
  - MTTR（平均恢复时间）
  - 更快的非修复操作
  - 节省时间

* 第 8 章：发布工程

- 这是 Google 的一个特定工作职能

** 发布工程师的角色

- 发布工程师与软件工程师和 SRE 合作，定义软件如何发布
- 允许开发团队专注于开发工作
- 定义最佳实践
  - 编译器标志、构建 ID 标签的格式等
- 发布自动化
- 团队之间的模型各不相同
  - 可以是&quot;绿色即推送&quot;并部署每个构建
  - 可以是每小时构建和部署
  - 等等
- 封闭式构建
  - 构建相同的修订版本号应始终给出相同的结果
  - 自包含——包括对编译器在内的所有版本的版本控制
  - 可以针对旧修订版本挑选修复来修复生产软件
- 几乎所有变更都需要代码审查
- 分支
  - 所有代码在主分支
  - 发布从主分支切出
  - 修复可以从 master 到分支
  - 分支永远不合并回去
- 测试
  - CI
  - 发布过程创建审计跟踪，运行测试并显示测试通过
- 配置管理
  - 看似简单，可能导致不稳定
  - 多种方案（都涉及将配置存储在源代码控制中并进行严格配置审查）
  - 主线配置——配置在 head 维护并立即应用
    - 最初用于 Borg（以及前 Borg 系统）
    - 二进制发布和配置变更解耦！
  - 将配置文件和二进制文件包含在同一包中
    - 简单
    - 紧密耦合二进制和配置——适用于配置文件很少或配置很少变更的项目
  - 将配置打包成&quot;配置包&quot;
    - 与代码相同的封闭式原则
- 发布工程不应是事后才考虑的！
  - 在开发周期开始时预算资源

* 第 9 章：简单性

- 稳定性与敏捷性
  - 可以通过冻结来使事物稳定——需要平衡两者
  - 可靠的系统可以增加敏捷性
  - 可靠的发布使得更容易将变更与 bug 关联
- 无聊的美德！
- 本质复杂性与偶然复杂性
  - SRE 应在引入偶然复杂性时予以抵制
- 代码是负债
  - 删除死代码或其他臃肿
- 最小化 API
  - 更小的 API 更容易测试，更可靠
- 模块化
- API 版本控制
  - 与代码相同，你会避免 misc/util 类
- 发布
  - 小发布更容易衡量
  - 如果同时发布 100 个变更，无法判断发生了什么

* 第 10 章：从时间序列数据告警

** Borgmon

- 类似 Prometheus
- 通用日志数据格式
- 数据用于仪表板和告警
- 形式化了遗留数据格式 &quot;varz&quot;，允许通过 HTTP 查看指标
  - 手动查看指标：http://foo:80/varz
  - 添加指标只需在代码中进行一次声明
  - 添加新指标的用户成本很低
- Borgmon 定期从每个目标获取 /varz
  - 还包括合成数据，如健康检查、名称是否已解析等
- 时间序列竞技场
  - 数据存储在内存中，并检查点到磁盘
  - 固定大小的分配
  - GC 在满时过期最旧的条目
  - 概念上是一个二维数组，一个轴是时间，另一个轴是项目
  - 每个数据点 24 字节 → 1M 个唯一时间序列，12 小时，1 分钟间隔 = 17 GB

** Borgmon 规则

- 代数表达式
- 从其他时间序列计算时间序列
- 规则在线程池上并行评估

** 计数器 vs 仪表

- 计数器：非递减的
- 仪表：可以取任何值
- 计数器优于仪表，因为仪表可能会根据采样间隔丢失信息

** 告警

- Borgmon 规则可以触发告警
- 有最小持续时间以防止&quot;抖动&quot;
- 通常设置为两个持续时间周期，以便遗漏的收集不会触发告警

** 扩展

- Borgmon 可以从其他 Borgmon 获取时间序列数据（使用二进制流协议而非基于文本的 varz 协议）
- 可以有多层过滤器

** 探测器

- 黑盒监控，监控用户看到的内容
- 可以用 varz 查询或直接发送告警给 Alertmanager

** 配置

- 规则定义与监控目标之间的分离

* 第 11 章：值班

** 典型响应时间

- 面向用户或其他时间关键任务：5 分钟
- 不太敏感的事情：30 分钟

** 响应时间与 SLO 关联

- 例如：季度 99.99% 是 13 分钟的停机时间；显然响应时间不能超过 13 分钟
- SLO 更宽松的服务可以有 10 分钟级别（或更多？）的响应时间

** 主要 vs 次要值班

- 工作分配因团队而异
- 在某些团队，次要可以是主要的备份
- 在其他团队，次要处理非紧急/非分页事件，主要处理分页

** 平衡的值班

- 数量定义：值班时间百分比
- 质量定义：值班期间发生的事件数

#+BEGIN_QUOTE
这很好。我们应该这样做。人们有时会连续几次值班轮换非常糟糕，考虑到值班轮换的低频率，没有理由期望这会在一两年内随机平衡。
#+END_QUOTE

** 数量上的平衡

- &gt;= 50% 的 SRE 时间用于工程
- 剩余时间中，不超过 25% 用于值班
- 首选多站点团队
  - 夜班对健康有害，多站点团队允许消除夜班

** 质量上的平衡

- 平均而言，处理一个事件（包括根因分析、修复、编写事后分析、修复 bug 等）需要 6 小时
- =&gt; 12 小时值班轮班中不应超过 2 个事件
- 要保持在上限内，需要非常平坦的分页分布，中位数为 0
- 补偿：值班的额外薪酬（休假或现金）

* 第 13 章：应急响应

** 测试引发的紧急情况

- [[http://queue.acm.org/detail.cfm?id=2371516][SRE 故意破坏系统看看会发生什么]]
  - 例如：想要发现分布式 MySQL 数据库上隐藏的依赖关系
  - 计划：阻止对 1/100 数据库的访问
  - 响应：依赖服务报告无法访问关键系统
  - SRE 响应：中止演练，尝试回滚权限变更
  - 回滚尝试失败
  - 恢复副本访问成功
  - 1 小时恢复正常运行
  - 做得好的地方：依赖团队立即升级问题，能够恢复访问
  - 学到的教训：对系统及其与其他系统的交互了解不足，未能遵循本应通知客户停机的事件响应流程，未在测试环境中测试回滚程序

** 变更引发的紧急情况

- 变更可能导致故障！
  - 例如：周五推送的滥用防护基础设施配置变更触发了崩溃循环 bug
  - 几乎所有面向外部的系统都依赖此基础设施，变得不可用
  - 许多内部系统也有依赖，变得不可用
  - 告警在几秒内开始触发
  - 在配置推送后 5 分钟内，推送变更的工程师回滚了变更，服务开始恢复
  - 做得好的地方：监控立即触发，事件管理运作良好，带外通信系统让人们保持最新状态即使许多系统已宕机，运气好（推送变更的工程师正在关注实时通信渠道，这并不是发布流程的一部分）
  - 学到的教训：推送到金丝雀没有触发相同问题，因为它没有命中特定的配置关键字组合；推送被认为是低风险的，经过了不太严格的金丝雀流程；停机期间告警太嘈杂

* 第 16 章：跟踪停机

- Escalator：集中式系统，跟踪对告警的确认，必要时通知其他人等
- Outalator：提供多个队列通知的时间交错视图
  - 还保存相关电子邮件，允许将某些消息标记为&quot;重要&quot;，可以折叠非重要消息等

#+BEGIN_QUOTE
我们的 Escalator 版本似乎还不错。但我们真的很需要一个像 Outalator 这样的东西。
#+END_QUOTE

* 第 18 章：SRE 中的软件工程

** Auxon：容量规划自动化工具

- 传统容量规划周期：
  1. 收集需求预测（提前数季度到数年）
  2. 规划分配
  3. 审查计划
  4. 部署和配置资源
- 传统方法的缺点：
  - 很多事情可能影响计划：效率提升、采用率增加、集群交付日期延迟等
  - 即使小变更也需要重新检查分配计划
  - 大变更可能需要完全重写计划
  - 劳动密集且容易出错
- Google 的解决方案：基于意图的容量规划
  - 指定需求，而非实现
  - 编码需求并自动生成容量计划
  - 除了节省劳动力，求解器可以比人工生成的方案做得更好 → 节省成本
- 意图驱动的层次示例：
  1. 想在集群 X、Y 和 Z 中有 50 个核心——为什么是那些资源在那些集群？
  2. 想在区域中任何 3 个集群中有 50 个核心的配置——为什么那么多资源和为什么 3 个？
  3. 想以 N+2 冗余满足需求——为什么 N+2？
  4. 想要 5 个 9 的可靠性。可能发现，例如 N+2 不够
- 发现最大收益来自达到第 (3) 层
- 一些复杂的服务可能追求第 (4) 层
- 将约束放入工具中允许权衡在整个机群中保持一致
  - 而非做出个别的临时决策
- Auxon 输入：
  - 需求（例如&quot;服务必须每个大陆 N+2&quot;，&quot;前端服务器距离后端服务器不超过 50ms&quot;）
  - 依赖
  - 预算优先级
  - 性能数据（服务如何扩展）
  - 需求预测数据（注意像 Colossus 这样的服务从依赖服务派生预测）
  - 资源供应和定价
- 输入进入求解器（混合整数或线性规划求解器）

* 第 20 章：数据中心内的负载均衡

** 流控制

- 需要避免不健康的任务
- 不健康任务的朴素流控制：
  - 跟踪到后端的请求数
  - 当达到阈值时将后端视为不健康
  - 缺点：通常很糟糕
- 基于健康的流控制
  - 后端任务可以处于三种状态之一：{健康, 拒绝连接, 跛脚鸭}
  - 跛脚鸭状态仍可接受连接，但向所有客户端发送反压请求
  - 跛脚鸭状态简化了干净关闭
- 子集化定义：限制客户端任务可交互的后端任务池
  - RPC 系统中的客户端维护到后端的连接池
  - 使用连接池比按需进行设置/拆除减少延迟
  - 不活跃的连接相对便宜，但并非免费

** 选择正确的子集

- 典型：20-100，根据工作负载选择
- 随机子集选择：利用率差
- 轮转子集选择：顺序是打乱的；每轮有自己的排列

** 负载均衡

- 子集选择用于连接均衡，但仍需平衡负载
- 轮循负载均衡：
  - 实践中，最高负载和最低负载之间有 2 倍差异
  - 实践中，最昂贵的请求可以比最便宜的请求贵 1000 倍
  - 此外，请求中还有随机不可预测的变化
- 最少已连接轮循：
  - 顾名思义：在最少已连接的后端之间轮循
  - 负载似乎以连接数衡量；可能不总是最佳指标
  - 这是每个客户端的，不是全局的，所以可能向来自其他客户端有很多请求的后端发送请求
  - 实践中，对于大型服务，发现最高负载任务使用的 CPU 是最低负载任务的两倍；与普通轮循类似
- 加权轮循：
  - 与上述相同，但用其他因素加权
  - 实践中，比最少已连接轮循有更好的负载分布

#+BEGIN_QUOTE
我想知道 Heroku 在回应 Rap Genius 时说&quot;经过广泛的研究和实验，我们尚未找到任何理论模型或实际实现能够超越随机路由到支持多个并发连接的 Web 后端的简单性和鲁棒性&quot;是什么意思。
#+END_QUOTE

* 第 21 章：处理过载

- 即使有&quot;好的&quot;负载均衡，系统也会过载
- 典型策略是提供降级的响应，但在非常高负载下可能做不到
- 将容量建模为 QPS 或请求的函数（例如请求读取多少个键）是容易出错的
  - 这些通常缓慢变化，但可能快速变化（例如因为一次代码提交）
- 更好的解决方案：直接测量可用资源
- CPU 利用率通常是一个好的配置信号
  - 对于 GC，内存压力转化为 CPU 利用率
  - 对于其他系统，可以配置其他资源使 CPU 可能成为限制因素
  - 在过度配置 CPU 太昂贵的情况下，考虑其他资源

#+BEGIN_QUOTE
像这样一般地过度配置 CPU 要花多少钱？
#+END_QUOTE

** 客户端节流

- 当客户达到配额时后端开始拒绝请求
- 即使被拒绝，请求仍然使用资源——没有节流的话，后端可能将大部分资源花在拒绝请求上

** 关键性

- 看起来像是优先级但换了个名字？
- RPC 系统中的一等概念
- 客户端节流为每个关键性级别保持单独的统计
- 默认情况下，关键性通过后续 RPC 传播

** 处理过载错误

- 如果 DC 过载，将负载分流到其他 DC
- 如果 DC 正常但一些后端过载，将负载分流到其他后端
- 客户端收到过载响应时重试
  - 每个请求的重试预算 (3)
  - 每个客户端的重试预算 (10%)
  - 客户端的失败重试导致向下游返回&quot;过载；不要重试&quot;响应

#+BEGIN_QUOTE
有&quot;不要重试&quot;的响应是&quot;显而易见&quot;的，但在实践中相对罕见。很多真实系统存在失败重试导致上游更多重试的问题。这在跨越硬件/软件边界时尤其明显（例如文件系统读取导致 DVD/SSD/机械硬盘上的多次重试，失败后在文件系统级别被重试），但在纯软件中似乎也普遍存在。
#+END_QUOTE

* 第 22 章：应对级联故障

** 典型故障场景

- 服务器过载
  - 例如：有两台服务器
    - 一台过载，开始失败
    - 另一台现在获得所有流量，也开始失败
- 资源耗尽
  - CPU/内存/线程/文件描述符等
  - 例如：资源之间的依赖关系
    1. Java 前端有调优不佳的 GC 参数
    2. 前端因 GC 耗尽 CPU
    3. CPU 耗尽使请求变慢
    4. 增加的队列深度使用更多内存
    5. 整个前端的固定内存分配意味着可用于缓存的内存减少
    6. 命中率降低
    7. 更多请求到后端
    8. 后端耗尽 CPU 或线程
    9. 健康检查失败，开始级联故障
    - 在停机期间很难确定原因
  - 注意：避免服务错误的服务器的策略可能使事情更糟
    - 可用的后端更少，获得过多请求，然后也不可用

** 防止服务器过载

- 负载测试！必须有真实环境
- 提供降级结果
- 过载时尽早廉价地失败
- 让高层系统拒绝请求（在反向代理、负载均衡器和任务级别）
- 进行容量规划

** 队列管理

- 队列在稳态下什么也不做
- 排队的请求消耗内存并增加延迟
- 如果流量相对稳定，最好保持较小的队列大小（例如线程池大小的 50% 或更少）
- 例如：Gmail 使用无队列服务器，在线程满时进行故障转移
- 对于突发性工作负载，队列大小应是线程数、每请求时间和突发大小/频率的函数
- 另见 [[http://queue.acm.org/detail.cfm?id=2839461][自适应 LIFO 和 CoDel]]

** 优雅降级

- 注意测试优雅降级路径很重要，也许通过定期在接近过载的情况下运行一小部分服务器，因为这条路径在正常情况下很少被使用
- 最好保持简单易懂

** 重试

- 始终使用随机化指数退避
- 参见前一章关于仅在单个级别重试的内容
- 考虑拥有服务器级别的重试预算

** 截止时间

- 不要在截止时间已过时继续工作（级联故障的常见主题）
- 在每个阶段检查截止时间是否已被命中
- 截止时间应该传播（例如，甚至通过 RPC）

** 双模延迟

- 例如：长截止时间的问题
  - 假设前端有 10 台服务器，每台 100 个线程（总共 1K 线程容量）
  - 正常运行：1K QPS，请求耗时 100ms → 占用 100 个工作线程（1K QPS × 0.1s）
  - 假设 5% 的操作未完成，截止时间为 100s
  - 消耗 5K 线程（50 QPS × 100s）
  - 前端超额 5 倍。成功率 = 1K / (5K + 95) = 19.6% → 80.4% 的错误率

#+BEGIN_QUOTE
使用截止时间而非超时很棒。我们应该更加系统地做这件事。
#+END_QUOTE

#+BEGIN_QUOTE
不允许系统被无意义的僵尸请求填满，通过设置合理的截止时间是&quot;显而易见&quot;的，但很多真实系统似乎有人为好记数字（30s、60s、100s 等）的任意超时，而不是考虑负载/级联故障而分配的截止时间。
#+END_QUOTE

** 级联故障的立即应对措施

- 增加资源
- 临时停止健康检查失败/死亡
- 重启服务器（仅在有帮组时——例如 GC 死亡螺旋或死锁）
- 丢弃流量——激进的，最后手段
- 进入降级模式——需要事先在服务中构建此功能
- 消除批量负载
- 消除不良流量

* 第 23 章：面向可靠性的分布式共识

** 核心问题

- 我们如何就以下问题达成一致：
  - 哪个进程是一组进程的领导者？
  - 一组中的进程集合是什么？
  - 消息是否已成功提交到分布式队列？
  - 进程是否持有特定租约？
  - 数据存储中特定键的值是什么？

** 分裂脑示例

- 服务在不同机架中有复制的文件服务器
- 必须避免同时写入一对文件服务器以防止数据损坏
- 每对文件服务器有一个领导者和一个跟随者
- 服务器通过心跳监控彼此
- 如果一台服务器无法联系另一台，它发送 STONITH（射杀另一个节点）
- 但如果网络慢或丢包怎么办？两台服务器都发出 STONITH 怎么办？

#+BEGIN_QUOTE
这让我想起了我最喜欢的分布式数据库事后分析之一。数据库配置为环形，每个节点与 5 台服务器的&quot;邻域&quot;通信并复制数据。如果邻域中的一些机器宕机，其他服务器加入邻域，数据得到适当复制。听起来不错，但当一台服务器出故障并决定没有数据存在且所有邻居都有问题时，它可以比任何邻居更快地返回结果，还可以告诉邻居它们都有问题。因为坏服务器没有数据所以非常快，可以比邻居报告它有问题更快地报告邻居有问题。糟糕！
#+END_QUOTE

** 故障转移需要人工干预

- 高度分片的 DB 每个分片有一个主节点，复制到另一个 DC 中的次节点
- 外部健康检查决定主节点是否应故障转移到其次节点
- 如果主节点无法看到次节点，它使自己不可用以避免&quot;分裂脑&quot;中的问题
- 这增加了运维负载
- 问题相关联，这在人们忙于其他问题时相对可能遇到问题
- 如果有网络问题，没有理由认为人类会比系统中的机器对世界状态有更好的视野

** 不可能性结果

- CAP：P 在真实网络中不可能，所以选择 C 或 A
- FLP：异步分布式共识在有不可靠网络时不能保证进展

** Paxos

- 一系列提案，可能被多数进程接受或拒绝
- 未被接受 → 失败
- 每个提案的序列号，必须在系统中唯一
- 提案过程：
  - 提议者向接受者发送序列号
  - 接受者如果没看到更高的序列号就同意
  - 提议者可以用更高的序列号重试
  - 如果提议者收到多数同意，通过发送带值的提交消息来提交
  - 接受者在接受时必须日志记录到持久存储

** 模式

- 分布式共识算法是低层原语
- 可靠的复制状态机
  - 数据配置/存储、锁定、领导者选举等的基本构建块
- 可靠的复制数据和配置存储
  - 非分布式共识系统通常使用时间戳：有问题，因为无法保证时钟同步
  - 参见 Spanner 论文作为使用分布式共识的例子
- 领导者选举
  - 等价于分布式共识
  - 当领导者的工作可以由一个进程完成或分片时，领导者选举模式允许像编写简单程序一样编写分布式系统
  - GFS 和 Colossus 使用
- 分布式协调和锁定服务
  - 屏障，例如在 MapReduce 中确保 Map 在 Reduce 继续之前完成
- 分布式队列和消息传递
  - 队列可以容忍工作节点的故障，但系统需要确保认领的任务被处理
  - 可以使用租约而非从队列中移除
  - 使用 RSM 意味着即使队列宕机，系统也可以继续处理

** 性能

- 共识算法不能用于高吞吐量低延迟系统的传统观念是错误的
- 分布式共识是许多 Google 系统的核心
- 规模使这对 Google 来说比大多数其他公司更糟，但它仍然有效

** Multi-Paxos

- 强领导者进程：除非尚未选出领导者或发生故障，只需一次往返即可达成共识
- 注意组中的其他进程可以随时提案
- 可能来回乒乓和伪活锁
- 标准解决方案是选出提议者进程或使用轮换提议者

** 扩展读密集型工作负载

- 例如：Photon 允许从任何副本读取
- 从陈旧副本读取需要额外工作，但不会产生不正确的结果
- 要保证读取是最新的，执行以下之一：
  1. 执行只读共识操作
  2. 从保证是最新的副本读取（稳定领导者可以提供此保证）
  3. 使用法定人数租约

** Fast Paxos

- 设计用于在 WAN 上更快
- 每个客户端可以直接向一组接受者发送 Propose，而不是通过领导者
- 不一定比经典 Paxos 更快——如果到接受者的 RTT 很长，我们用一次慢链路消息加 N 次并行快链路消息交换了 N 次慢链路消息

** 稳定领导者

- &quot;几乎所有为性能而设计的分布式共识系统都使用单一稳定领导者模式或轮换领导系统&quot;

* 第 25 章：数据处理管道

- 例如 MapReduce 或 Flume
- 在正常情况下方便且容易推理，但脆弱
- 初始安装通常没问题，因为工作器大小、分块、参数都经过仔细调整
- 随着时间推移，负载变化，导致问题

* 第 26 章：数据完整性

** 数据完整性的定义

- 定义不一定显而易见
- 如果界面 bug 导致 Gmail 无法显示消息，从用户的角度来看这与数据丢失相同
- 99.99% 正常运行时间意味着每年 1 小时停机。对大多数应用可能可以
- 99.99% 好字节在 2GB 文件中意味着 200K 损坏。对大多数应用可能不行

** 备份不简单

- 可能有事务性和非事务性备份和恢复的混合
- 不同版本的业务逻辑可能同时在线
- 如果服务独立版本控制，可能有许多版本组合
- 副本不够——副本可能同步损坏

** Google 19 次数据恢复工作的研究

- 最常见的用户可见数据丢失由删除或因软件 bug 导致的引用完整性丢失引起
- 最困难的情况是在数周到数月后才发现的低级别损坏

** 纵深防御

- 第一层：软删除
  - 用户应该能够删除他们的数据
  - 但这意味着用户会意外删除他们的数据
  - 账户劫持等
  - 意外删除也可能因 bug 发生
  - 软删除将实际删除延迟一段时间
- 第二层：备份
  - 需要确定恢复时丢失多少数据是可以接受的，恢复需要多长时间，备份需要追溯到多远
  - 希望备份追溯到永远，因为损坏可能数月（或更长）未被发现
  - 但代码和 schema 的变更可能使旧备份的恢复变得昂贵
  - Google 通常有 30 到 90 天的窗口，取决于服务
- 第三层：早期检测
  - 带外完整性检查
  - 做好很难！
  - 正确的变更可能导致检查器失败
  - 但放松检查可能导致遗漏故障

</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/14/google-sre-book-读书笔记/index.html</guid>
</item>
<item>
<title>Leiningen 学习笔记：Clojure 项目构建与管理从入门到实战配置</title>
<link>https://lujun9972.github.io/blog/2026/04/14/leiningen-学习笔记：clojure-项目构建与管理从入门到实战配置/index.html</link>
<description>本文是学习 [[https://dev.to/ivangavlik/leiningen-complete-tutorial-best-practices-3f8l][Leiningen — Complete Tutorial &amp; Best Practices]] 过程中的笔记整理。全文以一个 =greeting-api= 项目为统一示例，从零开始演示 Leiningen 的完整工作流。

* Leiningen 是什么
Leiningen（简称 lein）是 Clojure 的构建工具和项目管理器。如果你用过其他语言，可以这样类比：
- Node.js → npm
- Java → Maven
- Python → pip
- Clojure → Leiningen

它围绕 Clojure 的哲学设计，主要功能包括：
1. 创建和脚手架项目
2. 管理依赖（从 Clojars 和 Maven Central）
3. 运行 REPL、测试和构建
4. 编译打包应用（JAR/uberjar）
5. 通过插件运行自定义任务
6. 管理不同环境的配置（dev/test/prod）

** 安装
Leiningen 需要 JDK 8 或更高版本。安装步骤如下：

#+BEGIN_SRC shell
# 1. 下载 lein 脚本
curl -L -o ~/bin/lein https://raw.githubusercontent.com/technomancy/leiningen/stable/bin/lein

# 2. 赋予执行权限
chmod +x ~/bin/lein

# 3. 首次运行会自动完成安装
lein version
#+END_SRC

我的环境输出如下：
#+BEGIN_SRC fundamental
  Leiningen 2.10.0 on Java 21.0.10 OpenJDK 64-Bit Server VM
#+END_SRC

[注] 简单来说，Leiningen 就是 Clojure 世界里的&quot;瑞士军刀&quot;，项目从创建到部署几乎都离不开它。

* 创建项目
使用 =lein new app= 命令创建一个应用项目：

#+BEGIN_SRC shell :dir /tmp/
  lein new app greeting-api
#+END_SRC

实际输出：
#+BEGIN_SRC fundamental
  Generating a project called greeting-api based on the &#39;app&#39; template.
#+END_SRC

** 生成的目录结构
Leiningen 实际生成了如下文件：

#+BEGIN_SRC fundamental
  greeting-api/
  ├── CHANGELOG.md
  ├── LICENSE
  ├── README.md
  ├── doc/
  │   └── intro.md
  ├── project.clj              ← 核心：依赖、配置、构建定义
  ├── resources/               ← 静态文件、配置、资源
  ├── src/
  │   └── greeting_api/
  │       └── core.clj         ← 主命名空间
  └── test/
      └── greeting_api/
          └── core_test.clj    ← 测试文件
#+END_SRC

[注] 这里有个容易踩坑的地方：Clojure 命名空间用 =-= （连字符），但对应的文件夹和文件名用 =_= （下划线）。比如命名空间 =greeting-api.core= 对应的文件路径是 =src/greeting_api/core.clj= 。这个转换规则是 Clojure 的惯例，Leiningen 会自动处理，但如果手动创建文件时搞混了就会找不到命名空间。

** Leiningen 自动生成的初始文件
生成后 =project.clj= 的内容如下：

#+BEGIN_SRC clojure
  (defproject greeting-api &quot;0.1.0-SNAPSHOT&quot;
    :description &quot;FIXME: write description&quot;
    :url &quot;http://example.com/FIXME&quot;
    :license {:name &quot;EPL-2.0 OR GPL-2.0-or-later WITH Classpath-exception-2.0&quot;
              :url &quot;https://www.eclipse.org/legal/epl-2.0/&quot;}
    :dependencies [[org.clojure/clojure &quot;1.11.1&quot;]]
    :main ^:skip-aot greeting-api.core
    :target-path &quot;target/%s&quot;
    :profiles {:uberjar {:aot :all
                         :jvm-opts [&quot;-Dclojure.compiler.direct-linking=true&quot;]}})
#+END_SRC

生成后 =src/greeting_api/core.clj= 的内容如下：

#+BEGIN_SRC clojure
  (ns greeting-api.core
    (:gen-class))

  (defn -main
    &quot;I don&#39;t do a whole lot ... yet.&quot;
    [&amp; args]
    (println &quot;Hello, World!&quot;))
#+END_SRC

[注] 注意 =(:gen-class)= 声明——它告诉编译器为这个命名空间生成 Java 类。这是后面打包 uberjar 时能找到入口点的关键，如果忘了加，即使配置了 =:aot= ，JVM 也找不到入口。另外 =:main ^:skip-aot= 中的 =^:skip-aot= 表示日常开发时不做 AOT 编译，只在打包 uberjar 时才编译。

** 第一次运行
在项目根目录下执行：

#+BEGIN_SRC shell :dir /tmp
  cd greeting-api
  lein run
#+END_SRC

实际输出（首次运行会下载依赖，后续运行不再显示下载信息）：
#+BEGIN_SRC fundamental
  Retrieving nrepl/nrepl/1.0.0/nrepl-1.0.0.pom from clojars
  Retrieving nrepl/nrepl/1.0.0/nrepl-1.0.0.jar from clojars
  ...
  Hello, World!
#+END_SRC

* project.clj 详解
=project.clj= 是整个项目的核心配置文件。现在我们在初始版本的基础上逐步添加功能，将其改造为一个完整的配置。

下面是 =greeting-api= 项目最终的 =project.clj= ，每个部分会在后续章节中详细演示：

#+BEGIN_SRC clojure :tangle /tmp/greeting-api/project.clj
  (defproject greeting-api &quot;0.1.0-SNAPSHOT&quot;
    :description &quot;一个简单的问候 API 服务&quot;
    :url &quot;https://github.com/you/greeting-api&quot;
    :license {:name &quot;MIT&quot;}

    ;; ===== 依赖 =====
    ;; 格式：[group/artifact &quot;version&quot;]
    ;; 实际上就是 Maven 坐标的简写形式
    :dependencies [[org.clojure/clojure &quot;1.11.1&quot;]
                   [ring/ring-core &quot;1.13.0&quot;]      ; HTTP 抽象层
                   [hiccup &quot;1.0.5&quot;]]               ; HTML 生成

    ;; 入口命名空间（lein run 时调用其中的 -main 函数）
    :main greeting-api.core

    ;; 源码路径（这些是默认值，通常不需要显式配置）
    :source-paths [&quot;src&quot;]
    :test-paths [&quot;test&quot;]
    :resource-paths [&quot;resources&quot;]

    ;; ===== 插件（详见后文 Plugins 章节）=====
    :plugins [[lein-ring &quot;0.12.6&quot;]]

    ;; Ring 插件配置
    :ring {:handler greeting-api.core/app
           :port 3000}

    ;; ===== Aliases（详见后文 Aliases 章节）=====
    :aliases {&quot;fmt&quot;   [&quot;cljfmt&quot; &quot;fix&quot;]
              &quot;lint&quot;  [&quot;eastwood&quot;]
              &quot;build&quot; [&quot;with-profile&quot; &quot;uberjar&quot; &quot;uberjar&quot;]
              &quot;ci&quot;    [&quot;do&quot; &quot;clean,&quot; &quot;cljfmt&quot; &quot;check,&quot; &quot;eastwood,&quot; &quot;test,&quot; &quot;uberjar&quot;]}

    ;; ===== Profiles（详见后文 Profiles 章节）=====
    :profiles {:dev     {:dependencies [[ring/ring-mock &quot;0.4.0&quot;]]
                         :plugins [[lein-cljfmt &quot;0.9.2&quot;]
                                   [jonase/eastwood &quot;1.4.3&quot;]]
                         :source-paths [&quot;dev&quot;]}
               :uberjar {:aot :all
                         :omit-source true}})
#+END_SRC

[注] =defproject= 宏的第一个参数是项目名，第二个是版本号（遵循 [[https://semver.org/lang/zh-CN/][语义化版本]] 规范）。=0.1.0-SNAPSHOT= 中的 SNAPSHOT 表示开发版本，这在 Java/Clojure 生态中是惯例。

* 编写核心代码
现在我们来编写 =greeting-api= 的核心代码，后续所有配置演示都围绕这个项目。

** 修改 src/greeting_api/core.clj

#+BEGIN_SRC clojure :tangle /tmp/greeting-api/src/greeting_api/core.clj
  (ns greeting-api.core
    (:require [ring.util.response :as resp]
              [ring.middleware.params :refer [wrap-params]])
    (:gen-class))

  (defn greet
    &quot;返回问候字符串&quot;
    [name]
    (str &quot;Hello, &quot; name &quot;!&quot;))

  ;; Ring handler：处理 HTTP 请求
  (defn handler [request]
    (let [name (get-in request [:query-params &quot;name&quot;] &quot;World&quot;)]
      (resp/response (greet name))))

  ;; 用 wrap-params 中间件自动解析查询参数，然后供 lein-ring 使用
  (def app (wrap-params handler))

  (defn -main [&amp; args]
    (println (greet &quot;World&quot;)))
#+END_SRC

[注] 这里用到了 =wrap-params= 中间件，它的作用是将 URL 中的查询参数（如 =?name=Alice= ）解析到 =:query-params= 中。Ring 默认不解析查询参数，需要手动添加这个中间件。

** 编写测试 test/greeting_api/core_test.clj

#+BEGIN_SRC clojure :tangle /tmp/greeting-api/test/greeting_api/core_test.clj
  (ns greeting-api.core-test
    (:require [clojure.test :refer :all]
              [greeting-api.core :refer :all]))

  (deftest test-greet
    (testing &quot;按名字问候&quot;
      (is (= &quot;Hello, Alice!&quot; (greet &quot;Alice&quot;))))
    (testing &quot;默认问候&quot;
      (is (= &quot;Hello, World!&quot; (greet &quot;World&quot;)))))
#+END_SRC

** 运行测试

#+BEGIN_SRC shell :dir /tmp/greeting-api/
  lein test
#+END_SRC

#+RESULTS:

实际输出：
#+BEGIN_SRC fundamental
  lein test greeting-api.core-test

  Ran 1 tests containing 2 assertions.
  0 failures, 0 errors.
#+END_SRC

[注] =lein test= 会自动运行 =test/= 目录下所有命名空间中以 =deftest= 定义的测试。

** 运行项目
确保 =project.clj= 中配置了 =:main greeting-api.core= 后：

#+BEGIN_SRC shell
  lein run
#+END_SRC

实际输出：
#+BEGIN_SRC fundamental
  Hello, World!
#+END_SRC

也可以通过 =-m= 参数直接指定要运行的命名空间和函数：
#+BEGIN_SRC shell
  lein run -m greeting-api.core/-main
#+END_SRC

* Profiles（环境配置）
Profile 是 Leiningen 中非常实用的功能，它允许你为不同环境（开发、测试、生产）定义不同的配置，而这些配置会合并覆盖默认配置。

** 合并规则与默认激活
Profile 的合并规则是&quot;后面的覆盖前面的&quot;。Leiningen 有几个默认激活的 profile：
1. =:user= （来自 =~/.lein/profiles.clj= ）——总是激活
2. =:dev= ——在大多数命令中自动激活
3. =:test= ——在运行 =lein test= 时自动激活

这意味着你在 =:dev= 里配置的依赖和插件不需要手动指定就会生效。

** 在 greeting-api 中使用 Profiles
我们在 =greeting-api= 的 =project.clj= 中定义以下 profiles：

#+BEGIN_SRC clojure
  :profiles {:dev     {:dependencies [[ring/ring-mock &quot;0.4.0&quot;]]
                         :plugins [[lein-cljfmt &quot;0.9.2&quot;]
                                   [jonase/eastwood &quot;1.4.3&quot;]]
                         :source-paths [&quot;dev&quot;]}
               :uberjar {:aot :all
                         :omit-source true}})
#+END_SRC

逐项说明：
- =:dev= profile（开发时自动激活）：
  - 添加 =ring-mock= 依赖用于模拟 HTTP 请求
  - 添加 =lein-cljfmt= 插件用于代码格式化
  - 添加 =dev/= 作为额外源码目录，可以在 =dev/user.clj= 中放 REPL 辅助函数
- =:uberjar= profile（打包时使用）：
  - =:aot :all= 启用 AOT 编译所有命名空间
  - =:omit-source true= 不把源码打入 JAR

** 手动激活 Profile

#+BEGIN_SRC shell :dir /tmp/greeting-api/ :results raw
  lein with-profile uberjar uberjar    # 用 :uberjar profile 打包
  lein with-profile dev,test test      # 同时激活 :dev 和 :test
#+END_SRC

#+RESULTS:
#&#39;leiningen.core.project/project
Warning: The Main-Class specified does not exist within the jar. It may not be executable as expected. A gen-class directive may be missing in the namespace which contains the main method, or the namespace has not been AOT-compiled.
Created /tmp/greeting-api/target/greeting-api-0.1.0-SNAPSHOT.jar
Created /tmp/greeting-api/target/greeting-api-0.1.0-SNAPSHOT-standalone.jar
#&#39;leiningen.core.project/project

lein test user

Ran 0 tests containing 0 assertions.
0 failures, 0 errors.

** 常见使用模式

*** 开发工具不打入生产包
把开发工具放在 =:dev= profile 里是个好习惯。如上面的配置所示， =ring-mock= 、 =lein-cljfmt= 都只在开发时生效，打包 uberjar 时这些依赖不会被包含进去，保持生产包干净。

*** 额外的源码目录
在 =:dev= profile 中配置 =:source-paths [&quot;dev&quot;]= 后，Leiningen 会把项目根目录（即 =project.clj= 所在目录）下的 =dev/= 也作为源码目录。这样就可以在 =dev/user.clj= 中放 REPL 辅助函数和启动代码，方便开发但不影响生产构建。

例如创建 =dev/user.clj= ：
#+BEGIN_SRC clojure :tangle /tmp/greeting-api/dev/user.clj
  (ns user
    (:require [greeting-api.core :as core]))

  (defn start-dev []
    (println &quot;Dev mode started!&quot;)
    (core/greet &quot;Developer&quot;))
#+END_SRC

在 REPL 中，由于默认进入的是 =:main= 指定的命名空间（这里是 =greeting-api.core= ），且 =:source-paths= 只是让目录在 classpath 上可被找到，并不会自动加载其中的代码，所以需要先 =require= 加载 =user= 命名空间：

#+BEGIN_SRC fundamental
  greeting-api.core=&gt; (require &#39;user)
  nil
  greeting-api.core=&gt; (user/start-dev)
  Dev mode started!
  &quot;Hello, Developer!&quot;
#+END_SRC

也可以切换到 =user= 命名空间后直接调用：
#+BEGIN_SRC fundamental
  greeting-api.core=&gt; (in-ns &#39;user)
  nil
  user=&gt; (start-dev)
  Dev mode started!
  &quot;Hello, Developer!&quot;
#+END_SRC

* Plugins（插件体系）
插件用来扩展 Leiningen 的功能。根据使用场景，插件有三种放置位置。

这里我们以 =greeting-api= 项目为例，演示如何安装和使用插件。

** 项目级插件（团队共享）
放在 =project.clj= 的顶层 =:plugins= 中，所有人 clone 后都能使用。

在 =greeting-api= 的 =project.clj= 中添加：

#+BEGIN_SRC clojure
  :plugins [[lein-ring &quot;0.12.6&quot;]]
#+END_SRC

启动 Ring 服务器：
#+BEGIN_SRC shell
  lein ring server-headless
#+END_SRC

服务器启动后，用 curl 测试：
#+BEGIN_SRC shell
  curl http://localhost:3000
#+END_SRC

实际输出：
#+BEGIN_SRC fundamental
  Hello, World!
#+END_SRC

带参数访问：
#+BEGIN_SRC shell
  curl &quot;http://localhost:3000?name=Alice&quot;
#+END_SRC

实际输出：
#+BEGIN_SRC fundamental
  Hello, Alice!
#+END_SRC

按 =Ctrl+C= 停止服务器。

[注] =lein-ring= 的 =:handler= 配置指向 =core.clj= 中的 =app= 变量。注意我们的 =app= 使用了 =wrap-params= 中间件来解析查询参数。

** Profile 内插件（按需激活）
放在特定 profile 中，只在激活该 profile 时生效。

在 =greeting-api= 的 =project.clj= 的 =:dev= profile 中添加：

#+BEGIN_SRC clojure
  :profiles {:dev {:plugins [[lein-cljfmt &quot;0.9.2&quot;]
                             [jonase/eastwood &quot;1.4.3&quot;]]}}
#+END_SRC

使用 =lein-cljfmt= 检查代码格式：
#+BEGIN_SRC shell :dir /tmp/greeting-api/
  lein cljfmt check
#+END_SRC

实际输出（代码格式正确时）：
#+BEGIN_SRC fundamental
  All source files formatted correctly
#+END_SRC

如果有格式问题，用以下命令自动修复：
#+BEGIN_SRC shell
  lein cljfmt fix
#+END_SRC

** 全局插件（个人工具）
放在 =~/.lein/profiles.clj= 中，不进入项目仓库，适合个人常用工具：

#+BEGIN_SRC clojure
  {:user {:plugins [[lein-ancient &quot;0.7.0&quot;]
                    [lein-pprint &quot;1.3.2&quot;]]}}
#+END_SRC

使用 =lein-ancient= 检查依赖是否过时：
#+BEGIN_SRC shell
  lein ancient :all
#+END_SRC

#+begin_example
  Retrieving lein-ancient/lein-ancient/0.7.0/lein-ancient-0.7.0.jar from clojars
  [org.clojure/clojure &quot;1.12.4&quot;] is available but we use &quot;1.11.1&quot; (use :check-clojure to upgrade)
  [ring/ring-core &quot;1.15.4&quot;] is available but we use &quot;1.13.0&quot;
  [hiccup &quot;2.0.0&quot;] is available but we use &quot;1.0.5&quot;
  (warn)  [central] - Δ 13667ms - failure when checking ring/ring-mock: java.net.SocketTimeoutException: Read timed out
  [ring/ring-mock &quot;0.6.2&quot;] is available but we use &quot;0.4.0&quot;
#+end_example

** lein-ring 配置
在上面的配置中，我们为 =greeting-api= 配置了 =lein-ring= 插件：

#+BEGIN_SRC clojure
  :ring {:handler greeting-api.core/app    ; 指向 core.clj 中的 app 变量
         :port 3000}                        ; 端口号
#+END_SRC

=lein-ring= 还支持更多选项：
#+BEGIN_SRC clojure
  :ring {:handler greeting-api.core/app
         :port 3000
         :auto-reload? true           ; 自动重载（默认 true）
         :reload-paths [&quot;src&quot;]        ; 监听的目录
         :open-browser? false}        ; 是否自动打开浏览器
#+END_SRC

** 常用插件一览

| 类别     | 插件            | 用途                        | 常用命令            |
|----------+-----------------+-----------------------------+---------------------|
| Web 开发 | lein-ring       | 运行 Ring 应用，自动重载    | =lein ring server=  |
| 代码格式 | lein-cljfmt     | 格式化代码（类似 Prettier） | =lein cljfmt fix=   |
| 静态分析 | jonase/eastwood | 发现常见 bug                | =lein eastwood=     |
| 惯用法   | lein-kibit      | 建议更地道的写法            | =lein kibit=        |
| 依赖管理 | lein-ancient    | 检查过时的依赖              | =lein ancient :all= |

查找更多插件可以访问 [[https://clojars.org][Clojars]]（搜索 =lein-*= 前缀）或 [[https://github.com/technomancy/leiningen/wiki/Plugins][Leiningen 官方插件列表]] 。

* Aliases（自定义快捷命令）
Aliases 允许你在 =project.clj= 中定义快捷命令，避免每次输入一长串参数。

** 在 greeting-api 中配置 Aliases

#+BEGIN_SRC clojure
  :aliases {&quot;fmt&quot;   [&quot;cljfmt&quot; &quot;fix&quot;]
            &quot;lint&quot;  [&quot;eastwood&quot;]
            &quot;build&quot; [&quot;with-profile&quot; &quot;uberjar&quot; &quot;uberjar&quot;]}
#+END_SRC

** 使用简单别名

#+BEGIN_SRC shell
  lein fmt    # 等同于 lein cljfmt fix
  lein lint   # 等同于 lein eastwood
#+END_SRC

** 链式任务（do）
用 =do= 将多个任务串联执行。注意逗号是 Leiningen =do= 任务的语法要求——它分隔 =do= 中的不同任务表达式，每个任务（除最后一个）后需要逗号。

例如配置一个 CI 流水线：
#+BEGIN_SRC clojure
  :aliases {&quot;ci&quot; [&quot;do&quot; &quot;clean,&quot; &quot;cljfmt&quot; &quot;check,&quot; &quot;test,&quot; &quot;uberjar&quot;]}
#+END_SRC

运行：
#+BEGIN_SRC shell
  lein ci
  # 依次执行：clean → cljfmt check → test → uberjar
#+END_SRC

** Profile + 任务组合
=&quot;build&quot;= 别名演示了如何组合 profile 和任务：

#+BEGIN_SRC clojure
  &quot;build&quot; [&quot;with-profile&quot; &quot;uberjar&quot; &quot;uberjar&quot;]
#+END_SRC

等价于：
#+BEGIN_SRC shell
  lein with-profile uberjar uberjar
#+END_SRC

即用 =:uberjar= profile 来执行 =uberjar= 任务（触发 AOT 编译）。

* AOT 编译与打包
AOT 即 Ahead-Of-Time（提前编译）。正常情况下，Clojure 代码是在 JVM 启动时即时编译的。而 AOT 则是在构建阶段就把 Clojure 代码编译成 =.class= 文件。

** 为什么需要 AOT？
当你用 =lein run= 运行项目时，Leiningen 会启动 JVM、加载 Clojure 运行时、即时编译你的代码，然后调用 =-main= 函数——这个过程依赖 Leiningen 的运行时支持。

但当你想用 =java -jar greeting-api.jar= 直接运行一个 JAR 包时，JVM 需要一个标准的 Java 入口方法：
#+BEGIN_SRC java
  public static void main(String[] args)
#+END_SRC

Clojure 的 =-main= 函数本身不是这样的入口。AOT 编译会将 =:aot= 配置中指定的所有命名空间编译成 =.class= 文件，其中包含符合 Java 入口点规范的 =main= 方法，这样 JVM 就能直接运行了。

** 为什么只在 uberjar profile 中使用？
AOT 有几个缺点，不适合在开发时使用：
- 编译变慢：每次都要编译所有命名空间
- 过期的 =.class= 文件：修改代码后如果旧的 =.class= 文件没清理干净，可能引发难以排查的 bug
- 产物更大： =target/= 目录里多出一堆文件

因此标准做法是把 AOT 放在 =:uberjar= profile 中（这也是 =lein new app= 生成的默认配置）：

#+BEGIN_SRC clojure
  :profiles {:uberjar {:aot :all
                       :omit-source true}}
#+END_SRC

** 打包 greeting-api
确保 =src/greeting_api/core.clj= 中有 =(:gen-class)= 声明（前面已经添加），然后执行：

#+BEGIN_SRC shell
  lein build
  # 等价于: lein with-profile uberjar uberjar
#+END_SRC

实际输出：
#+BEGIN_SRC fundamental
  Compiling greeting-api.core
  Created /tmp/greeting-api/target/greeting-api-0.1.0-SNAPSHOT.jar
  Created /tmp/greeting-api/target/greeting-api-0.1.0-SNAPSHOT-standalone.jar
#+END_SRC

验证打包结果：
#+BEGIN_SRC shell
  java -jar target/greeting-api-0.1.0-SNAPSHOT-standalone.jar
#+END_SRC

实际输出：
#+BEGIN_SRC fundamental
  Hello, World!
#+END_SRC

[注] =standalone.jar= 就是 uberjar——它把 Clojure 运行时和所有依赖都打进了同一个 JAR 文件，可以直接用 =java -jar= 运行，无需安装 Leiningen 或 Clojure。

* 总结
通过这篇笔记，我们用一个 =greeting-api= 项目演示了 Leiningen 的完整工作流：

1. **安装与创建**：用 =lein new app= 创建项目，注意命名空间 =-= 与文件路径 =_= 的转换
2. **依赖与运行**：在 =project.clj= 中声明依赖，用 =lein run= 和 =lein test= 运行和测试
3. **Profiles**：用 =:dev= 放开发工具，用 =:uberjar= 做 AOT 编译，保持生产包干净
4. **Plugins**：通过插件扩展功能（如 =lein-ring= 运行 Web 服务）
5. **Aliases**：用 =:aliases= 定义快捷命令，统一团队工作流
6. **打包**：用 =lein uberjar= 生成可独立运行的 JAR 文件

** 推荐资源
- [[https://leiningen.org/][Leiningen 官网]]
- [[https://github.com/technomancy/leiningen/blob/master/doc/TUTORIAL.md][Leiningen Tutorial（官方教程）]]
- [[https://clojars.org][Clojars（Clojure 包仓库）]]
- [[https://dev.to/ivangavlik/leiningen-complete-tutorial-best-practices-3f8l][原文：Leiningen — Complete Tutorial &amp; Best Practices]]
</description>
<pubDate>2026-04-15</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/14/leiningen-学习笔记：clojure-项目构建与管理从入门到实战配置/index.html</guid>
</item>
</channel>
</rss>