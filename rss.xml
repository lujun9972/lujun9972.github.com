<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>暗无天日</title>
    <link>https://lujun9972.github.io</link>
    <description>=============&gt;DarkSun的个人博客</description>
    <pubDate>Mon, 04 May 2026 15:17:34 UTC</pubDate>
    <lastBuildDate>Mon, 04 May 2026 15:17:34 UTC</lastBuildDate>
    <docs>http://www.rssboard.org/rss-specification</docs>
    <generator>ego static site generator (https://github.com/emacs-china/ego)</generator>
<item>
<title>读：7 Techniques That Supercharged My Claude-Assisted Development</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：7-techniques-that-supercharged-my-claude-assisted-development/index.html</link>
<description>Claude Code 用了一段时间，算重度用户了。写博客、改配置、知识管理基本都在上面。DZone 上 Vivek Katarya 的这篇文章列出了 7 个进阶技巧，我对照着自己的使用经验梳理一遍，看看哪些已经在用了、哪些是新学的、哪些有不同看法。

* 技巧 1：停止审批每个命令

Claude Code 默认每个操作都要弹确认： =Run npm test? (Y/N)= 。安全是安全，但迭代的时候频繁打断很影响节奏。

解决方案是在 =~/.claude/settings.json= 里配一个白名单：

#+begin_src json  :tangle ~/.claude/settings.json
{
  &quot;permissions&quot;: {
    &quot;allow&quot;: [
      &quot;Read(~/Codebase)&quot;,
      &quot;Edit(~/Codebase)&quot;,
      &quot;Bash(ls:*)&quot;,
      &quot;Bash(cat:*)&quot;,
      &quot;Bash(tail:*)&quot;,
      &quot;Bash(grep:*)&quot;
    ]
  }
}
#+end_src

配了之后，这些操作就不再弹确认了。原文的 advice 是只放只读或低风险操作。我加了 =Bash(grep:*)= 和 =Bash(cat:*)= 用来做代码搜索，确实方便很多。涉及生产环境或密钥的操作继续弹确认，这个分寸自己把握。

原文还特意警告了 =--dangerously-skip-permissions= 这个 flag：等于给 Claude 开了 God Mode，风险远大于便利。别用。

* 技巧 2：让 Claude 自己改配置

Claude Code 能改自己的配置文件。想改什么直接说就行，不用翻文档找 JSON 路径。

比如我把换行快捷键改了：

: 把换行快捷键改成 Shift+Enter。

想调权限也一样：

: 更新权限，让 bash 命令 ls、grep、tail 不用确认就能执行。

这个我经常用。调整 verbose 级别、diff 格式、文件搜索范围，想到了就丢一句话过去，省了手动编辑 JSON 的功夫。

一个限制：改完配置后要手动重启 Claude 才生效，它没法给自己发 SIGHUP。

* 技巧 3：给 Claude 一个入口文件

Claude 默认会自动探索代码库来理解上下文。项目大了之后，它会在无关文件上浪费 token。

解决方案是写一个入口脚本（比如 =scripts/context.sh= ），只展示 Claude 真正需要的信息：

#+begin_src shell  :tangle scripts/context.sh
#!/bin/bash

echo &quot;README:&quot;
cat README.md

echo
echo &quot;Key services:&quot;
grep -r &quot;class .*Service&quot; src/
#+end_src

然后告诉 Claude：&quot;每次需要项目上下文时，先跑 =scripts/context.sh= &quot;。这句指令可以在对话里直接说。

再配合 =.claudeignore= 过滤掉 node_modules/、build/ 、*.log 这些噪音，效果更好。原理跟 =.gitignore= 一样，不让 Claude 在无关文件上浪费 token。

* 技巧 4：把 fix-test 循环交给 skill

原文举了一个例子：不用自己跑测试、看失败、再修，直接告诉 Claude：

: 跑测试，标记失败用例，修好，再跑。直到全通过，或者最多重试 3 次。

原本来回 15 分钟的循环变成一句话。然后可以把这条指令保存成 skill（ =~/.claude/commands/fix-test/SKILL.md= ），以后直接复用。

我的场景不太一样（写代码修测试不是日常），但这个模式本身通用。比如写博客的时候，lint 报了一堆格式错误，直接告诉 Claude &quot;把 lint 报的问题全修了，修完再跑一遍确认&quot;。跟 fix-test 循环是一个套路，把迭代交给它，我等结果就好。

* 技巧 5：链式调用 skills

有了几个基础 skill（write-unit-tests、fix-test、create-pr）之后，下一步是把它们串联成一个高阶 skill。

比如定义一个 =sanitize-and-publish= 的 skill，自动跑 fix-build → check-coverage → write-unit-tests → fix-tests → create-pr。原文形容得好：像管理一个轻量级的多代理流水线。

这个我还没系统性做，但思路对。比如我现在的 =url2blog= 其实已经在链式调用了，它内部又调了 brainstorming 和 人味儿 两个子 skill。按原文的思路，可以把这些拆成更细粒度的独立 skill，再组合出更高阶的流程。下次可以试试。

* 技巧 6：用 worktree 并行跑多个会话

单会话不够用时，可以用 Git worktree 开多个并行的 Claude 实例。一个修 bug、一个重构模块、一个搞原型：

: claude --worktree feature-x

每个实例有自己的工作目录，互不干扰。干完后把 worktree 合到对应分支就行。

superpowers 里其实已经有 =using-git-worktrees= skill 封装了这个流程，但我平时很少需要并行开多个会话。偶尔需要同时做两件事时（比如这篇博文写到一半，突然想试另一个写法），直接用 =claude --worktree= 起一个新实例，互不干扰。

* 技巧 7：复杂任务先出计划

原文说这是&quot;提升输出质量最管用的一个习惯&quot;：

: 先生成一个分步计划，识别边界情况，提出澄清问题。我确认后再执行。

Claude 的第一版计划通常 80% 对。那漏掉的 20% 正好是 bug 和回归问题所在。花两分钟确认，经常能省下两小时。

这一点完全同意。我现在用 =superpowers:brainstorming= 和 =writing-plans= 就是这个流程，先想清楚再动手。尤其在写代码或写博文之前，plan first 能把方向跑偏的风险降到最低。

* 几个补充

原文末尾还有几个技巧：

- *用 Orchestrator 管理多会话* ：会话多了以后切换成本很高，Orchestrator 这类工具帮助协调多个 Claude 实例
- *让 Claude 自我审查* ：在你自己做 code review 之前，让 Claude 先 review 自己的改动。它能抓到不少逻辑和流程上的问题
- *让 Claude 自己改进 skill* ：用 Skill Creator 让 Claude 迭代自己的工具集，工作流会自动演进

最后一个我最近在实践。做了 =log-reviewer= skill 之后，确实感觉到了&quot;让 AI 自己造工具&quot;的正循环。每次用 skill 写博文、改代码，都在积累经验，下次就更顺手。

* 总结

原文把 Claude Code 比作&quot;一个写代码极快、但需要明确指令的初级工程师&quot;。这个比喻贴切。权限白名单、入口文件、skill 链、plan-first，这些东西搭配在一起之后，重复劳动减少、迭代变快、脑子省出来想真正的问题。

这 7 个技巧里，fix-test 循环、链式 skill、plan-first 这几个我已经在用了，而且用出了自己的变体。权限白名单和 self-configure 属于&quot;知道但没认真配&quot;的，写这篇的时候顺便补上了。worktree 并行有现成 skill 可以用，只是平时场景不多。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：7-techniques-that-supercharged-my-claude-assisted-development/index.html</guid>
</item>
<item>
<title>读：AI 编码代理的四种工作流</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：ai-编码代理的四种工作流/index.html</link>
<description>&quot;AI 编码代理&quot;这个词涵盖的范围很广：有的在编辑器里给行内建议，有的在终端里改文件，有的在 PR 里自动审查，还有的在云端独立干活。RealPython 的一篇文章 [[https://realpython.com/ai-coding-agents-guide/][AI Coding Agents Guide]] 把这些交互模式梳理为四种工作流。而且 Claude Code 是少数横跨全部四种的工具。这篇文章就从 Claude Code 用户的视角来解读这个分类框架。

* 什么是 AI 编码代理

AI 编码代理和普通聊天机器人的根本区别在于一个持续执行的循环。聊天机器人给你一个答案就结束了，代理会反复执行四步：

- *Read* ：读取代码库中的相关文件
- *Reason* ：推理完成目标所需的逻辑步骤
- *Act* ：编辑文件、运行命令、调用外部工具
- *Evaluate* ：检查结果，判断是否需要继续

这个循环不断重复，直到任务完成或交还控制权。关键差别在于，代理做的是&quot;问→做→检查→再做&quot;的闭环，不是一次性的&quot;问→答&quot;。

代理的核心循环是一样的，但代理运行的环境决定了你如何与之交互。同一个代理在不同环境下会呈现不同的交互模式。这就是四种工作流的由来。

* IDE 代理：编辑器内实时协作

IDE 代理直接嵌入代码编辑器，在你写代码的同时给出行内建议、显示 diff、让你一键接受或拒绝。

这类工具有两种形态：

*AI Native IDE* ：从零围绕 AI 构建的编辑器，代表有 Cursor、Windsurf、Kiro。Kiro 支持 spec-driven 模式，你先描述需求，代理独立完成。Cursor 的 Cloud Agents 还能在后台跑长时间任务。

*IDE 插件* ：在现有编辑器上增加 AI 能力，如 GitHub Copilot 插件、VS Code 的 Claude Code 扩展、Gemini Code Assist。插件通常更适合&quot;先定位文件再修改&quot;的交互式工作流。

IDE 代理的优势是实时反馈，你在写代码时它就给出建议，就像有个搭档坐在旁边看。缺点是 cloud-backed 的 IDE 代理会把你的代码发到外部服务器做推理。如果公司政策不允许代码离开本地，可以看看 Continue 这类支持本地模型的工具。

*Claude Code 的 IDE 模式* ：Claude Code 有 VS Code 和 JetBrains 的原生插件。安装了扩展后可以在编辑器里直接启动 Claude Code 会话。不过我日常主要用终端模式，IDE 插件只在需要时打开。

* 终端代理：shell 里的副驾驶

终端代理运行在 shell 中。你描述任务，代理读取文件、提出修改、执行命令，每步都由你确认后才继续。这是我最常用的模式。

一个是适合复杂跨文件修改。你可以让代理从整个项目出发，追踪 import 链和关联文件，在多个模块之间协调修改。相比 IDE 代理的&quot;打开文件→修改→保存&quot;工作流，终端代理可以一次性理解整个项目的结构。我们经常说&quot;让 AI 理解上下文&quot;，终端模式天然就有全局上下文，因为它能访问整个文件系统和命令行。

另一个是分步确认模型。代理每做完一步都会停下来问你是否满意，让你始终保持控制。我日常的感受是，这种交互既保持了控制感，又让代理干掉了大部分重复劳动。你不用自己记住每个改动的细节，只需要做决策：这个改法对不对，下一步往哪个方向。在 Claude Code 里可以开启 auto mode 减少确认次数，但我通常保持手动模式，因为编程中很多决策不能跳过。

终端模式还有一些高级用法：可以把日志 pipe 给代理分析，与其他 CLI 工具链式组合，或者嵌入自动化脚本。原因是终端代理运行在 shell 中，和现有开发工作流的集成度最高。

终端模式的代表工具包括 Claude Code、Aider、Gemini CLI、OpenCode、Codex CLI。有些代理还能通过 Ollama 连接本地模型，适合不能把代码外发的场景。

* PR 代理：异步代码审查

PR 代理和前两种完全不同，它不和你实时交互，而是异步工作。通常在 PR 打开或更新时自动触发，跑完后留下审查意见，你稍后回来查看。

PR 代理操作的是团队可见的共享分支，审查结果需要人来决定是否采纳。它的角色是合入前的安全网，而不是编码时能指挥的工具。

典型工作流：你开 PR，代理过一会儿会发布审查意见，你像处理同事的 review 一样接受、修改或驳回。

主要运行在 GitHub、GitLab、Bitbucket 上。代表工具包括 CodeRabbit、GitHub Copilot Code Review。很多团队会统一管控 AI 审查工具的权限，隐私决策通常在组织层面而非个人层面。

*Claude Code 的 PR 模式* ：Anthropic 提供 Code Review 托管服务，也开源了 [[https://github.com/anthropics/claude-code-github-actions][Claude Code GitHub Actions]] 让团队在自己的 CI 流程中运行。我没有在生产项目中用过这个功能，但原理是清晰的：配置后每个 PR 自动触发代码审查。

* 云代理：让AI在后台放手干

云代理的自主性最高。你描述任务，它在远程环境里独立干活，之后返回分支、PR 或原型等你审查。

这让云代理很适合用于新项目原型搭建或耗时较长的任务。你可以通过 Slack、工单系统或网页来启动任务，不用一直守在屏幕前。

代表工具包括 Devin、Claude Code on the web、Codex web、Cursor Cloud Agents。

但自主性高也意味着实时控制少，代码跑在远程基础设施上，不在你本地。所以云代理最适合边界清晰、产出容易审查的任务，比如分支、PR、原型。云生成的代码人工审查更加重要，一个重要原则是没审查过的代码就不要推送。

*Claude Code 的云模式* ：Claude Code on the web 运行在 Anthropic 管理的云基础设施上。还可以在 Slack 里用 @Claude 分配任务，它会启动云端的 Claude Code 会话。我没有实际用过云模式，但它的定位很清晰：适合那些你不想守在屏幕前盯着看的长任务。

* 四种模式的共性

原文特意指出，有三款工具横跨全部四种工作流：

- *Claude Code* ：终端（CLI）、IDE（VS Code/JetBrains 插件）、云端（Claude Code on the web）、PR（Code Review + GitHub Actions）
- *Cursor* ：IDE（编辑器本体）、终端（Cursor CLI）、云端（Cloud Agents）、PR（Bugbot）
- *GitHub Copilot* ：IDE（Copilot 插件）、终端（Copilot CLI）、云端（Copilot cloud agent）、PR（Copilot code review）

这说明分类法描述的是交互模式，不是产品类别。同一款工具在不同场景下可以切换不同的角色。Claude Code 横跨四种模式跟它&quot;全能&quot;没关系，纯粹是它设计上就支持不同的入口：CLI、插件、云端、CI，每种入口对应一种交互模式。

* 注意事项

原文还指出了几个常见陷阱。

*不要假设一种代理能包打天下。* IDE 代理擅长交互式编辑，终端代理擅长大范围重构，PR 代理抓住合入前的问题，云代理搞定独立原型。按任务匹配模式比追求&quot;最强&quot;更有意义。

*隐私合规要提前确认。* 多数代理会把代码发送到外部服务器做推理。用之前查一下公司政策，必要时找本地模型方案。

*AI 生成代码必须审查。* 代理可能引入隐蔽的 bug、不完善的异常处理、或不符合团队惯例的写法。代理越自主，审查越重要。

* 总结

AI 编码代理的四种交互模式（IDE、终端、PR、云）给出了一个选择框架：与其问哪个代理最强，不如问当前任务适合哪种模式。Claude Code 横跨全部四种模式是工具设计的结果，但从用户角度，更重要的是理解每种模式适合什么场景。工具会迭代，但交互模式的分类框架不会过时。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：ai-编码代理的四种工作流/index.html</guid>
</item>
<item>
<title>读：Agent 的瓶颈不在模型，在基础设施</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：agent-的瓶颈不在模型，在基础设施/index.html</link>
<description>有一个让人笑不出来的事实：两个团队用同一个模型做 agent，性能可以差二十个排名。唯一的变量不是模型版本，不是提示词技巧，而是基础设施。

你的 agent 在生产环境崩溃、遗忘、胡说八道的时候，毛病通常出在模型外面那整圈你以为是包装、实际上是灵魂的系统结构。这一层被统称为 Agent Harness（代理基础设施），它才是真正决定系统表现的关键变量。

（本文基于 jdon.com 上的一篇长文，提炼一个核心观点：基础设施比模型更重要。）

* 你以为在调模型，其实在调操作系统

很多人卡在一个概念误区上：什么是代理？什么是基础设施？

说直接一点：代理是你看到的智能行为，基础设施是背后那堆干脏活累活的机器。当你嘴上说&quot;我做了一个代理&quot;，真实情况是，你写了一套基础设施，然后接了个模型。模型本身不会变成代理，代理是行为涌现的结果，而基础设施是让这种行为发生的物理条件。

这个区分很重要，因为它直接决定你的优化方向。如果你一直盯着模型调参数，那你是在调演员。但真正决定演出质量的是舞台、灯光、剧本和调度系统。演员再牛，舞台塌了照样完蛋。

* CPU 与操作系统的类比

原文给了一个非常精准的类比：大语言模型就像中央处理器，没有内存、没有磁盘、没有输入输出，它甚至不知道世界存在。上下文窗口是内存，外部数据库是硬盘，工具是驱动程序，而基础设施就是操作系统。这不是比喻，这是结构等价。

你买了台顶级 CPU，然后放在纸板上通电，最后怪 CPU 跑不动游戏。模型就是那个 CPU，它连今天星期几都不知道。所有的记忆、工具、流程控制，都是基础设施给它装上的假肢。

更扎心的是，很多工程师花几周时间调提示词，却不愿意花一天时间好好设计工具调用和错误处理机制。结果系统在演示时像个天才，一上线就像个白痴。然后怪模型太垃圾。

* 三层工程体系

围绕模型，一共有三层工程结构，每层控制不同维度：

*第一层：提示词工程。* 负责&quot;说什么&quot;，也就是输入指令设计。大多数人停在这一层，觉得自己是提示词大师。

*第二层：上下文工程。* 负责&quot;什么时候看到什么&quot;，也就是信息调度。少部分人理解这一层，开始设计记忆和检索。

*第三层：基础设施工程。* 这是全局系统，包括工具调用、状态管理、错误恢复、安全机制等。真正决定系统上限的是这一层。

绝大多数 agent 项目失败，不是因为模型不够强，而是因为第三层根本不存在或者写得像坨屎。对话两三轮之后上下文就乱了，模型开始答非所问；工具调用十次有三次解析失败；错误信息直接塞回给模型，模型看不懂反而开始输出&quot;抱歉我遇到了错误&quot;之类的废话。这就是典型的基础设施坍塌。

反过来看，那些表现稳定的 agent，往往模型很普通，但基础设施设计得非常扎实。它们有清晰的状态机、严格的错误分类、完善的日志和恢复机制。

* 循环保持简单，模型保持聪明

整个 agent 运行的心跳是一个循环：思考、行动、观察。听起来像个 while 循环，确实就是。但真正复杂的不是循环，而是循环里管理的一切。

这个循环最搞笑的地方在于，很多工程师非要往里面塞各种花哨逻辑。在循环里加一堆条件判断、状态缓存、优先级队列。结果模型还没开始思考，基础设施就已经把数据搞乱了。

正确的做法是：让循环保持简单，只做机械的搬运工作，不做任何智能判断。循环只负责三件事：把信息喂给模型，等模型输出，执行模型要求的动作。其他所有事，比如记忆压缩、工具注册、错误恢复，都应该在循环外面处理。你越想把智能塞进基础设施的判断逻辑里，系统就越蠢。

* 脚手架隐喻：基础设施应该越变越薄

原文还有一个设计哲学值得提：基础设施像建筑脚手架，帮助构建，但最终应该被移除。随着模型变强，基础设施应该变薄。

想象一下，你为了盖一栋楼，搭了极其复杂的脚手架。楼盖好了，脚手架不拆，反而继续加装电梯、空调、霓虹灯。最后整条街的人都来看脚手架，没人注意那栋楼。这就是很多 agent 项目的现状。

模型本来可以自己处理记忆了，你还在用外部向量数据库。模型本来可以原生调用工具了，你还在写解析正则。模型本来有超大上下文了，你还在做压缩。你加的这些基础设施，不是帮助模型，而是限制模型。

正确的演进路径是：模型能力弱的时候，基础设施厚。模型能力变强，基础设施就变薄。每隔三个月，审视一下你的基础设施，问问自己：哪些东西现在模型自己能做了？然后删掉。删不掉的是核心，删掉的是过度工程。

* 七个设计抉择

工程实践中必须做七个选择。每个选择都有代价，没有标准答案，只有权衡：

1. 单代理还是多代理
2. 思考-行动-观察循环还是计划执行
3. 上下文管理：全量还是压缩
4. 验证机制：规则还是模型评判
5. 权限控制：白名单还是黑名单
6. 工具数量：多而全还是少而精
7. 基础设施：薄还是厚

总原则：从简单开始。先用单代理、思考-行动-观察循环、全量上下文、规则验证、白名单、少工具、薄基础设施。跑通了再优化。别上来就搞核动力航母，结果连港口都没修好。

* 总结

AI 工程的难点已经不在模型，而在系统。真正的挑战是如何管理上下文，如何设计记忆，如何控制错误，如何构建验证，如何平衡复杂度。如果你的 agent 失败，不要怪模型，先看看基础设施是不是一团糟。

好消息是，基础设施是可以学习、可以改进、可以量化的。你不需要发明新的模型，只需要把现有的工程原则应用到 agent 系统上。状态机、错误分类、日志、监控、回滚，这些传统软件工程几十年的积累，在 agent 时代依然有效。因为 AI 是概率性的，确定性的工程手段反而变得更加重要。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：agent-的瓶颈不在模型，在基础设施/index.html</guid>
</item>
<item>
<title>读：EvoForge——用群体进化优化 AI Agent</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：evoforge——用群体进化优化-ai-agent/index.html</link>
<description>优化 AI Agent 的时候，你是在手动调 prompt 和参数吗？如果答案是&quot;是&quot;，那你的优化方式可能已经落后了。

有一组反直觉的数据：在一个叫 EvoForge 的开源系统上，GPT-5-nano（一个相对轻量的模型）跑出了 Codex CLI 2 倍的性能，是对照 baseline 的 10 倍。靠的是更聪明的优化流程，而不是更强的模型。

这说明真正的瓶颈不在模型本身，在&quot;如何组织优化过程&quot;。

（本文基于 jdon.com 上关于 EvoForge 的介绍，提取通用原则。原文篇幅较长，我从 EvoForge 的设计中看到了几个对任何 AI 开发者都有启发的思路。）

* 手动调参的陷阱

手动优化单 agent 有两个很难绕开的坑。

*局部最优。* 改一个 prompt、调一个参数，效果好了就留下来，但你可能只是在附近的小坡上反复横跳。真正的更优解在别处，手动搜索效率太低，很难跳出去。

*重复踩坑。* 你修好了一个 bug，过几周又犯了同样的错。没有系统性的机制把&quot;失败经验&quot;沉淀下来，每一轮都在重复交学费。

这两个问题的根因是同一个：手动优化本质上是单点搜索，没有结构化的探索和记忆机制。

* EvoForge 的思路：群体进化

EvoForge 的策略很简单：一个人反复刷题不如一整个班同时刷题、互相抄作业、总结错题、再进化下一轮。

具体来说，每一代会做以下几件事：

1. *变异* ：在当前最佳 agent 的基础上，生成多个变体（population）
2. *并行测试* ：每个变体独立跑 benchmark，输出 0.0 到 1.0 的分数
3. *失败分析* ：对每个变体的行为轨迹做语义分析，定位具体的失败点
4. *知识共享* ：汇总所有变体的失败模式，提取共性问题，写入共享知识库
5. *继承* ：下一代 agent 继承这些知识，避免重复同样的错误

这五步形成一个闭环，不断重复。

关键突破不在&quot;进化&quot;这个词本身，而在&quot;知识在群体中传播&quot;。每一代不是从零开始，而是带着前一代的经验继续前进。

* 反向直觉的设计：没有 agent 被淘汰

很多人以为进化算法就是&quot;强者留下，弱者淘汰&quot;。但 EvoForge 做了一个相反的设计：没有 agent 被抛弃，整个群体一起变强。

这是怎么做到的？

每一轮结束后，系统不是只保留最优 agent，而是汇总所有 agent 的失败模式，统一归纳错误类型，给出修复方向，写入共享知识库。下一代所有 agent 继承这些经验。

结果就是：哪怕某个 agent 本身很弱，它也不会一直弱，因为它继承了集体智慧。

这和单点优化形成了鲜明对比。单 agent 的问题是：你犯过的错，下次还可能再犯。而 EvoForge 的逻辑是：一个人踩坑，全体绕坑。

* 三层分离的设计哲学

EvoForge 还有一个设计：它把&quot;代码逻辑&quot;和&quot;进化策略&quot;彻底分开了。

系统核心由三个文件驱动：

- =evolve.md= ：定义进化规则（群体规模、选择策略、变异方式）
- =program.md= ：定义 meta-agent，它负责修改 agent 本身
- =agent.py= ：真正被优化的 agent 对象

人类不直接写 agent，而是写&quot;如何进化 agent&quot;。这个抽象层级比普通 prompt engineering 已经高了一层。

* 语义可观测性：知道为什么失败

AI 优化中最难的地方是：你根本不知道 agent 为什么失败。

EvoForge 引入了 Semantic Observability（语义可观测性），强制做&quot;失败解剖&quot;：

1. 分阶段分析 agent 行为
2. 找出具体的错误决策点
3. 解释为什么失败
4. 输出结构化的分析结果

这一步把&quot;感觉不行&quot;变成了&quot;具体哪里不行&quot;。没有这一步，进化就变成了盲目试错。

* 对 AI 开发者的启示

EvoForge 是一个具体的工具，但它的设计思路对所有 AI 开发者都有参考价值。

手动调 prompt 最大的问题是不可追溯、不可复现。如果你能把自己的优化过程拆成&quot;生成变体→测试→分析→总结→迭代&quot;的循环，哪怕不用 EvoForge，效率也会提升。

修好一个 bug 不叫优化，把 bug 的分析结果写进&quot;共享知识库&quot;才叫优化。对我来说，这意味着在 Claude Code 的使用中，每次失败的对话都应该被记录和分析，而不是用完就丢。

还有一点：GPT-5-nano + EvoForge 能跑赢 Codex CLI，说明流程的优化空间可能比模型升级更大。在追求更强模型之前，先问问自己：优化流程本身是不是已经最优了？

* 总结

EvoForge 不是在优化 agent，而是在优化&quot;如何产生 agent&quot;。它用群体进化代替手动调参，用语义分析代替盲目试错，用知识共享代替重复踩坑。真正的突破不在模型，而在流程设计。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：evoforge——用群体进化优化-ai-agent/index.html</guid>
</item>
<item>
<title>TIL：Google Stitch 的 DESIGN.md，给 AI 读的设计系统说明书</title>
<link>https://lujun9972.github.io/blog/2026/05/03/til：google-stitch-的-design.md，给-ai-读的设计系统说明书/index.html</link>
<description>* 是什么

DESIGN.md 是 Google Stitch 团队开源的设计系统协议。本质上是给 AI 看的设计规范，不是给人看的。它把 Figma 里的视觉语言、Tailwind 里的样式参数、设计师的审美判断，全部翻译成 AI 能直接执行的结构化 Markdown 文件。

* 为什么需要

传统设计流程有三个缺陷。一是 Figma 格式专有，AI 读不了。二是每次生成新页面都是独立处理，不会沿用之前的风格决策。三是对着 AI 说&quot;按钮稍微圆一点&quot;它不知道是 4px 还是 12px。DESIGN.md 把模糊的人类描述转成精确的技术参数，所有页面共用同一个规范文件。

* 怎么用

安装 =design-md= 技能（ =npx skills add google-labs-code/stitch-skills --skill design-md= ），把生成的 =DESIGN.md= 放项目根目录，然后在 AI 指令中引用&quot;根据 DESIGN.md 的规范生成&quot;。迭代时修改 DESIGN.md 这个源头，所有后续生成自动遵循新规则。Google 还提供了 =lint= / =diff= / =export= 等命令行工具辅助管理。

AI 控制 UI 输出分三个层级：初级直接写提示词，中级加示例图片，高级用 DESIGN.md 提供结构化上下文。区别在于提示词是临时的、输出不稳定，DESIGN.md 是持久化的、跨项目可复用。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/til：google-stitch-的-design.md，给-ai-读的设计系统说明书/index.html</guid>
</item>
<item>
<title>Emacs 批量搜索替换：从场景到命令</title>
<link>https://lujun9972.github.io/blog/2026/05/04/emacs-批量搜索替换：从场景到命令/index.html</link>
<description>大部分 Emacs 用户熟悉的搜索替换也就是 ~M-%~ 替换当前 buffer 的文本、 ~C-s~ 递增搜索当前 buffer。但遇到&quot;替换整个项目&quot;或&quot;搜所有标记的文件&quot;时，很多人就卡住了，不知道该用什么命令。

Emacs 的批量搜索替换命令分布在 rgrep、Dired、IBuffer、project、xref 等多个子系统里，每种正则语法还不一样。[[http://yummymelon.com/devnull/bulk-search-replace-commands-for-files-and-buffers-in-emacs.html][Charles Choi 最近整理了一份全景清单]]，把 20 多条命令列在一张大表里。本文在他的清单基础上，按使用场景重新分类，帮你快速找到对应的命令。

* 一个前提：两种正则语法

所有涉及正则表达式的搜索/替换命令，都需要了解 Emacs 用什么正则语法。总共有两种：

- *grep regexp* ：命令会调用外部 ~grep~ 程序，正则语法跟你用的 grep 版本有关（GNU grep、BSD grep 规则不同）。这类命令包括 ~rgrep~ 、 ~project-find-regexp~ 、 ~dired-do-find-regexp~
- *Emacs regexp* ：命令用 Emacs 自己的正则引擎，语法在 help 里查 ~(info &quot;(elisp) Syntax of Regexps&quot;)~ 。这类命令包括 ~tags-search~ 、 ~dired-do-query-replace-regexp~ 、 ~ibuffer-do-*-regexp~

两种正则的转义规则不同。不确定的时候，用 ~M-x re-builder~ 测试一下你的正则。

* 搜目录下的文件

这是最常用的场景：在一个目录（可能递归）里搜索所有文件。

** rgrep

~rgrep~ 是&quot;在目录中搜索&quot;的标准命令。它调用外部 grep，语法是 grep regexp。运行后会让你输入 pattern、文件匹配模式（默认 ~*.*~ ）和目录，结果展示在 ~*grep*~ buffer 里。

比如搜 ~src/~ 目录下所有 ~*.el~ 文件中包含 ~defun~ 的行：

#+BEGIN_SRC shell :dir /tmp :tangle /tmp/rgrep-demo.sh
mkdir -p /tmp/rgrep-demo/src
cat &gt; /tmp/rgrep-demo/src/utils.el &lt;&lt;&#39;EOF&#39;
(defun add (a b) (+ a b))
(defun subtract (a b) (- a b))
;; helper function
(defun multiply (a b) (* a b))
EOF

cat &gt; /tmp/rgrep-demo/src/main.el &lt;&lt;&#39;EOF&#39;
(defun main ()
  (message &quot;hello&quot;))
(defvar *version* &quot;1.0&quot;)
EOF

grep -rnH &quot;defun&quot; /tmp/rgrep-demo/src/
#+END_SRC

执行结果：

#+BEGIN_EXAMPLE
/tmp/rgrep-demo/src/main.el:1:(defun main ()
/tmp/rgrep-demo/src/utils.el:1:(defun add (a b) (+ a b))
/tmp/rgrep-demo/src/utils.el:2:(defun subtract (a b) (- a b))
/tmp/rgrep-demo/src/utils.el:4:(defun multiply (a b) (* a b))

#+END_EXAMPLE

~*grep*~ buffer 里的每行结果直接回车就能跳到那行。如果你装了 ~wgrep~ ，还能直接在结果 buffer 里编辑，改完 ~C-x C-s~ 批量写回文件。

** find-grep-dired

~find-grep-dired~ 搜索目录下匹配内容的文件，搜索结果不是 ~*grep*~ buffer，而是一个 Dired buffer。这样你可以直接在搜索结果上做标记、批量操作。

#+BEGIN_SRC shell :dir /tmp
grep -rl &quot;defun&quot; /tmp/rgrep-demo/src/
#+END_SRC

执行结果：

#+BEGIN_EXAMPLE
/tmp/rgrep-demo/src/main.el
/tmp/rgrep-demo/src/utils.el
#+END_EXAMPLE

** find-name-dired / find-lisp-find-dired

这两个按文件名而不是内容来搜索。 ~find-name-dired~ 用 shell pattern（ ~*.el~ ）， ~find-lisp-find-dired~ 用 Emacs regexp。

#+BEGIN_SRC shell :dir /tmp
find /tmp/rgrep-demo -name &quot;*.el&quot; -type f
#+END_SRC

执行结果：

#+BEGIN_EXAMPLE
/tmp/rgrep-demo/src/main.el
/tmp/rgrep-demo/src/utils.el
#+END_EXAMPLE

** vc-git-grep

如果你在 git 仓库里， ~vc-git-grep~ 调用 ~git grep~ 来搜索。结果展示在 ~*grep*~ buffer 里，和 ~rgrep~ 一样可以跳转。

* 搜/替 project

~project-find-regexp~ 和 ~project-query-replace-regexp~ 是 Emacs 28+ 引入的 project 功能。Emacs 根据 ~project-vc~ 自动识别项目根目录（一般就是 git 仓库根目录），不需要你手动指定目录。

- ~project-find-regexp~ ：调用外部 grep 搜索整个 project，结果在 ~*grep*~ buffer
- ~project-query-replace-regexp~ ：在 project 范围内逐一询问替换，用 Emacs regexp。替换时会遍历所有项目文件，每处问你 y/n/!

* 搜/替标记的文件（Dired 内操作）

在 Dired 里用 ~m~ 标记了若干文件后，可以对这批文件执行搜索替换。这是最灵活的方式。你可以混选不同目录的文件，不限某个 project。

- ~dired-do-isearch~ / ~dired-do-isearch-regexp~ ：在标记的文件里递增搜索。 ~C-s~ 跳到下一个匹配、 ~C-r~ 回退，在所有文件间无缝切换
- ~dired-do-query-replace-regexp~ ：在标记的文件里逐一替换（Emacs regexp）
- ~dired-do-find-regexp~ ：在标记的文件里搜索，结果展示在 ~*grep*~ buffer（grep regexp）。和 ~dired-do-find-regexp-and-replace~ 是一对：一个查一个替换
- ~dired-do-search~ ：在标记的文件里搜索第一个匹配（Emacs regexp），跳到该文件并高亮

这些都是在 Dired 里 ~m~ 标记文件后用的命令，不需要提前退出 Dired。

* 搜/替 buffer（IBuffer 内操作）

IBuffer 和 Dired 类似，但它操作的是 buffer 而不是文件。用 ~m~ 标记 buffer 后：

- ~ibuffer-do-isearch~ / ~ibuffer-do-isearch-regexp~ ：在标记的 buffer 里递增搜索
- ~ibuffer-do-query-replace~ / ~ibuffer-do-query-replace-regexp~ ：逐一替换（普通文本或 Emacs regexp）
- ~ibuffer-do-occur~ ：在标记的 buffer 中搜索匹配的行，集中展示在 ~*Occur*~ buffer 里

~ibuffer-do-occur~ 特别适合在多个 buffer 里&quot;看一眼哪些行匹配&quot;。结果展示在 ~*Occur*~ buffer 里，每条结果可以回车跳转。

* tags / xref / eglot

这三个不依赖于文件结构或 buffer 列表，而是基于代码语义或 TAGS 文件来搜索替换。

- ~tags-search~ ：基于 TAGS 文件的搜索（Emacs regexp）。适合没有 LSP 支持的项目，用 ~etags~/~ctags~ 生成 TAGS 文件后就能用
- ~xref-find-references-and-replace~ ：搜索并替换所有引用。Emacs 29+ 内置，适合有 TAGS 或 LSP 的项目
- ~xref-query-replace-in-results~ ：在 ~*xref*~ 结果 buffer 中按 ~r~ 触发替换
- ~eglot-rename~ ：重命名符号。需要 Eglot（LSP 客户端）支持，选好语言服务器后一键重命名当前符号

* 安全提醒

批量搜索替换命令就像电动工具：用好了效率翻倍，用砸了麻烦很大。

- 做替换前确认文件已经备份（git commit 或手动备份都行）
- 不确定正则对不对的时候，先用 ~re-builder~ 测试
- 部分命令只改 buffer 不自动存盘，替换完成后检查哪些 buffer 是 modified 状态，用 IBuffer 批量保存

* 速查表

| 场景 | 命令 | 正则类型 |
|------|------|----------|
| 搜目录下文件 | rgrep | grep |
| 搜目录，结果展示为 Dired | find-grep-dired | grep |
| 按文件名搜索目录 | find-name-dired | shell pattern |
| 按文件名 regexp 搜索 | find-lisp-find-dired | Emacs |
| git 仓库里搜 | vc-git-grep | grep |
| 搜 project | project-find-regexp | grep |
| project 范围替换 | project-query-replace-regexp | Emacs |
| Dired 标记文件后搜索 | dired-do-find-regexp | grep |
| Dired 标记文件后替换 | dired-do-find-regexp-and-replace | grep |
| Dired 标记文件后逐一确认替换 | dired-do-query-replace-regexp | Emacs |
| Dired 标记文件后递增搜索 | dired-do-isearch | 普通文本 |
| IBuffer 标记 buffer 后搜索 | ibuffer-do-find-regexp | grep |
| IBuffer 标记 buffer 后替换 | ibuffer-do-query-replace-regexp | Emacs |
| 基于 TAGS 搜索 | tags-search | Emacs |
| 搜索符号引用并替换 | xref-find-references-and-replace | 语义 |
| Eglot 重命名符号 | eglot-rename | 语义 |
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/04/emacs-批量搜索替换：从场景到命令/index.html</guid>
</item>
<item>
<title>TIL: image-mode 的 header-line 中显示图片尺寸</title>
<link>https://lujun9972.github.io/blog/2026/05/03/til-image-mode-的-header-line-中显示图片尺寸/index.html</link>
<description>Emacs ~image-mode~ 打开图片只显示图像，不告诉你尺寸多大、文件多大。弹出去用 ~identify~ 或 ~file~ 查又太麻烦。

其实一行钩子就能搞定：

#+BEGIN_SRC emacs-lisp
(defun my/image-mode-show-dimensions ()
  &quot;在 header-line 中显示图片像素和文件大小。&quot;
  (when (and (derived-mode-p &#39;image-mode)
             buffer-file-name
             (file-exists-p buffer-file-name))
    (condition-case err
        (let* ((image (or (image-get-display-property)
                          (create-image buffer-file-name)))
               (size (image-size image t))
               (width (car size))
               (height (cdr size))
               (bytes (file-attribute-size
                       (file-attributes buffer-file-name))))
          (setq header-line-format
                (format &quot; %d x %d px   %s&quot;
                        width height
                        (file-size-human-readable bytes))))
      (error
       (setq header-line-format
             (format &quot; image dimensions unavailable: %S&quot; err))))))

(add-hook &#39;image-mode-hook #&#39;my/image-mode-show-dimensions)
#+END_SRC

效果像这样（header-line 上多了一行）：

#+BEGIN_EXAMPLE
1920 x 1080 px  2.4M
#+END_EXAMPLE

* 几个要点

- ~image-get-display-property~ 优先：如果图片已经在 buffer 中显示，直接用已有 spec，省一次文件读取。没有的话才用 ~create-image~ 从文件创建
- ~image-size~ 第二个参数传 ~t~ ：返回像素尺寸而非 canvas 单位
- ~file-size-human-readable~ ：把 ~2516582~ 变成 ~2.4M~
- ~condition-case~ 兜底：imagemagick 出问题、格式不支持之类的情况，不在 =*Messages*= 里炸开，header-line 上安静显示错误消息

* 别忘了 C-c C-c 的情况

~image-mode~ 按 ~C-c C-c~ 会在图像和文本视图之间切换，切换后 header-line 不会自动刷新。需要多挂一个钩子：

#+BEGIN_SRC emacs-lisp
  (add-hook &#39;image-mode-new-window-functions
            (lambda (&amp;rest _) (my/image-mode-show-dimensions)))
#+END_SRC

这个钩子在 buffer 重新显示图像时触发，保证切换视图后尺寸信息还在。

原文：[[https://www.emacs.dyerdwelling.family/emacs/20260430074958-emacs--a-tiny-header-line-tweak:-image-dimensions-in-image-mode/][A tiny header-line tweak: image dimensions in image-mode]]
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/til-image-mode-的-header-line-中显示图片尺寸/index.html</guid>
</item>
<item>
<title>读：Anvil——把 Emacs 变成 AI 的工具服务器</title>
<link>https://lujun9972.github.io/blog/2026/04/23/读：anvil——把-emacs-变成-ai-的工具服务器/index.html</link>
<description>* 一个反直觉的想法

我们习惯的模式是&quot;人在编辑器里写代码，AI 在旁边辅助&quot;。但 anvil.el 这个项目提出了一个反过来的思路：让 AI 直接操作编辑器，让编辑器成为 AI 的工具。

这不是换个说法而已。传统的 AI 编码流程里，agent 要改一个文件，必须先把整个文件读进来，定位要改的地方，修改，再把整个文件写回去。如果文件很大，每一次读和写都在浪费 token——agent 的上下文窗口被大量无关内容占满，钱也花在了搬运无用数据上。

anvil.el 的做法是：Emacs 作为 MCP（Model Context Protocol，模型上下文协议）服务器运行，AI agent 通过 MCP 协议直接调用 Emacs 的能力。想改哪一行就只传那一行的变更。想读某个 org 标题下的内容就只返回那个子树。Emacs 几十年积累的编辑能力——org-mode、dired、magit、calc——全部变成了 AI 可以调用的工具，v0.4.0 已经有 198 个以上这样的工具了。

* 为什么做这个工具：一个人的三阶段进化

anvil.el 的作者 zawatton 在日本经营一家一人电气工程公司，大约维护 40 个客户的设备。他的日常工作依赖 Windows 上的 Excel 做巡检报告，用 Emacs 的 org-mode 做笔记和日程管理。

他使用 Claude 辅助工作的过程经历了三个阶段：

*阶段一：Claude Desktop。* Anthropic 的桌面应用能直接操作本地文件和浏览器，帮他把文档整理、报告起草、记账这些工作自动化了。但很快他就撞上了月费 200 美元（MAX 套餐）的使用上限。

*阶段二：Claude Code。* 他转到命令行版的 Claude Code，shell 和 git 集成更流畅了。但问题出在文件编辑上——他开始把巡检数据从 Excel 迁移到 org 文件，因为 AI 处理纯文本比处理 Excel 高效得多。然而 Claude Code 编辑大 org 文件（约 1.2 MB）时，内容频繁丢失，token 消耗也跟着暴涨。原因很简单：Claude Code 每次编辑都要读整个文件再写回去，对于 Emacs 用户来说习以为常的大文件，在 AI 的读写模式下效率极低。

*阶段三：造 Anvil。* 他的想法是：既然 Emacs 处理大 org 文件这么熟练（按行编辑、按标题导航），为什么不让 Claude 直接用 Emacs 的能力来编辑？于是他把 Emacs 做成了 MCP 服务器，AI 通过协议调用 Emacs 的编辑命令，而不是自己笨拙地读写整个文件。

* 核心设计：操作区域，而不是整个文件

理解 Anvil 的关键在于&quot;按区域操作&quot;这个设计原则。

传统模式下，agent 想在一个 1.2 MB 的 org 文件里替换一个字符串，流程是：读取 1.2 MB 内容 → 找到目标 → 修改 → 把修改后的 1.2 MB 写回去。往返一次，光搬运数据就消耗几万个 token。

Anvil 提供的是精准操作工具：

- ~anvil-file-replace-string~:只传要查找的字符串和替换内容，Emacs 在本地完成替换，不传文件内容
- ~anvil-org-read-headline~:只读某个标题下的子树，哪怕整个文件有 13000 行
- ~anvil-file-insert-at-line~:在指定行号插入内容，只传新增的那几行
- ~anvil-file-batch~:把对同一个文件的多次编辑合并成一次调用，一次性完成

实际效果如何？作者在真实项目（一个 16000+ 行 TypeScript 的游戏移植项目 newDTW）中测量了几个典型场景：

| 操作场景 | 传统方式 | Anvil 方式 | token 节省 |
|----------+----------+------------+------------|
| 同一文件 6 处编辑 | ~4800 token（6 次读写） | ~1500 token（1 次批量调用） | ~70% |
| 4000 行类型定义文件里加一个字段 | 数千 token（读写整个文件） | 几十个 token（只传插入位置和内容） | ~90%+ |
| 批量添加 200 个 i18n 翻译键值 | ~30000 token | ~8000 token | ~73% |
| 从 3407 行代码中提取 347 个结构化记录 | ~8000+ token | ~3000-5000 token | ~50% |

每次重构会话累计节省 15000-25000 个 token。这些 token 省下来之后，agent 可以把上下文窗口用在真正的推理上，而不是反复搬运文件内容。

* 更多的设计考量

*Worker 异步调度：* 慢操作（大型 tangle、全项目扫描、字节编译）不会阻塞 agent 的主连接。Anvil 把这些任务分发给后台的 worker Emacs 进程。实测中， ~org-babel-tangle~ 在主线程跑要 30 秒，用 worker 派发只要 2 秒——快了 15 倍，而且 agent 的主会话不会被卡住。

*Manifest 配置文件控制工具暴露数量：* Anvil 提供了不同的配置文件来控制向 agent 暴露多少工具。全量模式暴露 198+ 个工具（约 20400 token 的描述），精简模式（ultra）只暴露 17 个核心工具（约 2740 token）。在多 agent 并行的场景下，200 个子会话用全量配置光工具列表就消耗 400 万 token，用 ultra 只需 55 万——工具列表本身就省了 87%。

*不只是 org-mode：* 虽然 Anvil 的起源是 org 文件编辑问题，但它的工具覆盖了文件批量操作、JSON 编辑、git 操作、Elisp 求值、进程管理、剪贴板读写、Excel/PDF 处理等等。v0.4.0 还加入了基于 tree-sitter 的 Python、TypeScript、JavaScript 结构化编辑——agent 可以在 AST 层面做重构，而不是靠正则匹配。

*多 agent 编排：* ~anvil-orchestrator~ 能把一个 prompt 同时发给多个 AI CLI（Claude、Codex、Gemini、Ollama），收集结果后做共识判定，甚至可以让一个 meta-LLM 来评审。作者实测：三个不同 provider 的 consensus 调用（claude + codex + ollama）比单独跑三个 Claude 又快又便宜——因为 codex 和 ollama 没有按 token 计费。

* 怎么用

了解了设计思路之后，实际用起来并不复杂。以下是从零开始的三个步骤。

**安装：** 非 Emacs 用户可以用一行脚本自动安装（会自动安装 Emacs、clone 仓库、启动守护进程、注册 MCP 服务）：

#+begin_src shell
# Linux / macOS
curl -fsSL https://raw.githubusercontent.com/zawatton/anvil.el/master/install.sh | bash
#+end_src

Emacs 用户可以用包管理器安装，比如 straight.el：

#+begin_src emacs-lisp
(straight-use-package
 &#39;(anvil :type git :host github :repo &quot;zawatton/anvil.el&quot;
         :branch &quot;v0.4.1&quot;))
#+end_src

**配置并启动：** Anvil 的模块分默认和可选两类。默认模块在 ~anvil-enable~ 时自动加载，可选模块需要手动指定：

#+begin_src emacs-lisp
(require &#39;anvil)

;; 默认加载的模块（不改就不用设，这些是默认值）
(setq anvil-modules
      &#39;(worker eval org file host git proc fs emacs text clipboard data net))

;; 按需开启可选模块
(setq anvil-optional-modules
      &#39;(xlsx     ;; Excel 读写（需要 Python + openpyxl）
        pdf      ;; PDF 文本提取（需要 Python + pymupdf）
        ide      ;; xref、diagnostics、imenu、tree-sitter（需要 project.el）
        browser  ;; 网页抓取（需要 agent-browser CLI）
        http     ;; HTTP GET/HEAD + ETag 缓存（需要 Emacs 29+）
        orchestrator  ;; 多 agent 并行编排（需要 AI CLIs + Emacs 29+）
        cron     ;; 定时任务（配合 orchestrator 可做夜间自动重构）
        elisp    ;; Agentic Elisp 开发工具
        ))

(anvil-enable)         ;; 注册所有模块的 MCP 工具
(anvil-server-start)   ;; 启动 MCP 服务器
#+end_src

默认模块覆盖了日常场景：worker（后台任务派发）、eval（Elisp 求值）、org（org 操作）、file（文件操作）、git（git 查询）、host（系统信息）等。不需要的功能直接从列表里去掉就行，比如你不用 git 就可以把 ~git~ 删掉，Anvil 就不会注册相关的 MCP 工具。

**Spacemacs 配置：** 如果你用 Spacemacs，可以把 Anvil 放到某个 layer 里管理。比如在 =my-misc= layer 的 =packages.el= 中：

#+begin_src emacs-lisp
;; packages.el
(setq my-misc-packages
  &#39;(
    ;; ... 其他包 ...
    (anvil :location (recipe :fetcher github :repo &quot;zawatton/anvil.el&quot;
                             :branch &quot;v0.4.1&quot;))
    ;; ...
    ))

(defun my-misc/init-anvil ()
  (use-package anvil
    :config
    (setq anvil-optional-modules &#39;(ide http))
    (server-start)              ;; Anvil 需要 emacsclient 连接
    (anvil-enable)
    (anvil-server-start)))
#+end_src

有一个坑需要注意：Spacemacs 用 recipe 方式安装包时，编译后的 =.el= 文件和 =.sh= 脚本可能不在同一个目录，导致 ~M-x anvil-server-install~ 报&quot;Cannot find anvil-stdio.sh&quot;。解决办法是手动复制桥接脚本：

#+begin_src sh
# 从 clone 下来的仓库中复制（或从 GitHub 下载）
cp /path/to/anvil.el/anvil-stdio.sh ~/.emacs.d/anvil-stdio.sh
chmod +x ~/.emacs.d/anvil-stdio.sh
#+end_src

**连接 Claude Code：** Anvil 通过一个 shell 脚本（~anvil-stdio.sh~）作为 MCP 桥接。先在 Emacs 里运行 ~M-x anvil-server-install~ 安装桥接脚本，然后在终端注册两个 MCP 服务：

#+begin_src sh
claude mcp add -s user -t stdio anvil -- \
  ~/.emacs.d/anvil-stdio.sh \
  --server-id=anvil \
  --init-function=anvil-enable \
  --stop-function=anvil-disable

claude mcp add -s user -t stdio anvil-emacs-eval -- \
  ~/.emacs.d/anvil-stdio.sh \
  --server-id=emacs-eval
#+end_src

#+RESULTS:

为什么是两个？因为 Anvil 内部用两个 server-id 分别管理不同类别的工具（ ~anvil~ 管 eval/IDE， ~emacs-eval~ 管文件/org/buffer/worker）。注册两个入口就能让 Claude Code 看到全部 198+ 个工具。

之后正常使用 Claude Code 就行,Claude 会自动在需要时调用 Anvil 的工具，不需要手动触发。

**使用示例：** 假设你有一个 3000 行的 =todo.org= ，想用 Claude Code 把某个标题下的所有 TODO 项标记为 DONE，并移到归档位置。

没有 Anvil 时，Claude Code 的流程是：先用 Read 工具读取整个 3000 行文件（消耗几千 token），找到目标标题，修改状态，再用 Edit 工具写回去（又几千 token）。如果想同时移动子树到另一个位置，还要再来一轮读写。

有了 Anvil，Claude Code 的对话会变成这样：

#+begin_example
你：把 todo.org 里&quot;Q1 任务&quot;下面的 TODO 项全部标记为 DONE，然后 refiling 到&quot;归档&quot;标题下。

Claude：我来处理。
[调用 anvil-org-read-headline]  ;; 只读&quot;Q1 任务&quot;子树，约 80 行，而不是整个 3000 行
[调用 anvil-file-batch]         ;; 一次性把所有 TODO → DONE + 执行 refiling
完成。&quot;Q1 任务&quot;下 12 个 TODO 项已全部标记为 DONE 并 refiling 到&quot;归档&quot;标题下。
#+end_example

整个过程只传了目标子树的内容和变更指令，没有读写整个文件。传统方式可能消耗 6000-8000 token（读 + 写各一轮），Anvil 方式大约 500-800 token——省了将近 90%。

再比如批量重命名。没有 Anvil 时，Claude Code 要先 ~grep~ 找到所有包含旧名字的文件，然后逐个 Read、Edit、写回，几十个文件就是几十轮往返。有了 Anvil，一条 ~anvil-file-replace-string~ 或 ~emacs-eval~ 调用就能在 Emacs 侧一次性完成——作者实测在 7 个仓库 39 个文件里把 =zawatton21= 改为 =zawatton=，只花了一次往返。

* 对非 Emacs 用户的启示

但更重要的启示不在于这个工具本身，而在于它揭示的优化方向：AI agent 的效率瓶颈往往不是模型能力，而是它和编辑器之间的交互方式。每次读写整个文件是在浪费 token，按区域操作才能把上下文窗口留给真正重要的事。

这个思路不依赖 Emacs,任何编辑器只要能提供类似的精准操作接口，都能实现同样的效果。只是 Emacs 作为 Lisp 机器，天然具备无限可扩展性，所以率先把这个想法做出来了。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/23/读：anvil——把-emacs-变成-ai-的工具服务器/index.html</guid>
</item>
<item>
<title>dotfile仓库大扫除：清理过时的配置</title>
<link>https://lujun9972.github.io/blog/2026/05/02/dotfile仓库大扫除：清理过时的配置/index.html</link>
<description>用了近十年的 dotfile 仓库，里面堆积了不少过时的配置。趁着周末用 Claude Code 做了一次全面清理，记录一下发现的问题和修复过程。

* 发现的问题

** yaourt 全面废弃

早年 Arch Linux 用户都用 =yaourt= 作为 AUR 助手，但它早在 2018 年前后就被社区废弃了。仓库里几乎所有文件都在引用它：

- =dotfile.org= 中的 Awesome WM 配置
- =reinstall-arch.org= 安装笔记
- =arch-ansible/= 自动化部署剧本

统统替换为 =yay=，这个最省心，改就完了。

** 大量软件包已不存在或改名

检查 =reinstall-arch.org= 中的安装命令时，发现很多包要么从仓库移除了，要么改名了。用 =pacman -Si= 一个个验证，结果令人唏嘘：

| 旧包名 | 现状 | 替代 |
|--------|------|------|
| =shadowsocks= | 改名为 =shadowsocks-rust= | Rust 重写版 |
| =shadowsocks-qt5= | 已移除 | =v2ray-desktop= |
| =newsbeuter= | 改名为 =newsboat= | 功能更强 |
| =fcitx-im= | 已移除 | =fcitx5-im= + =fcitx5-chinese-addons= |
| =python-virtualenvwrapper= | 已移除 | pip install virtualenvwrapper |
| =wpa_actiond= | 已移除 | =iwd= 或 =NetworkManager= |
| =xorg-server-utils= | 已移除 | 功能已拆分为独立包 |
| =rox= | 已移除 | =pcmanfm= |
| =moc= | 已移除 | =cmus= |
| =finch= | 已移除 | =weechat= |
| =wyrd= | 已移除 | =calcurse= 或 Org-mode |
| =fbterm= | 仅 AUR 可用 | TTY 下凑合用 |
| =nutstore= | 仅 AUR 可用 | 仍可安装 |
| =anki20-bin= | AUR 已删除 | 已进官方 extra 源，直接 =pacman -S anki= |
| =netease-musicbox-git= | 长期未更新 | =go-musicfox-git= 或 =netease-cloud-music-gtk4= |

** Awesome WM 废弃 API

用 `awesome -k` 检查配置语法通过，但源码里翻出了好多 =awful.util.*= 调用，这些在官方文档里都已经标注废弃了：

- =awful.util.spawn= → =awful.spawn=
- =awful.util.spawn_with_shell= → =awful.spawn.with_shell=
- =awful.util.get_themes_dir()= → =gears.filesystem.get_themes_dir()=
- =awful.util.get_cache_dir()= → =gears.filesystem.get_cache_dir()=

好在当前版本（Awesome 4.3）还有兼容层，一次性全换了，10 处修改。

** Docker 镜像地址失效

自动化脚本中配置 Docker 镜像加速的地址 =http://31d9bf6d.m.daocloud.io= 还是远古 DaoCloud 格式。curl 实测可用性，替换为三个经过验证的国内镜像：

- =https://docker.xuanyuan.me=
- =https://docker.1ms.run=
- =https://docker.m.daocloud.io=

同时改掉了 =curl ... | sh= 的不安全安装方式，直接用 Ansible 写 =daemon.json=。

* 一些有意思的发现

** openssh 节装的是 git

=reinstall-arch.org= 中 openssh 安装章节写的是 =pacman -S git=，显然是复制粘贴时忘了改。这种错误要不是逐行过一遍根本发现不了。

** cmus 节装的是 moc

标题叫 cmus，命令装的却是 moc（而且 moc 也已经没了）。10 年前的笔记手误。

** fcitx 生态的变迁

fcitx4 已经彻底从 Arch 仓库移除了，取而代之的是 fcitx5。但环境变量（=GTK_IM_MODULE=、=QT_IM_MODULE=、=XMODIFIERS=）对 fcitx5 同样适用，这点没变。

* 经验总结

1. **定期清理 dotfile 仓库**，尤其是用了近十年的。很多配置当时写的时候没问题，但环境变了之后就成了死代码。

2. **用 Claude Code 做这种清理很顺手**：让它先全面分析找出问题，然后逐个修复。中间需要验证包是否存在、API 是否可用，直接让它执行命令就行了。发现存疑的地方，直接说&quot;验证一下&quot;就能得到结果。

3. **过时的安装文档比没有文档更危险**。如果有人（包括未来的自己）照着旧文档装系统，结果装到一半发现包不存在了，那体验很糟糕。要么保持更新，要么明确标注已过时。

4. **Arch Linux 的滚动更新对配置文件有持续影响**。不像 LTS 发行版安一次管五年，Arch 用户需要时不时审视自己的配置是否还跟得上。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/02/dotfile仓库大扫除：清理过时的配置/index.html</guid>
</item>
<item>
<title>读：The Art of Logging——日志规范清单</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：the-art-of-logging——日志规范清单/index.html</link>
<description>日志这件事，几乎所有团队都在做，但不是所有团队都做得好。好的日志帮你快速定位问题，坏的日志制造告警疲劳和存储浪费。DZone 上的 [[https://dzone.com/articles/the-art-of-logging-how-to-write-effective-logs][The Art of Logging]] 总结了日志最佳实践，我把要点整理成几条清单，方便对照。

* 日志级别选择

最常见的错误是什么都打成 ERROR。如果所有东西都是错误，错误信号就没有意义了。

- *INFO* ：有意义的业务事件或状态变更。用户下单、服务启动、定时任务执行完成。
- *WARN* ：非预期但已被妥善处理的情况，可能需要关注。第三方接口超时后重试成功、配置项使用了默认值。
- *ERROR* ：确实需要人工调查的故障。数据库连不上、数据损坏、未捕获的异常。
- *DEBUG* ：详细的排障信息，线上环境默认关闭。

关键原则：预期会发生的事件不应该打 ERROR。用户输错验证码、下游返回业务校验失败、第三方拒绝请求（因为参数不对），这些都是正常业务逻辑的一部分，记录日志可以，但不该打 ERROR。把预期内的业务失败也打 ERROR，告警系统就会失效，值班工程师也逐渐不再信任告警。

* 上下文该记什么

一条日志有没有用，取决于它能不能回答这几个问题：发生了什么、在哪发生的、是预期还是异常、有多严重、工程师接下来该查什么。

好的日志上下文包括：

- 请求 ID 或 trace ID
- 操作名称（如&quot;支付授权&quot;）
- 服务名称和环境
- 涉及的外部依赖
- 失败原因或错误码
- 关键业务标识（订单号、用户 ID）

看两个例子对比：

- 差： =Error occurred=
- 好： =Payment authorization failed for orderId=84219, provider=stripe, reason=card_declined, retryable=false=

后者有价值，是因为它把事情说清楚了：哪笔订单哪个环节出了什么问题，工程师看到这条日志可以直接判断怎么处理。

* 敏感数据清单

上下文有用，但不是什么东西都能往日志里写。以下内容不应该出现在日志中：

- 密码和 token
- 完整的信用卡号
- 个人身份信息（身份证号、手机号、地址）
- 敏感的业务 payload

记了不该记的东西，轻则违反合规要求，重则数据泄露。判断标准：这条日志如果被第三方看到了，你会不会紧张？

* 业务失败 vs 系统故障

这是区分日志级别的核心。很多告警系统在这件事上跑偏了。

*业务失败（预期内的）* ：信用卡被拒、密码输错、404（资源不存在）。这些是产品流程中的正常分支，用 INFO 或 WARN 记录，配业务元数据用于分析即可。不需要告警。

*系统故障（非预期）* ：超时、未捕获异常、依赖服务中断、数据损坏。这些需要打 ERROR 并触发告警。

两者的本质区别：前者是你的业务逻辑主动返回的&quot;不行&quot;，后者是你的系统&quot;本不该这样&quot;。监控应该把工程注意力引向真正出了问题的地方，而不是业务上不顺的地方。

* 避免过度记录

过度记录是日志系统退化的最快路径。每个函数调用都记、每个循环迭代都记、每个状态变更都记。那真正有用的信息就会被淹没。搜索变慢、仪表盘变乱、存储成本上升。

加日志之前问自己：

- 排障时我会搜这条日志吗？
- 它记录的是有意义的状态变更或决策点吗？
- 这条日志帮回答&quot;发生了什么&quot;吗？

如果答案都是否，就不该记。

* 最佳实践要点

最后几条实操建议：

- *统一格式* ：所有日志用相同格式输出，方便解析和查询
- *结构化日志* ：用 JSON 等结构化格式替代纯文本，比用 grep 搜字符串可靠得多
- *人类可读的 message* ：结构化的字段留给机器处理，日志正文还是要让人一眼看懂
- *关联 trace ID* ：跨服务的调用链靠 trace ID 串联，没有这个标识分布式排障基本靠猜
- *定期清理噪声* ：像重构代码一样定期审查日志，删掉那些已经没用的、过于聒噪的

* 总结

好的日志只记录重要的事情，而且要说清楚。用正确的级别、带上足够的上下文、区分预期失败和真故障。日志是系统和运维人员沟通的渠道，它不是代码的附属品。

这篇文章列的都是基础原则，不算新鲜。但偶尔翻出来对照一下清单，看看有没有跑偏的地方也不错。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：the-art-of-logging——日志规范清单/index.html</guid>
</item>
</channel>
</rss>