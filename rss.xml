<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>暗无天日</title>
    <link>https://lujun9972.github.io</link>
    <description>=============&gt;DarkSun的个人博客</description>
    <pubDate>Mon, 20 Apr 2026 07:29:40 UTC</pubDate>
    <lastBuildDate>Mon, 20 Apr 2026 07:29:40 UTC</lastBuildDate>
    <docs>http://www.rssboard.org/rss-specification</docs>
    <generator>ego static site generator (https://github.com/emacs-china/ego)</generator>
<item>
<title>一本书该&quot;线性读&quot;还是&quot;并行读&quot;</title>
<link>https://lujun9972.github.io/blog/2026/04/20/一本书该&quot;线性读&quot;还是&quot;并行读&quot;/index.html</link>
<description>* 默认的读书方式：从头到尾

大多数人读书的习惯是翻开第一章，读完再读第二章，依次推进到末尾。对于小说来说这是理所当然的——情节有因果链，跳着读就看不懂了。

但非虚构书不一样。很多非虚构书的章节之间并没有前置知识依赖。比如《21 世纪的 21 堂课》，第二章讲&quot;工作&quot;、第十七章讲&quot;后真相&quot;，两个话题完全独立。你完全可以在读第二章的同时也读第十七章，不需要先把中间的十几章读完。

这就是&quot;并行读&quot;的思路：同时推进多个章节，而不是等一章读完再开始下一章。

关键问题是——怎么判断哪些书能并行读？

* &quot;自包含&quot;测试

判断标准只有一个：看章节是否&quot;自包含&quot;。

一个&quot;自包含&quot;的章节指的是，它不需要前面章节的知识就能读懂。换句话说，你直接翻到这一章，不会因为缺少前置知识而看不懂。

用这个标准审视目录，很快就能判断一本书适合哪种读法。

*适合并行读的书：*
- 章节各自独立的非虚构书（如散文集、案例分析集、&quot;XX 个 XXX&quot;类书籍）
- 每章聚焦不同主题的科普书（如《思考，快与慢》的很多章节）
- 工具书、参考书

*必须线性读的书：*
- 小说、传记（有情节或时间线）
- 知识层层递进的书（如数学教材、《人类简史》类大历史叙事）
- 论证逐步展开的说理书（前面章节是后面章节的前提）

*教科书比较特殊——双层策略：*

以一本心理学入门教材为例，&quot;记忆与判断&quot;、&quot;智力与语言&quot;、&quot;情绪与动机&quot;这些章节各自聚焦不同主题，章节之间基本独立，可以并行读。但每一章内部的小节（3.1、3.2、3.3……）是有前后依赖的——3.2 假设你已经读了 3.1。

所以教科书适合&quot;章节级并行、小节级线性&quot;的混合策略。

* 为什么并行读有好处

并行读不只是&quot;可以这样做&quot;，而是有几个实在的好处：

1. *不会卡死在一个难点上。* 读线性书时，遇到一个难懂的章节就容易卡住，后面的内容也跟着读不下去。并行读意味着你可以先跳到另一个章节，等回头再攻克难点，不会因为一处受阻就整本书停滞。

2. *利用碎片时间。* 并行读意味着你可以根据手头时间的长短选择读哪个章节——时间充裕就啃难的章节，只有十分钟就翻翻轻松的章节。整本书的推进速度反而比线性读更快。

3. *保持阅读新鲜感。* 同时读不同主题的章节，大脑在不同话题之间切换，不容易疲劳。这比连续啃同一个话题几个小时效果更好。

* 用 Anki 实践并行阅读

SuperMemo 有一个叫&quot;渐进阅读&quot;（Incremental Reading）的功能，专门管理这种并行阅读的进度调度。但 SuperMemo 比较小众，国内用的人不多。相比之下，Anki 是更主流的间隔重复工具，也可以用来管理并行阅读。

核心思路是：用 Anki 的间隔重复来管理&quot;什么时候该回到某个章节继续读&quot;。

*基本做法：*

1. 在 Anki 中为这本书建一个 deck。
2. 为每个要并行读的章节创建一张卡片，卡片正面写&quot;继续读《书名》第 X 章：章节标题&quot;。
3. 读了一部分后，在卡片的&quot;答案&quot;区域记录当前进度（读到第几页/哪一小节）和已理解的关键要点。
4. Anki 会根据间隔重复算法自动安排下次阅读这张卡片（也就是回到这个章节）的时间。

这比用书签或待办清单好在哪？间隔重复保证了每个章节都不会被遗忘太久。线性读的问题是你读完第一章可能过了两周才读第二章，第一章的内容已经忘了大半。并行读配合 Anki 调度，每个章节都会在合适的间隔被复习到，记忆保持得更牢。

*四个按钮怎么用：*

Anki 每次弹出阅读卡片时，你会看到四个按钮——Again、Hard、Good、Easy。它们控制的是&quot;这个章节多久后再出现&quot;：

| 按钮 | 效果 | 阅读场景下什么时候用 |
|------|------|---------------------|
| Again (重来) | 间隔归零，卡片很快重新出现（同天内几分钟，或第二天） | 翻了一下但完全看不进去，想过一会儿再试 |
| Hard (困难) | 间隔缩短，比正常更早回来 | 稍微读了几页但觉得吃力，想缩短间隔、更早回来继续 |
| Good (良好) | 按默认间隔安排下次阅读 | 正常推进了一段，节奏合适 |
| Easy (简单) | 间隔拉长 | 读得很顺畅，进展比预期快 |

如果 Anki 调度到某个章节但你今天就是不想读，有两个选择：Bury（搁置）——右键菜单里有 Bury 选项，效果是今天不再出现，但明天会自动回来；或者直接点 Good，让 Anki 按正常间隔（比如 3-5 天后）再提醒你。不用有心理负担，并行读本身就是弹性的。

*不同章节难度不同怎么办：*

Anki 默认的 SM-2 算法给所有卡片相同的初始间隔。但章节难度不同，你可能希望难的章节间隔短一点、简单的章节间隔长一点。有几种做法：

1. *依赖 Anki 自适应。* 你只需要诚实地点按钮——难的章节你自然会频繁点 Hard，Anki 会自动缩短它的间隔；简单的章节你频繁点 Easy，间隔会自动拉长。几轮下来间隔就会自然适配章节难度，不需要手动设置。
2. *开启 FSRS 算法。* Anki 从 23.10 版本开始内置了 FSRS（Free Spaced Repetition Scheduler）算法，它比默认的 SM-2 更智能，能更准确地根据你的实际复习表现调整每张卡片的间隔。在 Deck Options → FSRS 中开启即可。
3. *分组管理。* 如果你读的书章节难度差异极大（比如有的章节是科普、有的章节是数学推导），可以为&quot;难&quot;和&quot;易&quot;分别建 deck，在 Deck Options 中设置不同的初始间隔和最大间隔。

*Anki 的渐进阅读插件：*

如果你想更进一步，把 SuperMemo 那种&quot;在 Anki 内直接导入文章、高亮、提取片段&quot;的完整渐进阅读体验搬到 Anki 里，可以试试 Incremental Reading v4 插件。它支持把网页、PDF、电子书片段直接导入 Anki，在 Anki 内部逐步阅读、高亮、提取 cloze 卡片。不过这个插件近年 Anki 大版本更新后兼容性不太稳定，安装前先确认是否支持你正在用的 Anki 版本。

* 一个具体例子

假设你在读《23 件关于资本主义他们没告诉你的事》。这本书的 23 章各自独立，非常适合并行读。

第一步，在 Anki 中建一个 deck，比如叫&quot;读书-23 Things&quot;。

第二步，浏览目录后，为第 1 章、第 5 章、第 12 章（假设这三章你最感兴趣）各创建一张 Basic 卡片：

- 正面：读《23 Things》Ch.1 - There is no such thing as a free market
- 背面：（空白，等你读完一部分后填写进度和要点）

第三步，每天打开 Anki 时，它会提醒你今天该回到哪些章节继续读。你可以根据心情和时间选择先读哪一章。

第四步，每读完一个章节的全部内容，就把这张卡片标记为&quot;已完成&quot;（可以 suspend），同时在 Anki 中添加该章核心观点的记忆卡片，用间隔重复来长期保持记忆。

* 实践步骤

把上面的内容整理成操作流程：

1. *先看目录。* 拿到一本书，快速浏览目录，对全书结构有个大致判断。
2. *做&quot;自包含&quot;测试。* 逐章判断：直接翻到这章开头，看需要多少前置知识？大部分章节都不需要前置知识的，就适合并行读。
3. *为每个独立章节创建 Anki 卡片。* 卡片正面写章节标题，背面用来记录进度和要点。
4. *按 Anki 的调度来读。* 每次打开 Anki 时，它会告诉你今天该回到哪些章节继续读。不想读就点 Good 推迟，没什么大不了。
5. *在每章内线性推进。* 并行读是章节级的，每一章内部还是要按顺序读。
</description>
<pubDate>2026-04-20</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/20/一本书该&quot;线性读&quot;还是&quot;并行读&quot;/index.html</guid>
</item>
<item>
<title>读 How to Monetize a Blog：一篇伪装成变现指南的讽刺文</title>
<link>https://lujun9972.github.io/blog/2026/04/20/读-how-to-monetize-a-blog：一篇伪装成变现指南的讽刺文/index.html</link>
<description>* 这篇&quot;教程&quot;在干什么

[[https://modem.io/blog/blog-monetization/][How to Monetize a Blog]] 是 modem.io 上的一篇文章。乍一看，这是一篇博客变现指南——讲 CPM、CPA、广告网络、受众画像，跟你搜到的任何一篇&quot;如何靠博客赚钱&quot;没什么区别。

但读完全文才发现，这是一篇精心设计的讽刺作品——它就是它所讽刺的那个东西。

* 四层递进的讽刺手法

** 第一层：正经教程的伪装

文章开头非常像回事。它解释了 CPM（千次展示付费）和 CPA（按行动付费）的区别，介绍了广告网络的运作方式，甚至讨论了如何根据受众画像投放定向广告。这些内容本身是准确的，读起来就像一篇标准的 SEO 变现教程。

这个伪装做得越逼真，后面的反转就越有力。

** 第二层：荒诞的假广告

从文章开头开始，就穿插着各种广告位。但仔细一看，这些广告卖的是：

- &quot;虾探测器&quot;（ Shrimp detectors ）
- &quot;定制塔罗牌&quot;
- &quot;蛇油&quot;（ Literal Snake Oil——&quot;从真正的蛇身上现榨的油&quot; ）
- &quot;药用盲盒&quot;（ Medicinal Loot Boxes——&quot;禁止医生入内&quot; ）
- &quot;和三明治交朋友&quot;（ Befriend A Sandwich——&quot;你的法式蘸酱三明治知道宝藏在哪吗？&quot; ）

每一则假广告的荒诞程度都在递增。而且这些广告和文章内容毫无关系——这本身就是一种讽刺：广告系统号称能精准匹配内容和广告，实际投放出来的东西根本和内容无关。

** 第三层：作者自我拆穿

写到大半的时候，作者突然摊牌了。他说：

#+begin_quote
this entire post is admittedly just meaningless fluff that I&#39;m not even proof-reading. It&#39;s all a thoughtless heap of words in disguise, an excuse to fill more ad space. And it is working!

这整篇文章说实话就是毫无意义的填充物，我连校对都没做。这是一堆词的伪装，一个塞更多广告位的借口。而且它奏效了！
#+end_quote

然后他&quot;建议&quot;你可以用 AI 来写内容，或者干脆用 lorem ipsum（排版时用来占位置的假文字）来填充文章后半部分——因为只要前面的文字看起来像人写的，读者就不会注意到后面是机器生成的废话。这正是内容农场的真实操作：用廉价内容填充页面，只要能展示广告就行，内容本身毫无价值。

这一层的巧妙之处在于：作者一边在做他正在批评的事（用废话填空间），一边告诉你他在这么做。你作为读者，明知被耍了，但广告已经展示在你面前了。

** 第四层：文风崩塌

最后几段，文章彻底放弃了教程的伪装。文风从幽默滑向了一种接近克苏鲁式的黑暗散文：

#+begin_quote
We invent new gods and we bury them in the same breath. We leave scars on the world like canyons carved from rivers of glass.

我们在制造新的神，又在同一口气中埋葬它们。我们在世界上留下伤疤，像河流冲刷出的玻璃峡谷。
#+end_quote

然后是一段&quot;拆了建、建了拆&quot;无限重复的文本，最后以一片黑暗中蠕动的触须意象收尾。

这个文风崩塌大概不是作者失控，而是一种有意的隐喻：当内容创作者完全被流量和广告驱动时，内容本身就会退化成这样的东西——没有意义、没有方向、无限循环。

* 中国互联网的镜像

这篇文章讽刺的是英文互联网的广告生态，但这些手法放在中国互联网上只会变本加厉。

百度的搜索结果页，前几条永远是广告。公众号文章读着读着突然插入一条带货链接。短视频平台的内容越来越像广告，广告越来越像内容。B 站的弹幕里混着&quot;恰饭&quot;的辩解。我们每天消费的内容，有多少是真正为读者写的，又有多少是&quot;有意义的填充物&quot;？和这篇讽刺文里的做法相比，区别只在于：这些平台可不会自爆。

modem.io 这篇文章最狠的地方在于，它没有用论述的方式告诉你&quot;广告正在毁掉互联网&quot;，而是让你亲身体验了一把——你读完了它，你的注意力被假广告收割了，然后文章告诉你：刚才经历的就是问题本身。

* 为什么这篇讽刺有效

好的讽刺不靠说理，靠的是让你读完之后回头一看，发现自己刚才的阅读体验本身就是被讽刺的对象。这篇文章做到了：

1. *形式即内容* ——它用广告充斥的排版来讽刺广告充斥的互联网，读者不需要&quot;被说服&quot;，只需要&quot;经历一遍&quot;
2. *诚实是最好的嘲讽* ——作者直接承认&quot;这篇文章就是垃圾&quot;，这种自曝比任何批评都尖锐
3. *递进的荒诞感* ——从正经教程到假广告到自我拆穿到文风崩溃，每一层都把讽刺推得更深

这种结构性的讽刺手法并不常见——大部分讽刺靠的是文字游戏和双关，而这篇文章让文章的结构本身成为了论点。
</description>
<pubDate>2026-04-20</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/20/读-how-to-monetize-a-blog：一篇伪装成变现指南的讽刺文/index.html</guid>
</item>
<item>
<title>Emacs Lisp 热重载实用指南</title>
<link>https://lujun9972.github.io/blog/2026/04/19/emacs-lisp-热重载实用指南/index.html</link>
<description>* 问题

Emacs 用户经常无意识地做一件事：修改 =init.el= 中的代码，求值，然后继续在同一个 Emacs 实例中工作。这就是 Lisp 的热重载——不需要重启进程，代码直接在运行中被替换。

大部分时候这很顺畅。但有一个陷阱： =defvar= 、 =defcustom= 、 =defface= 在重新求值时 *不会* 更新已有的值。

#+BEGIN_SRC elisp
;; 第一次求值
(defvar my-var &quot;hello&quot;)  ;; my-var → &quot;hello&quot;

;; 修改后重新求值
(defvar my-var &quot;world&quot;)  ;; my-var → 仍然是 &quot;hello&quot;！
#+END_SRC

这是故意这么设计的——加载库不应该覆盖用户的自定义值。但在开发包的时候，这个设计让人困惑：你改了默认值，重新求值，发现什么都没变。

下面是从轻到重的五种解决方案。

* 方法一：eval-defun（C-M-x）

这是最常用的方法，也是最容易被忽略的： =eval-defun= （=C-M-x=）对 =defvar= 、 =defcustom= 、 =defface= 有特殊处理——它会 *无条件* 设置变量的值，不管这个变量是不是已经有值。

#+BEGIN_SRC elisp
(defvar my-var &quot;new-value&quot;)
#+END_SRC

把光标放在这个 =defvar= 形式上，按 =C-M-x= ， =my-var= 就会被更新为 =&quot;new-value&quot;= 。

注意： =eval-buffer= 和 =eval-region= *不会*这样做——它们遵守正常的 =defvar= 语义。只有 =eval-defun= 是例外。

这是 Bozhidar Batsov 在开发 mode 时最常用的方法：只改了几个形式，就逐个 =C-M-x= 。

* 方法二：setq（M-:）

如果只需要快速重置一个变量：

#+BEGIN_SRC elisp
M-: (setq my-var &quot;new-value&quot;)
#+END_SRC

简单粗暴，但只改变量值，不会触发其他代码的重新执行（比如函数定义、hooks 设置等都不会变）。适合临时调参数。

* 方法三：load-file

=M-x load-file= 从磁盘加载 =.el= 文件。跟 =require= 的区别是： =require= 发现 feature 已加载就会跳过， =load-file= 总是重新读取并求值文件。

但 =load-file= 仍然遵守 =defvar= 语义——已绑定的变量不会被更新。它的优势在于可以加载非当前编辑的文件，比如包的依赖文件，或者没有 =provide= 形式的文件。

* 方法四：unload-feature + require（干净重载）

这是最彻底的非重启方案：

#+BEGIN_SRC elisp
;; 卸载 feature：移除它定义的变量、函数、hooks 等
(unload-feature &#39;my-package)

;; 重新加载
(require &#39;my-package)
#+END_SRC

=unload-feature= 会把 feature 定义的一切都清掉，然后 =require= 从头加载。这最接近&quot;干净的状态&quot;。

注意事项：

- =unload-feature= 可能有副作用——如果 feature 添加了 hooks 或 advice，卸载时应该会清理，但并非所有包都正确实现了清理逻辑，可能出现残留的 hook 或 advice
- 只对通过 =provide= / =require= 加载的 feature 有效；用 =load-file= 加载的文件没有 feature 可以卸载
- 某些复杂的包不能干净地卸载，但大多数简单的 major mode 没问题

可以绑一个快捷键方便开发时使用：

#+BEGIN_SRC elisp
(defun my-reload-package (package)
  &quot;Unload and reload PACKAGE.&quot;
  (interactive &quot;SReload package: &quot;)
  (unload-feature package)
  (require package)
  (message &quot;Reloaded %s&quot; package))
#+END_SRC

* 方法五：重启 Emacs

终极方案。当其他方法都不管用，或者你改了太多东西、不信任当前运行时状态时，重启。

有了 =desktop-save-mode= 或 session 管理器，重启的代价不大。Emacs 29+ 内置了 =restart-emacs= 命令，或者用 [[https://github.com/iqbalansari/restart-emacs][restart-emacs 包]]。

* 别忘了重新激活 Mode

无论用哪种方法重载了代码，还需要在已有 buffer 中重新激活 mode：

#+BEGIN_SRC elisp
M-x my-mode           ;; 重新运行 mode 函数，应用新的 keymap、hooks、font-lock 等
#+END_SRC

对于 minor mode，开关两次：

#+BEGIN_SRC elisp
M-x my-minor-mode     ;; 关闭
M-x my-minor-mode     ;; 重新打开
#+END_SRC

不做这一步，已有 buffer 仍在跑旧代码——因为 mode 激活时会把 keymap、font-lock 规则、hooks 等设置为 buffer-local 的值，重新加载代码只更新了函数定义，并没有更新这些 buffer 里已经设好的局部值。

* 推荐工作流

Bozhidar Batsov 在开发 major mode 时的典型流程：

1. 编辑源码
2. =C-M-x= 逐个求值修改过的形式
3. 切到测试 buffer， =M-x my-mode= 重新激活
4. 如果状态不对， =unload-feature= + =require= 干净重载
5. 改了 autoloads 或包元数据等根本性东西时才重启 Emacs

大多数时候，前三步就够了。

参考：[[https://emacsredux.com/blog/2026/03/25/reloading-emacs-lisp-code/][Reloading Emacs Lisp Code — Emacs Redux]]
</description>
<pubDate>2026-04-19</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/19/emacs-lisp-热重载实用指南/index.html</guid>
</item>
<item>
<title>Prot 的 Emacs 配置哲学</title>
<link>https://lujun9972.github.io/blog/2026/04/19/prot-的-emacs-配置哲学/index.html</link>
<description>Sacha Chua 在她的 [[https://sachachua.com/blog/2026/04/ye16-sacha-and-prot-talk-emacs/][Yay Emacs 第 16 期]] 直播中和 Prot 聊了 Emacs 配置管理的话题。Prot 分享了他组织配置的几条原则，这些原则不仅适用于他自己的 =prot-= 系列包，对任何想把 Emacs 配置打理得井井有条的人都有参考价值。

* 小函数，频使用

Prot 反复强调一个理念：把频繁执行的操作封装成小函数，绑定到快捷键上。

这听起来显而易见，但实际做起来需要养成习惯——每当你发现自己重复执行某个命令序列时，就该考虑写个函数了。Prot 自己的配置里充满了这类小函数，每个只做一件事，但因为用得频繁，节省的时间累积起来很可观。

Sacha 对此的回应是考虑在 header line 上轮播显示常用操作的提示，这样不需要记忆所有快捷键。实现思路很简单：

#+begin_src emacs-lisp
(defvar my-header-line-tips
  &#39;(&quot;C-c a → agenda&quot; &quot;C-c c → capture&quot; &quot;C-c t → todo&quot; &quot;C-c s → schedule&quot;)
  &quot;Header line 中轮播显示的快捷键提示。&quot;)

(defvar my-header-line-tip-index 0
  &quot;当前显示的提示索引。&quot;)

(defun my-rotate-header-line-tip ()
  &quot;切换到下一条提示。&quot;
  (setq my-header-line-tip-index
        (% (1+ my-header-line-tip-index) (length my-header-line-tips)))
  (force-mode-line-update))

;; 追加到 header line 末尾，不覆盖原有内容
(setq-default header-line-format
              (list (default-value &#39;header-line-format)
                    &#39;(:eval (format &quot; 💡 %s&quot;
                                    (nth my-header-line-tip-index
                                         my-header-line-tips)))))

;; 每 10 秒切换一条提示
(run-at-time t 10 #&#39;my-rotate-header-line-tip)
#+end_src

* 统一命名前缀

Sacha 在这次直播中提到，她把所有函数从 =my-= 前缀重命名为 =sacha-= 。Prot 用 =prot-= 。好处有两个：

1. *避免命名冲突* — =my-= 太常见，复制别人的代码时容易覆盖同名函数
2. *标识来源* — 看到 =sacha-org-capture-= 或 =prot-window-= 就知道这个函数来自谁的配置

#+begin_src emacs-lisp
;; 不好的做法：用通用的 my- 前缀
(defun my-org-capture-region ...)

;; 好的做法：用自己的标识做前缀
(defun sacha-org-capture-region-contents-with-metadata (start end parg)
  ...)
#+end_src

* defcustom 替代 defvar

当代码可能被别人使用时，Prot 建议用 =defcustom= 替代 =defvar= 。

#+begin_src emacs-lisp
;; 用 defvar 定义变量，别人只能改源码
(defvar my-indent-width 2 &quot;Default indentation width.&quot;)

;; 用 defcustom 定义变量，别人可以通过 Customize 接口修改
(defcustom my-indent-width 2
  &quot;Default indentation width.&quot;
  :type &#39;integer
  :group &#39;convenience)
#+end_src

区别在于 =defcustom= 让用户通过 =M-x customize-variable= 或 =use-package= 的 =:custom= 关键字来修改，不需要直接编辑你的代码。如果你的配置是公开的，或者你写了可能被别人复制的代码，这个习惯尤其重要。

* autoload 延迟加载 + docstring 注明来源

从别人配置中复制代码是个常见做法，但如何管理这些&quot;外来&quot;代码？Sacha 的策略是：

1. *优先用 autoload* — 如果只需要别人配置中的少量函数，不要把整个文件加载进来。在函数定义前加 =;;;###autoload= 注解，然后在你的配置中用 =use-package= 的 =:commands= 引用：

#+begin_src emacs-lisp
;; 别人的文件中（加 autoload 注解）
;;;###autoload
(defun prot-comment-timestamp-keyword ()
  &quot;Insert timestamp keyword in comment.&quot;
  ...)

;; 你的配置中（只注册命令，不加载整个文件）
(use-package prot-comment
  :load-path &quot;~/vendor/prot-dotfiles/emacs/.emacs.d/prot-lisp&quot;
  :commands (prot-comment-timestamp-keyword)
  :bind
  (:map prog-mode-map
        (&quot;C-x M-;&quot; . prot-comment-timestamp-keyword)))
#+end_src

关键在 =:commands= — 它告诉 use-package 只注册这个命令名，真正调用时才加载文件。没有 =:commands= ，=use-package= 会在启动时加载整个 =:load-path= 下的文件。

2. *在 docstring 中注明来源* — 如果 autoload 不行，至少在函数文档里写清楚出处：

#+begin_src emacs-lisp
;;;###autoload
(defun sacha-org-capture-region-contents-with-metadata (start end parg)
  &quot;Write selected text between START and END to currently clocked `org-mode&#39; entry.

With PARG, kill the content instead.
If there is no clocked task, create it as a new note in my inbox instead.

From https://takeonrules.com/2022/10/16/adding-another-function-to-sacha-workflow/,
modified slightly so that it creates a new entry if we are not currently clocked in.&quot;
  (interactive &quot;r\nP&quot;)
  (let ((text (sacha-org-region-contents-get-with-metadata start end)))
    (if (car parg)
        (kill-new text)
      (org-capture-string (concat &quot;-----\n&quot; text)
                          (if (org-clocking-p) &quot;c&quot; &quot;r&quot;)))))
#+end_src

这样做的好处是：将来想检查上游是否有更新时，直接从 docstring 里找 URL 就行。

* 重写代码适应自己的风格

Prot 的做法看起来费时间：他不直接复制粘贴别人的代码，而是重写成自己的命名规范和代码风格。

但 Prot 的逻辑是：如果你不理解这段代码到能重写的程度，你就不应该把它放进自己的配置。重写的过程本身就是理解的过程。而且，长期来看，风格统一的配置比东拼西凑的配置更容易维护。

* 小结

这五条原则构成了一个完整的配置管理思路：

- =小函数 + 快捷键= 解决 *效率* 问题
- =命名前缀= 解决 *冲突* 问题
- =defcustom= 解决 *可定制性* 问题
- =autoload + 来源标注= 解决 *依赖管理* 问题
- =重写适应风格= 解决 *长期维护* 问题

不用一次全做，从其中一条开始，逐步养成习惯就好。
</description>
<pubDate>2026-04-19</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/19/prot-的-emacs-配置哲学/index.html</guid>
</item>
<item>
<title>TIL: 从直播对谈中学到的三个 Emacs 技巧</title>
<link>https://lujun9972.github.io/blog/2026/04/19/til-从直播对谈中学到的三个-emacs-技巧/index.html</link>
<description>来源：[[https://sachachua.com/blog/2026/04/ye16-sacha-and-prot-talk-emacs/][YE16: Sacha and Prot talk Emacs]]

* qrencode：在 Emacs 里生成 QR 码

=qrencode= 包能把任意字符串渲染成字符画 QR 码，直接显示在 Emacs buffer 里。Sacha 用它在直播时显示 URL，观众用手机扫码就能打开链接，不用暂停视频手打地址。

#+begin_src emacs-lisp
;; 安装
(use-package qrencode
  :ensure t)

;; 生成 QR 码并插入当前 buffer
(qrencode-region (point-min) (point-max))
;; 或者指定字符串
(insert (qrencode-string &quot;https://sachachua.com&quot;))
#+end_src

生成的 QR 码是用字符方块拼成的，不是图片。可以直接在终端里使用。

* helpful + elisp-demos：增强 Emacs Lisp 文档体验

=helpful= 是 =describe-function= 和 =describe-variable= 的增强替代品，显示更多信息（源码、调用位置、引用）。 =elisp-demos= 则在帮助 buffer 中注入函数用法示例。

#+begin_src emacs-lisp
;; 安装
(use-package helpful
  :ensure t
  :bind
  ((&quot;C-h f&quot; . helpful-callable)
   (&quot;C-h v&quot; . helpful-variable)
   (&quot;C-h k&quot; . helpful-key)))

(use-package elisp-demos
  :ensure t
  :after helpful
  :config
  (advice-add &#39;helpful-update :after #&#39;elisp-demos-advice-helpful-update))
#+end_src

装完之后 =C-h f mapcar= 就能看到 =mapcar= 的实际用法示例，而不是只有干巴巴的函数签名。

* keyd + emacsclient：系统级快捷键

=keyd= 是 Linux 下的键盘重映射守护进程，配置文件放在 =~/.config/keyd/default.conf= 。它能把按键的长按变成修饰键——比如长按空格变成 Super 键，短按还是空格。配合 =emacsclient= 可以实现系统级的快捷键触发 Emacs 命令。

整体思路分三步：

1. Emacs 中定义命令
2. keyd 中定义按键映射，用 =command()= 直接调用 emacsclient
3. 长按空格 + 按对应键即可触发

#+begin_src emacs-lisp
;; Step 1: 在 Emacs 中定义一个通过 emacsclient 调用的命令
(defun my-toggle-dark-mode ()
  &quot;切换深色/浅色主题。&quot;
  (interactive)
  (if (eq (car custom-enabled-themes) &#39;modus-operandi)
      (load-theme &#39;modus-vivendi t)
    (load-theme &#39;modus-operandi t)))
#+end_src

#+begin_src conf :tangle no
# ~/.config/keyd/default.conf

[ids]
*

[main]
# 短按空格 = 空格，长按空格 = 激活 layer 层
space = overload(space, layer)

[layer]
# 长按空格 + c → 直接运行 emacsclient 命令
c = command(emacsclient -e &#39;(my-toggle-dark-mode)&#39;)
#+end_src

这样长按空格再按 =c= ，即使焦点不在 Emacs 窗口上，也能切换主题。keyd 的 =command()= 直接调用系统命令，不需要额外的快捷键管理工具。
</description>
<pubDate>2026-04-19</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/19/til-从直播对谈中学到的三个-emacs-技巧/index.html</guid>
</item>
<item>
<title>TIL: 自动使用项目虚拟环境的 Python</title>
<link>https://lujun9972.github.io/blog/2026/04/19/til-自动使用项目虚拟环境的-python/index.html</link>
<description>在 Emacs 里按 =C-c C-p= 打开 Python shell，默认用的是系统 Python。如果项目有虚拟环境，shell 里就找不到项目安装的包。

Einar Mostad 写了一个函数，自动在项目目录中递归查找 Python 可执行文件，找到就用虚拟环境的，找不到就用系统的：

#+BEGIN_SRC emacs-lisp
(defun emo-python-virtualenv ()
  &quot;Sets the python shell to python from a virtual environment if one exists.&quot;
  (when (project-current)
    (let ((pythonpath
           (nth 0 (directory-files-recursively
                   (nth 2 (project-current))
                   (if (eq system-type &#39;gnu/linux) &quot;python$&quot; &quot;python.exe$&quot;)))))
      (when (file-exists-p pythonpath)
        (setq-local python-shell-interpreter pythonpath)))))
#+END_SRC

加到 =python-mode-hook= 上，每次打开 Python 文件时自动设置：

#+BEGIN_SRC emacs-lisp
(add-hook &#39;python-mode-hook &#39;emo-python-virtualenv)
#+END_SRC

三个关键点：

1. ~directory-files-recursively~ 接受正则表达式，用 =&quot;python$&quot;= 匹配虚拟环境 =bin/= 目录下的 Python 可执行文件（不匹配 =python3= 、 =python-config= 等）
2. ~project-current~ 返回当前项目信息， ~(nth 2 (project-current))~ 取项目根目录
3. ~setq-local~ 把 =python-shell-interpreter= 设为 buffer-local，不影响其他 buffer

参考：[[https://einar.codeberg.page/use-python-shell-from-virtual-environment-if-there-is-one-in-Emacs.html][Use python shell from virtual environment if there is one in Emacs]]
</description>
<pubDate>2026-04-19</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/19/til-自动使用项目虚拟环境的-python/index.html</guid>
</item>
<item>
<title>TIL: 让 Help buffer 自动获得焦点</title>
<link>https://lujun9972.github.io/blog/2026/04/19/til-让-help-buffer-自动获得焦点/index.html</link>
<description>在 Emacs 里查看函数或变量的文档（ =C-h f= 、 =C-h v= 等），Help buffer 打开后焦点还留在原来的 buffer。想滚动内容、点击链接、或者按 =q= 退出，都得先 =C-x o= 切过去。

一行配置解决：

#+BEGIN_SRC emacs-lisp
(setq help-window-select t)
#+END_SRC

启用后，Help buffer 一打开就自动获得焦点。用完按 =q= 直接关闭。

Prot 在自己的配置里也启用了这个选项，原因是：用 Help 的频率比想象中高得多，每次打开都希望焦点在 Help buffer 里，启用后一次都没想过要焦点留在原 buffer。

想先试试不改配置？在 =*scratch*= 里 eval 这行就行，立即生效。

参考：[[https://emacsredux.com/blog/2026/04/07/stealing-from-the-best-emacs-configs/][Stealing from the Best Emacs Configs]]（Bozhidar Batsov 收集的 Emacs 配置技巧）
</description>
<pubDate>2026-04-19</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/19/til-让-help-buffer-自动获得焦点/index.html</guid>
</item>
<item>
<title>一条命令让本地开发用上 HTTPS —— slim 工具介绍</title>
<link>https://lujun9972.github.io/blog/2026/04/19/一条命令让本地开发用上-https-——-slim-工具介绍/index.html</link>
<description>* 本地开发 HTTPS 的痛点

做本地 Web 开发时，你可能经常看到浏览器提示&quot;您的连接不是私密连接&quot;。这不影响开发，但会带来两个问题：

1. *开发体验差* — 每次打开页面都要点&quot;继续前往&quot;
2. *功能受限* — 很多浏览器 API 只在 HTTPS（安全来源）下可用：
   - Service Worker（PWA 开发必备）
   - Geolocation API
   - Clipboard API
   - Camera / Microphone (=getUserMedia=)
   - HTTP/2（浏览器只在 TLS 上协商 HTTP/2）
   - Secure Cookies（带 =Secure= 标志的 Cookie 只在 HTTPS 下发送）

传统做法是手动生成自签名证书、添加到系统信任库、改 =/etc/hosts= 、配反向代理——第一次折腾半小时，之后每次都嫌麻烦。

* slim 是什么

[[https://github.com/kamranahmedse/slim][slim]] 是一个 Go 写的轻量级反向代理和本地域名管理器。一条命令搞定上述所有事情：

#+BEGIN_SRC shell
slim start myapp --port 3000
#+END_SRC

执行后，项目通过 =https://myapp.test= 访问，有合法证书，无浏览器警告。支持 HTTP/2、WebSocket、HMR（热模块替换），Next.js、Vite 等开发服务器开箱即用。

* 工作原理

=slim start= 第一次运行时自动完成四件事：

1. *证书颁发机构（CA）* — 生成本地根 CA，注册到系统信任库（Linux CA store 或 macOS Keychain）。之后每个域名按需签发叶子证书，通过 SNI 提供。
2. *反向代理* — 启动后台守护进程，用 Go 内置的 =httputil.ReverseProxy= ，把 HTTPS 流量从域名转发到本地开发服务器的端口。
3. *本地 DNS* — 在 =/etc/hosts= 中添加条目，让域名解析到 =127.0.0.1= ，不需要额外的 DNS 服务器。
4. *端口转发* — 用 iptables（Linux）或 pfctl（macOS）把 80/443 端口重定向到 10080/10443，代理进程不需要 root 权限。

* 安装

一行命令安装：

#+BEGIN_SRC shell
curl -sL https://slim.sh/install.sh | sh
#+END_SRC

安装前可以先审查脚本内容：在浏览器中打开 =https://slim.sh/install.sh= 查看源码。

确认安装成功：

#+BEGIN_SRC shell
slim --version
#+END_SRC

* 实际使用

假设你的开发服务器运行在 3000 端口，启动 slim：

#+BEGIN_SRC shell
slim start myapp --port 3000
#+END_SRC

第一次运行时，slim 会生成根 CA、注册系统信任库、为 =myapp.test= 签发证书、更新 =/etc/hosts= 、启动后台代理。

打开浏览器访问 =https://myapp.test= ，项目通过 HTTPS 加载，证书有效，没有警告。

*Note:* slim 默认使用 =.test= 域名。不要用 =.local= —— 它保留给 mDNS 使用，在 macOS/Linux 上可能导致 DNS 解析缓慢或不一致。 =.test= 是 RFC 2606 专门为本地开发保留的域名。

* 日常管理

查看所有活跃域名：

#+BEGIN_SRC shell
slim list
#+END_SRC

停止指定域名：

#+BEGIN_SRC shell
slim stop myapp
#+END_SRC

完全卸载（删除 CA、证书、hosts 条目、端口转发规则、配置文件）：

#+BEGIN_SRC shell
slim uninstall
#+END_SRC

诊断检查：

#+BEGIN_SRC shell
slim doctor
#+END_SRC

启动时的有用选项：

#+BEGIN_SRC shell
slim start myapp --port 3000 --log verbose  # 详细日志（记录每个请求）
slim start myapp --port 3000 --log quiet    # 静默模式
slim start myapp --port 3000 --wait         # 等待上游端口就绪后再开始代理
#+END_SRC

所有运行时数据在 =~/.slim/= 目录下。需要手动检查或备份证书时去那里。

* 为什么本地开发需要 HTTPS

即使不考虑浏览器警告，本地开发环境应该尽量接近生产环境。以下功能在 HTTP 下行为不同或完全不工作：

| API / 功能 | HTTPS 下的行为 | HTTP 下的行为 |
|------------|---------------|--------------|
| Service Worker | 正常注册和运行 | 无法注册 (除 =localhost= 外) |
| Geolocation | 需要用户授权后可用 | 直接被拒绝 |
| Clipboard API | 读写剪贴板 | 不可用 |
| Camera / Microphone | =getUserMedia= 正常工作 | 被浏览器阻止 |
| HTTP/2 | 正常协商 | 不支持（浏览器只在 TLS 上用 HTTP/2） |
| Secure Cookies | 正常设置和发送 | 永远不会被发送 |

如果你的项目依赖这些功能，在 HTTP 下测试的结果和生产环境不一致。slim 让本地 HTTPS 变得几乎零成本。
</description>
<pubDate>2026-04-19</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/19/一条命令让本地开发用上-https-——-slim-工具介绍/index.html</guid>
</item>
<item>
<title>用 fsck 检查和修复 Linux 文件系统</title>
<link>https://lujun9972.github.io/blog/2026/04/19/用-fsck-检查和修复-linux-文件系统/index.html</link>
<description>来源：[[https://www.tecmint.com/fsck-repair-file-system-errors-in-linux/][How to Use fsck to Check and Repair Linux Filesystem Errors]]

* 基础知识

** fsck 是什么

=fsck= （File System Consistency Check）是 Linux 内置的文件系统检查和修复工具，类似 Windows 的 =chkdsk= 。它可以开机时自动运行，也可以由管理员手动执行。

=fsck= 会根据文件系统类型调用对应的后端检查程序—— =ext2/3/4= 调用 =e2fsck= ， =xfs= 调用 =fsck.xfs= ， =vfat= 调用 =fsck.vfat= 。

** 什么时候需要跑 fsck

- 系统无法启动，掉进 emergency/rescue mode
- 读写文件时出现 I/O 错误
- 硬盘、U 盘、SD 卡行为异常
- 异常关机后（断电、kernel panic、强制重启）
- 内核日志中出现文件系统损坏信息

** 确认文件系统类型

跑 =fsck= 前先确认分区的文件系统类型：

#+begin_src shell
lsblk -f
#+end_src

或者指定分区：

#+begin_src shell
blkid /dev/sdb1
#+end_src

* 使用方法

** 基本检查（交互式）

逐个错误提示确认：

#+begin_src shell
fsck /dev/sdb1
#+end_src

** 自动修复

对所有修复提示自动回答&quot;是&quot;，适合生产环境自动化：

#+begin_src shell
fsck -y /dev/sdb1
#+end_src

** 干跑（不修改任何东西）

只看 =fsck= 会做什么，不改数据：

#+begin_src shell
fsck -N /dev/sdb1
#+end_src

生产环境建议先干跑，确认没问题再真正执行。

** 强制检查

文件系统标记为&quot;干净&quot;时 =fsck= 默认跳过。用 =-f= 强制检查：

#+begin_src shell
fsck -f /dev/sdb1
#+end_src

** 检查所有文件系统（跳过根分区）

=-A= 读取 =/etc/fstab= 中的所有文件系统， =-R= 跳过根分区：

#+begin_src shell
fsck -AR -y
#+end_src

** 带进度条的详细输出

#+begin_src shell
fsck -C -V /dev/sdb1
#+end_src

大容量磁盘检查时有用。

** 指定文件系统类型

#+begin_src shell
fsck -t ext4 /dev/sdb1
#+end_src

** 检查根分区

根分区挂载着没法直接 =fsck= ，有三种方法。

方法一，在根目录创建标记文件，下次启动时自动检查：

#+begin_src shell
touch /forcefsck
reboot
#+end_src

完成后标记文件会被自动删除。注意：基于 systemd 的系统（RHEL 7+、Ubuntu 16.04+）可能不支持 =forcefsck= ，用下面的方法。

方法二，用 =tune2fs= 把挂载计数设为 1，下次启动时触发 =fsck= （ext4）：

#+begin_src shell
tune2fs -C 1 /dev/sda1
#+end_src

方法三，进入救援模式手动操作：

1. 重启，启动时按住 Shift 进入 GRUB 菜单
2. 选择&quot;Advanced options&quot;
3. 选择对应内核的&quot;Recovery mode&quot;
4. 在恢复菜单中选择&quot;fsck&quot;
5. 提示重新挂载根文件系统时选&quot;Yes&quot;
6. 完成后选择&quot;Resume&quot;继续正常启动

** LVM 和软件 RAID

LVM 逻辑卷需要先停用再检查：

#+begin_src shell
lvdisplay
lvchange -an /dev/vg_data/lv_data
fsck -y /dev/vg_data/lv_data
lvchange -ay /dev/vg_data/lv_data
#+end_src

RAID 阵列先确认状态正常再检查。降级阵列应先重建，不要直接 =fsck= ：

#+begin_src shell
cat /proc/mdstat
fsck -y /dev/md0
#+end_src

** 定期自动检查

用 =tune2fs= 设置定期检查（ext4）：

基于时间（每 6 个月）：

#+begin_src shell
tune2fs -i 6m /dev/sda1
#+end_src

基于挂载次数（每 30 次）：

#+begin_src shell
tune2fs -c 30 /dev/sda1
#+end_src

查看当前设置：

#+begin_src shell
tune2fs -l /dev/sda1 | grep -E &quot;Mount count|Maximum mount|Check interval|Last checked&quot;
#+end_src

** 查看 fsck 日志

#+begin_src shell
# RHEL/CentOS/Rocky
grep -i fsck /var/log/messages

# Ubuntu/Debian
grep -i fsck /var/log/syslog

# systemd journal（当前启动）
journalctl -b | grep -i fsck
#+end_src

* 注意事项

** 绝对不要在挂载的分区上跑 fsck

在已挂载的分区上运行 =fsck= 会导致严重的数据损坏。先检查分区是否已挂载：

#+begin_src shell
mount | grep /dev/sdb
#+end_src

如果已挂载，先卸载：

#+begin_src shell
umount /dev/sdb1
#+end_src

如果卸载失败，提示&quot;target is busy&quot;，说明有进程在使用这个分区。用 =lsof= 找出是哪些进程：

#+begin_src shell
lsof /dev/sdb1
#+end_src

找到占用进程后，停掉或关闭它们，再重新 =umount= 。如果进程不好停（比如某个后台服务），可以用懒卸载：

#+begin_src shell
umount -l /dev/sdb1
#+end_src

懒卸载会立刻把挂载点从文件系统命名空间中移除（新进程看不到这个挂载点了），但实际卸载要等所有占用进程释放后才完成。这样做的好处是你不需要一个个去停进程，但风险是：在所有进程释放之前， =fsck= 仍然无法安全执行。所以用懒卸载后，要等一会儿确认设备真的卸载了（ =mount | grep /dev/sdb= 不再返回结果），再跑 =fsck= 。

* 退出码说明

=fsck= 执行完返回退出码，用 =echo $?= 查看：

| 退出码 | 含义 |
|--------|------|
| 0 | 未发现错误 |
| 1 | 错误已修复 |
| 2 | 建议重启系统 |
| 4 | 错误未修复 |
| 8 | 操作错误 |
| 16 | 用法或语法错误 |
| 32 | 用户取消检查 |
| 128 | 共享库错误 |

退出码可以叠加。比如退出码 3 即错误已修复（1）+ 需要重启（2）。

在脚本中应该捕获退出码：

#+begin_src shell :tangle no
fsck -y /dev/sdb1
EXIT_CODE=$?
if [ $EXIT_CODE -ge 4 ]; then
    echo &quot;ALERT: 文件系统错误无法修复 /dev/sdb1&quot; | mail -s &quot;fsck Alert&quot; admin@example.com
fi
#+end_src

* 常用选项速查

| 命令 | 说明 |
|------|------|
| =fsck /dev/sdb1= | 交互式检查 |
| =fsck -y /dev/sdb1= | 自动修复 |
| =fsck -N /dev/sdb1= | 干跑（不修改） |
| =fsck -f /dev/sdb1= | 强制检查 |
| =fsck -AR -y= | 检查所有文件系统（跳过根分区） |
| =fsck -C -V /dev/sdb1= | 带进度条的详细输出 |
| =fsck -t ext4 -A -y= | 只检查 ext4 文件系统 |
</description>
<pubDate>2026-04-19</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/19/用-fsck-检查和修复-linux-文件系统/index.html</guid>
</item>
<item>
<title>排查Linux进程&quot;卡死&quot;实战：从strace到gdb全流程</title>
<link>https://lujun9972.github.io/blog/2026/04/19/排查linux进程&quot;卡死&quot;实战：从strace到gdb全流程/index.html</link>
<description>* 故障现象

某天，我在 Orange Pi 上跑了一个批量处理 URL 的脚本，用 =claude= CLI 把网上的文章自动转成知识卡片。脚本跑了一晚上，第二天发现它好像&quot;卡住了&quot;——终端没有任何输出，进度也不再推进。

怎么确认它真的卡住了、又卡在哪里呢？下面是完整的排查过程。

* 排查过程

** 用 strace 查看进程在干什么

=strace= 是 Linux 下排查进程问题的神器，它能追踪一个进程正在调用的所有系统调用（system call）。系统调用是程序向内核请求服务的接口，比如读文件、写网络数据、等待定时器等。

先用 =ps= 找到卡住的进程 PID，然后 attach 上去：

#+begin_src shell
  sudo strace -f -p 169047 -o /tmp/169047.log
#+end_src

- =-f= ：追踪子进程和子线程
- =-p 169047= ：attach 到 PID 为 169047 的进程
- =-o /tmp/169047.log= ：将输出写入日志文件

等了几秒钟后按 =Ctrl+C= 停止，然后查看日志。

** 分析 strace 输出

日志有 7049 行，初看很吓人，但关键是找到规律。先看前面几行：

#+begin_example
169080 read(16,  &lt;unfinished ...&gt;
169059 epoll_pwait2(13,  &lt;unfinished ...&gt;
169047 epoll_pwait2(4,  &lt;unfinished ...&gt;
169048 futex(...) = -1 ETIMEDOUT (Connection timed out)
...
#+end_example

每一行的格式是： =线程ID 系统调用(参数) = 返回值=

这里有多个不同的数字（169047、169048、169049……），它们是什么？同一个进程可以有多个 *线程* ，每个线程有自己的 ID（TID），但它们共享同一个进程的内存和文件描述符。

*** 初步归类

过滤掉重复的 =sched_yield= （让出 CPU）和 =futex= （线程同步）后，我发现了这些有意义的操作：

| 线程                | 系统调用                           | 含义                         |
|---------------------+------------------------------------+------------------------------|
| *169047* （主线程） | =epoll_pwait2(4, [], ...)= 反复超时返回空 | 事件循环在等事件，但什么都没来 |
| *169080*            | =read(16, ...)= 从第1行卡到最后一行       | 一直阻塞在读操作上           |
| *169049-169053*     | 大量 =sched_yield=                  | 工作线程在空转               |
| *169059*            | =epoll_pwait2(13, ...)= 周期性返回事件   | 有事件到来，但……             |
| *169057-169061*     | 周期性 =statx= 检查 .git 文件        | 正常的文件监控轮询           |

主线程 169047 在做事件循环（event loop）：等事件 → 超时 → 检查一些状态 → 回去继续等。epoll 每次返回的都是空数组 =[]= ，说明主事件循环上 *没有事件就绪* 。

线程 169080 从日志开始到结束一直卡在 =read(16, ...)= 上，从未返回。看到 =read()= 阻塞，我的第一反应是： *这个进程大概在等网络响应* 。毕竟它是一个调用 API 的程序， =read()= 卡住最常见的原因就是网络请求发了出去，但服务端的响应迟迟没到。但这个猜测对不对呢？

#+BEGIN_QUOTE
*小知识：什么是文件描述符（fd）？*

在 Linux 中，所有 I/O 操作都通过文件描述符进行。打开一个文件、建立一个网络连接、创建一个管道，都会得到一个 fd。它就是一个整数编号（0=标准输入，1=标准输出，2=标准错误，3 以上由程序自己分配）。

fd 不仅对应普通文件，还可以对应网络 socket、管道、定时器、inotify 实例等。知道了 fd 的编号，不等于知道它的类型——我们需要到 =/proc= 文件系统中去查看。
#+END_QUOTE

** 确认 fd 16 的类型：不是网络 socket

#+begin_src shell
  ls -la /proc/169047/fd/16
#+end_src

输出：

#+begin_example
lr-x------ 1 orangepi orangepi 64 /proc/169047/fd/16 -&gt; anon_inode:inotify
#+end_example

fd 16 是一个 *inotify* 实例！inotify 是 Linux 的文件系统事件监控机制，用来监听文件变化（比如文件被修改、创建、删除）。 =read()= 在 inotify 上阻塞是 *设计上就是这样的* ——它只在有文件变化时才返回数据，没有变化时阻塞等待是天经地义的事。

但是，一个 inotify 线程阻塞，并不能解释整个进程&quot;卡住&quot;的现象。这个线程只是一个后台的文件监控器，它的职责就是安静地等文件变化。进程之所以&quot;卡住&quot;，是因为它无法完成自己的核心任务——处理 URL。所以 fd 16 不是故障原因，我需要继续找。

** 转向线程 169059：epoll fd 13 只收到定时器事件

strace 日志中，线程 169059 也在做一个 =epoll_pwait2= 。单独提取它的记录：

#+begin_src shell
  grep &#39;169059&#39; /tmp/169047.log
#+end_src

输出：

#+begin_example
169059 epoll_pwait2(13,  &lt;unfinished ...&gt;
169059 &lt;... epoll_pwait2 resumed&gt;[{events=EPOLLIN, data=...}], ...) = 1
169059 read(14, &quot;\1\0\0\0\0\0\0\0&quot;, 8)  = 8
169059 epoll_pwait2(13,  &lt;unfinished ...&gt;
169059 &lt;... epoll_pwait2 resumed&gt;[{events=EPOLLIN, data=...}], ...) = 1
169059 read(14, &quot;\1\0\0\0\0\0\0\0&quot;, 8)  = 8
169059 epoll_pwait2(13,  &lt;unfinished ...&gt;
...（重复多次）
#+end_example

线程 169059 在反复做一件事： =epoll_pwait2(13, ...)= → 返回 1 个事件 → =read(14, ...)= → 回到 =epoll_wait= 。

注意一个规律：每次 epoll 返回后，线程读的都是 *fd 14* （后面可以看到这是个定时器）。也就是说，线程在重复等待 epoll fd 13 上的事件。那 epoll fd 13 到底监控了哪些 fd？我们需要查两样东西：进程的完整 fd 列表（搞清楚每个 fd 是什么），以及 epoll fd 13 的监控清单（搞清楚它在等谁的数据）。

** 查看 fd 全貌和 epoll 监控清单

先看进程一共有哪些文件描述符：

#+begin_src shell
  ls -la /proc/169047/fd/
#+end_src

输出中能看到的 fd 类型包括：

| fd          | 类型                   | 说明                     |
|-------------+------------------------+--------------------------|
| 0           | /dev/pts/1             | 标准输入（终端）         |
| 1, 2, 11, 12 | pipe                 | 管道（stdout/stderr 重定向） |
| 3, 9        | /dev/urandom           | 随机数生成器             |
| 4           | anon_inode:[eventpoll] | 主事件循环的 epoll 实例  |
| 5, 7, 8     | anon_inode:[timerfd]   | 定时器                   |
| 6           | anon_inode:[eventfd]   | 事件通知                 |
| 10          | /proc/169047/statm     | 进程内存统计             |
| 13, 32      | anon_inode:[eventpoll] | 另外两个 epoll 实例      |
| 16          | anon_inode:inotify     | 文件系统监控             |
| *28*        | *socket:[616763]*      | *唯一的网络 socket！*    |

进程里有三个 epoll 实例（fd 4、fd 13、fd 32）。查看 epoll fd 13 的监控清单：

#+begin_src shell
  cat /proc/169047/fdinfo/13
#+end_src

输出：

#+begin_example
tfd:       28 events:       19 data:2611c3c00a0    ← 网络 socket
tfd:       15 events: 80000019 data:2611c0a0100    ← eventfd
tfd:       14 events:       19 data:2611c0a00c0    ← timerfd
#+end_example

=/proc/PID/fdinfo/EPOLL_FD= 中的 =tfd= 字段就是被监控的目标 fd 编号。epoll fd 13 同时监控了三个 fd：fd 28（网络 socket）、fd 15（eventfd）、fd 14（定时器）。

strace 中只看到 fd 14（定时器）触发，fd 28 和 fd 15 都没有。但 fd 15 不触发并不奇怪——它是一个 *eventfd* ，这是一种线程间内部通知机制，只有当其他线程主动往里写数据时才会触发。如果内部没有需要通知的事情，它安静待着是正常的。

而 fd 28 就不同了。它是 *网络 socket* ，是进程与外部世界通信的唯一通道。这个进程是 =claude= CLI，它向 API 服务器发出了请求，正在等响应。socket 长时间不触发，意味着服务端的响应数据迟迟没到——这才是进程&quot;卡住&quot;的根本原因。

** 检查网络连接状态

找到了网络 socket fd 28，它的 inode 号是 616763。接下来要搞清楚这个 socket 连接的是谁、状态如何。

*** 从 /proc/net/tcp 找到连接详情

=/proc/net/tcp= 文件列出了系统所有 TCP 连接。我们需要从中找到 inode 为 616763 的那一条：

#+begin_src shell
  cat /proc/169047/net/tcp
#+end_src

输出很长，但关键的一行是：

#+begin_example
sl  local_address    rem_address   st tx_queue:rx_queue ... inode  ...
13: 191FA8C0:CF72   33551777:01BB 01 00000000:00000000  ... 616763 ...
#+end_example

inode 616763 匹配上了！但 =local_address= 和 =rem_address= 都是十六进制，怎么看懂？

*** 解码 /proc/net/tcp 的地址格式

=/proc/net/tcp= 中的地址格式是 =IP地址:端口= ，其中 IP 地址是 *十六进制、小端序* 编码的。以远端地址 =33551777:01BB= 为例：

#+begin_example
远端 IP:   33551777 → 每 2 位拆成一个字节: 33 55 17 77 → 反转字节序: 77 17 55 33 → 119.23.85.51
远端端口:  01BB → 十六进制转十进制 → 443 (即 HTTPS)
#+end_example

同理，解码本地地址 =191FA8C0:CF72= ：

#+begin_example
本地 IP:   191FA8C0 → 19 1F A8 C0 → 反转: C0 A8 1F 19 → 192.168.31.25
本地端口:  CF72 → 53106
#+end_example

所以这个连接是：本机 =192.168.31.25:53106= → 远端 =119.23.85.51:443=

#+BEGIN_QUOTE
*小知识：为什么要反转字节序？*

=/proc/net/tcp= 中的 IP 地址使用 *网络字节序* （大端序）存储，但在 x86/ARM 等 *小端序* 机器上，内核为了存储效率，按小端序输出十六进制。所以 =33551777= 实际上是 =0x77175533= 的小端序表示，对应 IP =119.23.85.51= 。

简单记忆： *把十六进制字符串每两位切一刀，然后倒着拼回去，再转成十进制 IP* 。
#+END_QUOTE

*** 用 ss 查看连接的详细状态

拿到了远端 IP 后，用 =ss= 命令查看这个连接的详细信息：

#+begin_src shell
  ss -ti dst 119.23.85.51
#+end_src

- =-t= ：只显示 TCP 连接
- =-i= ：显示详细信息（RTT、超时、收发字节数等）
- =dst 119.23.85.51= ：只看连接到这个 IP 的

输出中最关键的字段：

#+begin_example
ESTAB  0  0  192.168.31.25:53106  119.23.85.51:https
  bytes_sent:2114416  bytes_acked:2114417
  bytes_received:586087
  lastsnd:10621788  lastrcv:10621508
#+end_example

解读：

| 指标               | 值                          | 含义                           |
|--------------------+-----------------------------+--------------------------------|
| 状态               | ESTABLISHED                 | TCP 连接看起来还活着           |
| bytes_sent / bytes_acked | ~2MB / ~2MB         | 请求已完整发送并被服务端确认   |
| bytes_received     | ~572KB                      | 收到了部分响应                 |
| *lastsnd*          | *10,621,788 ms ≈ 2.95 小时* | *最后一次发送数据是近 3 小时前* |
| *lastrcv*          | *10,621,508 ms ≈ 2.95 小时* | *最后一次接收数据也是近 3 小时前* |

*** 真相大白：TCP 半死连接

连接的状态是 ESTABLISHED，但近 3 小时没有任何数据流动。这说明：

1. 进程发送了约 2MB 的 API 请求（完整到达服务端）
2. 服务端回了约 572KB 数据后，传输中断了
3. 中间的网络设备（NAT/防火墙）可能丢弃了这个长时间空闲连接的状态
4. 由于没收到 TCP RST 或 FIN，进程端以为连接还活着，一直在等

这就是经典的 *TCP 半死连接* （half-open connection）问题。TCP 本身不会因为长时间无数据就主动关闭连接（除非开启了 TCP keepalive），如果应用层也没有设置读取超时，进程就会 *无限期等待* 。

* 恢复过程

** 问题 1：能不能安全 kill？

在决定 kill 之前，需要了解进程之间的关系：

#+begin_src shell
  ps -ef --forest | grep -A5 url-to-cards
#+end_src

#+begin_example
bash url-to-cards.sh (138905)        ← 主脚本
  └── bash (169046)                  ← command substitution 子 shell
      └── claude (169047)            ← 卡住的 claude 进程
#+end_example

脚本开头设置了 =set -euo pipefail= ， =set -e= 会让脚本在任何命令返回非零时立即退出。但脚本中有这样的写法：

#+begin_src shell
  output=$(claude ...) || true
  #                    ^^^^^^^^
#+end_src

=|| true= 的作用是：即使 claude 命令失败，表达式整体也会返回成功（ =true= 的返回值是 0）。这样 =set -e= 就不会触发，脚本不会退出。

所以，kill 掉卡住的 claude 进程（169047）后：

1. claude 收到信号退出
2. 子 shell 也退出（非零）
3. =|| true= 兜住错误
4. 脚本继续处理下一个 URL

** 问题 2：原始 URL 列表丢失了

脚本读取的 URL 文件 =/tmp/feed2mail.urls.txt= 已经被另一个程序覆盖了，原始内容丢失了。但 bash 进程（138905）启动时已经把 URL 列表读进了内存中的 =urls= 数组。

** 用 gdb 从进程内存恢复数据

=gdb= （GNU Debugger）虽然主要用来调试程序，但也能 attach 到运行中的进程，读取它的内存。

#+begin_src shell
  # 先找到堆内存的地址范围
  cat /proc/138905/maps | grep heap
  # 输出: aaaaec588000-aaaaec60c000 rw-p ... [heap]

  # 用 gdb 导出堆内存
  sudo gdb -batch -p 138905 \
    -ex &#39;dump memory /tmp/bash_heap.bin 0xaaaaec588000 0xaaaaec60c000&#39; \
    -ex &#39;detach&#39;

  # 从导出的内存中搜索 URL
  strings /tmp/bash_heap.bin | grep -oE &#39;https?://[^[:space:]&gt;&quot;]+&#39; | sort -u
#+end_src

- =dump memory= ：将指定地址范围的内存写入文件
- =strings= ：从二进制数据中提取可打印字符串
- =grep -oE &#39;https?://...&#39;= ：用正则匹配 URL
- =sort -u= ：去重排序

成功恢复了 *189 个 URL* ！

#+BEGIN_QUOTE
*小知识：为什么要从堆（heap）恢复？*

进程的内存分为多个区域：
- *代码段（text）* ：存放程序指令
- *数据段（data）* ：存放全局变量
- *堆（heap）* ：动态分配的内存（bash 变量存在这里）
- *栈（stack）* ：函数调用栈

bash 的变量（包括数组）存储在堆内存中，所以我们从堆区域提取数据。
#+END_QUOTE

* 总结

** 排查流程回顾

#+begin_example
进程卡住了
  │
  ├─ 1. strace -f 看进程在做什么系统调用
  │     → 发现多个线程：主线程 epoll 空转，线程 169080 read 阻塞，线程 169059 epoll 周期触发
  │
  ├─ 2. ls -la /proc/PID/fd/16 看 fd 16 指向什么
  │     → 是 inotify（文件监控），阻塞是正常行为，排除
  │
  ├─ 3. 回到 strace，细看线程 169059
  │     → epoll_pwait2(13) 每次返回后都读 fd 14（定时器），从不读 fd 28（网络 socket）
  │
  ├─ 4. cat /proc/PID/fdinfo/13 确认 epoll 监控清单
  │     → epoll fd 13 监控了 fd 28（网络 socket），但 socket 从未触发事件
  │
  ├─ 5. ss -ti 查看网络连接状态
  │     → lastsnd/lastrcv 约 3 小时前 → TCP 半死连接
  │
  ├─ 6. 分析代码确认 kill 安全（|| true 兜底）
  │
  └─ 7. gdb dump memory 恢复丢失的数据
        → 从堆内存中提取出 189 个 URL
#+end_example

** 工具速查

| 工具                       | 用途                         | 示例                             |
|----------------------------+------------------------------+----------------------------------|
| =strace -f -p PID=         | 追踪进程的系统调用           | 看进程卡在哪个 syscall           |
| =ls -la /proc/PID/fd/=     | 查看进程打开的所有文件描述符 | 确认 fd 是 socket、pipe 还是 inotify |
| =cat /proc/PID/fdinfo/N=   | 查看 fd 的详细信息           | 看 epoll 监控了哪些 fd           |
| =cat /proc/PID/maps=       | 查看进程内存布局             | 找到堆地址范围                   |
| =ss -ti=                   | 查看 TCP 连接详情            | 看 RTT、超时、最后收发时间       |
| =ps -ef --forest=          | 查看进程树                   | 理解父子进程关系                 |
| =gdb -batch -p PID=        | attach 到运行中的进程        | 导出内存、查看变量               |

* 反思与改进

这次事件暴露了几个可以改进的地方：

1. *应用层超时* ：claude CLI 应该给 API 请求设置合理的读取超时（如 5-10 分钟），避免死连接无限挂起
2. *TCP keepalive* ：开启 TCP keepalive 可以让内核自动检测死连接
3. *脚本健壮性* ：可以在脚本中为每个 claude 调用加上 =timeout= 命令：
   #+begin_src shell
     timeout 600 claude -p ...  # 最多等 10 分钟
   #+end_src
4. *URL 文件保护* ：脚本应该在读入 URL 后立即备份，或者用管道传递而非依赖临时文件
</description>
<pubDate>2026-04-19</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/19/排查linux进程&quot;卡死&quot;实战：从strace到gdb全流程/index.html</guid>
</item>
</channel>
</rss>