<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>暗无天日</title>
    <link>https://lujun9972.github.io</link>
    <description>=============&gt;DarkSun的个人博客</description>
    <pubDate>Wed, 22 Apr 2026 16:19:44 UTC</pubDate>
    <lastBuildDate>Wed, 22 Apr 2026 16:19:44 UTC</lastBuildDate>
    <docs>http://www.rssboard.org/rss-specification</docs>
    <generator>ego static site generator (https://github.com/emacs-china/ego)</generator>
<item>
<title>Elisp 性能优化的六个实战教训</title>
<link>https://lujun9972.github.io/blog/2026/04/22/elisp-性能优化的六个实战教训/index.html</link>
<description>有人用 Elisp 写了一个 Org-mode 静态站点生成器，处理 10000 篇博文的时间从 5 分半压到 1 分 15 秒，热重载单篇只需 7 毫秒。实现过程中踩过的坑和发现的优化技巧，对任何在 Emacs 里处理大量文件的人都有参考价值。本文提取其中的工程实践，用你能在自己项目里直接用的方式讲清楚。

* 只解析你需要的东西

Elisp 里解析 Org 文件的标准方式是调用 =org-element-parse-buffer= 。它的默认行为是完整解析整个 buffer——包括行内标记、链接、脚注，全部递归展开。

但如果你只需要提取文件开头的标题和日期，完整解析就是巨大的浪费。 =org-element-parse-buffer= 接受一个可选的 =GRANULARITY= 参数：

- =&#39;headline= :: 只解析标题结构
- =&#39;greater-element= :: 不递归进入大元素内部，只解析顶层
- =&#39;element= :: 解析所有元素但不解析行内对象
- =&#39;object= :: 完整解析（默认）

原文作者只需要标题和日期等元数据，把粒度从默认的 =&#39;object= 改成 =&#39;greater-element= ，单文件解析时间直接砍了一截。这不是微优化——对 10000 个文件来说，每个文件少解析几百个行内元素，累积效果显著。

#+begin_src emacs-lisp
;; 完整解析（慢）
(org-element-parse-buffer)

;; 只解析顶层元素（快得多）
(org-element-parse-buffer &#39;greater-element t)
#+end_src

选择哪个粒度取决于你需要什么。提取元数据用 =&#39;greater-element= ，需要处理链接和行内标记才用 =&#39;object= 。

* 别让 find-file-noselect 拖慢批量操作

Emacs 有两种方式读取文件内容：

1. =find-file-noselect= —— 正经打开文件，创建一个完整的 buffer
2. =with-temp-buffer= + =insert-file-contents= —— 把文件内容塞进临时 buffer

原文作者一开始选了 =find-file-noselect= ，觉得它更&quot;正确&quot;。然后用 Emacs 内置的性能分析器（ =M-x profiler-start= ）一看，发现这个函数吃掉了 13% 的 CPU。原因： =find-file-noselect= 会触发文件锁、版本控制查询、major-mode hook、org-persist 缓存写入等一系列交互式操作。对每个文件都来一遍，10000 个文件就是在重复 10000 次不必要的开销。

#+begin_src emacs-lisp
;; 慢：触发完整的 buffer 初始化流程
(org-with-file-buffer filename
  (org-element-parse-buffer &#39;greater-element t))

;; 快：用临时 buffer 直接读内容
(with-temp-buffer
  (insert-file-contents filename)
  (org-element-parse-buffer &#39;greater-element t))
#+end_src

切换到 =with-temp-buffer= 后，10000 篇博文的处理时间从 3 分 44 秒降到 1 分 15 秒。教训：批量处理文件时，用临时 buffer 而不是 =find-file-noselect= 。后者是为交互式编辑设计的，带着整套&quot;官僚系统&quot;。

* 用 cl-progv 隔离全局状态

Emacs 的状态管理建立在全局变量上—— =org-export-with-toc= 、 =org-html-link-home= 等都是全局变量。在同一个进程里处理多个 route 时，一个 route 设置的变量会&quot;泄漏&quot;到下一个 route。

直觉上的做法是用 =let= 绑定：

#+begin_src emacs-lisp
(let ((org-export-with-toc t))
  (opd-route ...))   ;; 这一步只是把 route 配置记下来

;; 另一个 route
(opd-route ...)

(opd-export)         ;; 真正的导出在这里执行
#+end_src

问题是： =opd-route= 不会立即执行导出，它只是把 route 的配置（输入模式、输出路径、过滤器等）存到一个列表里。真正的 HTML 生成发生在 =opd-export= 被调用的时候——但那时 =let= 的作用域已经结束了， =org-export-with-toc= 的局部绑定已经不存在了。换句话说，你在一个 =let= 块里填了一张表单，但处理表单的人要等到 =let= 块结束之后才看它，那时候表单上写的&quot;特殊要求&quot;已经没人记得了。

那能不能把 =opd-export= 放进 =let= 里？也不行，因为不同 route 需要*不同的*变量值——blog route 要目录（ =org-export-with-toc= 为 =t= ），RSS route 不要目录（ =nil= ）。 =opd-export= 一次执行所有 route，你没法在一个 =let= 里同时给不同 route 设不同的值。

解决方案是用 =cl-progv= ，这个来自 Common Lisp 的宏可以在运行时动态绑定一组变量。每个 route 把自己的变量设置写在 =:env= 属性里，引擎处理到那个 route 时才临时绑定，处理完自动恢复：

#+begin_src emacs-lisp
(cl-progv
    &#39;(org-export-with-toc org-html-link-home)  ;; 变量名列表
    &#39;(nil &quot;http://localhost:8080&quot;)               ;; 对应的值列表
  ;; 在这个作用域里，org-export-with-toc 为 nil
  ;; org-html-link-home 为 &quot;http://localhost:8080&quot;
  (org-export-as &#39;rss))
#+end_src

=cl-progv= 接受变量名列表和值列表，在执行 body 期间动态绑定这些变量，执行完后自动恢复。这比手动在每个 lambda 里 =let= 绑定更干净——你可以把变量配置做成数据，从 route 定义里传入，而不需要在每个导出函数里硬编码。

* 让数据形状决定行为，而不是手动标记

原文遇到一个问题：blog route 和 rss route 都匹配 =*.org= 文件，两者都在全局注册表里写入 URL，互相覆盖。最初的解法是加一个 =:canonical= 标记，手动指定哪个 route 有写入权限。

后来发现根本不需要这个标记。关键是观察聚合（aggregate）后数据的形状：

- 1:1 route（每个文件对应一个输出）：数据的顶层有 =abspath= 属性
- 1:N route（多个文件聚合为一个输出，如 RSS）： =abspath= 藏在嵌套列表里

#+begin_src emacs-lisp
(defun opd--tree-has-abspath-p (tree path)
  &quot;递归搜索 Lisp 树中是否包含 (abspath . PATH) 。&quot;
  (cond
   ((and (consp tree)
         (eq (car tree) &#39;abspath)
         (equal (cdr tree) path)) t)
   ((consp tree)
    (or (opd--tree-has-abspath-p (car tree) path)
        (opd--tree-has-abspath-p (cdr tree) path)))
   (t nil)))
#+end_src

用数据结构本身的特征来区分行为，而不是引入额外的标记。这是 duck typing 的思想：不问&quot;你是什么&quot;，而是看&quot;你长什么样&quot;。消除了一个配置项，也消除了用户忘记设置它导致的 bug。

* 先 profile，再优化

原文最有价值的教训之一：不要猜瓶颈在哪，用 Emacs 自带的性能分析器。

#+begin_src emacs-lisp
;; 启动 CPU profiler
;; M-x profiler-start cpu

;; 执行你的操作

;; 查看报告
;; M-x profiler-report
#+end_src

原文作者的直觉告诉他瓶颈在字符串模板处理上。profiler 显示真正的罪魁祸首是 =find-file-noselect= （13% CPU）和 =org-persist-write-all-buffer= （12% CPU）。直觉和数据的差距巨大。

性能优化的正确顺序：

1. 先写正确的代码
2. 用 profiler 找到真正的瓶颈
3. 只优化 profiler 指出的热点

这个顺序对任何语言都适用，但在 Elisp 里尤其重要——因为 Elisp 的性能特征和直觉经常不一致。你以为慢的地方可能根本不是瓶颈，真正的热点藏在你不注意的地方。

* 哈希表替代线性扫描

当文件数量从几十个增长到几千个时，线性扫描列表的成本从可以忽略变成不可接受。原文的处理方式很直接：任何需要按文件名查找的场景，用哈希表代替列表。

#+begin_src emacs-lisp
;; 慢：线性扫描，O(n)
(member target-file file-list)

;; 快：哈希表查找，O(1)
(gethash target-file file-hash-table)
#+end_src

原文在路由注册表（registry）、文件缓存（filecache）、依赖图（depcache）三个地方都用哈希表，消除了所有线性扫描。对于 10000 个文件的场景，这个差异是&quot;每个文件多查 10000 次&quot;和&quot;每个文件查 1 次&quot;的差距。

这不是过早优化——当你的数据量明确会达到千级别时，选择哈希表是基本的数据结构选择，就像去超市买东西用购物袋而不是一趟趟用手捧。

* 总结

从这些优化中可以提炼出几条通用的 Elisp 工程原则：

1. *用正确的工具做正确的事* —— 临时 buffer 读文件， =find-file-noselect= 留给交互式场景
2. *只处理你需要的数据* —— =org-element-parse-buffer= 的粒度参数是个宝藏
3. *用 profiler 驱动优化* —— 不要猜，让数据说话
4. 用 =cl-progv= 隔离状态 —— 全局变量是 Emacs 的设计遗产， =cl-progv= 是在批量场景下控制它的有效手段
5. *让数据形状驱动逻辑* —— duck typing 比手动标记更不容易出错
6. *数据量大了就用哈希表* —— 这不是过早优化，是基本的数据结构选择
</description>
<pubDate>2026-04-23</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/22/elisp-性能优化的六个实战教训/index.html</guid>
</item>
<item>
<title>fcitx5 下 Emacs 无法切换输入法的排查</title>
<link>https://lujun9972.github.io/blog/2026/04/22/fcitx5-下-emacs-无法切换输入法的排查/index.html</link>
<description>* 背景

我之前从 fcitx4 升级到了 fcitx5，解决了键盘输入卡顿的问题（详见[[file:fcitx启动后键盘输入卡顿的排查.org][fcitx 启动后键盘输入卡顿的排查]]）。升级后，普通应用（浏览器、终端等）都能正常切换输入法，唯独 =Emacs= 不行——按切换快捷键毫无反应，只能输入英文。

* 故障现象

- fcitx5 在其他应用（Firefox、终端等）中正常工作
- 在 Emacs 中无法切换到中文输入法
- Emacs 是通过 =systemd user service= 以 daemon 模式启动的
- 系统为 Arch Linux，Emacs 版本 31.0.50，使用 Lucid（Xaw）工具包

* 排查过程

** 第一步：检查环境变量

输入法框架依赖三个关键环境变量来告诉应用程序&quot;该用哪个输入法&quot;。我先检查了当前 shell 中的值：

#+begin_src shell
  echo &quot;GTK_IM_MODULE=$GTK_IM_MODULE&quot;
  echo &quot;QT_IM_MODULE=$QT_IM_MODULE&quot;
  echo &quot;XMODIFIERS=$XMODIFIERS&quot;
#+end_src

#+begin_example
  GTK_IM_MODULE=fcitx5
  QT_IM_MODULE=fcitx5
  XMODIFIERS=@im=fcitx5
#+end_example

三个变量都有值，看起来没问题。

#+BEGIN_QUOTE
*小知识：这三个环境变量是干什么的？*

Linux 下的应用程序通过三种不同的协议连接输入法：

1. =XMODIFIERS=@im=xxx= ：告诉 *所有 X11 应用* ，通过 XIM 协议连接名为 =xxx= 的输入法服务器。这是最古老的方案，几乎所有 X 应用都支持，但功能最基础。
2. =GTK_IM_MODULE=xxx= ：让 *GTK 应用* 加载专门的输入法模块，比 XIM 体验更好（支持光标跟随、预编辑文字等）。
3. =QT_IM_MODULE=xxx= ：同理，让 *Qt 应用* 加载专门的输入法模块。

简单来说：=XMODIFIERS= 是万能兜底方案，=GTK_IM_MODULE= 和 =QT_IM_MODULE= 是各自框架的增强方案。
#+END_QUOTE

** 第二步：运行 fcitx5-diagnose

fcitx5 自带了一个诊断工具，可以自动检查各种常见配置问题：

#+begin_src shell
  fcitx5-diagnose
#+end_src

诊断结果里出现了多处警告：

#+begin_example
  环境变量 XMODIFIERS 的值被设为了&quot;@im=fcitx5&quot;而不是&quot;@im=fcitx&quot;。
  请检查您是否在某个初始化文件中错误的设置了它的值。

  环境变量 GTK_IM_MODULE 的值被设为了&quot;fcitx5&quot;而不是&quot;fcitx&quot;。
  环境变量 QT_IM_MODULE 的值被设为了&quot;fcitx5&quot;而不是&quot;fcitx&quot;。
#+end_example

诊断工具建议把所有值从 =fcitx5= 改成 =fcitx= 。看起来很合理——虽然软件叫 =fcitx5= ，但输入法模块注册的名字可能不带版本号。

于是我按建议修改了 =~/.xinitrc= ：

#+begin_src shell
  # 修改前
  export GTK_IM_MODULE=fcitx5
  export QT_IM_MODULE=fcitx5
  export XMODIFIERS=&quot;@im=fcitx5&quot;

  # 修改后
  export GTK_IM_MODULE=fcitx
  export QT_IM_MODULE=fcitx
  export XMODIFIERS=&quot;@im=fcitx&quot;
#+end_src

但问题没有解决——Emacs 还是无法切换输入法。

#+BEGIN_QUOTE
*小知识：=.xinitrc= 是什么？*

当你用 =startx= 命令启动图形界面时，=X 服务器= 会读取 =~/.xinitrc= 这个文件，执行里面的命令来初始化桌面环境。通常在里面设置环境变量、启动输入法、启动窗口管理器等。
#+END_QUOTE

** 第三步：发现 Emacs 是 systemd 服务启动的

我意识到一个关键问题：我的 Emacs 不是从 =.xinitrc= 启动的，而是通过 =systemd user service= 以 daemon 模式在后台运行的。

#+BEGIN_QUOTE
*小知识：Emacs daemon 模式*

Emacs 可以以&quot;守护进程&quot;方式运行（=emacs --fg-daemon= 或 =emacs --daemon=），在后台常驻。之后用 =emacsclient= 连接到这个后台进程来打开窗口。好处是：
- 启动速度极快（不用每次都重新加载配置）
- 多个客户端共享同一个 Emacs 进程（状态、缓冲区等）
#+END_QUOTE

这意味着 Emacs 的环境变量 *不来自 =.xinitrc=* ，而是来自 systemd service 文件中的 =Environment= 配置。查看服务文件：

#+begin_src shell
  cat ~/.config/systemd/user/emacs.service
#+end_src

#+begin_example
  [Service]
  Type=simple
  ExecStart=/usr/bin/emacs --fg-daemon
  Environment=... GTK_IM_MODULE=fcitx QT_IM_MODULE=fcitx XMODIFIERS=&quot;@im=fcitx&quot; ...
#+end_example

果然！Emacs 的环境变量是独立的：
- =GTK_IM_MODULE=fcitx=
- =QT_IM_MODULE=fcitx=
- =XMODIFIERS=&quot;@im=fcitx&quot;

看起来都是正确的

** 第四步：发现根因——XIM 服务器名称不匹配

既然 =.xinitrc= 和 =emacs.service= 都设了 =XMODIFIERS= ，那问题出在哪里？我检查了 fcitx5 实际注册到 X 服务器的 XIM 服务名称：

#+begin_src shell
  xprop -root XIM_SERVERS
#+end_src

#+begin_example
  XIM_SERVERS(ATOM) = @server=fcitx5
#+end_example

然后检查 Emacs 进程实际的 =XMODIFIERS= 值：

#+begin_src shell
  cat /proc/$(pgrep -x emacs)/environ | tr &#39;\0&#39; &#39;\n&#39; | grep XMODIFIERS
#+end_src

#+begin_example
  XMODIFIERS=@im=fcitx
#+end_example

*找到根因了！*

- fcitx5 注册的 XIM 服务器名： =@server=fcitx5=
- Emacs 的 =XMODIFIERS= 值： =@im=fcitx=

两者不匹配！XIM 协议要求 =XMODIFIERS= 的值必须和 XIM 服务器的注册名一致。Emacs 拿着 =@im=fcitx= 去找名为 =fcitx= 的 XIM 服务器，但实际注册的名字是 =fcitx5= ，当然找不到。

#+BEGIN_QUOTE
*小知识：XIM 协议的连接机制*

XIM（X Input Method）是 X11 的输入法协议，工作流程是：

1. 输入法框架（如 fcitx5）启动后，在 X 服务器上注册一个 XIM 服务器，名字形如 =@server=fcitx5=
2. 应用程序读取 =XMODIFIERS= 环境变量（如 =@im=fcitx5=）
3. 应用程序拿着这个名字去 X 服务器上查找对应的 XIM 服务器
4. 找到后建立连接，输入法就可以工作了

如果 =XMODIFIERS= 的名字和 XIM 服务器注册的名字对不上，应用程序就找不到输入法服务器，输入法自然无法使用。
#+END_QUOTE

这也说明 =fcitx5-diagnose= 的建议是 *误导* 的：它建议把 =XMODIFIERS= 改成 =@im=fcitx= ，但 fcitx5 的 XIM 服务器偏偏注册为 =fcitx5= 。对于通过 XIM 协议连接输入法的应用（比如使用 Lucid 工具包的 Emacs），这个建议反而是错的。

** 第五步：确认 Emacs 走的是 XIM 而非 GTK 模块

为了确认我的 Emacs 确实走 XIM 协议，我检查了 Emacs 的构建特性：

#+begin_src shell
  emacs --batch --eval &#39;(princ (format &quot;%s\n&quot; system-configuration-features))&#39;
#+end_src

#+begin_example
  ... X11 XDBE XIM XINPUT2 ... LUCID ...
#+end_example

关键信息：
- =XIM= ——Emacs 支持 XIM 协议
- =LUCID= ——Emacs 使用 Lucid（Xaw）工具包，不是 GTK

Lucid 工具包不使用 =GTK_IM_MODULE= ，只走 XIM 协议。所以对 Emacs 来说，唯一重要的环境变量就是 =XMODIFIERS= ，而且它的值必须和 XIM 服务器注册名一致。

* 解决方案

明确了根因后，修复很简单：

** 修改 emacs.service 中的 XMODIFIERS

将 =XMODIFIERS=&quot;@im=fcitx&quot;= 改为 =XMODIFIERS=&quot;@im=fcitx5&quot;= ：

#+begin_example
  # 修改前
  Environment=... XMODIFIERS=&quot;@im=fcitx&quot; ...

  # 修改后
  Environment=... XMODIFIERS=&quot;@im=fcitx5&quot; ...
#+end_example

** 重启 Emacs 服务

#+begin_src shell
  systemctl --user daemon-reload
  systemctl --user restart emacs
#+end_src

修改后确认 Emacs 进程的环境变量已更新：

#+begin_src shell
  cat /proc/$(pgrep -x emacs)/environ | tr &#39;\0&#39; &#39;\n&#39; | grep XMODIFIERS
  # XMODIFIERS=@im=fcitx5
#+end_src

重启后 Emacs 中顺利切换到中文输入法，问题解决。

** 恢复 .xinitrc

由于我的桌面环境也使用 fcitx5 ，且 fcitx5 的 XIM 服务器注册名为 =fcitx5= ，所以 =~/.xinitrc= 中的值保持为 =fcitx5= 是正确的：

#+begin_src shell
  export GTK_IM_MODULE=fcitx5
  export QT_IM_MODULE=fcitx5
  export XMODIFIERS=&quot;@im=fcitx5&quot;
#+end_src

* 复盘

** 根因链条

#+begin_example
  fcitx5 注册 XIM 服务器名为 &quot;@server=fcitx5&quot;
    → emacs.service 中 XMODIFIERS=&quot;@im=fcitx&quot;
      → 名称不匹配
        → Emacs 通过 XIM 找不到输入法服务器
          → 无法切换输入法
#+end_example

** 走过的弯路

这次排查最大的弯路是 *轻信了 =fcitx5-diagnose= 的建议* 。它建议把所有环境变量从 =fcitx5= 改成 =fcitx= ，我照做了，但这反而让 XMODIFIERS 和 XIM 服务器名不匹配的问题更加恶化了。

** 关键经验

1. *=xprop -root XIM_SERVERS= 可以查看 XIM 服务器注册名* ：排查 XIM 问题时，先确认服务器叫什么名字，再和 =XMODIFIERS= 对比
2. *systemd 服务的环境变量是独立的* ：不继承 =.xinitrc= 或 shell 的环境，需要在 service 文件中单独设置。遇到服务启动的程序出问题时，用 =cat /proc/$PID/environ | tr &#39;\0&#39; &#39;\n&#39; | grep XXX= 检查实际的环境变量
3. *诊断工具的建议不一定全对* ：=fcitx5-diagnose= 的建议在大部分场景下是对的，但对于 XIM 这种特殊场景会给出错误建议。理解原理比盲目执行建议更重要
4. *Emacs 的工具包决定了它用什么输入法协议* ：Lucid 走 XIM（看 =XMODIFIERS= ），GTK 走 GTK IM Module（看 =GTK_IM_MODULE= ）。排查前先搞清楚 Emacs 用的什么工具包
</description>
<pubDate>2026-04-23</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/22/fcitx5-下-emacs-无法切换输入法的排查/index.html</guid>
</item>
<item>
<title>fcitx 启动后键盘输入卡顿的排查</title>
<link>https://lujun9972.github.io/blog/2026/04/22/fcitx-启动后键盘输入卡顿的排查/index.html</link>
<description>* 背景

=fcitx= 是 Linux 下常用的输入法框架，用于在英文环境中输入中文、日文、韩文等语言。它的工作方式是&quot;拦截&quot;应用程序的键盘输入，在用户敲拼音时弹出候选词窗口，选中后把中文字符提交给应用程序。

这个&quot;拦截&quot;机制意味着 =fcitx= 会插入到键盘事件的传递链路中。如果这条链路的某个环节出了问题，键盘输入就会受影响——而我遇到的就是这种情况。

* 故障现象

某天，我发现启动 =fcitx= 后，整个键盘输入变得 *非常卡* ——敲一个键要等半秒甚至更久才有反应。但鼠标完全正常，丝毫不受影响。不启动 =fcitx= 就一切如常。

键盘卡而鼠标没事，这个现象很诡异。直觉告诉我：一定是 =fcitx= 做了什么只影响键盘、不影响鼠标的事情。

* 排查过程

** 第一步：收集基础信息

先搞清楚环境。我的系统是 Arch Linux，窗口管理器是 =awesome= ，显示服务器是 =Xorg= ， =fcitx= 版本是 4.2.9.9（fcitx4）。

#+begin_src shell
  uname -r
  # 6.19.12-arch1-1

  fcitx --version
  # fcitx version: 4.2.9.9
#+end_src

然后运行 =fcitx= 自带的诊断工具：

#+begin_src shell
  DISPLAY=:0 fcitx-diagnose
#+end_src

诊断结果暴露了大量问题，下面列出关键部分并逐一说明。

*** Fcitx 状态：DBus 通信断裂

#+begin_example
# Fcitx State:
4.  `fcitx-remote`:

    **Cannot connect to fcitx correctly.**

5.  DBus interface:

    **Cannot find DBus name `org.fcitx.Fcitx` owner.**

    **Cannot find pid of DBus name `org.fcitx.Fcitx` owner.**
#+end_example

=fcitx-remote= 无法连接 =fcitx= ，DBus 总线上也找不到 =org.fcitx.Fcitx= 这个服务名。这意味着 =fcitx= 虽然进程在跑，但它 *没有成功注册到会话总线* 。

#+BEGIN_QUOTE
*小知识：什么是 DBus？*

DBus 是 Linux 桌面环境中的 *进程间通信* （IPC）机制。你可以把它想象成一个&quot;内部公告栏&quot;：各个程序在上面发布自己的服务，其他程序通过公告栏找到并调用这些服务。

对 =fcitx= 来说，DBus 的作用包括：
- 输入法切换通知（从中文切到英文）
- 输入法状态查询（当前是中文还是英文模式？）
- 配置变更同步（修改了快捷键后立即生效）

如果 DBus 通信断了，这些操作要么失败，要么阻塞等待超时。
#+END_QUOTE

更进一步，检查进程列表发现 =fcitx= 启动了一个 *私有的 dbus-daemon= ：

#+begin_example
  lujun9972  27481  /usr/bin/dbus-daemon --syslog --fork ... \
    --config-file /usr/share/fcitx/dbus/daemon.conf
#+end_example

正常情况下， =fcitx= 应该使用桌面会话的 DBus（ =/run/user/1000/bus= ），但它退而求其次启动了自己的私有 DBus 实例。这说明 =fcitx= 启动时会话总线不可用（或者地址不对）。私有总线意味着其他应用无法通过标准 DBus 接口与 =fcitx= 通信，输入法切换、状态同步等功能全部受影响。

*** 前端模块：环境变量缺失

#+begin_example
# Frontends setup:
## Xim:
1.  `${XMODIFIERS}`:

    **XMODIFIERS is not set**

## Qt:
1.  qt4 - `${QT4_IM_MODULE}`:

    **Please set environment variable QT4_IM_MODULE ...**

2.  qt5 - `${QT_IM_MODULE}`:

    **Please set environment variable QT_IM_MODULE ...**

## Gtk:
1.  gtk - `${GTK_IM_MODULE}`:

    **Please set environment variable GTK_IM_MODULE ...**
#+end_example

三个关键环境变量（ =XMODIFIERS= 、 =QT_IM_MODULE= 、 =GTK_IM_MODULE= ）全部未设置。不过这一条有 *误导性* ——我检查了 =~/.xinitrc= ，里面确实有设置这些变量，只是 =fcitx-diagnose= 运行所在的 shell 环境没继承到 X 会话的环境变量。所以这个问题的严重程度需要打个问号。

#+BEGIN_QUOTE
*小知识：为什么需要三个环境变量？*

- =XMODIFIERS=@im=fcitx= ：告诉 *所有 X11 应用* 通过 XIM 协议连接输入法服务器。这是最底层的兼容方案，几乎所有 X 应用都支持。
- =GTK_IM_MODULE=fcitx= ：让 *GTK 应用* 使用专门的 fcitx IM 模块，比 XIM 性能更好、体验更流畅（支持光标跟随、预编辑等）。
- =QT_IM_MODULE=fcitx= ：同理，让 *Qt 应用* 使用专门的 fcitx IM 模块。

三个变量的作用范围不同但互补： =XMODIFIERS= 是兜底方案， =GTK_IM_MODULE= 和 =QT_IM_MODULE= 是针对特定框架的优化方案。
#+END_QUOTE

*** crash.log：BadWindow 错误

#+begin_example
  3.  `~/.config/fcitx/log/crash.log`:

      fcitx: BadWindow (invalid Window parameter)
#+end_example

=crash.log= 中记录了一次 =BadWindow= 错误。这是 X11 协议中的经典错误，表示 =fcitx= 尝试操作一个不存在或已销毁的窗口。这通常发生在窗口关闭时 =fcitx= 还没来得及清理对该窗口的引用。虽然未必直接导致卡顿，但说明 =fcitx= 的窗口管理与 X 服务器的状态存在不同步。

*** 配置工具：GTK2 模块缺失

#+begin_example
# Fcitx Configure UI:
2.  Config GUI for gtk2:

    **Config GUI for gtk2 not found.**

...

## Gtk:
1.  gtk - gtk-query-immodules:

    **Failed to find fcitx in the output of `/usr/bin/gtk-query-immodules-2.0`**
    **Cannot find fcitx im module for gtk 2.**
#+end_example

GTK2 的 fcitx 输入法模块缺失。这在 2026 年影响不大——GTK2 应用已经很少见了，而且如果 =XMODIFIERS= 设置正确，GTK2 应用仍可通过 XIM 协议使用输入法。

*** Locale 警告

#+begin_example
3.  XIM for Emacs:

    **Your LC_CTYPE is set to en_US.UTF-8 instead of one of zh, ja, ko.
    You may not be able to use input method in emacs because of
    an really old emacs bug that upstream refuse to fix for years.**
#+end_example

这是一个 Emacs 的古老 bug ：当 =LC_CTYPE= 不是中日韩 locale 时，Emacs 的 XIM 输入可能不工作。对我来说影响不大，因为我用的是 Emacs 内置的输入法支持，不走 XIM 。

** 第二步：发现键盘设备被禁用

=fcitx-diagnose= 虽然暴露了很多问题，但没有一个能直接解释&quot;为什么键盘卡&quot;。我需要换个角度思考：

- *键盘卡，鼠标没事* → 两种设备的处理路径不同
- 在 X11 中，键盘和鼠标是 *独立的设备* ，各有各的事件传递管道
- 如果 =fcitx= 只干扰了键盘的管道而没有干扰鼠标的，那问题一定出在键盘设备的 *X 层面配置* 上

所以我用 =xinput= （X Input 扩展的命令行工具）来检查 X11 是怎么看待键盘设备的：

#+begin_src shell
  DISPLAY=:0 xinput list-props &quot;AT Translated Set 2 keyboard&quot;
#+end_src

输出中有一行：

#+begin_example
  Device Enabled (176):  0
#+end_example

=Device Enabled= 是 *0* ！物理键盘在 X 层面被 *禁用* 了！

再看设备列表：

#+begin_src shell
  DISPLAY=:0 xinput list
#+end_src

#+begin_example
  ⎡ Virtual core pointer                      id=2  [master pointer  (3)]
  ⎜   ↳ Virtual core XTEST pointer            id=4  [slave  pointer  (2)]
  ⎣ Virtual core keyboard                     id=3  [master keyboard (2)]
      ↳ Virtual core XTEST keyboard           id=5  [slave  keyboard (3)]
  ∼ AT Translated Set 2 keyboard              id=10 [floating slave]
  ∼ ThinkPad Extra Buttons                    id=13 [floating slave]
  ...
#+end_example

物理键盘前面的符号是 =~= （tilde），表示它是 *floating slave* ——一个浮动的、没有挂载到任何 master 设备上的从设备。正常情况下，物理键盘应该是 =↳= （attached slave），挂载在 master keyboard 下面。

#+BEGIN_QUOTE
*小知识：X11 的输入设备层级*

X11 的输入设备分为三层：

1. *Master 设备* ：虚拟的核心设备（master keyboard + master pointer），是应用程序看到的&quot;键盘&quot;和&quot;鼠标&quot;
2. *Slave 设备* ：物理设备，attach 到 master 上，通过 master 将事件传递给应用程序
3. *Floating slave* ：物理设备但 *没有* attach 到任何 master，事件走另一条路径

当物理键盘变成 floating slave 且被禁用时，键盘事件只能通过更慢的回退路径（如 XIM 或 XKB 扩展）传递，这就是输入卡顿的原因。
#+END_QUOTE

** 第三步：临时修复——重新启用键盘

我尝试了两个操作：

#+begin_src shell
  # 将键盘重新挂载到 master keyboard
  DISPLAY=:0 xinput reattach 10 3

  # 重新启用键盘设备
  DISPLAY=:0 xinput enable &quot;AT Translated Set 2 keyboard&quot;
#+end_src

执行后确认：

#+begin_example
  Device Enabled (176):  1
#+end_example

键盘恢复了正常。这证实了： *键盘被禁用和分离就是卡顿的直接原因* 。

** 第四步：定位根因——fcitx-xkb 插件

为什么键盘会被禁用？回到 =fcitx-diagnose= 的输出，我注意到 =fcitx= 加载了大量插件：

#+begin_example
  Found 36 enabled addons:
      fcitx-xkb
      fcitx-xkbdbus
      fcitx-x11
      fcitx-xim
      ...
#+end_example

其中 =fcitx-xkb= 和 =fcitx-xkbdbus= 这两个插件引起了我的注意。 =fcitx-xkb= 的功能是接管 X 的键盘布局管理（XKB），当它启动时会：

1. 接管 XKB 布局控制权
2. 通过自己的管道处理键盘事件

它是怎么把物理键盘搞成 disabled + floating 的？我没有在源码中逐行确认，但推理链条很清楚：

- =fcitx-xkb= 启动前：键盘正常（ =Device Enabled = 1= ，attached slave ）
- =fcitx-xkb= 启动后：键盘被禁用且分离
- 停止 =fcitx= 后手动 =xinput enable= + =reattach= ：键盘恢复正常
- 鼠标设备完全不受影响（ =fcitx-xkb= 只管键盘布局，不管鼠标）

三个事实指向同一个结论： *=fcitx-xkb= 在接管键盘布局时，错误地禁用并分离了物理键盘设备* 。

而鼠标走的是完全独立的 XInput 路径，不受 XKB 插件影响。这完美解释了&quot; *键盘卡而鼠标没事* &quot;的现象。

同时，诊断还发现了 DBus 通信问题：

#+begin_example
  Cannot connect to fcitx correctly.
  Cannot find DBus name org.fcitx.Fcitx owner.
#+end_example

=fcitx-remote= 也无法连接 =fcitx= ，说明 DBus 通信链路是断的。 =fcitx-xkbdbus= 插件需要通过 DBus 传递键盘状态，如果 DBus 通信断了，每次按键都可能在等待一个永远不会到来的 DBus 响应，进一步加剧延迟。

*** 根因链条

#+begin_example
  fcitx4 启动
    → fcitx-xkb 插件接管 X 键盘布局
      → 禁用/分离物理键盘设备
        → 键盘事件走慢速回退路径
          → 再叠加 DBus 通信断裂
            → 每次按键都卡
#+end_example

* 解决方案

我选择了升级到 =fcitx5= ，因为 =fcitx4= 太老了（4.2.9.9），它的 XKB 集成有 bug。 =fcitx5= 已经完全重写了 XKB 管理模块，不会有这个问题。

不过，如果你暂时不想升级，也有两个轻量的替代方案：

1. *禁用 fcitx-xkb 插件* ：在 =~/.config/fcitx/conf/= 中创建 =fcitx-xkb.conf= ，设置 =Enabled=False= ，然后重启 =fcitx= 。代价是失去通过 =fcitx= 管理键盘布局的能力（如果你只用英文布局，无影响）。
2. *在 .xinitrc 中加修复命令* ：在 =fcitx= 启动之后加一行 =xinput enable &quot;AT Translated Set 2 keyboard&quot;= ，强制重新启用键盘。

** 升级到 fcitx5

#+begin_src shell
  # 移除 fcitx4 所有包
  sudo pacman -Rns fcitx fcitx-configtool fcitx-googlepinyin \
    fcitx-mozc fcitx-qt4 fcitx-qt5 fcitx-qt6 fcitx-rime \
    fcitx-sogoupinyin fcitx-sunpinyin
#+end_src

#+begin_src shell
  # 安装 fcitx5 全家桶
  sudo pacman -S fcitx5 fcitx5-configtool fcitx5-gtk fcitx5-qt \
    fcitx5-chinese-addons fcitx5-mozc fcitx5-rime fcitx5-pinyin-zhwiki
#+end_src

** 更新环境变量

=fcitx4= 和 =fcitx5= 的环境变量值不同，需要修改 =~/.xinitrc= ：

#+begin_example
  # 旧的 (fcitx4)
  export GTK_IM_MODULE=fcitx
  export QT_IM_MODULE=fcitx
  export XMODIFIERS=&quot;@im=fcitx&quot;

  # 新的 (fcitx5)
  export GTK_IM_MODULE=fcitx5
  export QT_IM_MODULE=fcitx5
  export XMODIFIERS=&quot;@im=fcitx5&quot;
#+end_example

#+BEGIN_QUOTE
*小知识：为什么环境变量值要改？*

=GTK_IM_MODULE= 和 =QT_IM_MODULE= 告诉 GTK/Qt 应用加载哪个输入法模块。 =fcitx= 和 =fcitx5= 是两套独立的 IM 模块共享库，名字不同。如果环境变量还指向 =fcitx= ，应用会尝试加载一个已经不存在的 =im-fcitx.so= ，结果就是找不到输入法。
#+END_QUOTE

升级后重新登录 X 会话，一切正常，键盘输入丝般顺滑。

* 输入法对照表

升级过程中，输入法引擎也需要对应替换：

| fcitx4 包             | fcitx5 包                | 说明             |
|-----------------------+--------------------------+------------------|
| =fcitx=               | =fcitx5=                 | 核心框架         |
| =fcitx-configtool=    | =fcitx5-configtool=      | 图形配置工具     |
| =fcitx-sunpinyin=     | =fcitx5-chinese-addons=  | 拼音（内置更优） |
| =fcitx-sogoupinyin=   | =fcitx5-chinese-addons=  | 拼音（内置更优） |
| =fcitx-googlepinyin=  | =fcitx5-chinese-addons=  | 拼音（内置更优） |
| =fcitx-mozc=          | =fcitx5-mozc=            | 日文输入         |
| =fcitx-rime=          | =fcitx5-rime=            | Rime 输入        |
| =fcitx-qt5/6=         | =fcitx5-qt=              | Qt IM 模块       |
| （无）                | =fcitx5-gtk=             | GTK IM 模块      |
| （无）                | =fcitx5-pinyin-zhwiki=   | 维基百科词库     |

值得一提的是， =fcitx-sogoupinyin= （搜狗拼音）没有 fcitx5 版本，但 =fcitx5-chinese-addons= 内置的拼音引擎带云拼音功能，体验不逊于搜狗。

* 复盘

** 排查思路

这次排查的关键转折点是用 =xinput list-props= 检查物理键盘的状态。如果只停留在&quot; =fcitx= 导致卡顿&quot;的表面，可能会一直猜测是 =fcitx= 性能问题，而不会发现真正的原因是 =X11= 层面的设备被禁用了。

整个排查过程遵循了一个核心原则： *先找根因，再动手修* 。

#+begin_example
  现象：键盘输入卡顿
    │
    ├─ 1. fcitx-diagnose 收集信息
    │     → 发现 DBus 断裂、环境变量缺失等多个异常
    │
    ├─ 2. xinput list-props 检查键盘设备
    │     → Device Enabled = 0，键盘被禁用！
    │
    ├─ 3. xinput list 看设备拓扑
    │     → 物理键盘是 floating slave，未挂载到 master
    │
    ├─ 4. xinput enable + reattach 临时修复
    │     → 卡顿消失，确认根因
    │
    └─ 5. 定位 fcitx-xkb 插件为罪魁祸首
          → 升级到 fcitx5 彻底解决
#+end_example

** 经验教训

1. *=xinput= 是排查 X11 键盘/鼠标问题的利器* ： =xinput list= 看设备拓扑， =xinput list-props= 看设备属性， =xinput enable/disable= 控制设备开关
2. *输入法问题不一定在输入法层面* ：这次表面是 =fcitx= 的锅，实际上是 =fcitx= 的 XKB 插件把 X11 的设备搞乱了，根因在 X 输入子系统
3. *老软件就该升级* ： =fcitx4= 已经停止维护， =fcitx5= 不仅修复了这类 bug ，架构也更现代。在 Arch Linux 这种滚动更新的发行版上，抱着旧版本不放只会积累技术债
</description>
<pubDate>2026-04-22</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/22/fcitx-启动后键盘输入卡顿的排查/index.html</guid>
</item>
<item>
<title>TIL: 早期网页的图片热区导航</title>
<link>https://lujun9972.github.io/blog/2026/04/22/til-早期网页的图片热区导航/index.html</link>
<description>读到 Heydon Pickering 的一篇文章，才知道 HTML 里有个 =&lt;area&gt;= 元素，专门用来在一张图片上定义可点击区域——这种技术叫 /image map/ 。

#+BEGIN_SRC html
&lt;map name=&quot;mySite&quot;&gt;
  &lt;area
    shape=&quot;poly&quot;
    coords=&quot;48,249,0,96,129,138&quot;
    href=&quot;https://example.com/about&quot;
    alt=&quot;关于本站&quot; /&gt;
  &lt;area
    shape=&quot;poly&quot;
    coords=&quot;68,259,10,106,139,148&quot;
    href=&quot;https://example.com/contact&quot;
    alt=&quot;联系我&quot; /&gt;
&lt;/map&gt;
&lt;img usemap=&quot;#mySite&quot; src=&quot;/path/to/homepage.jpg&quot; alt=&quot;欢迎访问&quot; /&gt;
#+END_SRC

用 =&lt;map&gt;= 把多个 =&lt;area&gt;= 包起来，再在 =&lt;img&gt;= 上用 =usemap= 属性关联，就能让一张图片的不同区域指向不同的链接。 =shape= 支持 =rect= （矩形）、 =circle= （圆形）和 =poly= （多边形）， =coords= 是像素坐标。

这种做法在 2000 年代初很流行。那时候没有 =border-radius= ，没有 web fonts ，CSS 能做的事很少。很多&quot;网页&quot;其实就是设计师在 Photoshop 或 FrontPage 里画好的一张大图，整个塞进 HTML ，然后用 image map 标出导航区域。比起当时更流行的表格布局（在 Dreamweaver 里拖半天单元格，打开浏览器发现全乱了），image map 至少坐标不会跑位。

当然问题也很大：图片大加载慢，文字不可选中不可搜索，屏幕阅读器只能读出&quot;一张图片加几个链接&quot;，而且 =coords= 是像素值，图片一缩放区域就全歪了。后来 CSS 能力跟上了，这种做法就自然消失了。

参考：[[https://heydonworks.com/article/the-area-element/][The area element - HeydonWorks]]
</description>
<pubDate>2026-04-22</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/22/til-早期网页的图片热区导航/index.html</guid>
</item>
<item>
<title>用 Emacs 自动生成每周链接推荐</title>
<link>https://lujun9972.github.io/blog/2026/04/21/用-emacs-自动生成每周链接推荐/index.html</link>
<description>原文：[[https://localghost.dev/blog/automated-weekly-links-posts-with-raindrop-io-and-eleventy/][Automated weekly links posts with raindrop.io and Eleventy]]

Sophie Koonin 在一篇博客中描述了她的自动化链接推荐方案：用 Raindrop.io（一个书签管理工具）收集链接，用 Eleventy（一个静态网站生成器）把链接渲染成博文，再用 GitHub Actions 每周日定时跑脚本，自动 commit 和发布。整条链路不需要手动操作，唯一要做的事就是平时看到好文章时随手收藏一下。

这套方案的核心思路—— *把&quot;每周手动发博文&quot;拆解成&quot;收集 → 生成 → 发布&quot;三步自动化* ——并不依赖特定工具。下面我用 Emacs + Org-mode 重新实现同样的效果，因为我的博客本身就是用 EGO（Emacs Git Org，一个基于 Org-mode 的静态站点生成器）构建的。

** 整体链条

#+begin_src
看到好文章 → org-capture 保存链接到 links.org
                      ↓
          GitHub Actions 每周日定时运行 elisp 脚本
                      ↓
         从 links.org 提取本周链接 → 生成 Org 博文
                      ↓
            自动 commit → 触发博客构建 → 发布
#+end_src

** 第一步：org-capture 收集链接

在博客仓库中创建一个 =links.org= 文件专门存放链接，然后配一个 org-capture 模板：

#+begin_src emacs-lisp
(add-to-list &#39;org-capture-templates
  &#39;(&quot;l&quot; &quot;收集链接&quot; entry (file &quot;~/blog/links.org&quot;)
    &quot;* %? %t\n  %x\n&quot; :empty-lines 1))
#+end_src

使用方法：在浏览器里复制 URL，切到 Emacs， =M-x org-capture= ，选 =l= ，输入链接标题， =C-c C-c= 保存。 =%t= 插入当天日期（ =&lt;2026-04-19 Sat&gt;= 格式）， =%x= 粘贴剪贴板中的 URL。

收集到的内容示例：

#+begin_src org
,* 一篇不错的 Rust 教程 &lt;2026-04-19 Sat&gt;
  https://example.com/rust-tutorial

,* Hacker News 上关于 Emacs 的讨论 &lt;2026-04-20 Sun&gt;
  https://news.ycombinator.com/...
  这篇讨论串里有几个关于 tramp 配置的好建议
#+end_src

** 第二步：elisp 脚本生成博文

接下来需要一个 elisp 函数，读取 =links.org= ，筛选出本周的链接，生成一篇 EGO 格式的博文。

#+begin_src emacs-lisp
(defun my/generate-weekly-links-post (links-file output-dir)
  &quot;从 LINKS-FILE 提取本周链接，生成博文到 OUTPUT-DIR.&quot;
  (interactive &quot;fLinks file: \nDOutput directory: &quot;)
  (let* ((today (format-time-string &quot;%Y-%m-%d&quot;))
         (today-prefix (format &quot;&lt;%s&quot; today))
         (week-ago-time (time-subtract (current-time) (days-to-time 7)))
         (week-ago-prefix (format &quot;&lt;%s&quot;
                                  (format-time-string &quot;%Y-%m-%d&quot; week-ago-time)))
         links)
    (with-temp-buffer
      (insert-file-contents links-file)
      (goto-char (point-min))
      (while (re-search-forward
              &quot;^\\* \\(.+?\\) \\(&lt;[0-9]\\{4\\}-[0-9]\\{2\\}-[0-9]\\{2\\} [^&gt;]+&gt;\\)&quot;
              nil t)
        (let* ((title (match-string 1))
               (ts (match-string 2))
               (body-start (1+ (line-end-position)))
               (body-end (save-excursion
                           (if (re-search-forward &quot;^\\*&quot; nil t)
                               (1- (match-beginning 0))
                             (point-max))))
               (body (string-trim
                      (buffer-substring-no-properties body-start body-end))))
          (when (and (not (string&lt; ts week-ago-prefix))
                     (string&lt; ts today-prefix))
            (push (list title body) links)))))
    (when links
      (let ((post-file (expand-file-name
                        (format &quot;每周链接推荐-%s.org&quot; today) output-dir))
            (links-count (length links)))
        (with-temp-buffer
          (insert (format &quot;#+TITLE: 每周链接推荐 %s\n&quot; today))
          (insert &quot;#+AUTHOR: lujun9972\n&quot;)
          (insert &quot;#+TAGS: Emacs之怒\n&quot;)
          (insert (format &quot;#+DATE: [%s %s]\n&quot;
                          today (let ((system-time-locale &quot;zh_CN.utf-8&quot;))
                                  (format-time-string &quot;%a&quot;))))
          (insert &quot;#+LANGUAGE: zh-CN\n&quot;)
          (insert &quot;#+OPTIONS: H:6 num:nil toc:t \\n:nil ::t |:t ^:nil -:nil f:t *:t &lt;:nil\n\n&quot;)
          (insert &quot;本周收集的有意思的链接。\n\n&quot;)
          (dolist (link (nreverse links))
            (let ((title (nth 0 link))
                  (body (nth 1 link)))
              (if (string-empty-p body)
                  (insert (format &quot;- %s\n&quot; title))
                (insert (format &quot;- %s\n  %s\n&quot; title body)))))
          (write-file post-file)
          (message &quot;生成完毕: %s (%d 条链接)&quot; post-file links-count))))))
#+end_src

函数逻辑：

1. 用正则匹配每个一级标题的时间戳（ =&lt;2026-04-19 Sat&gt;= 格式）
2. 用字符串比较筛选：日期 &gt;= 7 天前 且 &lt; 今天
3. 拼接成 EGO 格式的 Org 博文，写到博客目录

字符串比较能正确工作的原因是时间戳格式为 =&lt;YYYY-MM-DD ...&gt;= ，日期部分在前面，字典序和日期序一致。

** 第三步：GitHub Actions 定时发布

在仓库中添加一个 workflow 文件（如 =.github/workflows/weekly-links.yml= ）：

#+begin_src yaml
name: &quot;生成每周链接推荐&quot;
on:
  schedule:
    - cron: 0 10 * * 0  # 每周日 UTC 10:00（北京时间 18:00）
jobs:
  generate-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: source
      - name: 安装 Emacs
        run: sudo apt-get update &amp;&amp; sudo apt-get install -y emacs-nox
      - name: 生成博文
        run: |
          emacs --batch -l generate_weekly_links.el
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: &quot;每周链接推荐&quot;
#+end_src

其中 =generate_weekly_links.el= 就是上面的函数，末尾加一行调用：

#+begin_src emacs-lisp
;; generate_weekly_links.el — 放在博客仓库根目录
;; ... 函数定义同上 ...
(my/generate-weekly-links-post &quot;links.org&quot; &quot;Emacs之怒/&quot;)
#+end_src

这个 workflow 往 =source= 分支 push 新博文，而现有的博客构建 workflow 已经在监听 =source= 分支的 push 事件，所以新博文会自动构建发布，不需要额外的部署步骤。

** 小结

整个方案的精髓： *把一个需要坚持的习惯，拆解成&quot;收集、生成、发布&quot;三步自动化* 。你唯一需要做的就是看到好文章时随手 =org-capture= 一下。

不管你用的是 Raindrop.io + Eleventy 还是 Emacs + EGO，思路都一样——找到你的&quot;收集入口&quot;，写一个&quot;提取脚本&quot;，再用 CI 的定时任务跑起来。工具不同，模式相同。
</description>
<pubDate>2026-04-21</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/21/用-emacs-自动生成每周链接推荐/index.html</guid>
</item>
<item>
<title>读：ASCII control characters in my terminal</title>
<link>https://lujun9972.github.io/blog/2026/04/21/读：ascii-control-characters-in-my-terminal/index.html</link>
<description>Julia Evans 写了一篇文章梳理终端中所有 33 个 ASCII 控制字符的作用。这些控制字符是你每天按 Ctrl-C 终止程序、按 Ctrl-Z 挂起进程、按 Ctrl-W 删一个词时实际发送的那些字节。本文是对她文章的解读。

** 只有 33 个控制码

ASCII 表的前 32 个位置（0-31）加上第 127 个位置，一共 33 个，留给&quot;控制字符&quot;——它们不对应可打印字符，而是表示某种控制动作。其中 26 个对应 =Ctrl-A= 到 =Ctrl-Z= ，另外 7 个对应 Ctrl 加上 =@= 、 =[= 、 =\= 、 =]= 、 =^= 、 =_= 、 =?= 。

这意味着 =Ctrl-1= 、 =Ctrl-2= 这种组合在终端里根本不存在——按下 =Ctrl-1= 跟直接按 =1= 效果一样，因为 ASCII 没有为数字键预留控制码的位置。同样， =Ctrl+Shift+C= 也不是控制码，它由终端模拟器自己处理（用来复制），根本不会发送到终端里运行的程序。

** 三层处理：谁来响应你的按键

这 33 个控制码并不是统一由某一层处理的，而是分成了三股道：

1. *OS 终端驱动*直接处理的：比如 =Ctrl-C= （发送 SIGINT 信号终止程序）、 =Ctrl-Z= （发送 SIGTSTP 挂起程序）、 =Ctrl-D= （发送 EOF）。这些按键按下后，OS 的终端驱动会直接拦截并产生相应动作，程序本身收不到这个字节。

2. *readline 库*处理的：比如 =Ctrl-W= （删一个词）、 =Ctrl-U= （删整行）、 =Ctrl-R= （搜索历史命令）。这些在 bash、python REPL 等使用 readline 的程序中工作，但在 =cat= 这种不用 readline 的程序中就不起作用。

3. *应用程序自己定义*的：比如 =Ctrl-X= 在一般终端程序里没有固定含义，但 Emacs 把它用作了大量快捷键的前缀。

用 =stty -a= 可以看到 OS 终端驱动处理的所有控制码映射：

#+begin_src shell
stty -a
#+end_src

在我的机器上输出如下（只列出控制字符映射部分）：

#+begin_src text
intr = ^C; quit = ^\; erase = ^?; kill = ^U; eof = ^D; eol = &lt;undef&gt;;
eol2 = &lt;undef&gt;; swtch = &lt;undef&gt;; start = ^Q; stop = ^S; susp = ^Z;
rprnt = ^R; werase = ^W; lnext = ^V; discard = ^O; min = 1; time = 0;
#+end_src

这里 ~intr = ^C~ 表示 =Ctrl-C= 触发中断信号， ~erase = ^?~ 表示退格键（byte 127）触发删除字符， ~kill = ^U~ 表示清行， ~werase = ^W~ 表示删一个词。所有这些映射都可以通过 =stty= 命令修改——比如 =stty intr ^X= 可以把中断信号改到 =Ctrl-X= 上。不过实际上几乎没有人改这些映射，改了只会让自己在各种教程和问答里找不到北。

** Ctrl-M 就是 Enter，Ctrl-I 就是 Tab

这是一个容易让人困惑的设计： =Ctrl-M= 发送的字节值是 13，跟按 =Enter= 键完全一样； =Ctrl-I= 发送的字节值是 9，跟按 =Tab= 键完全一样。所以如果你想给终端程序设置 =Ctrl-M= 或 =Ctrl-I= 作为快捷键，会发现它们的效果跟 =Enter= 和 =Tab= 一样——因为从程序的角度看，收到的是同一个字节。

这也解释了为什么很多终端程序（包括 bash 的 readline）的快捷键覆盖了 =Ctrl-A= 到 =Ctrl-Z= 的大部分，唯独跳过了 =Ctrl-I= 和 =Ctrl-M= ——不是故意这样设计的，而是这两个位置已经被 =Tab= 和 =Enter= 占了。

** canonical 模式 vs noncanonical 模式

=Ctrl-W= 和 =Ctrl-U= 的行为取决于终端当前处于哪种模式：

- *canonical 模式*（规范模式）：OS 终端驱动负责行编辑。你按 =Backspace= 删字符、按 =Ctrl-W= 删词、按 =Ctrl-U= 清行，这些都是 OS 在缓冲区里处理的，程序只有在你按 =Enter= 之后才能看到整行输入。 =cat= 、 =grep= 等非交互程序通常用这种模式。

- *noncanonical 模式*（原始模式）：OS 不做任何行编辑，每按一个键程序就立刻收到。 =Ctrl-W= 和 =Ctrl-U= 的删词、清行功能需要程序自己实现。bash、python REPL、vim 等交互式程序用这种模式。

可以用 =strace= 观察一个程序设置了哪些终端模式：

#+begin_src shell
strace -tt -o /tmp/strace-out vim
# 退出 vim 后：
grep ioctl /tmp/strace-out | grep SET
#+end_src

你会看到 vim 启动时关闭了 =ISIG= （不再由 OS 处理信号）和 =ICANON= （关闭 canonical 模式），改为 raw 模式自己处理所有输入。vim 退出时又把这些设置恢复回去。

** Backspace 的混乱历史

按 =Backspace= 键时，终端到底发送哪个字节？这个问题居然没有统一答案：

- 有些机器发 byte =127= （ASCII 名 =DEL= ）
- 有些机器发 byte =8= （ASCII 名 =BS= ，Backspace）

在 Linux 上， =Backspace= 键发送的是 byte =127= ，OS 终端驱动和 readline 都把它映射为&quot;删除前一个字符&quot;。 =Ctrl-H= 发送 byte =8= ，在 readline 里它的效果跟 =Backspace= 一样（都是删除一个字符），但在 =cat= 这种只用 canonical 模式的程序里， =Ctrl-H= 只会打印出 =^H= 而不会删字符——因为 canonical 模式只认识 =stty= 里 =erase= 设置的那个字节（127）。

如果你的 =Backspace= 键行为异常（按了不删字符反而显示 =^H= ），原因就是你的终端发的是 byte =8= 而 =stty= 期望的是 byte =127= 。修复方法是 =stty erase ^H= ，把删除字符的映射从 127 改到 8。

** Ctrl-S 冻屏之谜

=Ctrl-S= 发送 byte =19= （ASCII 名 =XOFF= ），OS 终端驱动收到后会暂停向终端输出——这就是古老的&quot;软件流控制&quot;。在 90 年代的低速串口终端上这很有用（输出太快来不及看就按 =Ctrl-S= 暂停， =Ctrl-Q= 恢复），但今天几乎没人需要这个功能了。

问题在于 =Ctrl-S= 这个按键被 XOFF 占了，导致 readline 无法用它做&quot;前向搜索历史命令&quot;（ =Ctrl-R= 是反向搜索， =Ctrl-S= 本应是正向搜索）。解决方法是用 =stty -ixon= 关闭软件流控制，这样 =Ctrl-S= 就不再触发 XOFF，而是被传给 readline 使用。

** ASCII 名字可以忽略

每个控制码在 ASCII 标准里都有一个正式名字，比如 byte =3= 叫 =ETX= （End of Text）、byte =26= 叫 =SUB= （Substitute）。但这些名字是为 1960 年代的电报机设计的——那个时代的 =ETX= 表示&quot;报文结束&quot;，跟今天终端里 =byte 3= 的作用（发送 SIGINT 终止程序）毫无关系。33 个控制码里，大约一半的 ASCII 名字跟它今天在终端中的功能对不上，所以不如直接忽略这些名字。

原文链接： [[https://jvns.ca/blog/2024/10/31/ascii-control-characters/][ASCII control characters in my terminal]]
</description>
<pubDate>2026-04-21</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/21/读：ascii-control-characters-in-my-terminal/index.html</guid>
</item>
<item>
<title>读 What to learn</title>
<link>https://lujun9972.github.io/blog/2026/04/21/读-what-to-learn/index.html</link>
<description>原文：[[https://danluu.com/learn-what/][What to learn]]

这篇文章回答一个问题：你应该学什么？Dan Luu 的答案是，不要跟风学别人推荐的东西，而是找到少数几个适合自己天赋的技能，学深学透，然后去找一群高手扎堆的环境。他用自己的踩坑经历证明了跟风学技术是低 ROI 的。

* 别学别人让你学的东西

Steve Yegge 有一系列博客文章建议学编译器。理由是：懂了编译器，你就会到处看到编译器问题，然后发现很多人在用半吊子方案解决本质上是编译器的问题，而你可以用编译器知识又快又好地解决。

这话不能说错，但问题在于——你可以把&quot;编译器&quot;换成排队论、计算机体系结构、数学优化、运筹学……哪个领域都能讲出同样的道理。所以&quot;学编译器&quot;不是编译器有多特殊，而是 Steve Yegge 恰好懂编译器。大部分职业建议的本质就是&quot;学我学过的东西&quot;。

Dan Luu 早年照着互联网建议学了 Haskell、Lisp、Forth。他甚至参与造了一颗 2GHz 的 Forth 处理器——可能至今仍是世界上性能最高的 Forth 处理器。他花了比绝大多数人都多的精力精通 Forth。结果呢？Forth 的倡导者 Chuck Moore 声称 Forth 比普通语言生产力高 100 倍，Dan Luu 的体验是：连 1 倍都不到。

回头看，Dan Luu 认为跟风学技术的 ROI 非常低。真正高 ROI 的学习，没有一个是网上有人推荐的。

* 高手只靠几招吃遍天

数学家 Gian Carlo Rota 讲过一个故事：一位老数学家瞧不起 Paul Erdős 的工作，说 Erdős 的所有证明都能归结为几个反复使用的技巧。Rota 后来读了 Hilbert（比 Erdős 地位更高的数学家）的不变量理论论文，发现 Hilbert 的证明也靠同样的几个技巧反复用。 *即使 Hilbert 也只有几个技巧。*

柔道也一样。对世界级柔道选手的分析发现，大多数人只靠少数几个投技赢比赛。柔道的本质是专精——你只用最适合你的那几招，练到变成自动反应。动画和电视剧里角色通过学更多招数来变强，但现实中，把已有的招数练到极致往往比囤积几百个&quot;技能&quot;更有效。

Joy Ebertz 给过一个建议： *放大你的优势，而不是修补你的弱点。* 每个人都有强项和弱项，我们花很多时间谈论&quot;改进空间&quot;，但如果某个领域你真的弱，花大力气可能只挪动一点点。更好的策略是把本来就很强的事情变成你的超级能力——前提是你没有真正糟糕的短板。

Dan Luu 在竞技游戏里验证过这一点。把他从&quot;不错&quot;提升到&quot;很强&quot;的关键，不是补练他不擅长的东西，而是放弃弱项，集中强化他本来就比别人强的那几个方面，把优势拉大到别人追不上的程度。他觉得这个策略在职场比在游戏和体育里更有效——因为在竞技场景里对手会专门攻击你的弱点，但在工作中没有对手会阻止你选择发挥优势的项目。

* 怎么找到自己的&quot;几招&quot;

Dan Luu 自己真正在用、在持续精进的技能有两个：&quot;看数据&quot;和&quot;对抗性思维&quot;。这两个都不是传统意义上的学科——&quot;看数据&quot;不是统计学（他很少用到逻辑回归那么复杂的东西），&quot;对抗性思维&quot;也不对应某个计算机科学分支。它们是跨领域的思维方式。

怎么发现这种东西？Dan Luu 给了两个方法：

1. *问很了解你的人。* 他的经理和 Ben Kuhn 独立指出了同一个他没意识到的技能——跨多个抽象层次提出解决方案。你自己未必能看清自己最擅长什么。

2. *找那些你忍不住要做、但大多数人不做的事。* Dan Luu 测试一个新 bug 追踪系统时，主动往输入框里塞各种异常数据看它崩不崩。有人觉得莫名其妙，有人（包括 Dan Luu 本人）很高兴看到系统被推到极限。对系统做压力测试对他来说不是工作——他要克制自己才会不去这么做。这种&quot;不做反而难受&quot;的事情，就是你应该深化的方向。

* 找对环境比找对方向更重要

找到适合自己的技能只是第一步，还有一个关键因素： *你在什么样的环境里学。*

Dan Luu 第一份工作在 Centaur（一家芯片公司），那里的验证团队用比同行少得多的人做到了同样甚至更好的效果。这是一个高密度的学习环境。他觉得在那里学到的对抗性测试思维，靠自己看书上网学不到——真正厉害的人脑子里有太多无法压缩进书本的隐性知识。

对于&quot;看数据&quot;这个技能，他在过去几年（在一个能和高水平同事反复讨论数据局限性的环境里）每年的进步幅度， *超过了之前十年的总和* 。

值得注意的是，环境是局部的，不是公司级别的。Dan Luu 说他现在的雇主可能是他待过的三家大公司里最不数据驱动的，但他周围的同事恰好非常擅长理解数据的局限性，而他每天花大量时间和这些人一起工作——这就够了。

Dan Luu 发现一个规律：高手密度最高的团队，往往在非常优秀的管理者手下。他见过的两个高手最密集的团队都是如此。原因也说得通——好的管理者留人能力强，外面排着队想加入，自然容易吸引和留住顶尖人才。

* 随机游走也没什么不好

很多职业建议会告诉你：要有长期目标和规划，因为定向行走能走 n 步远，随机游走只能走 sqrt(n) 步远。

Dan Luu 认为这个建议低估了一件事： *找到适合自己的方向的难度。* 他认识的人里，不少人花了很多年（有些人超过十年）才发现自己走的方向根本不适合自己——要么性格不匹配，要么能力不匹配。相比之下，瞎逛然后发现自己 sqrt(n) 步处在一个开心的位置上，并不比闷头走了 n 步却发现不开心更差。

Dan Luu 自己的技能就是通过试错找到的，而他的学习动机也不是为了职业发展——纯粹是因为学东西好玩。这么做二十年之后碰巧变得有用了，但在相当长一段时间里他并不知道会这样，也不知道硬件领域学到的技能能迁移到软件行业。

* 总结：你应该怎么做

1. *别跟风。* 别人推荐你学的东西，对他可能有用，对你大概率没用。Dan Luu 跟风学 Forth 的 ROI 接近于零。

2. *找少数几个适合你的技能，学深。* 你不需要什么都会。Erdős、Hilbert、世界级柔道选手都只靠几招。关键是这几招得适合你的天赋——不是别人擅长的，是你自己擅长的。

3. *放大优势，而不是修补弱点。* 把本来就很强的事变成超级能力，比费力补短板收效更大。工作中没有对手攻击你的弱点，你可以主动选择发挥优势的项目。

4. *怎么发现自己的技能：* 问很了解你的人；留意那些你忍不住要做但大多数人不会做的事。

5. *找到高手扎堆的环境。* 你的学习速度取决于身边的同事，不是公司名气。在高手身边一年能学到比自己摸索十年更多的东西。

6. *别怕随机游走。* 没想清楚方向就先试，试错了再换。瞎逛走到一个开心的位置，比闷头走一条不开心的路强。
</description>
<pubDate>2026-04-21</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/21/读-what-to-learn/index.html</guid>
</item>
<item>
<title>Lisp 的括号之痛——一个愚人节玩笑揭开的老伤疤</title>
<link>https://lujun9972.github.io/blog/2026/04/21/lisp-的括号之痛——一个愚人节玩笑揭开的老伤疤/index.html</link>
<description>2026 年 4 月 1 日，Clojure 咨询公司 Flexiana [[https://flexiana.com/news/clojure/new-era-for-clojure-infix-syntax-3][发布了一篇文章]]，宣布开源一个叫 Infix 的库，让 Clojure 程序员可以用中缀运算符写数学表达式。

#+begin_src clojure
;; 以前这么写
(+ (* a b) (/ c d))

;; 有了 Infix 之后这么写
(infix a * b + c / d)
#+end_src

代码是真的，测试是通过的，库已经上传到 GitHub。但他们特意选在愚人节发布，文章里到处是自嘲：&quot;The only day of the year when the Clojure community might forgive us&quot;（一年中 Clojure 社区唯一可能原谅我们的日子）。

这个玩笑之所以好笑，是因为它戳中了一个老伤疤：Lisp 的前缀表示法，到底算不算一个问题？

* 前缀 vs 中缀：为什么新手总是被劝退

绝大多数编程语言用的是中缀表示法——运算符写在两个操作数中间：

#+begin_src text
a * b + c / d
#+end_src

Lisp 家族用的是前缀表示法——函数（运算符）写在最前面，操作数跟在后面，整个表达式用括号包起来：

#+begin_src clojure
(+ (* a b) (/ c d))
#+end_src

前缀表示法有一个显而易见的好处：*一致性*。不管是加法、函数调用还是控制流，语法结构都一样—— =(operator arg1 arg2 ...)= 。没有运算符优先级的记忆负担，没有&quot;乘法比加法先算&quot;的歧义。

但一致性不等于可读性。Flexiana 的文章里举了个真实的例子——一个定价规则：

#+begin_src clojure
(&lt;= (count (:items order)) (* 2 (get-in config [:limits :base])))
#+end_src

换成人话就是：&quot;订单里的商品数量，是不是不超过配置里设的基础限制的两倍？&quot; 同样的逻辑用中缀写：

#+begin_src text
count(items) &lt;= 2 * config.limits.base
#+end_src

哪个更容易一眼看懂，不言自明。

* Infix 库的技术实现：编译期的语法糖

Flexiana 并不是第一个试图给 Lisp 加中缀语法的。历史上类似的尝试不少，但 Infix 的实现方式值得说说。

它的核心是一个 *宏* （macro）。Lisp 宏和 C 语言的预处理器宏完全不是一回事——Lisp 宏操作的是代码本身（因为 Lisp 代码就是数据结构），在编译期把一种形式的表达式转换成另一种。

Infix 用的是经典的 *Shunting Yard 算法*（调度场算法），由计算机科学家 Edsger Dijkstra 在 1961 年发明。这个算法能把中缀表达式（人类习惯的写法）转换成前缀或后缀表达式（机器容易处理的形式）。整个库只有大约 300 行代码，分四个命名空间：解析器、优先级表、编译器和公开 API。

关键点在于：*转换发生在编译期，运行时零开销*。最终跑在 JVM 上的代码和你手写的前缀表达式完全一样。Infix 只改变了你 *写* 代码的方式，不改变代码 *跑* 起来的方式。

除了基本的算术，Infix 还做了几件更激进的事：

- *箭头 lambda* ：=(infix x =&gt; x * x + 1)= 替代 Clojure 的匿名函数语法 =#(+ (* % %) 1)=
- *线程宏中缀化* ：把 Clojure 的 =-&gt;&gt;= （thread-last）变成中缀操作符
- *函数定义中缀化* ：=infix-defn= 让函数体里可以用中缀表达式
- *提前返回* （guard clause）：在 =infix-defn= 里支持 =(return nil)= 形式的提前返回

每一项都在试探 Clojure 社区的容忍底线。

* 为什么 Lisp 社区对&quot;中缀&quot;这么敏感

Flexiana 自己也承认这是&quot;异端&quot;（heresy），并且在文章里列出了反对中缀语法的经典论点：

1. *S-expressions 是同像的* （homoiconic）——代码和数据长一模一样，你写的 =(+
1 2)= 就是一个列表，和用代码构建的列表没有区别。这是 Lisp 宏系统的根基，也是 Lisp 最强大的特性
2. *前缀表示法消除了歧义* ——不需要记住运算符优先级，不会因为优先级记错而产生 bug
3. *运算符优先级本身就是 bug 来源* ——几乎所有 C 系语言的使用者都踩过优先级的坑

这些论点都对。但它们回答的是&quot;前缀表示法 *理论上* 更好&quot;，而不是&quot;前缀表示法 *实践中* 更好用&quot;。

Lisp 社区有一个长期存在的张力：语言设计者认为前缀表示法优越且一致，而大量普通程序员觉得前缀表示法难以阅读。Paul Graham 在《Beating the Averages》里把 Lisp 称为 Viaweb 的&quot;秘密武器&quot;——竞争对手看不懂他们的代码，也追不上他们的迭代速度。Lisp 的括号天然抬高了入门门槛，这到底是筛选还是障碍，取决于你站在哪一边。

Flexiana 的文章里有句话很直白：&quot;We&#39;ve all been in that meeting where a data scientist looks at =(+ (* a b) (/ c d))= and quietly opens a Python tab.&quot;（我们都经历过那种会议：数据科学家看到前缀表达式，默默打开了 Python 标签页。）

这不仅是笑话。这是 Clojure 在数据科学领域几乎无法和 Python 竞争的原因之一。

* 语法糖的价值：一个没有结论的争论

Infix 库引发的争论，本质上是编程语言设计中一个永恒的问题：*语法糖到底有没有价值？*

&quot;语法糖&quot;（syntactic sugar）指的是那些不增加语言表达能力、但让代码更好写的语法特性。反对者认为语法糖只是障眼法——它掩盖了语言的真相，让初学者以为语言是某种样子，实际上底层完全是另一回事。Python 的列表推导式、C# 的 LINQ、Kotlin 的作用域函数，都是语法糖的经典案例。

支持者则认为，代码是写给人看的，不是写给机器看的。如果一种语法让代码更容易被更多人理解，那它就是有价值的——即使底层实现不变。

Infix 库恰好站在这个争论的正中央：它 *不改变* Clojure 的任何语义，只改变表达式的书写形式。如果你认为语法糖有价值，那 Infix 就是合理的工具；如果你认为语法糖是技术债，那 Infix 就是用宏实现的自我欺骗。

Flexiana 选在愚人节发布，也许正是因为他们自己也没有答案——他们只是在用玩笑的方式，把这个问题重新摆在所有人面前。

* 后记

我查了一下，[[https://github.com/Flexiana/infix][Infix 库]]确实存在于 GitHub 上，Apache-2.0 许可，代码简洁优雅。至于它到底是不是愚人节玩笑——Flexiana 在文章最后写道：&quot;The library is real. The tests pass. The macros expand.&quot;（库是真的。测试通过了。宏正确展开了。）

至于你该不该用——这个问题留给你的良心。
</description>
<pubDate>2026-04-21</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/21/lisp-的括号之痛——一个愚人节玩笑揭开的老伤疤/index.html</guid>
</item>
<item>
<title>一本书该&quot;线性读&quot;还是&quot;并行读&quot;</title>
<link>https://lujun9972.github.io/blog/2026/04/20/一本书该&quot;线性读&quot;还是&quot;并行读&quot;/index.html</link>
<description>* 默认的读书方式：从头到尾

大多数人读书的习惯是翻开第一章，读完再读第二章，依次推进到末尾。对于小说来说这是理所当然的——情节有因果链，跳着读就看不懂了。

但非虚构书不一样。很多非虚构书的章节之间并没有前置知识依赖。比如《21 世纪的 21 堂课》，第二章讲&quot;工作&quot;、第十七章讲&quot;后真相&quot;，两个话题完全独立。你完全可以在读第二章的同时也读第十七章，不需要先把中间的十几章读完。

这就是&quot;并行读&quot;的思路：同时推进多个章节，而不是等一章读完再开始下一章。

关键问题是——怎么判断哪些书能并行读？

* &quot;自包含&quot;测试

判断标准只有一个：看章节是否&quot;自包含&quot;。

一个&quot;自包含&quot;的章节指的是，它不需要前面章节的知识就能读懂。换句话说，你直接翻到这一章，不会因为缺少前置知识而看不懂。

用这个标准审视目录，很快就能判断一本书适合哪种读法。

*适合并行读的书：*
- 章节各自独立的非虚构书（如散文集、案例分析集、&quot;XX 个 XXX&quot;类书籍）
- 每章聚焦不同主题的科普书（如《思考，快与慢》的很多章节）
- 工具书、参考书

*必须线性读的书：*
- 小说、传记（有情节或时间线）
- 知识层层递进的书（如数学教材、《人类简史》类大历史叙事）
- 论证逐步展开的说理书（前面章节是后面章节的前提）

*教科书比较特殊——双层策略：*

以一本心理学入门教材为例，&quot;记忆与判断&quot;、&quot;智力与语言&quot;、&quot;情绪与动机&quot;这些章节各自聚焦不同主题，章节之间基本独立，可以并行读。但每一章内部的小节（3.1、3.2、3.3……）是有前后依赖的——3.2 假设你已经读了 3.1。

所以教科书适合&quot;章节级并行、小节级线性&quot;的混合策略。

* 为什么并行读有好处

并行读不只是&quot;可以这样做&quot;，而是有几个实在的好处：

1. *不会卡死在一个难点上。* 读线性书时，遇到一个难懂的章节就容易卡住，后面的内容也跟着读不下去。并行读意味着你可以先跳到另一个章节，等回头再攻克难点，不会因为一处受阻就整本书停滞。

2. *利用碎片时间。* 并行读意味着你可以根据手头时间的长短选择读哪个章节——时间充裕就啃难的章节，只有十分钟就翻翻轻松的章节。整本书的推进速度反而比线性读更快。

3. *保持阅读新鲜感。* 同时读不同主题的章节，大脑在不同话题之间切换，不容易疲劳。这比连续啃同一个话题几个小时效果更好。

* 用 Anki 实践并行阅读

SuperMemo 有一个叫&quot;渐进阅读&quot;（Incremental Reading）的功能，专门管理这种并行阅读的进度调度。但 SuperMemo 比较小众，国内用的人不多。相比之下，Anki 是更主流的间隔重复工具，也可以用来管理并行阅读。

核心思路是：用 Anki 的间隔重复来管理&quot;什么时候该回到某个章节继续读&quot;。

*基本做法：*

1. 在 Anki 中为这本书建一个 deck。
2. 为每个要并行读的章节创建一张卡片，卡片正面写&quot;继续读《书名》第 X 章：章节标题&quot;。
3. 读了一部分后，在卡片的&quot;答案&quot;区域记录当前进度（读到第几页/哪一小节）和已理解的关键要点。
4. Anki 会根据间隔重复算法自动安排下次阅读这张卡片（也就是回到这个章节）的时间。

这比用书签或待办清单好在哪？间隔重复保证了每个章节都不会被遗忘太久。线性读的问题是你读完第一章可能过了两周才读第二章，第一章的内容已经忘了大半。并行读配合 Anki 调度，每个章节都会在合适的间隔被复习到，记忆保持得更牢。

*四个按钮怎么用：*

Anki 每次弹出阅读卡片时，你会看到四个按钮——Again、Hard、Good、Easy。它们控制的是&quot;这个章节多久后再出现&quot;：

| 按钮 | 效果 | 阅读场景下什么时候用 |
|------|------|---------------------|
| Again (重来) | 间隔归零，卡片很快重新出现（同天内几分钟，或第二天） | 翻了一下但完全看不进去，想过一会儿再试 |
| Hard (困难) | 间隔缩短，比正常更早回来 | 稍微读了几页但觉得吃力，想缩短间隔、更早回来继续 |
| Good (良好) | 按默认间隔安排下次阅读 | 正常推进了一段，节奏合适 |
| Easy (简单) | 间隔拉长 | 读得很顺畅，进展比预期快 |

如果 Anki 调度到某个章节但你今天就是不想读，有两个选择：Bury（搁置）——右键菜单里有 Bury 选项，效果是今天不再出现，但明天会自动回来；或者直接点 Good，让 Anki 按正常间隔（比如 3-5 天后）再提醒你。不用有心理负担，并行读本身就是弹性的。

*不同章节难度不同怎么办：*

Anki 默认的 SM-2 算法给所有卡片相同的初始间隔。但章节难度不同，你可能希望难的章节间隔短一点、简单的章节间隔长一点。有几种做法：

1. *依赖 Anki 自适应。* 你只需要诚实地点按钮——难的章节你自然会频繁点 Hard，Anki 会自动缩短它的间隔；简单的章节你频繁点 Easy，间隔会自动拉长。几轮下来间隔就会自然适配章节难度，不需要手动设置。
2. *开启 FSRS 算法。* Anki 从 23.10 版本开始内置了 FSRS（Free Spaced Repetition Scheduler）算法，它比默认的 SM-2 更智能，能更准确地根据你的实际复习表现调整每张卡片的间隔。在 Deck Options → FSRS 中开启即可。
3. *分组管理。* 如果你读的书章节难度差异极大（比如有的章节是科普、有的章节是数学推导），可以为&quot;难&quot;和&quot;易&quot;分别建 deck，在 Deck Options 中设置不同的初始间隔和最大间隔。

*Anki 的渐进阅读插件：*

如果你想更进一步，把 SuperMemo 那种&quot;在 Anki 内直接导入文章、高亮、提取片段&quot;的完整渐进阅读体验搬到 Anki 里，可以试试 Incremental Reading v4 插件。它支持把网页、PDF、电子书片段直接导入 Anki，在 Anki 内部逐步阅读、高亮、提取 cloze 卡片。不过这个插件近年 Anki 大版本更新后兼容性不太稳定，安装前先确认是否支持你正在用的 Anki 版本。

* 一个具体例子

假设你在读《23 件关于资本主义他们没告诉你的事》。这本书的 23 章各自独立，非常适合并行读。

第一步，在 Anki 中建一个 deck，比如叫&quot;读书-23 Things&quot;。

第二步，浏览目录后，为第 1 章、第 5 章、第 12 章（假设这三章你最感兴趣）各创建一张 Basic 卡片：

- 正面：读《23 Things》Ch.1 - There is no such thing as a free market
- 背面：（空白，等你读完一部分后填写进度和要点）

第三步，每天打开 Anki 时，它会提醒你今天该回到哪些章节继续读。你可以根据心情和时间选择先读哪一章。

第四步，每读完一个章节的全部内容，就把这张卡片标记为&quot;已完成&quot;（可以 suspend），同时在 Anki 中添加该章核心观点的记忆卡片，用间隔重复来长期保持记忆。

* 实践步骤

把上面的内容整理成操作流程：

1. *先看目录。* 拿到一本书，快速浏览目录，对全书结构有个大致判断。
2. *做&quot;自包含&quot;测试。* 逐章判断：直接翻到这章开头，看需要多少前置知识？大部分章节都不需要前置知识的，就适合并行读。
3. *为每个独立章节创建 Anki 卡片。* 卡片正面写章节标题，背面用来记录进度和要点。
4. *按 Anki 的调度来读。* 每次打开 Anki 时，它会告诉你今天该回到哪些章节继续读。不想读就点 Good 推迟，没什么大不了。
5. *在每章内线性推进。* 并行读是章节级的，每一章内部还是要按顺序读。
</description>
<pubDate>2026-04-20</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/20/一本书该&quot;线性读&quot;还是&quot;并行读&quot;/index.html</guid>
</item>
<item>
<title>读 How to Monetize a Blog：一篇伪装成变现指南的讽刺文</title>
<link>https://lujun9972.github.io/blog/2026/04/20/读-how-to-monetize-a-blog：一篇伪装成变现指南的讽刺文/index.html</link>
<description>* 这篇&quot;教程&quot;在干什么

[[https://modem.io/blog/blog-monetization/][How to Monetize a Blog]] 是 modem.io 上的一篇文章。乍一看，这是一篇博客变现指南——讲 CPM、CPA、广告网络、受众画像，跟你搜到的任何一篇&quot;如何靠博客赚钱&quot;没什么区别。

但读完全文才发现，这是一篇精心设计的讽刺作品——它就是它所讽刺的那个东西。

* 四层递进的讽刺手法

** 第一层：正经教程的伪装

文章开头非常像回事。它解释了 CPM（千次展示付费）和 CPA（按行动付费）的区别，介绍了广告网络的运作方式，甚至讨论了如何根据受众画像投放定向广告。这些内容本身是准确的，读起来就像一篇标准的 SEO 变现教程。

这个伪装做得越逼真，后面的反转就越有力。

** 第二层：荒诞的假广告

从文章开头开始，就穿插着各种广告位。但仔细一看，这些广告卖的是：

- &quot;虾探测器&quot;（ Shrimp detectors ）
- &quot;定制塔罗牌&quot;
- &quot;蛇油&quot;（ Literal Snake Oil——&quot;从真正的蛇身上现榨的油&quot; ）
- &quot;药用盲盒&quot;（ Medicinal Loot Boxes——&quot;禁止医生入内&quot; ）
- &quot;和三明治交朋友&quot;（ Befriend A Sandwich——&quot;你的法式蘸酱三明治知道宝藏在哪吗？&quot; ）

每一则假广告的荒诞程度都在递增。而且这些广告和文章内容毫无关系——这本身就是一种讽刺：广告系统号称能精准匹配内容和广告，实际投放出来的东西根本和内容无关。

** 第三层：作者自我拆穿

写到大半的时候，作者突然摊牌了。他说：

#+begin_quote
this entire post is admittedly just meaningless fluff that I&#39;m not even proof-reading. It&#39;s all a thoughtless heap of words in disguise, an excuse to fill more ad space. And it is working!

这整篇文章说实话就是毫无意义的填充物，我连校对都没做。这是一堆词的伪装，一个塞更多广告位的借口。而且它奏效了！
#+end_quote

然后他&quot;建议&quot;你可以用 AI 来写内容，或者干脆用 lorem ipsum（排版时用来占位置的假文字）来填充文章后半部分——因为只要前面的文字看起来像人写的，读者就不会注意到后面是机器生成的废话。这正是内容农场的真实操作：用廉价内容填充页面，只要能展示广告就行，内容本身毫无价值。

这一层的巧妙之处在于：作者一边在做他正在批评的事（用废话填空间），一边告诉你他在这么做。你作为读者，明知被耍了，但广告已经展示在你面前了。

** 第四层：文风崩塌

最后几段，文章彻底放弃了教程的伪装。文风从幽默滑向了一种接近克苏鲁式的黑暗散文：

#+begin_quote
We invent new gods and we bury them in the same breath. We leave scars on the world like canyons carved from rivers of glass.

我们在制造新的神，又在同一口气中埋葬它们。我们在世界上留下伤疤，像河流冲刷出的玻璃峡谷。
#+end_quote

然后是一段&quot;拆了建、建了拆&quot;无限重复的文本，最后以一片黑暗中蠕动的触须意象收尾。

这个文风崩塌大概不是作者失控，而是一种有意的隐喻：当内容创作者完全被流量和广告驱动时，内容本身就会退化成这样的东西——没有意义、没有方向、无限循环。

* 中国互联网的镜像

这篇文章讽刺的是英文互联网的广告生态，但这些手法放在中国互联网上只会变本加厉。

百度的搜索结果页，前几条永远是广告。公众号文章读着读着突然插入一条带货链接。短视频平台的内容越来越像广告，广告越来越像内容。B 站的弹幕里混着&quot;恰饭&quot;的辩解。我们每天消费的内容，有多少是真正为读者写的，又有多少是&quot;有意义的填充物&quot;？和这篇讽刺文里的做法相比，区别只在于：这些平台可不会自爆。

modem.io 这篇文章最狠的地方在于，它没有用论述的方式告诉你&quot;广告正在毁掉互联网&quot;，而是让你亲身体验了一把——你读完了它，你的注意力被假广告收割了，然后文章告诉你：刚才经历的就是问题本身。

* 为什么这篇讽刺有效

好的讽刺不靠说理，靠的是让你读完之后回头一看，发现自己刚才的阅读体验本身就是被讽刺的对象。这篇文章做到了：

1. *形式即内容* ——它用广告充斥的排版来讽刺广告充斥的互联网，读者不需要&quot;被说服&quot;，只需要&quot;经历一遍&quot;
2. *诚实是最好的嘲讽* ——作者直接承认&quot;这篇文章就是垃圾&quot;，这种自曝比任何批评都尖锐
3. *递进的荒诞感* ——从正经教程到假广告到自我拆穿到文风崩溃，每一层都把讽刺推得更深

这种结构性的讽刺手法并不常见——大部分讽刺靠的是文字游戏和双关，而这篇文章让文章的结构本身成为了论点。
</description>
<pubDate>2026-04-20</pubDate>
<guid>https://lujun9972.github.io/blog/2026/04/20/读-how-to-monetize-a-blog：一篇伪装成变现指南的讽刺文/index.html</guid>
</item>
</channel>
</rss>