<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>暗无天日</title>
    <link>https://lujun9972.github.io</link>
    <description>=============&gt;DarkSun的个人博客</description>
    <pubDate>Sun, 03 May 2026 23:32:11 UTC</pubDate>
    <lastBuildDate>Sun, 03 May 2026 23:32:11 UTC</lastBuildDate>
    <docs>http://www.rssboard.org/rss-specification</docs>
    <generator>ego static site generator (https://github.com/emacs-china/ego)</generator>
<item>
<title>读：7 Techniques That Supercharged My Claude-Assisted Development</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：7-techniques-that-supercharged-my-claude-assisted-development/index.html</link>
<description>Claude Code 用了一段时间，算重度用户了。写博客、改配置、知识管理基本都在上面。DZone 上 Vivek Katarya 的这篇文章列出了 7 个进阶技巧，我对照着自己的使用经验梳理一遍，看看哪些已经在用了、哪些是新学的、哪些有不同看法。

* 技巧 1：停止审批每个命令

Claude Code 默认每个操作都要弹确认： =Run npm test? (Y/N)= 。安全是安全，但迭代的时候频繁打断很影响节奏。

解决方案是在 =~/.claude/settings.json= 里配一个白名单：

#+begin_src json  :tangle ~/.claude/settings.json
{
  &quot;permissions&quot;: {
    &quot;allow&quot;: [
      &quot;Read(~/Codebase)&quot;,
      &quot;Edit(~/Codebase)&quot;,
      &quot;Bash(ls:*)&quot;,
      &quot;Bash(cat:*)&quot;,
      &quot;Bash(tail:*)&quot;,
      &quot;Bash(grep:*)&quot;
    ]
  }
}
#+end_src

配了之后，这些操作就不再弹确认了。原文的 advice 是只放只读或低风险操作。我加了 =Bash(grep:*)= 和 =Bash(cat:*)= 用来做代码搜索，确实方便很多。涉及生产环境或密钥的操作继续弹确认，这个分寸自己把握。

原文还特意警告了 =--dangerously-skip-permissions= 这个 flag：等于给 Claude 开了 God Mode，风险远大于便利。别用。

* 技巧 2：让 Claude 自己改配置

Claude Code 能改自己的配置文件。想改什么直接说就行，不用翻文档找 JSON 路径。

比如我把换行快捷键改了：

: 把换行快捷键改成 Shift+Enter。

想调权限也一样：

: 更新权限，让 bash 命令 ls、grep、tail 不用确认就能执行。

这个我经常用。调整 verbose 级别、diff 格式、文件搜索范围，想到了就丢一句话过去，省了手动编辑 JSON 的功夫。

一个限制：改完配置后要手动重启 Claude 才生效，它没法给自己发 SIGHUP。

* 技巧 3：给 Claude 一个入口文件

Claude 默认会自动探索代码库来理解上下文。项目大了之后，它会在无关文件上浪费 token。

解决方案是写一个入口脚本（比如 =scripts/context.sh= ），只展示 Claude 真正需要的信息：

#+begin_src shell  :tangle scripts/context.sh
#!/bin/bash

echo &quot;README:&quot;
cat README.md

echo
echo &quot;Key services:&quot;
grep -r &quot;class .*Service&quot; src/
#+end_src

然后告诉 Claude：&quot;每次需要项目上下文时，先跑 =scripts/context.sh= &quot;。这句指令可以在对话里直接说。

再配合 =.claudeignore= 过滤掉 node_modules/、build/ 、*.log 这些噪音，效果更好。原理跟 =.gitignore= 一样，不让 Claude 在无关文件上浪费 token。

* 技巧 4：把 fix-test 循环交给 skill

原文举了一个例子：不用自己跑测试、看失败、再修，直接告诉 Claude：

: 跑测试，标记失败用例，修好，再跑。直到全通过，或者最多重试 3 次。

原本来回 15 分钟的循环变成一句话。然后可以把这条指令保存成 skill（ =~/.claude/commands/fix-test/SKILL.md= ），以后直接复用。

我的场景不太一样（写代码修测试不是日常），但这个模式本身通用。比如写博客的时候，lint 报了一堆格式错误，直接告诉 Claude &quot;把 lint 报的问题全修了，修完再跑一遍确认&quot;。跟 fix-test 循环是一个套路，把迭代交给它，我等结果就好。

* 技巧 5：链式调用 skills

有了几个基础 skill（write-unit-tests、fix-test、create-pr）之后，下一步是把它们串联成一个高阶 skill。

比如定义一个 =sanitize-and-publish= 的 skill，自动跑 fix-build → check-coverage → write-unit-tests → fix-tests → create-pr。原文形容得好：像管理一个轻量级的多代理流水线。

这个我还没系统性做，但思路对。比如我现在的 =url2blog= 其实已经在链式调用了，它内部又调了 brainstorming 和 人味儿 两个子 skill。按原文的思路，可以把这些拆成更细粒度的独立 skill，再组合出更高阶的流程。下次可以试试。

* 技巧 6：用 worktree 并行跑多个会话

单会话不够用时，可以用 Git worktree 开多个并行的 Claude 实例。一个修 bug、一个重构模块、一个搞原型：

: claude --worktree feature-x

每个实例有自己的工作目录，互不干扰。干完后把 worktree 合到对应分支就行。

superpowers 里其实已经有 =using-git-worktrees= skill 封装了这个流程，但我平时很少需要并行开多个会话。偶尔需要同时做两件事时（比如这篇博文写到一半，突然想试另一个写法），直接用 =claude --worktree= 起一个新实例，互不干扰。

* 技巧 7：复杂任务先出计划

原文说这是&quot;提升输出质量最管用的一个习惯&quot;：

: 先生成一个分步计划，识别边界情况，提出澄清问题。我确认后再执行。

Claude 的第一版计划通常 80% 对。那漏掉的 20% 正好是 bug 和回归问题所在。花两分钟确认，经常能省下两小时。

这一点完全同意。我现在用 =superpowers:brainstorming= 和 =writing-plans= 就是这个流程，先想清楚再动手。尤其在写代码或写博文之前，plan first 能把方向跑偏的风险降到最低。

* 几个补充

原文末尾还有几个技巧：

- *用 Orchestrator 管理多会话* ：会话多了以后切换成本很高，Orchestrator 这类工具帮助协调多个 Claude 实例
- *让 Claude 自我审查* ：在你自己做 code review 之前，让 Claude 先 review 自己的改动。它能抓到不少逻辑和流程上的问题
- *让 Claude 自己改进 skill* ：用 Skill Creator 让 Claude 迭代自己的工具集，工作流会自动演进

最后一个我最近在实践。做了 =log-reviewer= skill 之后，确实感觉到了&quot;让 AI 自己造工具&quot;的正循环。每次用 skill 写博文、改代码，都在积累经验，下次就更顺手。

* 总结

原文把 Claude Code 比作&quot;一个写代码极快、但需要明确指令的初级工程师&quot;。这个比喻贴切。权限白名单、入口文件、skill 链、plan-first，这些东西搭配在一起之后，重复劳动减少、迭代变快、脑子省出来想真正的问题。

这 7 个技巧里，fix-test 循环、链式 skill、plan-first 这几个我已经在用了，而且用出了自己的变体。权限白名单和 self-configure 属于&quot;知道但没认真配&quot;的，写这篇的时候顺便补上了。worktree 并行有现成 skill 可以用，只是平时场景不多。
</description>
<pubDate>2026-05-04</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：7-techniques-that-supercharged-my-claude-assisted-development/index.html</guid>
</item>
<item>
<title>读：Matt Pocock 的 skills——需求拷问机与 AI 技能的自我进化</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：matt-pocock-的-skills——需求拷问机与-ai-技能的自我进化/index.html</link>
<description>你让 AI 写一段登录功能的代码。放在以前，AI 听完就开始噼里啪啦输出几百行代码，你读完发现逻辑不对，删除重来。现在装了 grill-me 技能后，AI 听完你的需求，一句话不写，反手就问你 20 个问题。

这就是 Matt Pocock 的 [[https://github.com/mattpocock/skills][skills]] 项目做的事情。它提供了一套可安装的技能模板，每个技能强制 AI 在执行前先完成特定的思考流程。本文聚焦其中两个最有特色的设计：grill-me（需求拷问机）和 write-a-skill（元技能）。

* grill-me：先问 20 个问题，再写一行代码

grill-me 的核心机制很简单：当你提出一个需求时，AI 不会直接开始写代码，而是先对你发起一连串的追问。

你说&quot;帮我实现一个登录功能&quot;，它会问：

- 目标用户是普通网友还是公司内部员工？
- 用邮箱、手机号还是用户名登录？
- 输错三次密码怎么办，锁账号还是等五分钟再试？
- SQL 注入怎么防？
- 同时一万人登录扛得住吗？

这些问题覆盖了功能开发的各个维度：用户场景、边界条件、安全防护、性能要求。每一个问题背后都是一个常见的&quot;以为说清楚了，其实没有&quot;的坑。

** 为什么 grill-me 有效

AI 编程中最难的环节是需求对齐，写代码反而是简单的那部分。你说&quot;处理一下文本文件&quot;，AI 以为你要写一个完整的文本分析工具。你心里想的其实是&quot;把第一列提取出来排序&quot;。双方都自信满满地理解错了，结果就是写完 400 行再删除重来。

这个场景的本质是：人类说需求时习惯省略大量&quot;常识性&quot;的背景信息。默认对方&quot;应该懂&quot;。但 AI 没有常识。它不会追问&quot;你讲的太模糊了，能具体一点吗？&quot;，而是直接按自己的&quot;最佳猜测&quot;开始写，而且写得特别快、特别自信。你快它也快，最后两个人都白忙。

grill-me 打断了这个&quot;快进到失败&quot;的节奏。它让 AI 在执行前显式地做一个&quot;需求验证&quot;的步骤，把所有需要确认的假设摊在桌面上，一条一条问你。这个过程看起来慢了（要回答 20 个问题才动手），但避免了&quot;写完再删&quot;的循环，实际上是省时间。

这和 [[file:读-Conducty的工程原则.org][Conducty 的&quot;追踪弹&quot;原则]] 是一个道理：用最小的成本先在假设层面验证，确认方向对了再批量执行。

** 从行为层面改变 AI

Matt Pocock 的 skills 项目和 addyosmani 的 [[https://github.com/addyosmani/agent-skills][agent-skills]] 有一个本质区别：agent-skills 是模板，你复制一份到项目里用；Matt Pocock 的 skills 是可安装的交互式技能包。安装后 AI 的行为会实时改变。它多了一套&quot;遇到需求先质疑&quot;的行为模式，不止是一份参考文档。

你可以把 grill-me 理解成面向 AI 的&quot;行为矫正器&quot;。大多数 AI 编码工具默认的行为模式是&quot;你说→我做&quot;，这个模式在需求模糊时会产生大量废代码。grill-me 把它改成了&quot;你说→我问→你答→我做&quot;。多加的这一步&quot;我问&quot;，恰恰是区分生手工程师和资深工程师的关键动作。

** grill-me 与方案探索技能的区别

grill-me 看起来和 Claude Code 生态中的方案探索类技能（比如 superpowers 中的 brainstorming）有点像——都是在写代码之前追问问题。但它们的层次不同。

brainstorming 解决的是&quot;该不该做、做什么方案&quot;——你有一个模糊的想法，它帮你拆解成几个可行的方向，评估取舍，产出一份设计文档再动手。它工作在方案层。grill-me 解决的是&quot;方案已经定了，边界条件是什么&quot;——你决定做登录了，它帮你补上忘了说的细节：用什么方式登录、出错怎么处理、安全边界在哪里。它工作在实现层。

两者可以串联使用。先跑 brainstorming 确定整体方案，然后到具体实现时，grill-me 自动补上需求细节。一个负责战略层，一个负责战术层。

如果你想更进一步，可以用 write-a-skill 把两个流程合并成一个自定义技能，从方案探索到边界定义在一个文件里走完。当然，更简单的做法是在 prompt 里直接加一句&quot;在我确认之前不要写代码，先问清楚所有边界条件&quot;——这已经能达到 grill-me 八九成的效果。

* write-a-skill：技能的可编程化

如果说 grill-me 是一个具体的技能，那 write-a-skill 就是制造技能的技能，也就是元技能（meta-skill）。

它的工作方式很简单：你对 AI 说&quot;帮我写一个&#39;代码审查&#39;的技能&quot;，AI 会按照标准流程来执行。它先问你审查标准是什么、关注哪些维度、输出什么格式，然后自动生成一个符合规范的新技能文件。安装后就能用 =npx skills@latest add= 加载到项目中。

这意味着 skills 项目不是一个封闭的工具集，而是一个可扩展的生态系统。你可以消费社区提供的技能，也可以自己生产技能。消费和生产之间的门槛被 write-a-skill 抹平了。

** 从消费到生产的转变

这个设计有一个更深层的含义：AI 技能的创建权和定制权回到了用户手里。

之前我们讨论过 [[file:读：从写提示词到构建技能——三层技能框架与Claude Code的认知升级.org][三层技能框架]]，其中提到&quot;个人技能&quot;来自你踩过的坑、交过的学费，是最稀缺的东西。但稀缺归稀缺，很多人的经验从来没有被结构化成可复用的形式。原因不是懒，而是&quot;把经验写成 AI 能理解的结构化指令&quot;这件事本身有门槛。

write-a-skill 把这层门槛拆掉了。你不是在写一个格式复杂的配置文件，你是在和 AI 对话。AI 引导你把自己的工作流说出来，它帮你生成对应的技能。在这个过程中，你的角色从&quot;写代码的人&quot;变成了&quot;描述工作流的人&quot;。这是一个抽象层级的提升。

** write-a-skill 与 skill-creator 的差异

如果你接触过 Anthropic 官方的 skill-creator，可能会问它和 write-a-skill 有什么区别。它们都生成技能，但定位完全不同。

skill-creator 是一个完整的技能创作框架。自带脚手架（ =init_skill.py= 生成目录结构）、打包工具（ =package_skill.py= 打包成 =.skill= 文件分发）、校验流程（ =quick_validate.py= 自动检查格式）。生成的技能可以包含 Python 脚本、参考文档和模板文件，元数据常驻上下文，核心指令按需触发。适合正式开发、对外分发的技能。

write-a-skill 是一个轻量的对话式生成器。你说&quot;帮我写一个代码审查的技能&quot;，AI 追问你审查标准是什么，然后生成一个 prompt 模板。没有目录结构、没有校验脚本、没有打包工具。适合快速给自己的项目写一个生效的规则。

可以用一句话概括：write-a-skill 和 skill-creator 的差异本质上是&quot;敏捷创作 vs 工程框架&quot;的对立。Matt Pocock 的 write-a-skill 定位就是轻量对话生成，适合快速写 prompt 模板；Anthropic 官方的 skill-creator 是完整工程框架，有脚手架 + 校验 + 打包的完整 pipeline。

* 几个值得注意的配套技能

除了 grill-me 和 write-a-skill，项目中还有几个思路值得提一下。

triage-issue 是一个 bug 定位技能。它不是让你描述 bug，而是先在项目代码里扫描异常模式、画出错误路径、定位到具体文件行号。然后基于 TDD 流程给出修复方案——先写一个能复现 bug 的测试，再改代码让测试通过。

git-guardrails 拦截危险的 git 操作。你输入 =git reset --hard= 时，AI 会拦住你：&quot;本地的改动还没提交，你确定要丢掉吗？&quot;它像一个唠叨的副驾驶，在你犯错之前拍一下你的手。

tdd 技能强制走&quot;红-绿-重构&quot;循环。先写测试（红灯），再写让测试通过的最简代码（绿灯），最后回头看结构能不能优化（重构）。每一步都小到不会出错，出了也能立刻定位。

这些技能的共同模式是：它们不教 AI 新的知识，而是改变 AI 的执行顺序。把&quot;先做再说&quot;变成&quot;先想再做&quot;。

* 总结

Matt Pocock 的 skills 项目展示了一个趋势：AI 编码工具的下一个进化方向是更好的交互协议，而不是更强的模型。grill-me 强制需求对齐，write-a-skill 让技能生产民主化。背后都是同一个思路——把资深工程师的工作习惯打包成可安装的互动流程，而不是扔给 AI 一本操作手册让它自己读。

安装只需要一行命令：

#+begin_src bash
npx skills@latest add mattpocock/skills/grill-me
#+end_src

比起花时间调提示词，装一个 skill 可能是更便宜的改进方式。因为提示词解决的是&quot;这一问怎么说&quot;，skill 解决的是&quot;每次遇到这类问题怎么做&quot;。
</description>
<pubDate>2026-05-03</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：matt-pocock-的-skills——需求拷问机与-ai-技能的自我进化/index.html</guid>
</item>
<item>
<title>读：从写提示词到构建技能——三层技能框架与 Claude Code 的认知升级</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：从写提示词到构建技能——三层技能框架与-claude-code-的认知升级/index.html</link>
<description>[[https://www.jdon.com/91532-AI-Isnt-The-Product-but-Context-Is.html][原文链接]]

大部分人用 AI 还在琢磨怎么把提示词写得更精巧。但真正拉开差距的地方，是你给 AI 灌了什么上下文。这篇文章解读一套三层技能框架，以及这套框架如何改变你用 Claude Code 的方式。

* 一个反直觉的事实

企业搞 AI 的现状很残酷：投入几十亿，95% 的项目没有任何产出。不是模型不行，模型越来越强了，问题是所有人都把注意力放在了工具上，很少有人认真想过自己要往里面塞什么内容。

这个判断背后的逻辑很直接：模型的能力在趋同。今天你用 GPT-4o 还是 Claude Sonnet，差距远没有想象中那么大。真正拉开产出的，是你有没有围绕自己的知识、工作流程和判断标准构建一套上下文系统。往里面喂垃圾，吐出来的是高级垃圾。往里面喂结构化的认知，输出就能放大你的专业能力。

* 三层模型：大多数人只用了最浅的一层

要理解上下文为什么重要，得先拆开 AI 的调用结构。它分三层，大多数人只用了前两层。

** 第一层：预训练

这一层决定模型&quot;能干什么&quot;：写文章、写代码、总结文档。模型公司花几十亿训练出来的能力底座。你完全控制不了它，买回来是什么样就是什么样。这一层不是你的竞争力，因为别人也能买到。

** 第二层：提示词

提示词决定&quot;这一刻你具体问什么&quot;。大多数人就停在这一层，以为把提示词写好就能出好结果。提示词当然重要，但它是一次性的、临时性的指令。每次问不同的问题都要重新写，没有积累，没有复用。

** 第三层：技能

技能是一个可复用的上下文模块。它定义了你是谁、你怎么工作、你对结果的质量标准是什么。它像一个长期存在的&quot;专业人格说明书&quot;，在你每次调用模型时自动注入进去，让模型知道自己在为谁工作。

同一个模型，加载不同的技能，输出质量天差地别。一个加载了产品经理技能的模型和一个没加载任何技能的模型，写出来的需求文档完全是两个物种。问题在于，绝大多数人根本没有构建这一层。

* 三种技能类型

技能框架把&quot;经验&quot;这种模糊的东西拆成了三层清晰的结构。

** 通用技能：定义什么叫好

通用技能不教具体做什么任务，它只定义判断标准。比如产品经理这个角色，本质是平衡用户需求、商业目标和技术约束之间的张力。优秀的产品经理知道一个功能该不该上线、该做到什么程度、优先级在哪。这些判断标准来自经验积累，但平时没人会写下来。

这种东西平时没人会写下来，因为大家都默认&quot;懂的人自然懂&quot;。但你不写清楚，AI 就永远学不到这套标准，团队成员也无法统一工作方式。通用技能的价值就是把判断标准固化下来，让 AI 和团队都有统一的参照系。

** 项目技能：在冲突中做选择

通用技能太抽象了，放在任何项目上都能用，但也正因为太通用，它落不了地。项目技能告诉你在某个具体环境里，哪些东西优先级更高。比如金融合规项目里，合规的重要性一定压过交付速度。

现实世界里的决策没有一件是在真空中发生的。用户体验和商业目标常打架，交付速度和质量标准互相拉扯，你想创新又怕风险。你不把这些优先级写清楚，AI 就会按自己的&quot;平均理解&quot;来脑补，结果就是一片混乱。项目技能的本质是把模糊的约束变成明确的决策边界。

在 Claude Code 里， =CLAUDE.md= 就是一个典型的项目技能。它写在项目根目录下，告诉 AI 这个项目用什么语言、什么测试框架、什么编码规范、什么分支策略。每次会话自动加载，不用重复说。

** 个人技能：真正稀缺的东西

个人技能来自你踩过的坑、交过的学费。什么时候该怀疑一个需求，什么时候该推翻一个定了的方案，这些东西不存在于任何理论课本里，只能靠不断犯错和复盘积累。

问题在于，大多数人从来没有把这些经验写下来。它们存在于你的直觉里、肌肉记忆里，不具备任何可复用性。新人进来，每次都重新踩一遍你当年踩过的坑。

在 Claude Code 的语境里，这就是 skills 目录下的技能文件。比如你写了一个&quot;代码审查技能&quot;，里面包含你对代码质量的判断标准、你踩过的坑、你发现过的典型错误模式。加载这个技能后，AI 在审查代码时就会用你的经验标准来判断，而不是用&quot;互联网平均标准&quot;来审查。

* 冲突不会消失，但会被显性化

把三层技能同时加载给 AI，就会发生冲突。

比如通用技能说用户体验第一，项目技能说合规优先，这两个目标在很多场景下天生冲突。模型不会替你解决这个冲突，但它会把冲突点摆在你面前：你的通用技能倾向于 A 方案，项目技能要求你必须选 B 方案，请你自己做决定。

这反而是好事。现实世界的决策从来没有什么最优解，只有&quot;在约束条件下的最不坏解&quot;。关键是你清不清楚自己在权衡什么，比能不能找到完美方案重要得多。

* 技能即代码：不维护就变毒药

很多人犯一个错误：写一次技能就不管了，觉得一劳永逸。结果是上下文过时了。市场变了、产品变了、团队也变了，但 AI 还在执行旧规则。输出的质量已经很差了，但读起来依然流畅自信——你很难发现它已经过时。

技能必须像代码一样持续维护。项目方向变了要改，团队踩了新坑要补充，认知升级了更要迭代版本。不维护的技能比没有技能更可怕，因为它给你一种虚假的安全感。

关键是把维护成本降到足够低。每次项目复盘时顺手更新项目技能，每次发现自己做了一次漂亮的判断，顺手写进个人技能。维护不能是额外的工作负担，它得嵌入日常工作流程里才能长期运转。

* 扩展心智：信任是前提

心理学里有一个概念叫&quot;扩展心智&quot;：工具可以成为你思考过程的一部分。笔记本就是你记忆的延伸，你不需要把所有事情记在脑子里。但前提是这个工具必须随手可用、稳定可靠、你愿意信任它。

技能也是一样的道理。如果每次调用 AI 前都要手动加载技能文件，或者技能经常过期失效，它永远成不了你的&quot;第二大脑&quot;，你只会把它当偶尔用一下的辅助工具。

只有当加载是自动的、质量是稳定的、更新是无痛的，你才会真正开始信任它，把它纳入你的认知系统。在 Claude Code 里，这就是 =CLAUDE.md= 和 =~/.claude/skills/= 这两层机制的价值：自动加载、无需手动干预。

* AI 的定位：缩短从想法到草稿的距离

AI 最实际的价值，是帮你快速生成第一版草稿。以前花几小时憋一篇文章、写一个方案、搭一个框架，现在几分钟搞定，而且格式工整、逻辑通顺。

但草稿不是结果，只是起点。真正体现你专业能力的部分，是你如何评估、修改甚至推翻 AI 生成的输出。你能看出哪里有问题，能补充它遗漏的关键点，能调整它判断错的优先级。这些判断力才是你真正值钱的地方。

这意味着工作的核心正在转变。写东西这个动作变得不值钱了，但判断什么东西值得写、什么东西写错了、什么东西需要删掉，这些判断能力比以前更值钱。你不再是写方案的人，而是评估方案、选择方案、否决方案的人。

* 总结

AI 不会自动给你带来任何竞争优势，它只会放大你已经拥有的能力。如果你有清晰的判断力、扎实的专业知识、良好的工作习惯，AI 会让你的产出翻倍。如果你没有这些，AI 不会帮你弥补缺陷，只会把你的缺陷放大成更大的灾难。

真正的分水岭，是谁先跳出&quot;写提示词&quot;的阶段，开始构建自己的上下文系统，给 AI 提供高质量的上下文。这件事短期看是技术问题：怎么写技能、怎么维护技能。长期看是一个认知问题：想清楚自己的专业判断标准是什么，你的经验里哪些真正值钱，你愿意把哪些主动权交给 AI。

那些最终能用好 AI 的人，往往不是技术最牛的。他们对自己专业理解最深、最愿意把自己的经验写成结构，愿意花时间写技能、维护技能，因为他们知道这是在给未来的自己造一个永远在线、永不疲倦的智能分身。
</description>
<pubDate>2026-05-03</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：从写提示词到构建技能——三层技能框架与-claude-code-的认知升级/index.html</guid>
</item>
<item>
<title>读：你的 CLAUDE.md 缺少灵魂——SOUL.md 与 AI 智能体的人格文件</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：你的-claude.md-缺少灵魂——soul.md-与-ai-智能体的人格文件/index.html</link>
<description>用 Claude Code 的人大多知道 =CLAUDE.md= 的重要性：在项目根目录写一份文件，定义编码规范、测试框架、分支策略，AI 每次会话自动加载。但光有项目规矩不够，AI 还缺人格。[[https://www.jdon.com/91632-three-files-articulate-agent.html][这篇文章]]介绍一套三文件框架：SOUL.md 定义 AI 是谁、USER.md 定义你是谁、AGENTS.md 定义怎么做。

* CLAUDE.md 管不了的事

CLAUDE.md 解决了一个问题：让 AI 知道这个项目的规矩。语言用 Python、测试用 pytest、分支用 Git Flow。这些都是约束条件，不是人格。

AI 遵守这些规矩的时候，写出来的代码语法正确、格式工整、逻辑通顺，但读起来还是 AI 味。因为它只是在执行一套&quot;正确的规则&quot;，不知道自己应该用什么腔调说话、什么算好,什么算差、哪些事情要坚决拒绝。它像一个不会出错但也没有性格的同事。你让他写代码，他写好了。你问他技术问题，他回答了。但你不会觉得他在&quot;和你一起工作&quot;。

这中间的差距，就是 SOUL.md 要填的东西。

* SOUL.md：从系统提示到章程

SOUL.md 应该是一套&quot;章程&quot;，而不是普通的系统提示词。它定义的是身份，不单纯是任务。具体来说包括：

- 语气：说话用什么腔调，节奏是快是慢
- 价值观：什么是对的什么是错的，哪些不能妥协
- 运行原则：什么样的输出算好、什么样的输出算差
- 拒绝边界：哪些请求应该直接拒绝

这里有一个关键区别。系统提示词是给模型的指令，它假设模型在&quot;执行一个任务&quot;。章程是给智能体的身份设定，它假设智能体在&quot;扮演一个角色&quot;。同样的内容，用提示词写和用章程写，效果完全不同。

泛泛的指令只会得到泛泛的输出。如果你写&quot;要有帮助且简洁&quot;，你得到的就是 ChatGPT 默认的回答，没错但无聊。如果你写&quot;必须简洁，一句话能说清楚就不说第二句，真话再难听也要说，说话一针见血&quot;，AI 输出的风格就会立刻转变。

原文还举了一个更极端的例子：在 SOUL.md 里写&quot;可以讲脏话&quot;。大多数 AI 产品公司打死也不会把这个写入默认系统提示，但它恰恰是让智能体&quot;有生命力&quot;的关键。因为&quot;可以讲脏话&quot;的背后是一整套价值观：真实比礼貌重要，准确比温和重要，用户想知道真相而不是被哄着。

这本质上是约束设计的逻辑在起作用。通用 AI 智能体之所以让人觉得像客服，是因为它们被要求&quot;乐于助人&quot;，而这个约束太宽泛了，等于没有约束。而明确写出&quot;可以讲脏话&quot;&quot;永远不要用好问题来开头&quot;&quot;必须带幽默感&quot;，这些具体约束反而让 AI 的输出边界清晰，结果更有生命力。

* USER.md：让 AI 真正理解你

大多数人的 USER.md 是一份简历：我叫 XX，我在 XX 公司工作，我会 XX 技术。但这远远不够。

真正的 USER.md 是一份深度画像。文章里提到的例子包括：思维习惯、长处、盲点、性格、什么会让你生气、你在乎什么。内容不该是&quot;我是一个程序员&quot;，应该是&quot;我习惯先想清楚再动手，讨厌没有上下文的需求，对性能问题特别敏感，做决定之前需要看到数据&quot;。这些东西才是让 AI 理解&quot;你这个人&quot;的关键。

原文说作者的 USER.md 大约 4000 字。这份文档靠记忆写不完，得长期积累。每次发现自己对 AI 的输出不满意，想一想：是不是 USER.md 里少了什么信息？补上。

* AGENTS.md：运行规则手册

如果说 SOUL.md 管人格、USER.md 管画像，AGENTS.md 管的就是流程。每次收到消息要检查什么、绝对不能做什么、查询信息的顺序是什么、遇到错误怎么处理。

这其实和 CLAUDE.md 的功能最接近，只是颗粒度更细。CLAUDE.md 写的是&quot;项目用什么语言和框架&quot;，AGENTS.md 写的是&quot;收到一个 bug 报告后应该按什么顺序排查&quot;。一个是项目维度的规矩，一个是任务维度的流程。

* Claude Code 用户的配置方案

原文的三文件框架可以直接映射到 Claude Code 的现有机制上。

SOUL.md 对应的是 =~/.claude/skills/= 目录下的技能文件。只不过大部分人的技能文件只写了&quot;怎么做任务&quot;，没有写&quot;应该用什么语气和价值观&quot;。如果你在技能描述里加上语气要求、好坏输出标准、拒绝边界，技能的质量会上一个台阶。

USER.md 对应的是记忆文件（memory/ 目录）和你自己的 =CLAUDE.md= 中的 user profile 部分。但这些内容目前分散在各处，没有形成一个集中的&quot;用户画像&quot;文件。如果你在 memory 目录下专门维护一份类似 USER.md 的文档，把思维习惯、盲点、偏好写清楚，AI 对你的理解会深很多。

AGENTS.md 可以融入已有的技能文件，也可以单独作为一个&quot;通用工作流&quot;技能。重点是把操作流程固化为可复用的步骤，而不是每次重复说。

* 总结

CLAUDE.md 让 AI 遵守项目规矩，SOUL.md 让 AI 有性格，USER.md 让 AI 理解你，AGENTS.md 让 AI 正确执行。四者缺一不可。

大多数人只走到 CLAUDE.md 这一步。这一步当然有用，但不完整。下一步是给 AI 一个&quot;人格&quot;：用 SOUL.md 告诉它怎么说话、用 USER.md 告诉它你在乎什么、用 AGENTS.md 告诉它怎么干活。这样配置出来的 AI 看起来才不像客服，更像有判断力的同事。
</description>
<pubDate>2026-05-03</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：你的-claude.md-缺少灵魂——soul.md-与-ai-智能体的人格文件/index.html</guid>
</item>
<item>
<title>读：用免费模型跑 Claude Code——free-claude-code 代理设置指南</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：用免费模型跑-claude-code——free-claude-code-代理设置指南/index.html</link>
<description>有一篇 jdon 的热文介绍了一个免费使用 Claude Code 的方法，原理是用一个代理程序把 Claude Code 的请求转发给其他免费或本地模型。GitHub 上七千三百多颗星，看起来很多人都想省这笔钱。本文是一篇实操指南，告诉你这玩意儿怎么装、怎么用、以及最重要的：代价是什么。

* 原理：一个电话总机

Claude Code 的收费方式是按使用量计费。用得重的话一个月账单确实能上千美元。free-claude-code 的原理很简单：在你的电脑上跑一个代理程序，它假装自己是 Anthropic 的 API 服务器。Claude Code 把请求发给这个代理，代理再转发给其他模型提供商（NVIDIA NIM、OpenRouter、DeepSeek，或者你本地的模型），然后把回答返回给 Claude Code。NVIDIA NIM 是英伟达提供的模型推理服务平台，免费层可以调用 Llama、Gemma 等开源模型，适合入门试用。

中间多了一层转发，所以响应速度比直连 Anthropic 慢，而且免费模型的工具调用能力远不如 Claude 自身。

* 安装

** 前提

- Python 3.10 以上，需要装好 pip
- 一个 [[https://www.nvidia.com/en-us/ai/][NVIDIA NIM]] 的免费 API 密钥（推荐新手用这个，不用绑信用卡）
- 或者装好 LM Studio / Ollama 用来跑本地模型

** 第一步：装 uv

uv 是一个 Python 包管理器，这项目用它来管理依赖。

#+begin_src bash
curl -LsSf https://astral.sh/uv/install.sh | sh
#+end_src

这是 uv 官方安装方式，脚本来自 Astral 官方域名。如果对管道执行脚本不放心，也可以用 =pip install uv= 替代。装完后重启终端让 uv 命令生效。

** 第二步：克隆项目

#+begin_src bash
git clone https://github.com/joshuakirsch/free-claude-code.git
cd free-claude-code
uv sync
#+end_src

=uv sync= 会下载所有依赖，需要一两分钟。

** 第三步：配置

复制配置文件：

#+begin_src bash
cp .env.example .env
#+end_src

编辑 =.env= 文件，找到以下行去掉注释并填入你的 API 密钥：

#+begin_src text
PROVIDER=NVIDIA_NIM
NVIDIA_NIM_API_KEY=你的密钥
#+end_src

NVIDIA NIM 的 API 密钥去 [[https://build.nvidia.com/][NVIDIA build 平台]] 注册获取。如果选本地模型，PROVIDER 改成 =LM_STUDIO= 或 =OLLAMA= 。

* 运行

启动代理，保持这个终端开着：

#+begin_src bash
uv run python -m free_claude_code
#+end_src

看到 =Starting proxy on :8082= 就说明代理在跑了。

新开一个终端，设置环境变量让 Claude Code 把请求发到代理：

#+begin_src bash
export ANTHROPIC_BASE_URL=http://localhost:8082
export ANTHROPIC_AUTH_TOKEN=&quot;fake-key-please-ignore&quot;
#+end_src

然后在这个终端里启动 Claude Code：

#+begin_src bash
claude
#+end_src

首次运行会问 API 密钥，填 =fake-key-please-ignore= 就行。问要不要发数据给 Anthropic，选 no。然后就能用了。

* 最关键的代价：工具调用能力

这可能是整个方案最大的坑。Claude Code 的强大在于它擅长调用各种工具：读文件、写文件、执行命令、搜索代码。这是模型能力的一部分，不是代理能模拟的。

免费模型在这方面的表现差距很大。你让代理改文件，它可能输出一段文字说&quot;好的我现在来修改&quot;，但实际上什么也没发生。free-claude-code 的作者写了一个启发式解析器来猜模型的意图，把&quot;我想改文件&quot;这种文字翻译成真正的文件写入操作。但这个解析器的准确率大概七成，也就是说三次里有一次会猜错。

解决方法是把任务拆得特别细。不要让它自己判断用什么工具，直接告诉它具体命令。比如不要说&quot;帮我看看这个目录&quot;，而是说&quot;请执行 ls -la&quot;。

* 模型映射玩法

这个代理支持一个实用的功能：给 Claude Code 的不同模型层级指定不同的后端。Claude Code 自己会根据任务复杂度选模型——简单任务用 Haiku，中等用 Sonnet，最难用 Opus。在 =.env= 中可以配置映射：

#+begin_src text
MODEL_MAPPING={&quot;opus&quot;: &quot;nvidia/llama-3.1-405b-instruct&quot;, &quot;sonnet&quot;: &quot;nvidia/llama-3.1-70b-instruct&quot;, &quot;haiku&quot;: &quot;google/gemma-2-9b-it&quot;}
#+end_src

这样简单任务走小模型节省资源，复杂任务走大模型尽量保证质量。当然，再怎么映射也达不到真 Claude 的水平，只是优化有限资源的使用策略。

* 本地模型

如果在意数据隐私，可以完全在本地跑。用 LM Studio 下载一个模型（比如 Llama 3.2 3B 或 Qwen2.5 7B），启动本地 API 服务，然后修改 =.env= 把 PROVIDER 改成 =LM_STUDIO= ，指向本地的 API 地址。所有请求都不出本机。

但本地小模型的智商差距更明显。处理复杂嵌套函数、跨文件重构这类任务基本靠不住。适合批量重命名、查字符串、生成配置模板这类简单操作。

* 什么场景值得用

免费代理最适用的场景：

- *学习阶段* ：刚学编程，需要 AI 帮忙解释代码、出练习题。模型笨一点反而逼你多思考
- *离线环境* ：长途飞机、没有网络的地方。提前搭好本地模型，至少能查语法补全括号
- *非敏感项目* ：个人博客、公开的开源项目。内容本来公开，泄露了也没损失
- *模型对比评测* ：快速切换不同后端，比较各模型在编码任务上的表现

不适用场景：涉及公司代码、商业项目、客户交付的产品。一两百美元的月费不够赔一个生产事故。如果用量不大，Claude Pro 二十美元一个月其实够用。

* 总结

free-claude-code 是一个巧妙的工具，它证明了 Claude Code 的接口设计足够开放：只要中间人协议搭好，就能把不同模型接进来。但接口开放不意味着能力等价。免费模型的工具调用能力是硬伤，无法通过代理层弥补。

如果你想省那二十美元，用这个代理解闷学习完全没问题。如果你写的是正经代码，那二十美元换来的稳定性和模型能力，比自己折腾代理划算得多。
</description>
<pubDate>2026-05-03</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：用免费模型跑-claude-code——free-claude-code-代理设置指南/index.html</guid>
</item>
<item>
<title>读：2026 年构建 Python 库的现代工具链</title>
<link>https://lujun9972.github.io/blog/2026/05/03/读：2026-年构建-python-库的现代工具链/index.html</link>
<description>* 开篇：Python 工具链的&quot;大统一&quot;时代

如果你在 2020 年问一个 Python 开发者&quot;用什么工具构建库&quot;，你得到的答案会因人而异。Poetry、pipenv、flit、hatch，每个都有各自的拥趸。Lint 用 flake8，格式化用 black，类型检查用 mypy，三套工具各管一摊。到了 2025–2026 年，情况已经大为改观。

Stephen Funk 在 [[https://stephenlf.dev/blog/python-library-in-2026/][Building a Python Library in 2026]] 中总结了现代 Python 库开发的实操经验。核心信息是：工具链已经收敛到了 Astral 一家的产品上。这篇文章基于原文内容，结合中国开发者的实际环境做了解读。

* 项目初始化：uv 的一站式体验

当前 Python 生态里受到最多关注的新工具是 Astral 公司开发的 =uv= 。它同时取代了 pip、poetry、pyenv、virtualenv、hatch 等多个工具的功能，而且性能比 pip 快 10–100 倍。

初始化一个新库只需要一条命令：

#+begin_src shell
uv init --lib my-package
#+end_src

这条命令会生成以下目录结构：

#+begin_example
.
├── .git
│   └── ...
├── .gitignore
├── pyproject.toml
├── .python-version
├── README.md
└── src
    └── my_package
        ├── __init__.py
        └── py.typed
#+end_example

这里采用了 =src/= 布局：库代码放在 =src/my_package/= 下，而非直接在项目根目录下。这样做的好处是，测试时导入的是安装后的包而非源代码目录，能提前暴露包打包配置的问题。

=pyproject.toml= 包含了项目的元数据，它遵循 Python 官方的 pyproject.toml 规范。[project] 部分中 =description= 和 =authors= 字段对 PyPI 页面渲染很重要， =keywords= 和 =classifiers= 可以让 LLM（如 Claude）帮忙填写。

版本号遵循 SemVer 标准（主版本号.次版本号.修订号，对应 major.minor.patch）。用 uv 管理版本，不要手动改文件：

#+begin_src shell
uv version --bump {major,minor,patch}
#+end_src

* Linting 和格式化：ruff 接管一切

几年前，社区还在用 flake8（lint）加 black（format）加 isort（排序 import）的组合。现在 Astral 的 =ruff= 可以替代这三个工具。

#+begin_src shell
uv add --dev ruff
#+end_src

日常使用方式：

#+begin_src shell
uv run ruff check      # lint 检查
uv run ruff check --fix # lint 检查并自动修复
uv run ruff format      # 格式化代码
#+end_src

由于这些命令会频繁运行，原文建议用 Makefile 或 just 等命令运行器来记录它们，避免每次都翻文档。

#+begin_src makefile
# Makefile
lint:
    uv run ruff check

format:
    uv run ruff format --check

fix:
    uv run ruff check --fix
#+end_src

* 类型检查：三个选择，一个结论

类型注解在 2026 年已经是 Python 库的标配。缺少类型注解的库很难声称自己有&quot;质量保证&quot;。类型签名能让 IDE 自动补全和静态检查发现潜在 bug。

类型检查工具有三个选项：

| 工具 | 开发者 | 特点 |
|------|--------|------|
| =mypy= | 社区标杆 | 生态最完善，文档最全，兼容性最好 |
| =ty= | Astral（ruff 作者） | 与 uv/ruff 同源，但尚在早期 |
| =pyrefly= | Meta | 较新，仍在快速迭代 |

原文建议三个都试一遍再做选择。uv 让试错变得简单：

#+begin_src shell
# 临时安装并运行，不影响项目依赖
uv run --with mypy mypy src/
uv run --with ty ty check
uv run --with pyrefly pyrefly check

# 选定后作为 dev 依赖加入
uv add --dev mypy
#+end_src

=mypy= 目前仍是事实标准，生态最完善。如果你刚开始接触类型注解，原文推荐 [[https://github.com/anthonywritescode/typing-puzzles][typing-puzzles]] 这个练习仓库来练手。

* 测试：pytest 就够了

测试文件通常放在项目根目录下的 =tests/= 目录中，可以进一步分为 =tests/unit= 和 =tests/integration= 。

=unittest= 是 Python 内置的测试框架，不依赖第三方库就能用。但大多数项目选择 =pytest= ，因为它写起来更简洁（不需要继承 TestCase 类，用普通 assert 即可断言），插件生态也更丰富。

#+begin_src shell
uv add --dev pytest pytest-cov
#+end_src

=pytest-cov= 用来统计代码覆盖率。原文特别提到，覆盖率指标虽然名声不太好（容易被滥用为 KPI），但在实践中确实帮助他发现了很多 bug。

#+begin_src makefile
# 单元测试，频繁运行
test:
    uv run pytest --cov=my_package \
    --cov-report=term-missing tests/unit

# 集成测试，发布前运行
test-integration:
    uv run pytest tests/unit tests/integration
#+end_src

** 多版本测试

很多性能敏感的 Python 库依赖 C 扩展，而 C 扩展在新 Python 版本发布时偶尔会出现兼容问题。原文展示了用 uv 测试多版本的方法，不需要 tox 或 nox 这类额外工具（tox 是传统的多环境测试框架，nox 是其现代替代品，它们都需要额外的配置文件和学习成本）：

#+begin_src makefile
test-versions:
  uv python find 3.12 \
    || uv python install 3.12
  uv run --python 3.12 pytest tests/unit
  uv python find 3.13 \
    || uv python install 3.13
  uv run --python 3.13 pytest tests/unit
#+end_src

这里 =uv python find= 先检查本地是否有该版本，没有则自动安装。 =uv run --python 3.12= 则指定用哪个解释器运行。

* 代码质量维护：CI 和 pre-commit

** CI 脚本

没有自动化检查，代码质量撑不了多久。原文提供了一个极简的 GitHub Actions CI 脚本，核心逻辑是：检查版本号是否变了，如果变了就运行 lint、类型检查和测试，全部通过后再构建发布。

#+begin_src bash
#!/usr/bin/env bash

uv sync

# 获取当前版本号
CURR_VER=$(uv version --short)

# 获取上一个提交的版本号
git restore -s HEAD~ -W pyproject.toml
PRIOR_VER=$(uv version --short)
git restore .

if [ &quot;$CURR_VER&quot; = &quot;$PRIOR_VER&quot; ]; then
  # 版本号没变，说明没有新发布
  exit
fi

# 代码质量检查
uv run ruff check
uv run ruff format --check
uv run mypy src/
uv run pytest tests/

# 构建并发布
uv build
uv publish
#+end_src

这个脚本的思路很有意思：不是每次 push 都跑完整的质量检查，而是只在版本号变更时才触发构建和发布管道。平时的提交只跑 lint 和测试，这样 CI 的开销会小很多。

原文还特别提醒，GitHub Actions marketplace 上的第三方 action 曾经是供应链攻击的目标，建议将关键 action 的代码 vendored（复制到仓库内管理），而不是直接引用 marketplace 上的版本。

** pre-commit 钩子

CI 检查的问题是反馈周期太长：提交代码，等几分钟，才知道格式不对。 =pre-commit= 工具把同样的检查移动到 =git commit= 之前执行，在本地就能发现问题。

#+begin_src shell
uv add --dev pre-commit
uv run pre-commit sample-config  # 生成 .pre-commit-config.yaml
# ... 编辑配置文件添加 hooks ...
uv run pre-commit install  # 安装 git hooks
#+end_src

社区已经预先配置了大量可用的 pre-commit hooks。例如 =astral-sh/ruff-pre-commit= 可以在提交前自动运行 =ruff= 的 lint 和格式化检查。

把安装脚本也放进 Makefile，方便其他开发者设置：

#+begin_src makefile
setup:
    uv sync
    uv run pre-commit install
#+end_src

* 发布：不一定需要 PyPI

=uv build &amp;&amp; uv publish= 是标准的发布流程。但原文分享了一个不常见的做法：对于小团队来说，不一定要把库发布到 PyPI。

原文作者的公司只有三个活跃的 Python 开发者，不值得为这三个人搭建私有 PyPI 仓库。但由于合规要求，他们也不能把内部库发布到公共 PyPI。他们的方案是直接从源码安装：

#+begin_src shell
cd ~/my-personal-project/
uv add ~/my-package
#+end_src

=uv add= 接受本地路径，uv 会自动处理构建、缓存和链接，效果和从 PyPI 安装几乎一样。唯一需要做的是用一个 cron 定时任务定期更新仓库代码。

这个方案对国内开发者尤其有用：访问 PyPI 需要换源或代理，但直接从 Git 仓库安装就没有这个烦恼了。

* 参考项目：大厂怎么用

原文列举了两个参考项目：

| 项目 | Lint | Format | 构建 | 测试 | 类型检查 | CI |
|------|------|--------|------|------|----------|-----|
| openai-python | ruff | black | hatchling(rye) | pytest+nox | pyright+mypy | GitHub Actions |
| polars | ruff | ruff | setuptools | pytest+cov | mypy | GitHub Actions |

OpenAI 的 Python SDK 创建于 2020 年，使用的还是 =rye=(uv 的前身)。考虑到 OpenAI 刚收购了 Astral，他们大概率会迁移到 uv。

Polars 的情况比较特殊，它是 Rust 实现的 Python 库，构建系统受限于 setuptools-rust。即便如此，uv 仍然可以正常管理其依赖和虚拟环境。

OpenAI SDK 同时用了 =pyright= 和 =mypy= 两个类型检查工具。这是因为不同的类型检查器对类型注解的解析行为有细微差异，同时用两个可以覆盖各自盲区。

* 总结

2026 年的 Python 库开发已经形成了一个清晰的工具栈：

- =uv= 管理一切（项目创建、依赖、虚拟环境、构建、发布）
- =ruff= 负责 lint 和格式化
- =mypy= 负责类型检查
- =pytest= + =pytest-cov= 负责测试
- pre-commit + CI 负责自动化执行以上检查

这个栈最大的好处是工具数量少（主要来自 Astral 一家），各工具之间没有配置冲突。对于 2020 年需要同时维护 flake8 配置、black 配置、isort 配置、poetry 配置的时代来说，这无疑是一个进步。
</description>
<pubDate>2026-05-03</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/读：2026-年构建-python-库的现代工具链/index.html</guid>
</item>
<item>
<title>一次埋点，任意后端：OpenTelemetry 的可观测性标准化设计</title>
<link>https://lujun9972.github.io/blog/2026/05/03/一次埋点，任意后端：opentelemetry-的可观测性标准化设计/index.html</link>
<description>本文从 SigNoz 博客的一篇 OpenTelemetry FastAPI 实战指南出发,提炼 OTel 的核心设计思想。原文是面向 FastAPI 的实操教程,但这套方案背后是可观测性的通用标准化思路。不限于 Python,不限于 FastAPI,也不限于 SigNoz。

* 可观测性的碎片化困境

微服务架构流行起来之后,一个请求会经过多个服务。你想追踪它,就得在每个服务里埋点采集数据。问题是,每家的监控方案不一样:用 Jaeger 是一套 SDK,用 Prometheus 又是一套,用商业 APM 又是一套。换一个后端,之前埋的点全废了。

这种&quot;绑定式&quot;的可观测性方案维护成本很高:一家企业用了三套监控工具,就有三套埋点代码,升级一个后端的 SDK 版本可能要改一个系统的全部 tracing 代码。OpenTelemetry(简称 OTel)要解决的就是这个问题:定义一套与后端无关的标准化采集规范,你埋一次点,就能对接任意兼容的后端。

* 三层分离:Instrumentation / Export / Backend

OTel 的核心设计可以概括为三层分离:

- *Instrumentation 层(采集)*:负责在代码中创建 span、记录 metrics、收集 logs。这一层与后端无关,只关心&quot;数据怎么采&quot;。
- *Export 层(传输)*:通过 exporter 把采集到的数据发给后端。OTLP(OpenTelemetry Protocol)是 OTel 自家的传输协议,支持 gRPC 和 HTTP 两种方式。
- *Backend 层(展示)*:接收并可视化数据。Jaeger、Zipkin、Prometheus、SigNoz,甚至国产监控平台,只要支持 OTLP 就能接入。

这三层的关系可以用一个类比来理解:Instrumentation 是拍照,Export 是洗照片和邮寄,Backend 是相册。你拍照的方式跟最后用哪本相册无关,胶卷冲出来之后放哪本相册都行。

* 统一的数据模型

OTel 定义了三种核心数据类型,合在一起构成&quot;可观测性三大支柱&quot;:

- *Traces(链路追踪)*:记录一个请求在系统中的完整行程。每个服务处理这个请求时创建一个 span,span 之间通过 parent-child 关系串联起来。
- *Metrics(指标)*:聚合后的性能数据,比如请求延迟 P99、错误率、QPS。适合做仪表盘和告警。
- *Logs(日志)*:应用中产生的事件记录。OTel 用 OpenTelemetry Logs Bridge API 把日志和 trace 关联起来,不替代现有日志框架。

Traces 是 OTel 最核心的设计。它的关键机制是上下文传播(context propagation):通过 HTTP 头(标准是 =traceparent=)或 gRPC metadata,把 trace ID 从一个服务传到下一个。这样一来,即使请求跨了 5 个服务,也能在同一个火焰图里看到整条链路,不需要人工拼接。

* 以 FastAPI 为例看 Instrumentation 层怎么工作

原文用 FastAPI(一个 Python web 框架)作为示例。抛开 FastAPI 的具体细节,只看 OTel 的 instrumentation 流程:

1. *安装 distro*: =opentelemetry-distro= 会自动配置常见选项,比如加载 exporter、设置 SDK provider等。这是为了降低上手成本,避免新手自己拼装各种组件。
2. *安装对应框架的 instrumentation 包*: 运行 =opentelemetry-bootstrap --action=install= 会检测你的应用用了哪些库,自动装对应的埋点包(如 FastAPI、httpx、Redis 等)。
3. *通过 CLI 启动*:在启动命令前加 =opentelemetry-instrument=,它会在运行时注入埋点代码,不需要修改业务代码。

三步下来,请求延迟、调用关系、错误信息就能自动采集了,不需要改一行业务代码。

用环境变量控制 exporter 的配置也很直观:

#+begin_src shell
OTEL_TRACES_EXPORTER=console,otlp \
  OTEL_SERVICE_NAME=sample-fastapi-app \
  OTEL_RESOURCE_ATTRIBUTES=deployment.environment=local \
  OTEL_EXPORTER_OTLP_ENDPOINT=&quot;https://ingest.your-region.signoz.cloud:443&quot; \
  OTEL_EXPORTER_OTLP_HEADERS=&quot;signoz-ingestion-key=your-key&quot; \
  OTEL_EXPORTER_OTLP_PROTOCOL=grpc \
  opentelemetry-instrument uvicorn app.main:app --host localhost --port 5002
#+end_src

这里 =console= 表示同时输出到控制台方便调试,=otlp= 表示发到后端。用环境变量而非代码配置,也让不同环境(开发、测试、生产)的 exporter 配置可以分离。

* 进阶设计:自动采集之外的扩展点

OTel 的自动 instrumentation 覆盖了常见场景,但业务逻辑的观测需要手动加埋点。OTel 提供了几个扩展点,让你在自动采集之外按需定制:

** SpanProcessor:在 span 生命周期中介入

你可以在 span 创建和结束时附加自定义逻辑。下面的例子在 span 开始时就注入环境标识,同时在 span 结束时打印信息:

#+begin_src python
from opentelemetry.context import Context
from opentelemetry import trace
from opentelemetry.sdk.trace import SpanProcessor, Span, ReadableSpan, TracerProvider

tracer_provider: TracerProvider = trace.get_tracer_provider()

class AppContextSpanProcessor(SpanProcessor):
    def on_start(self, span: Span, parent_context: Context | None = None):
        common_attributes = {
            &quot;app.environ&quot;: &quot;canary&quot;,
            &quot;app.release&quot;: &quot;2026.04&quot;,
            &quot;app.contract_type&quot;: &quot;ai_cloud&quot;,
        }
        span.set_attributes(common_attributes)

        if span.name == &quot;sensitive_operation&quot;:
            span.set_attribute(&quot;app.contract_type&quot;, &quot;[REDACTED]&quot;)

    def on_end(self, span: ReadableSpan):
        print(&quot;on span end:&quot;, span.name, span.attributes)

tracer_provider.add_span_processor(AppContextSpanProcessor())
#+end_src

这个模式对运维场景特别有用:你可以在一层统一的 processor 中给所有 span 注入环境标签(集群名、机房、版本号),而不需要每个业务方法单独加。上面的代码还演示了敏感操作的脱敏处理:span 名称匹配 =sensitive_operation= 时,contract_type 会被替换为 =[REDACTED]=

** Custom Sampler:按业务逻辑决定采样

对于高流量应用,全量采样成本太高。OTel 支持自定义采样器,按请求属性决定哪些 trace 保留。下面的示例只保留带 =high_priority= 属性的 span:

#+begin_src python
from opentelemetry import trace
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.sampling import Sampler, SamplingResult, Decision, ParentBased

class AppContextSampler(Sampler):
    def should_sample(self, parent_context, trace_id, name, kind, attributes, links=None, trace_state=None):
        if attributes is not None and attributes.get(&quot;high_priority&quot;):
            return SamplingResult(Decision.RECORD_AND_SAMPLE,
                                  attributes=attributes, trace_state=trace_state)
        return SamplingResult(Decision.DROP, trace_state=trace_state)

    def get_description(self):
        return &quot;AppContextSampler&quot;

sampler = ParentBased(root=AppContextSampler())
provider = TracerProvider(sampler=sampler)
trace.set_tracer_provider(provider)
#+end_src

=ParentBased= 包装器的作用是:如果上游服务决定采样某个请求,你的服务也尊重这个决定,避免父子 span 断裂。丢弃一个 root span 没问题,但如果丢弃了 child span 却保留了 parent span,火焰图里就会出现残缺的调用链。

** Middleware:在 Web 框架层面添加上下文

在 web 框架层面,可以用 middleware 给每个请求注入上下文信息,比如处理耗时和服务器标识:

#+begin_src python
from fastapi import FastAPI, Request
from opentelemetry import trace
from time import time

app = FastAPI()

@app.middleware(&quot;http&quot;)
async def handle_request_metadata(request: Request, call_next):
    start_time = time()
    response = await call_next(request)
    process_time = time() - start_time

    span = trace.get_current_span()
    span.add_event(&quot;process_time&quot;, {&quot;value&quot;: process_time})
    span.set_attribute(&quot;served_by&quot;, f&quot;{app.title}-{app.version}&quot;)

    return response
#+end_src

这三个扩展示例:自动采集做 80%,手动扩展做剩下的 20%。自动 instrumentation 覆盖通用框架,自定义 processor/sampler/middleware 覆盖业务特定需求。两者通过同一套数据模型协作,不冲突。

* 实际意义:可观测性标准化带来的自由

OTel 的标准化设计有几个特别值得关注的实际意义:

1. *避免厂商锁定*:OTel 是 CNCF 毕业项目,与云厂商无关。你可以在自建的 Jaeger + Prometheus 上做开发和调试,生产环境对接商业 APM 或国产监控平台,只需改 exporter 配置,采集代码不用动。

2. *适合多云和混合部署*:如果服务部分在公有云、部分在私有机房,OTel 的统一采集层可以让两边用同一套 instrumentation,数据发到同一个后端或分别发到不同后端。

3. *团队分工更清晰*:Instrumentation 层由开发团队维护(他们熟悉代码),Backend 层由运维团队维护(他们熟悉监控平台)。中间的 exporter 配置只是环境变量,不涉及代码改动。这个分层跟 Linux 的&quot;一切皆文件&quot;设计哲学类似,上层不用关心下层实现,只要接口一致就能协作。

* 总结

OpenTelemetry 解决的核心问题是:怎么让采集与后端解耦。它的三层架构和数据模型标准化,让可观测性不再是每换一个工具就重来一次的重复劳动。

这种&quot;一次埋点,任意后端&quot;的设计思路,跟 USB-C 统一充电接口类似:接口标准化之后,两端可以各自演进。后端平台竞争功能和价格,instrumentation 层专注数据的质量和覆盖度。你不需要在两者之间做绑定式选择。
</description>
<pubDate>2026-05-03</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/一次埋点，任意后端：opentelemetry-的可观测性标准化设计/index.html</guid>
</item>
<item>
<title>从CSS选择器到自然语言：网页自动化的两种范式与取舍框架</title>
<link>https://lujun9972.github.io/blog/2026/05/02/从css选择器到自然语言：网页自动化的两种范式与取舍框架/index.html</link>
<description>* 引子：为什么写爬虫这么容易坏

假设你要从某个电商网站抓取笔记本电脑的价格。传统做法是打开浏览器的开发者工具, 找到价格元素的 CSS 类名, 然后这样写：

#+begin_src python
price = await card.locator(&quot;h4.price&quot;).text_content()
#+end_src

这条代码今天能跑。但明天呢？设计稿一改, 这个类名很可能就变了。你得重新打开开发者工具, 重新定位, 重写选择器。页面改版一次, 爬虫就死一次。

维护 CSS 选择器的成本常常被低估：初版写爬虫花 1 小时, 后续维护可能花 10 小时。而且每次改动都是重复劳动, 没有任何累积价值。

如果换一种思路呢？不说&quot;提取 h4.price 的内容&quot;, 而是说&quot;帮我找到所有 NVIDIA 显卡的游戏本, 提取名字、价格和 GPU 型号&quot;。让程序自动理解页面, 自动定位元素, 而不是你替它定位。

这就引出了网页自动化的两种不同范式。

* 两种范式

** 精确指令范式

这是传统的做法：你告诉程序每一步怎么做。

#+begin_src text
1. 打开 URL
2. 找到 class=&quot;product-card&quot; 的元素
3. 在它里面找到 class=&quot;price&quot; 的元素
4. 读取文本内容
#+end_src

优点是指令精确, 执行速度快, 结果确定。但缺点也很明显：你对页面的结构做了假设, 页面一改假设就失效。每次页面更新都是一次人工维护。

** 目标描述范式

这是新做法：你告诉程序你想要什么, 它自己想办法。

#+begin_src text
目标：找到 RTX 50 系列的游戏本, 预算 1500 美元以内, 提取名称、价格和显卡型号
#+end_src

程序（一个 LLM 驱动的 Agent）自己打开网页, 自己识别哪些元素是产品卡片, 自己找到价格和规格信息。你不需要关心页面的 HTML 结构。

缺点是慢（每次执行都需要 LLM 推理）, 贵（每次调用都有 Token 成本）, 而且结果不保证一致。同一个任务跑两次可能拿到不同的数据。

** 核心区别

| 维度 | 精确指令 | 目标描述 |
|------|----------|----------|
| 你写什么 | 操作步骤 | 最终目标 |
| 对页面结构的依赖 | 强, 类名/层级固定 | 弱, 语义理解 |
| 页面改版后 | 代码需要重写 | 通常还能跑 |
| 执行速度 | 毫秒到秒级 | 秒到分钟级 |
| 运行成本 | 几乎为零 | 每次有 LLM 推理成本 |
| 结果确定性 | 完全相同 | 每次可能不同 |
| 适用任务 | 规则明确的操作 | 需要判断和综合的任务 |

这个表格说明一件事：两个范式没有绝对优劣。重点不在&quot;哪个更好&quot;, 得先搞清楚&quot;什么时候用哪个&quot;。

* 自主化的自动化：Agent + Judge 架构

目标描述范式内部是一套闭环系统, 不是调一次 LLM 就完事。

** Worker 循环

Agent 拿到任务后进入一个循环：

1. *执行 (Action)* ：从内置工具箱中选一个操作（导航、点击、提取、滚动）
2. *评估 (Eval)* ：上一步操作成功了吗？如果失败就重试
3. *记忆 (Memory)* ：记录已经完成的工作, 避免重复
4. *规划 (Next Goal)* ：基于当前进度决定下一步做什么

这个循环的好处是能自我纠正。传统脚本碰到意外情况就直接崩溃, Agent 的 Eval 步骤能发现失败并调整策略。比如某个按钮没找到, 不会抛出异常退出, 而是尝试其他方式定位。

** Judge 独立审查

Worker 完成后, 第二个 LLM 作为 Judge 审查结果。

这个设计的思路是这样的：人做复杂任务时容易自我感觉良好, 看不到自己的盲区。LLM 也一样。Worker 看到的是完整的思考过程, 容易觉得自己&quot;做得还不错&quot;。Judge 只看到原始任务和最终结果, 能以新视角找出问题。

在该示例中, Judge 发现 Worker 把一篇关于加密技术的文章错误归类为 AI 相关文章。这个分类错误并不显眼, 但 Judge 抓住了。

** 通用设计原则

Agent + Judge 架构不限于网页爬虫。它的本质是两个角色的分离：

- 一个能做决策的执行器 + 一个能发现问题的评审器
- 执行器负责灵活应对变化, 评审器负责把住质量底线
- 两者视角不同, 才能相互制衡

如果你在设计 AI 工作流, 可以试试这个分离模式。

* 四个决策维度

下面是四个核心取舍维度, 帮助你判断哪种范式适合当前任务。

** 速度 vs 灵活性

精确指令范式快。定位一个元素是毫秒级的操作。但这个快建立在&quot;页面结构稳定&quot;的假设上。页面一改, 快就没意义了, 因为代码跑不了。

目标描述范式慢。每次操作都有 LLM 推理延迟, 一个简单的数据提取任务可能花 30 到 60 秒。但这个慢换来了适应性：页面结构变了, Agent 通常还能自己找路。

取舍逻辑：如果页面稳定、任务频繁执行, 精确指令更划算。如果任务是一次性的、或者目标站点经常改版, 目标描述的总体成本（开发时间加维护时间）反而更低。

** 确定性 vs 适应性

精确指令范式每次执行结果完全相同。这在很多场景下是硬要求。比如金融数据的定时采集, 结果不一致比慢更可怕。

目标描述范式每次都可能走不同路径。同一个提取任务, 两次运行之间分类结果可能不一样。

取舍逻辑：需要审计级可靠性的场景选精确指令。能容忍&quot;今天的结果和昨天有细微差异&quot;的场景再考虑目标描述。

** 成本结构

精确指令范式的成本是一次性的开发成本。写爬虫花时间, 但跑起来几乎不花钱。

目标描述范式正相反。每次执行都有 Token 成本。Hacker News 示例跑了 42,339 个 Token, 花费约 12 美分（用 GPT-4o）。看起来不多, 但请注意：后续步骤会携带完整对话历史, 步骤越长上下文越大, 成本会加速增长。

降低成本有三种方式：

- 换便宜模型。GPT-4o-mini 或 Haiku 能把成本降到两美分以下
- 用本地模型。Ollama 或 LM Studio 可以把单次运行成本降到零。注意需要 30B+ 参数级别的模型才能可靠输出结构化结果
- 缩短提示词。每少一步推理, 上下文就少几千 Token

** 任务类型

精确指令适合规则匹配型任务。&quot;提取 class=price 的文本内容&quot;、&quot;点击 class=next-page 的按钮&quot;。这些任务不需要 LLM, 用了反而添乱。

目标描述适合判断综合型任务。&quot;找出所有 AI 相关的故事&quot;、&quot;总结这些产品的共同点&quot;、&quot;挑出性价比最高的那一款&quot;。这些任务需要某种程度的理解, 纯规则无法完成。

* 新范式什么时候翻车

目标描述范式不是万能药。Newegg 购物网站的实验就是一个典型的失败案例。

任务是筛选&quot;1500 美元以内、RTX 50 系列、32GB 内存&quot;的游戏本。Agent 出了三个问题：

1. 价格过滤器输入了数值但没有点击 APPLY。页面没刷新, 结果里混入了超出预算的产品
2. 32GB 内存条件被静默忽略。最终返回的全部是 16GB 内存的笔记本
3. 翻页只做了第一页, 没有收集全部结果

三个问题指向同一个根因：LLM 对精确约束条件不够敏感。Agent 倾向于&quot;差不多就行了&quot;, 而不是&quot;必须严格满足每个条件&quot;。

翻译成更通用的表述：目标描述范式适合&quot;软约束&quot;任务（帮我找有趣的故事）, 但不适合&quot;硬约束&quot;任务（必须精确满足 N 个条件）。如果把硬约束任务交给目标描述范式, 约束条件很可能被突破。

* 决策树

综合上面的分析, 下面是一个简单的决策流程：

1. 任务涉及精确的数字过滤吗（预算 1500 元、32GB 内存）？
   - 是 → 用精确指令范式。把条件写死在代码里, 确保每个都被严格执行
   - 否 → 看第 2 条
2. 任务需要判断或分类吗（&quot;哪篇是 AI 相关文章&quot;&quot;这个产品的卖点是什么&quot;）？
   - 是 → 目标描述范式很合适
   - 否 → 看第 3 条
3. 任务频率高、对速度敏感吗？
   - 是 → 精确指令范式更好
   - 否 → 两个都能用, 按开发时间成本选

决策核心只有一句话： *规则清晰、频繁执行的任务用精确指令；需要判断、页面多变的任务用目标描述。*

* 不是二选一

评论区有一个观点值得单独拿出来说。读者 Jason Zions 提了一个思路：让 LLM 帮你生成 CSS 选择器。

具体做法是这样的：

1. 用目标描述范式跑一次。Agent 在页面上找到你要的数据
2. 把 Agent 实际用到的定位方式（CSS 选择器、XPath）记录下来
3. 以后的任务直接用这些选择器跑精确指令, 又快又省钱
4. 页面结构改了怎么办？再跑一次 Agent, 更新选择器

这个办法把两种范式串联成一个工作流。LLM 做它最擅长的&quot;适应变化&quot;部分, 精确脚本做它最擅长的&quot;重复执行&quot;部分。谁适合干什么就干什么, 不做非此即彼的选择。

相比直接做单选题, 这个组合方案可能更值得在实际项目中尝试。

* 总结

网页自动化正在经历从&quot;写指令&quot;到&quot;说目标&quot;的转变。但新范式不是在替换旧范式, 而是在旧范式之上增加了一层能力。

精确指令范式适合高频执行、需要确定性、精确约束的任务。
目标描述范式适合需要判断和综合、页面多变、一次性的任务。

两者之间的选择不是技术选型, 而是对任务本质的判断：你的任务更像一条流水线, 还是更像一次探索？
</description>
<pubDate>2026-05-03</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/02/从css选择器到自然语言：网页自动化的两种范式与取舍框架/index.html</guid>
</item>
<item>
<title>从画图理解声明式编程：Altair 的三块积木</title>
<link>https://lujun9972.github.io/blog/2026/05/03/从画图理解声明式编程：altair-的三块积木/index.html</link>
<description>* 引子：两个画图程序员的对话

假设你要画一张散点图：横轴是电影制作预算，纵轴是全球票房，每个点按类型着色。

以下是 18 部电影的数据，保存为 ~/tmp/movies.csv~ 。在 Org-mode 中按 ~C-c C-v t~ 生成文件，或直接复制数据手动创建：

#+begin_src csv :tangle /tmp/movies.csv
Title,Production Budget,Worldwide Gross,Major Genre,IMDB Rating,MPAA Rating
The Whole Ten Yards,30000000.0,26323969.0,Comedy,5.1,PG-13
Dream With The Fishes,1000000.0,542909.0,Drama,6.6,R
Godzilla,125000000.0,376000000.0,Action,4.8,PG-13
Exit Wounds,33000000.0,79958599.0,Action,5.2,R
Doom,70000000.0,54612337.0,Horror,5.2,R
The Avengers,60000000.0,48585416.0,Action,3.4,PG-13
First Daughter,30000000.0,10419084.0,Romantic Comedy,4.7,PG
Raising Victor Vargas,800000.0,2811439.0,Drama,7.1,R
Tin Cup,45000000.0,75854588.0,Romantic Comedy,6.1,R
Pirates of the Caribbean: Dead Man&#39;s Chest,225000000.0,1065659812.0,Adventure,7.3,PG-13
Fighting Tommy Riley,300000.0,10514.0,Drama,6.6,R
The Funeral,12500000.0,1412799.0,Drama,6.4,R
Under Siege 2: Dark Territory,60000000.0,104324083.0,Action,5.1,R
Banlieue 13,15000000.0,11208291.0,Action,7.1,R
&quot;You, Me and Dupree&quot;,54000000.0,130402010.0,Comedy,6.1,PG-13
Supercross,30000000.0,3252550.0,Action,2.9,PG-13
Emma,5900000.0,37831658.0,Romantic Comedy,6.8,PG
Woman on Top,8000000.0,10192613.0,Romantic Comedy,4.9,R
#+end_src

用 Matplotlib 写：

#+begin_src python :python /home/lujun9972/github/lujun9972.github.com/.venv/bin/python :results output file
import matplotlib
matplotlib.use(&#39;Agg&#39;)
import matplotlib.pyplot as plt
import pandas as pd

movies = pd.read_csv(&quot;/tmp/movies.csv&quot;)
fig, ax = plt.subplots(figsize=(10, 6))
for genre in movies[&quot;Major Genre&quot;].unique():
    subset = movies[movies[&quot;Major Genre&quot;] == genre]
    ax.scatter(subset[&quot;Production Budget&quot;], subset[&quot;Worldwide Gross&quot;],
               label=genre, alpha=0.7)
ax.set_xlabel(&quot;Production Budget&quot;)
ax.set_ylabel(&quot;Worldwide Gross&quot;)
ax.legend()
plt.tight_layout()
plt.savefig(&quot;images/matplotlib-scatter.png&quot;)
plt.close()
print(&quot;images/matplotlib-scatter.png&quot;, end=&quot;&quot;)
#+end_src

#+RESULTS:
[[file:images/matplotlib-scatter.png]]

用 Altair 写：

#+begin_src python :python /home/lujun9972/github/lujun9972.github.com/.venv/bin/python :results output file
import altair as alt
import pandas as pd

movies = pd.read_csv(&quot;/tmp/movies.csv&quot;)
chart = alt.Chart(movies).mark_point().encode(
    x=&quot;Production Budget:Q&quot;,
    y=&quot;Worldwide Gross:Q&quot;,
    color=&quot;Major Genre:N&quot;
)
chart.save(&quot;images/altair-basic.png&quot;)
print(&quot;images/altair-basic.png&quot;, end=&quot;&quot;)
#+end_src

#+RESULTS:
[[file:images/altair-basic.png]]

两段代码做了同一件事，但思维方式完全不同。Matplotlib 是命令式的：你一步步告诉它 &quot;创建画布 → 添加坐标轴 → 遍历数据 → 画点 → 设标签 → 加图例 → 调整布局&quot;。代码是你的操作清单。Altair 是声明式的：你告诉它 &quot;数据在这里，横轴是制作预算，纵轴是票房，按类型着色&quot;。剩下的事情：坐标轴范围、刻度位置、图例排列、颜色映射：Altair 自己决定。

这两种范式差异的实质是 /描述意图（what）vs 控制过程（how）/ ，不是 &quot;哪个库更方便&quot; 的技术选型问题。

* Altair 的三块积木：Data→Mark→Encode

Altair 的每个图表都遵循同一个模式，由三个部分组成：

#+begin_src python
alt.Chart(数据).mark_形状().encode(列 → 视觉属性的映射)
#+end_src

用更直观的说法：

- *Data* ：你的数据 ： 一个 pandas DataFrame
- *Mark* ：你想要的视觉形状 ： 柱状条（mark_bar）、点（mark_point）、线（mark_line）、弧（mark_arc）
- *Encode* ：数据列到视觉属性的映射 ： 哪列放 X 轴、哪列染色、哪列控制点的大小

拿一周每日步数数据试一下：

#+begin_src python :python /home/lujun9972/github/lujun9972.github.com/.venv/bin/python :results output file
import altair as alt
import pandas as pd

steps = pd.DataFrame({
    &quot;Day&quot;: [&quot;Mon&quot;, &quot;Tue&quot;, &quot;Wed&quot;, &quot;Thu&quot;, &quot;Fri&quot;, &quot;Sat&quot;, &quot;Sun&quot;],
    &quot;Steps&quot;: [5432, 7890, 6210, 8765, 4567, 10234, 9430]
})

weekly_steps = alt.Chart(steps).mark_bar().encode(
    x=&quot;Day:O&quot;,
    y=&quot;Steps:Q&quot;
)
weekly_steps.save(&quot;images/altair-weekly.png&quot;)
print(&quot;images/altair-weekly.png&quot;, end=&quot;&quot;)
#+end_src

#+RESULTS:
[[file:images/altair-weekly.png]]

这段代码在 Jupyter Notebook 中会生成一张柱状图，X 轴是星期，Y 轴是步数。 

这种声明式写法的核心特点就是：每行代码都在描述数据的一个维度，而不是在操作图表的一个部件。

* COMMENT 声明式为什么强大

声明式的好处是 /增量成本极低/ 。

*加一个维度就是加一行编码通道*

前面那张散点图，只要在 ~.encode()~ 里加一行，就能把评分也映射上去：

#+begin_src python :python /home/lujun9972/github/lujun9972.github.com/.venv/bin/python :results output file
import altair as alt
import pandas as pd

movies = pd.read_csv(&quot;/tmp/movies.csv&quot;)
chart = alt.Chart(movies).mark_point().encode(
    x=&quot;Production Budget:Q&quot;,
    y=&quot;Worldwide Gross:Q&quot;,
    color=&quot;Major Genre:N&quot;,
    size=&quot;IMDB Rating:Q&quot;
)
chart.save(&quot;images/altair-size.png&quot;)
print(&quot;images/altair-size.png&quot;, end=&quot;&quot;)
#+end_src

#+RESULTS:
[[file:images/altair-size.png]]

每个点现在按类型着色、按评分大小。Altair 自动生成图例和比例尺，不需要手工设置。

*分面（Faceting）也是加一个参数的事*

#+begin_example
  分面（也叫&quot;小多组图&quot; small multiples）是按分类变量的不同值把数据拆成多个并排子图的技术。每个子图只展示一个类别的数据，坐标轴对齐、图例共享，方便跨类别对比。在 Altair 里，分面只是 ~.encode()~ 里的又一个参数。
#+end_example

把上面那张图按 MPAA 分级拆成并排子图，只需要加一个 ~column~ 参数：

#+begin_src python :python /home/lujun9972/github/lujun9972.github.com/.venv/bin/python :results output file
import altair as alt
import pandas as pd

movies = pd.read_csv(&quot;/tmp/movies.csv&quot;)
chart = alt.Chart(movies).mark_point().encode(
    x=&quot;Production Budget:Q&quot;,
    y=&quot;Worldwide Gross:Q&quot;,
    color=&quot;Major Genre:N&quot;,
    size=&quot;IMDB Rating:Q&quot;,
    column=&quot;MPAA Rating:O&quot;
)
chart.save(&quot;images/altair-faceted.png&quot;)
print(&quot;images/altair-faceted.png&quot;, end=&quot;&quot;)
#+end_src

#+RESULTS:
[[file:images/altair-faceted.png]]

子图的坐标轴自动同步，图例自动共享。在 Matplotlib 里做到同样的效果，需要 ~plt.subplots()~ 、手动同步坐标轴范围、单独处理图例：至少多写十几行样板代码。

声明式的逻辑是： /你描述数据的结构，工具推断出展示方式/ 。加一个 ~column=&quot;MPAA Rating:O&quot;~ ，Altair 立刻明白 &quot;按这个字段的不同值分开展示&quot;。

*交互式联动也不是 &quot;另一个问题&quot;*

在 Altair 里，交互是图表语法的一部分，不是额外功能：

#+begin_src python :python /home/lujun9972/github/lujun9972.github.com/.venv/bin/python :results output file
import altair as alt
import pandas as pd

movies = pd.read_csv(&quot;/tmp/movies.csv&quot;)

brush = alt.selection_interval()

scatter = alt.Chart(movies).mark_point().encode(
    x=&quot;Production Budget:Q&quot;,
    y=&quot;Worldwide Gross:Q&quot;,
    color=alt.condition(brush, &quot;Major Genre:N&quot;, alt.value(&quot;lightgray&quot;))
).add_params(brush)

bar = alt.Chart(movies).mark_bar().encode(
    x=&quot;mean(IMDB Rating):Q&quot;,
    y=&quot;Major Genre:N&quot;
).transform_filter(brush)

(scatter &amp; bar).save(&quot;images/altair-interactive.png&quot;)
print(&quot;images/altair-interactive.png&quot;, end=&quot;&quot;)
#+end_src

#+RESULTS:
[[file:images/altair-interactive.png]]

这段代码做了一件事：拖拽散点图上的区域，下面的柱状图自动显示选中电影的平均评分。

整个交互逻辑大约 15 行。没有 JavaScript、没有回调、没有事件绑定。

声明式的思维方式在这里体现得最彻底：你把选择定义为一个参数（ ~selection_interval()~ ），在编码通道里引用它（ ~condition(brush, ...)~ ），把联动定义为另一个图表的过滤器（ ~transform_filter(brush)~ ）。每行都在描述 &quot;是什么&quot;，不是在写 &quot;怎么实现&quot;。

* 你其实早就用过声明式

Altair 的 Data→Mark→Encode 不是独创。声明式编程你可能每天都在用，只是没意识到它属于同一个范式。

*SQL：声明式语言的代表*

#+begin_src sql
SELECT department, AVG(salary)
FROM employees
WHERE years_of_experience &gt; 3
GROUP BY department
HAVING AVG(salary) &gt; 50000
#+end_src

你描述你要什么：各部门中工龄 3 年以上员工的平均薪资，只显示平均薪资超过 5 万的部门。数据库自己决定用哪个索引、什么连接顺序、怎么并行执行。

SQL 和 Altair 共享同一个核心哲学： /你描述结果，系统选择路径/ 。

*正则表达式：用模式匹配替代过程*

~^\d{3}-\d{2}-\d{4}$~ 就是一个声明：&quot;我要一个 XXX-XX-XXXX 格式的字符串&quot;。你不需要写循环逐字符检查，正则引擎帮你做。

*Clojure 的 core.logic*

函数式语言中的逻辑编程库更是直接把 &quot;声明约束条件&quot; 作为核心范式：

#+begin_src clojure
(run* [q]
  (membero q [1 2 3])
  (membero q [2 3 4]))
;; =&gt; (2 3)
#+end_src

你不是在写 &quot;遍历第一个列表，对每个元素检查是否在第二个列表中&quot;。你只是在声明约束条件：&quot;q 要在第一个列表中，也要在第二个列表中&quot;。然后 core.logic 自己找出所有满足条件的 q。

这些例子的共同模式是： /你描述问题，而不是实现方案/ 。

* 什么时候不该用声明式

声明式不是银弹。Altair 和声明式编程都有适用范围：

*像素级定制*

当你要精确控制每个元素的颜色值、字体大小、位置偏移时，声明式的 &quot;自动推断&quot; 就成了障碍。Altair 允许覆盖默认值，但语法会更啰嗦，而且失去声明式的简洁感。这种场景 Matplotlib 仍是更好的选择。

*3D 可视化*

Altair 严格 2D。需要 3D 图得回 Matplotlib 或 Plotly。声明式通常不擅长处理 &quot;非常规&quot; 的视觉映射。

*大数据集*

Altair 把数据嵌入图表规范为 JSON，超过 5000 行会触发 ~MaxRowsError~ 。解决方案有 VegaFusion（服务端聚合）或通过 URL 传递数据，但相比 Matplotlib 的直接渲染，额外步骤不少。

这就像用 SQL 做复杂的数据清洗：虽然也能做，但用 Python 写个循环可能更直接。声明式在你需要 &quot;标准做法的 80%&quot; 时最高效，剩下 20% 的非常规需求可能反而更费力。

* 总结

Altair 用一个简单的模式（Data→Mark→Encode）展示了声明式编程在数据可视化中的力量。它的每个例子都遵循一个原则： /描述数据，而不是操作图表/ 。

这个原则你早就见过：SQL 声明你要什么数据而不说怎么查，正则描述文本模式而不写逐字符匹配的循环，Clojure core.logic 声明约束条件而不写遍历逻辑。

声明式的价值不是省几行代码，而是让代码表达意图而非实现细节。当你能用一句 ~color=&quot;Major Genre:N&quot;~ 而不是一个 for 循环加 ~ax.scatter()~ 来做同一件事时，你就能腾出精力去想真正重要的问题：数据在说什么。
</description>
<pubDate>2026-05-03</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/03/从画图理解声明式编程：altair-的三块积木/index.html</guid>
</item>
<item>
<title>读：Emacs newcomers-presets theme —— 30+ 项新手预设一览</title>
<link>https://lujun9972.github.io/blog/2026/05/02/读：emacs-newcomers-presets-theme-——-30+-项新手预设一览/index.html</link>
<description>Emacs 开发版（2026 年 2 月起）里塞了一个叫 =newcomers-presets= 的 theme。加载它之后，Emacs 一口气给你设好 30+ 项配置，不用自己一个个去找。

这个 theme 会在 Emacs 31 中正式发布。想现在试的，跑：

#+begin_src emacs-lisp
M-x load-theme RET newcomers-presets RET
#+end_src

启动界面上也有入口。想永久启用的话， =M-x customize-themes= 勾上再保存就行。

Sacha Chua 在 [[https://sachachua.com/blog/2026/04/what-s-in-the-emacs-newcomers-presets-theme/][博客]] 里逐条做了记录，我按功能分类整理如下。

* 键盘快捷键

#+begin_src emacs-lisp
(setopt repeat-mode t)
#+end_src

启用 repeat-mode 后，部分命令可以用快捷键的最后一段来重复。比如用 =C-x n n= 进入 =narrow= 状态后，再按 =n= 就能继续 narrow，不用每次按完整的 =C-x n n= 。

* 外观

#+begin_src emacs-lisp
(pixel-scroll-mode 1)
#+end_src

滚动平滑了，不再是一行一行跳。

#+begin_src emacs-lisp
(add-hook &#39;prog-mode-hook &#39;display-line-numbers-mode)
(add-hook &#39;text-mode-hook &#39;display-line-numbers-mode)
#+end_src

代码和文本 buffer 默认都显示行号。

#+begin_src emacs-lisp
(setopt column-number-mode t)
#+end_src

模式行显示列号。


#+begin_src emacs-lisp
(setopt font-use-system-font t)
#+end_src

Emacs 使用系统默认等宽字体，而非自身配置的字体。

#+begin_src emacs-lisp
(setopt frame-resize-pixelwise t)
(setopt window-resize-pixelwise t)
#+end_src

窗口和 frame 可以按像素调整，不受字符网格的限制。

#+begin_src emacs-lisp
(setopt frame-inhibit-implied-resize t)
#+end_src

改了字体或调整界面元素时 frame 大小不变，不会出现改个字体窗口就跟着跳的情况。

#+begin_src emacs-lisp
(setopt mode-line-compact &#39;long)
#+end_src

模式行太宽时自动压缩，连续空格合并成单个。

* 会话持久化

#+begin_src emacs-lisp
(setopt savehist-mode t)
#+end_src

Minibuffer 历史跨会话保存。下次打开 Emacs， =M-x= 里用 =M-p= =M-n= 还能翻到上次敲过的命令。

#+begin_src emacs-lisp
(setopt save-place-mode t)
#+end_src

记录每个文件的上次光标位置。

#+begin_src emacs-lisp
(setopt recentf-mode t)
#+end_src

记录最近打开的文件， =M-x recentf-open-files= 或 =C-x C-r= 快速打开。

* 补全

这一组配置影响补全候选列表的行为（ =M-x TAB= 或其他需要补全的场景）。

#+begin_src emacs-lisp
(setopt minibuffer-visible-completions t)
#+end_src

可以用上下箭头在 minibuffer 中直接选择补全候选， =RET= 确认选中项。

#+begin_src emacs-lisp
(setopt completions-detailed t)
#+end_src

补全列表展示更多细节（如 =C-h o= 的函数说明）。

#+begin_src emacs-lisp
(setopt completions-group t)
#+end_src

候选按命令或来源分组显示。

#+begin_src emacs-lisp
(setopt completion-auto-select &#39;second-tab)
#+end_src

第一次按 TAB 显示补全列表，第二次 TAB 跳转到补全 buffer。

#+begin_src emacs-lisp
(setopt completion-eager-update t)
#+end_src

补全 buffer 随输入实时更新筛选。

#+begin_src emacs-lisp
(setopt completion-styles &#39;(basic emacs22 flex))
#+end_src

补全风格设为三种：
- =basic= ：按前缀匹配
- =emacs22= ：光标前后分别作为前缀和后缀匹配
- =flex= ：模糊匹配（ =a-b= 可以匹配 =apple-banana= ）

#+begin_src emacs-lisp
(setopt global-completion-preview-mode t)
#+end_src

在光标位置自动显示补全预览。TAB 接受建议， =M-i= 补全最长公共前缀。

#+begin_src emacs-lisp
(setopt tab-always-indent &#39;complete)
#+end_src

TAB 优先尝试缩进，如果当前行已经缩进过了就触发补全。部分编程语言有自己的变量控制这个行为（如 =c-tab-always-indent= ），可能需要额外调整。

* 帮助

#+begin_src emacs-lisp
(setopt which-key-mode t)
#+end_src

按完快捷键前缀（比如 =C-c= ）后稍等片刻，Emacs 会弹出后续可用的按键列表。对新手熟悉快捷键很有帮助。

* Tab bar

#+begin_src emacs-lisp
(setopt tab-bar-show 0)
#+end_src

始终显示 tab bar。Tab 可以保存窗口布局和 buffer 组合，点击 tab 切换配置，点 + 新建 tab。

#+begin_src emacs-lisp
(setopt tab-bar-history-mode t)
#+end_src

Tab 配置变更的历史可前后导航，类似浏览器前进/后退。

* Dired

#+begin_src emacs-lisp
(setopt dired-auto-revert-buffer t)
#+end_src

每次重新进入目录时自动刷新 Dired buffer。

#+begin_src emacs-lisp
(setopt dired-mouse-drag-files t)
#+end_src

支持鼠标拖放文件。Ctrl+左键拖放复制文件，Shift+左键拖放移动，Meta+左键拖放创建链接。X11、Haiku、macOS、GNUstep 上还可以拖到 Emacs 外的应用程序。

#+begin_src emacs-lisp
(setopt shell-command-prompt-show-cwd t)
#+end_src

执行 shell 命令时显示当前目录。

* 包管理

#+begin_src emacs-lisp
(setopt package-autosuggest-mode t)
#+end_src

打开一个文件时，如果 Emacs 知道有可选包能提供额外支持，会在模式行显示 [Upgrade?] 提示。比如打开 =Dockerfile= 时提示安装 =dockerfile-mode= 。

#+begin_src emacs-lisp
(setopt package-menu-use-current-if-no-marks nil)
#+end_src

在 =M-x list-packages= 中， =x= （ =package-menu-execute= ）不再默认操作当前行。需要先用 =i= 标记安装、 =d= 标记删除、 =u= 取消标记，再用 =x= 执行。防止误操作。

* 代码

#+begin_src emacs-lisp
(add-hook &#39;prog-mode-hook &#39;flyspell-mode)
#+end_src

在代码 buffer 中启用拼写检查。

#+begin_src emacs-lisp
(setopt compilation-scroll-output &#39;first-error)
#+end_src

=M-x compile= 时，编译输出窗口会自动滚动，但停在第一个错误处。

#+begin_src emacs-lisp
(setopt global-xref-mouse-mode t)
#+end_src

Ctrl+左键点击函数名，跳转到定义（ =xref-find-definitions-at-mouse= ）。

#+begin_src emacs-lisp
(setopt electric-pair-mode t)
#+end_src

自动插入匹配的括号、花括号、方括号。

#+begin_src emacs-lisp
(setopt indent-tabs-mode nil)
#+end_src

缩通用空格而非制表符。

#+begin_src emacs-lisp
(setopt editorconfig-mode t)
#+end_src

支持 =.editorconfig= 文件，跨项目统一缩进风格。

#+begin_src emacs-lisp
(setopt etags-regen-mode t)
#+end_src

保存文件时自动重新生成 TAGS 表。用 etags 做代码跳转的场景下比较有用。

* 版本控制

#+begin_src emacs-lisp
(setopt vc-auto-revert-mode t)
#+end_src

版本控制更新的文件自动从磁盘重新加载。

#+begin_src emacs-lisp
(setopt vc-dir-save-some-buffers-on-revert t)
#+end_src

目录有版本控制变更但你有未保存的修改时，Emacs 会询问是否先保存再回退。

#+begin_src emacs-lisp
(setopt vc-find-revision-no-save t)
#+end_src

=vc-find-revision= 查看历史版本时在临时 buffer 中显示，不替换当前文件。

#+begin_src emacs-lisp
(setopt vc-follow-symlinks t)
#+end_src

打开版本控制仓库中的符号链接时，自动跳转到真实文件。

#+begin_src emacs-lisp
(setopt vc-use-incoming-outgoing-prefixes t)
#+end_src

=C-x v I= 和 =C-x v O= 的子命令增加快捷键——如 =C-x v I L= 是 =vc-root-log-incoming= ， =C-x v O L= 是 =vc-root-log-outgoing= 。用 =C-x v I C-h= 和 =C-x v O C-h= 查看全部子命令。

#+begin_src emacs-lisp
(setopt vc-deduce-backend-nonvc-modes t)
#+end_src

在所有 buffer 中自动检测版本控制系统。标准 Emacs 只在 Dired、shell、eshell、compilation-mode 中检测。

* 总结

这 30+ 项配置的完整代码可以到 =etc/themes/newcomers-presets-theme.el= 查看。如果你用的是 Emacs 30 或更早版本，可以把需要的配置项挑出来加到自己的配置里。

想一次性体验全部效果的话，上面列出的配置按顺序放到 =init.el= 里就能用。注意 =newcomers-presets= 是一个自定义 theme，和普通 theme 互不影响，你可以同时启用一个颜色 theme 和这个预设 theme。
</description>
<pubDate>2026-05-02</pubDate>
<guid>https://lujun9972.github.io/blog/2026/05/02/读：emacs-newcomers-presets-theme-——-30+-项新手预设一览/index.html</guid>
</item>
</channel>
</rss>